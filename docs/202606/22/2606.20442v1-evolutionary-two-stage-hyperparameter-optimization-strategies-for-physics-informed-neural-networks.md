---
title: Evolutionary Two-Stage Hyperparameter Optimization Strategies for Physics-Informed Neural Networks
title_zh: 基于物理信息的神经网络的进化两阶段超参数优化策略
authors: "Fedor Buzaev, Dmitry Efremenko, Egor Bugaev, Andrei Ermakov, Denis Derkach, Daria Pugacheva, Fedor Ratnikov"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20442v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 用于物理信息神经网络的进化两阶段超参数优化
tldr: 物理信息神经网络（PINN）求解偏微分方程时，因损失函数高度非凸且由多项组成，导致训练不稳定、易陷入平台期，且对架构和优化超参数敏感。传统超参数优化方法易陷入局部最优。本文提出基于进化算法的两阶段超参数优化策略：第一阶段使用低保真度截断训练快速筛选候选配置，第二阶段仅对最有潜力的候选进行完整梯度训练。在平流、Klein-Gordon和Helmholtz方程上的实验表明，该方法在固定计算预算下显著降低均方误差，提升求解精度和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20442v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20442v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1377, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20442v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 863, \"height\": 488, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20442v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1440, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20442v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1439, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20442v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1445, \"height\": 392, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20442v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1000, \"height\": 1226, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20442v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1401, \"height\": 707, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20442v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1147, \"height\": 381, \"label\": \"Table\"}]"
motivation: PINN训练因超参数敏感且损失函数非凸，现有局部或梯度优化方法易陷入次优，需要鲁棒的全局搜索机制。
method: 两阶段进化优化：第一阶段低保真截断训练筛选配置，第二阶段仅对最优候选进行标准梯度优化求解。
result: 在三个经典PDE问题上，该方法相比标准训练获得更低平均误差，且计算资源受限时优势明显。
conclusion: 所提进化两阶段策略有效提升PINN超参数搜索的鲁棒性和求解精度，适用于异构混合参数空间。
---

## 摘要
物理信息神经网络（PINNs）通过将物理定律嵌入神经网络训练来求解偏微分方程（PDEs）。然而，由于物理信息损失函数的高度非凸和多术语结构，其性能常常受到收敛不稳定、训练平台期以及对架构和优化超参数强敏感性的影响。在这种情况下，外环超参数搜索是一个关于异质参数的噪声黑盒优化问题，传统的局部或基于梯度的策略容易陷入次优区域。进化算法凭借其基于种群的探索能力和处理混合、不可微搜索空间的能力，为发现有前景的配置提供了更稳健的机制。我们提出并研究了一种基于进化算法的两阶段方法，该方法结合了PINNs训练的探索和利用部分，以在固定计算预算下提高求解精度和稳健性。在第一阶段，我们使用截断的周期进行低保真度训练运行，以快速筛选候选配置，将超参数选择视为黑盒外环问题。在第二阶段，仅对最有希望的候选者使用标准基于梯度的优化器进行完整训练，以细化求解。在三个常见问题（即平流方程、Klein-Gordon方程和Helmholtz方程）上进行评估，我们的方法始终优于标准训练，并在有限的计算资源内实现了显著更低的平均误差。

## Abstract
Physics-Informed Neural Networks (PINNs) solve Partial Differential Equations (PDEs) by embedding physical laws into neural network training. However, their performance suffers from unstable convergence, training plateaus, and strong sensitivity to architectural and optimization hyperparameters due to the highly non-convex and multi-term structure of the physics-informed loss. In this setting, the outer-loop hyperparameter search is a noisy and black-box optimization problem over heterogeneous parameters, where classical local or gradient-based strategies are easily trapped in suboptimal regions. Evolutionary algorithms, with their population-based exploration and ability to handle mixed, non-differentiable search spaces, provide a more robust mechanism for discovering promising configurations. We propose and investigate a two-stage approach based on evolutionary algorithms that combines exploration and exploitation parts of PINNs training to improve solution accuracy and robustness under fixed computational budgets. In the first stage, we perform low-fidelity training runs with truncated epochs to rapidly screen candidate configurations, treating hyperparameter selection as a black-box outer-loop problem. In the second stage, only the most promising candidates are fully trained with standard gradient-based optimizers to refine the solution. Evaluated on three popular problems, namely Advection, Klein-Gordon and Helmholtz equations, our method consistently outperforms standard training and achieves significantly lower mean error within constrained computational resources.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

物理信息神经网络（PINNs）通过将物理定律（偏微分方程）嵌入损失函数来求解 PDEs，在流体、热传导、量子力学等领域具有潜力。然而，PINNs 训练面临三大障碍：

- **收敛不稳定**：损失函数高度非凸且由多项项组成，容易陷入次优局部极小点或出现训练平台期。
- **超参数敏感性**：网络架构（层数、神经元数、激活函数）与优化参数（学习率、调度器、损失项权重）之间的复杂交互会严重影响收敛质量。
- **外环优化困难**：超参数选择本质上是一个双层优化（内层训练网络权重，外层选择超参数），外环目标函数是计算昂贵的黑盒，且不可导。

经典局部或梯度方法（如网格搜索、随机搜索、贝叶斯优化）在处理多模态、高维、异质（混合离散/连续）超参数空间时容易陷入次优区域。因此，**本文的核心动机是：利用进化算法的种群探索能力与适应混合搜索空间的特性，设计一种更稳健、高效的两阶段超参数优化策略，在固定计算预算下提升 PINNs 的求解精度与鲁棒性。**

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：两阶段优化，将计算预算明确划分为“探索”和“利用”两个阶段。

- **第一阶段（探索阶段）**：使用低保真度截断训练（训练少量 epoch）快速评估大量候选超参数配置，将超参数选择视为黑盒外环问题。采用基于种群的进化算法（JADE、LSHADE、Grey Wolf Optimizer、Whale Optimization Algorithm）进行全局搜索。
- **第二阶段（利用阶段）**：只将探索阶段选出的最有潜力的少数几个候选配置进行完整训练（梯度优化器，如 AdamW + 学习率调度器）直至收敛，从而得到高精度解。

**关键技术细节**：

- 超参数搜索空间包括：架构（层数 2–5、神经元数 16–128、激活函数 {tanh, ReLU, GELU, SiLU}）、优化器参数（AdamW 学习率 10⁻⁴–10⁻²，对数均匀采样）、学习率调度器类型（Reduce on Plateau / Cosine Annealing / None）及其相关参数（factor、patience、Tmax、η_min）。调度器相关参数仅在对应调度器被选时激活，降低有效维度。
- 探索阶段的评价目标：若已知参考解，则直接最小化 RMSE；否则，最小化 PINN 总损失（L(ψ) = λ_pde L_pde + λ_bc L_bc）。论文验证了 RMSE 与总损失强相关（图3），因此即使无参考解也可按照损失值筛选。
- 两阶段预算分配：建议探索阶段使用约 10% 的标准全训练 epoch（如 1000 epoch）作为每候选的评价周期，能在固定总预算下获得约 40% 的基线误差改善。

**公式表示（文字说明）**：  
内层优化： min_ψ L(ψ; h) （对于给定超参数 h）  
外层优化： min_h error(ψ*(h), h) （error 为 RMSE 或总损失）  
将外层优化拆分为探索（低保真度）和利用（全训练）两个子阶段。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

**测试问题（三个经典 PDE benchmark）**：

1. **平流方程（Advection）**：二维线性，∂u/∂t + ∂u/∂x + ∂u/∂y = 0，域 [0,1]²×[0,1]，周期边界。
2. **Klein–Gordon 方程**：非线性双曲型 ∂²u/∂t² - ∂²u/∂x² - ∂²u/∂y² + u² = f，域 [0,10]×[-1,1]²。
3. **Helmholtz 方程**：三维椭圆型 ∂²u/∂x²+∂²u/∂y²+∂²u/∂z² + k²u = f，域立方体，波数 k=1。

每个问题均通过构造已知精确解的方法生成残差源项，使得可以定量计算 RMSE。

**对比方法**：

| 类别 | 方法 |
|------|------|
| 基线 | 固定配置单阶段全训练（10,000 epoch） |
| 经典方法 | 网格搜索、随机搜索、Nelder-Mead、贝叶斯优化（BO） |
| 进化方法 | GWO（灰狼）、WOA（鲸鱼）、JADE、LSHADE |

实验共使用 5 个独立随机种子，每个优化方法在三种方程上重复 5 次。

**关键结果**：

- 所有进化算法（尤其是 JADE）在三种方程上均优于经典方法，最终 RMSE 更低，标准差更小。
- 探索 epoch 数（100–10,000）的消融实验表明：1,000 epoch（10% 全训练）达到最优性价比，RMSE 降低 28%–77%（相比全训练基线）。
- 进化算法在 150 次迭代后误差改善饱和（图2），因此固定探索迭代数 150 为默认。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

文中明确说明：

- **GPU**：单张 NVIDIA Tesla V100（32 GB 显存）。
- **单次全训练（10,000 epoch）**：约 11 分钟。
- **探索阶段总预算**：默认执行 150–200 次迭代，每次迭代训练 100–10,000 epoch（根据消融实验）。总训练时长因配置而异，但固定计算预算约束下，探索阶段总 GPU 时数不超过 32 GPU 小时（对应 150 次 1,000 epoch 的探索）。
- **一次完整实验（所有种子、方程、方法）**：计算量较大，但未给出精确总时长。文中指出“计算资源来自 HSE University HPC 设施”。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

**实验数量**：

- 三种 benchmark PDE（平流、Klein-Gordon、Helmholtz）。
- 7 种优化方法对比（Grid、Random、Nelder-Mead、BO、GWO、WOA、JADE、LSHADE）。
- 5 个独立随机种子。
- 探索 epoch 消融实验：7 档（100, 200, 500, 1,000, 2,000, 5,000, 10,000）。
- 两种优化目标（RMSE vs 损失）对比。
- 收敛稳定性分析：基于 3000 个随机配置绘制概率曲线。

**充分性与公平性**：

- 每个实验重复多次，报告均值和标准差，统计合理。
- 固定总计算预算，公平比较不同方法；探索阶段 epoch 数影响被充分考虑。
- 对比了经典优化方法和多种进化变体，没有忽略常见基线（如 BO）。
- 消融实验系统分析了探索长度对最终精度的影响，提出了经验性的“10% epoch 最佳”规则。
- **不足之处**：未在更复杂或实际工程问题（如 Navier-Stokes）上验证；未与 NAS 方法（Auto-PINN）进行直接对比；未详细体现超参数间的交互效应（仅在图8给出单一二维切面）。

总体而言，实验设计较为充分、客观、公平，限于三个经典方程但覆盖典型 PDE 类型。

### 6. 论文的主要结论与发现

1. **两阶段进化策略有效**：在固定计算预算下，探索阶段（10% epoch）加利用阶段（全训练）能稳定发现优于单阶段全训练的超参数配置，平均误差降低 40%（可达 77%）。
2. **进化算法优于经典方法**：JADE、LSHADE、GWO、WOA 一致优于网格搜索、随机搜索、Nelder-Mead 和贝叶斯优化；其中 JADE 表现最好。
3. **RMSE 与总损失强相关**（图3、4），因此即使无参考解也可用损失作为代理目标。
4. **最优探索预算约为 10% 全训练 epoch**（如 1,000/10,000），低于此比例筛选不准，高于此比例收益递减。
5. **超参数影响复杂**：激活函数相对独立，而层数、层大小等呈现强交互效应，手动调参困难，适合黑盒优化。
6. **无免费午餐定理**：无单一进化方法在所有问题上最佳，但差分进化类（JADE）较稳健。

### 7. 优点：方法或实验设计上的亮点

- **方法新颖性**：将进化算法与两阶段（低精筛选+高精训练）结合，首次系统对比多种进化变体用于 PINN 超参数优化。
- **实践导向**：明确预算分配指南（10% epoch），提供工程可复现的规则。
- **全面基准**：涵盖三个不同难度和维度的 PDE，同时对比经典和进化方法，统计严谨。
- **代理目标可用性**：验证了 RMSE 与损失的相关性，扩展了方法适用性（无参考解也可用）。
- **开源可复现**：使用公开优化器实现，实验配置详细（附录含最佳配置表 1）。
- **清晰可视化**：多张图（探索 epoch 影响、优化迭代饱和、方法对比等）辅助理解。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖**：仅测试了三个特定 PDE，未涉及更复杂或真实应用（如 Navier-Stokes、多尺度问题、反问题）。方法泛化性需进一步验证。
- **计算资源限制**：仅使用单张 V100 GPU，未探索多 GPU 或分布式训练；实际大规模问题可能需要更大资源。
- **对比方法缺失**：未与 Auto-PINN 等 NAS 专门方法对比，也未与其它强化学习或多保真度优化对比。
- **搜索空间有限**：架构方面未涉及跳跃连接、注意力机制等现代模块；优化器仅使用 AdamW，未尝试 L-BFGS 或混合策略。
- **超参数相关分析浅显**：仅给出激活函数二维热图（图8），缺乏更深入的重要性分析或特征消除实验。
- **随机性评估**：尽管重复 5 次，但每次的 PINN 权重初始化也引入噪声，可能影响结论稳定性；未进行毫秒级精确计时或能源消耗报告。
- **可重复性风险**：未提供代码仓库（截至当前版本），仅给出算法名称，具体实现细节（如种群大小、交叉率等）需从原文献引用，可能带来复现差异。

（完）
