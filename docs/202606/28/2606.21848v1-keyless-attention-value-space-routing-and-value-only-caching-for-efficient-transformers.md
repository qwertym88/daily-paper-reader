---
title: "Keyless Attention: Value-Space Routing and Value-Only Caching for Efficient Transformers"
title_zh: 无键注意力：面向高效Transformer的值空间路由与仅值缓存
authors: Xin Gao
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21848v1"
tags: ["query:neural-arch"]
score: 9.0
evidence: 消除键投影的新型高效注意力机制
tldr: "标准注意力机制中的键投影导致KV缓存内存和访问开销大。本文提出无键注意力，完全移除键投影，仅使用查询和值，并引入值空间路由矩阵实现深度3注意力分解，同时采用值仅缓存。实验表明，该方法在GPT-2、Pythia、Qwen2、Llama等5个模型上，4个模型的困惑度匹配或超越标准注意力，零样本常识推理基准上4/5提升，同时KV缓存减少50%。无键注意力在保持性能的同时显著降低内存，为高效Transformer推理提供了简洁而有效的方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21848v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1024, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1028, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1706, \"height\": 287, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1697, \"height\": 683, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21848v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1369, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 817, \"height\": 537, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1033, \"height\": 419, \"label\": \"Table\"}]"
motivation: 标准注意力的键投影导致KV缓存占据大量内存，限制了推理效率与长序列处理能力。
method: 移除键投影，使用查询和值通过值空间路由矩阵实现深度3注意力分解，并采用值仅缓存替代KV缓存。
result: "5个模型中4个困惑度匹配或超越标准注意力；零样本评估5个基准中4个提升，KV缓存减少50%。"
conclusion: 无键注意力在保持性能的同时将KV缓存减半，为高效Transformer推理提供新范式。
---

## 摘要
我们提出无键注意力，一种完全消除键投影的注意力机制，仅基于查询和值操作。这产生了仅值缓存，与标准注意力相比，KV缓存内存和访问开销精确减少50%，同时匹配或超过标准注意力的解码吞吐量。除了效率提升，我们引入深度-m注意力分解：标准注意力计算注意力双线性形式的深度-2分解，而无键注意力实现了该系列的深度-m实例。当m=3时，无键注意力通过一个值空间路由矩阵匹配标准注意力的投影矩阵数量，该矩阵替换了键投影并引入了路由与检索之间的耦合。在五个模型和四种架构（GPT-2 280M、GPT-2 557M、Pythia 410M、Qwen2 1.5B和Llama 3.2 1B）上的实验表明，在5个模型中的4个上，无键注意力在困惑度方面匹配或优于标准QKV注意力。在下游零样本评估中（GPT-2 557M），无键注意力在5个常识推理基准中的4个上表现更优，同时在整个过程中实现了50%的KV缓存减少。

## Abstract
We propose Keyless Attention, an attention mechanism that eliminates the key projection entirely, operating over queries and values only. This yields a Value-Only Cache that reduces KV cache memory and access overhead by exactly 50% over standard attention, while matching or exceeding standard attention's decode throughput. Beyond efficiency, we introduce Depth-$m$ Attention Factorization: standard attention computes a depth-2 factorization of the attention bilinear form, while Keyless Attention realizes a depth-$m$ instance of this family. At m=3, Keyless Attention matches the projection matrix count of standard attention via a value-space routing matrix that replaces the key projection and introduces a coupling between routing and retrieval. Experiments across five models and four architectures (GPT-2 280M, GPT-2 557M, Pythia 410M, Qwen2 1.5B, and Llama 3.2 1B) show that Keyless Attention matches or outperforms standard QKV attention on perplexity in 4 out of 5 models. On downstream zero-shot evaluation (GPT-2 557M), Keyless Attention outperforms on 4 out of 5 commonsense reasoning benchmarks, while achieving 50% KV cache reduction throughout.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：Transformer 架构中的标准缩放点积注意力依赖查询（Q）、键（K）、值（V）三个投影。自回归推理时，KV 缓存（保存过去 token 的 K 和 V）严重消耗内存，已成为长序列和大规模部署的主要瓶颈。现有优化方法（如稀疏注意力、线性注意力、低秩近似、量化、修剪等）多是对 KV 缓存的“事后”压缩或管理，并未质疑键投影本身的必要性。
- **核心问题**：能否从源头重新设计注意力机制，消除显式的键构造，从而在保持模型表达能力的同时减少 KV 缓存需求？
- **整体意义**：本文提出一种**无键注意力（Keyless Attention）**，完全移除键投影，仅使用查询和值，并引入**值空间路由**和**深度-m 注意力分解**框架。该方法在五个模型和四种架构上匹配或超越标准注意力，同时将 KV 缓存内存精确减少 50%。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：注意力分数可直接在查询与值之间计算，无需独立的键空间。存储时仅缓存值（Value-Only Cache），消除键缓存。
- **关键技术细节**：
  - **无键注意力公式**：Attention(Q, V) = softmax(QV^T/√dₖ) V，其中 Q = XW_Q，V = XW_V，无 W_K。
  - **深度-m 注意力分解**：将注意力双线性形式的参数化视为深度 m 的矩阵分解。标准注意力为深度 2（QKV），本文重点探索深度 3 的 QVV(3)：W_Q = W_Q1 W_Q2（两个矩阵相乘），V = XW_V，保持总投影矩阵数与标准注意力相同。
  - **值空间路由**：路由分数直接基于值表示，耦合路由与检索，带来更强的语义归纳偏置和隐式正则化（梯度纠缠）。
  - **理论等价性**：定理证明，在值投影满秩等条件下，存在无键注意力的参数使得其注意力 logits 与标准注意力完全一致。
  - **跨注意力和分组/多查询变体**：无键注意力可扩展到编码器-解码器、多模态以及 MQA/GQA 等场景，仅值缓存始终有效。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：语言建模使用 WikiText-103（30M token 子集）；零样本下游评估使用 HellaSwag、ARC-Challenge、BoolQ、SciQ、StoryCloze。
- **基准**：与标准 QKV 注意力（原版实现）对比，所有超参数和训练条件保持一致，仅注意力机制不同。
- **模型与架构**：
  - 主实验：GPT-2 280M（12层）、GPT-2 557M（36层）。
  - 跨架构验证：Pythia 410M（partial RoPE、并行残差）、Qwen2 1.5B（GQA、RoPE）、Llama 3.2 1B（GQA、RoPE）。
  - 消融实验：比较不同分解深度 m（QVV(2)、QVV(3)、QVV(4)、QKV(2)、QKV(3) 等）。

### 4. 资源与算力

- 文中明确说明：所有实验在单张 **NVIDIA A100-SXM4-80GB GPU** 上完成。
- 训练时长未明确给出，仅提到使用 WikiText-103 的 30M token 子集，训练多个 epoch（基于验证损失早停，通常 4 个 epoch）。因此算力需求相对较低。

### 5. 实验数量与充分性

- **数量**：共涵盖 **5 个模型**（GPT-2 两种规模、Pythia、Qwen2、Llama3.2）和 **4 种架构**（不同位置编码、残差设计、注意力分组方式）。每个 GPT-2 实验使用 3 个种子取均值和标准差。零样本评估覆盖 5 个基准。额外做了消融实验（分解深度 m）和基准测试（解码头量）。
- **充分性**：实验设计较为充分，控制变量（仅替换注意力机制），跨不同规模、架构、训练数据集。结果一致性高，无键注意力在 4/5 模型困惑度和 4/5 下游任务上胜出，且过拟合更少。消融实验验证了 QVV(3) 为最佳权衡。结论客观、可靠。

### 6. 论文的主要结论与发现

- **核心结论**：显式的键投影并非必要。无键注意力（QVV(3)）匹配或超越标准 QKV 注意力，同时将 KV 缓存内存精确减少 50%。
- **性能发现**：
  - GPT-2 557M 上最佳困惑度从 33.50 降至 33.26；Pythia 410M 上 39.22 vs 40.99；Qwen2 1.5B 上 33.79 vs 34.38；Llama 3.2 1B 上 38.59 vs 39.09。
  - 零样本评估：4/5 任务提升（HellaSwag +0.9, StoryCloze +1.9），SciQ 略降。
  - 过拟合更少：所有模型在最佳 epoch 后验证损失上升更慢，体现了值空间路由带来的隐式正则化。
- **效率**：在 Qwen2-1.5B 上，无键注意力解码吞吐量略高于标准注意力（如 512 上下文时 24.05 vs 22.27 tok/s），且 KV 缓存大小减半。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：从根源上消除键投影，提出“值空间路由”和“深度-m 注意力分解”理论框架，为注意力设计提供新维度。
- **理论支撑**：定理证明无键注意力与标准注意力在满秩条件下等价，确保了表达能力不损失。
- **简洁高效**：实现简单，无需量化、修剪或系统级优化，可与其他技术（MQA、GQA、MLA）正交组合。
- **实验设计公平**：严格控制变量，跨多种主流架构验证，结果稳健。
- **开放与可复现**：数据集和代码均为公开许可，实验细节充分。

### 8. 不足与局限

- **实验覆盖受限**：仅使用 WikiText-103（30M token 子集）进行语言模型训练，未在更大规模数据集（如 C4、The Pile）或极长上下文（>8K  tokens）上充分验证。下游评估仅包含 5 个常识推理基准，缺乏更广泛的任务（如翻译、摘要、代码生成）。
- **理论分析不足**：“梯度纠缠导致隐式正则化”的假说未通过机制性实验（如梯度分析、特征可视化）加以证明，仍属推测。
- **泛化风险**：在 SciQ 上无键注意力表现下降（约 -2.6 点），提示在事实检索场景可能存在 trade-off，未深入分析原因。
- **未考虑推理系统级优化**：虽然提及与 PagedAttention 等兼容，但未在批量请求、高并发场景下评估实际吞吐提升。目前批量大小为 1 时计算受限，预期的大批量优势未验证。
- **代码开源**：论文未明确提供代码仓库，复现可能需要自行实现。

（完）
