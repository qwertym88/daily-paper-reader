---
title: Running hardware-aware neural architecture search on embedded devices under 512MB of RAM
title_zh: 在512MB内存下的嵌入式设备上运行硬件感知的神经架构搜索
authors: "Andrea Mattia Garavagno, Edoardo Ragusa, Paolo Gastaldo, Antonio Frisoli"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14824v1"
tags: ["query:neural-arch"]
score: 9.0
evidence: 硬件感知神经架构搜索用于高效网络
tldr: 针对嵌入式设备资源受限的问题，提出一种硬件感知神经架构搜索方法，能在512MB内存下运行。该方法生成微型卷积神经网络，适用于低端MCU设备。在Visual Wake Word数据集上取得最先进结果，且可在网关设备上离线执行，保障数据隐私。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14824v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 726, \"height\": 239, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14824v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 174, \"label\": \"Table\"}]"
motivation: 现有NAS方法计算量大，难以在内存有限的嵌入式设备上运行，无法满足边缘端隐私保护需求。
method: 提出资源感知的搜索策略，在内存约束下联合优化网络架构和硬件性能，生成适配MCU的紧凑CNN。
result: 在Visual Wake Word数据集上达到SOTA，模型可在512MB RAM的嵌入式设备上高效运行。
conclusion: 方案实现了嵌入式设备上的实时NAS部署，兼顾效率与隐私，拓展了TinyML应用场景。
---

## 摘要
本文提出了一种新颖的硬件感知神经架构搜索（HW NAS）方法，该方法考虑了运行该搜索的计算平台上的可用资源，使其能够在各种嵌入式设备上执行。所提出的HW NAS生成针对低端微控制器单元（MCU）的微小卷积神经网络（CNN），这些MCU通常用于物联网（IoT）或可穿戴机器人领域，从而开辟了新的用例。网关可以运行该搜索以基于采集的数据定制CNN架构，而无需使用外部服务器，从而确保隐私。所提出的技术在标准TinyML基准测试——Visual Wake Word数据集上，在多个嵌入式设备上的人体识别任务中实现了最先进的结果。

## Abstract
This document proposes a novel approach to hardware-aware neural architecture search (HW NAS) that considers the resources available on the computing platform running it, enabling its execution on various embedded devices. The presented HW NAS produces tiny convolutional neural networks (CNNs) targeting low-end microcontroller units (MCUs), typically involved in the Internet of Things (IoT) or wearable robotics, opening new use cases. A gateway could run it to tailor CNNs' architecture on the acquired data without using external servers, ensuring privacy. The proposed technique achieves state-of-the-art results in the human-recognition tasks on the Visual Wake Word dataset, a standard TinyML benchmark, on several embedded devices.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有硬件感知神经架构搜索（HW NAS）方法（如MNASNet、MCUNet）通常需要大量GPU算力和内存（例如MNASNet需40,000 GPU小时，MCUNet需300 GPU小时），无法在资源受限的嵌入式设备（如内存低于512MB的网关节点）上直接运行。这限制了边缘端离线定制网络架构的能力，且依赖外部服务器可能引发隐私泄露。
- **整体含义**：本文提出一种**资源感知的HW NAS**，不仅考虑目标硬件（如低端MCU）的约束，还考虑**运行搜索的嵌入式平台**的可用资源（尤其是RAM），从而使得整个NAS流程可在内存仅512MB的设备上完成。这为物联网、可穿戴机器人等场景提供了在网关节点上离线、私密地生成紧凑CNN的可能性。

## 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：修改现有的HW NAS方法[8]，在优化问题中新增一个约束条件：搜索过程中生成的候选网络架构必须能够**适配运行搜索的嵌入式设备的内存容量**（φ_E ≤ ξ_E），否则丢弃。这样可确保NAS算法本身能在嵌入式设备上顺利执行。
- **搜索空间**：基于堆叠的细胞结构，每个细胞包含：最大池化层（分辨率减半）→ 批归一化层 → 卷积层（3×3，步长1）。细胞堆叠在第一个卷积层之后，输入经过标准化和批归一化预处理。最后使用全局平均池化、Dropout和全连接层进行分类。网络架构由两个超参数决定：第一个卷积层的核数k和细胞数量c。搜索空间包含数千种可能架构。
- **优化问题**：如公式(1)所示，目标是最大化验证精度f(x)，同时满足四个约束：
  - φ_E(x) ≤ ξ_E：运行搜索的嵌入式设备可用RAM约束（新增）
  - φ_R(x) ≤ ξ_R：目标硬件RAM占用约束
  - φ_F(x) ≤ ξ_F：目标硬件Flash占用约束
  - φ_M(x) ≤ ξ_M：浮点运算量（MACC）约束
- **搜索策略**：使用基于无导数优化的方法[8]求解上述优化问题，无需梯度计算，适合资源受限场景。

## 3. 实验设计

- **数据集**：Visual Wake Words数据集——TinyML领域标准人体识别基准，用于评估小型CNN在低端设备上的性能。
- **基准测试（Benchmark）**：在四个嵌入式平台运行NAS本身，并将生成的模型部署到目标硬件STM32F412 MCU上评估。输入的图像尺寸为(50,50,3)。
- **对比方法**：
  - MCUNet [4]：在STM32F412上结果（Acc=87.4%, RAM=168.5 KiB, Flash=530.52 KiB, MACC=6 MM）
  - Micronets [6]：在STM32F446RE上结果（Acc=76.8%, RAM=70.5 KiB, Flash=273.81 KiB, MACC=3.3 MM）
- **运行NAS的嵌入式平台**：Jetson Nano (4GB RAM, MAXN模式)、Raspberry Pi 4B (4GB)、Raspberry Pi 3B (1GB)、Raspberry Pi Zero 2W (512MB)。所有平台采用ZRAM压缩和swap（2/3内存），并尽可能缓存数据集到RAM。

## 4. 资源与算力

- **搜索算力**：与以往方法不同，本文**未使用GPU**，而是直接在目标嵌入式设备（ARM CPU）上运行NAS。搜索时间在1.3~4.6天之间（RPi 4最快1.3天，RPi Zero 2W最慢4.6天）。论文指出这些设备不是为运行NAS设计的，因此时间较长；可使用10k代理数据集加速。
- **对比方法算力**：
  - MNASNet [1]：40,000 GPU小时
  - MCUNet [4]：300 GPU小时
  - 基础方法[8]：3.5小时（笔记本电脑，无GPU）
- **本方法未明确GPU型号或数量**，但运行平台均为嵌入式开发板，未使用独立GPU。

## 5. 实验数量与充分性

- 仅在一个数据集（Visual Wake Words）和一个目标硬件（STM32F412）上进行评估，**实验数量较少**。
- 没有进行消融实验（如对不同约束权重、不同搜索空间大小的分析），也没有在不同目标硬件（如不同MCU型号）上验证。
- 公平性方面：对比方法使用了不同的MCU（MCUNet用STM32F412，Micronets用STM32F446RE），论文解释为“选择最大的最小部署目标”以平衡。但严格来说，硬件差异可能影响比较结果。
- 尽管实验覆盖有限，作为概念验证发表在ICCE 2024，展示了可行性，但**充分性不足**。

## 6. 主要结论与发现

- 提出的HW NAS能在内存低至512MB的嵌入式设备（RPi Zero 2W）上成功运行，生成适用于低端MCU的极紧凑CNN。
- 生成的模型（在STM32F412上部署）具有极小的资源占用：
  - RAM：25~36 KiB
  - Flash：13.95~38.96 KiB
  - MACC：0.91~3.9 MM
  - 精度：75.1%~78%（与Micronets的76.8%相当，但远低于MCUNet的87.4%）
- **搜索时间**在1.3~4.6天之间，其中RPi 4最快，因为无GPU占用更多内存可供数据集缓存。
- **关键发现**：执行时间不与计算能力线性相关——内存大小影响更大；RPi 4因无GPU、内存更充足而表现出最佳搜索时间。

## 7. 优点

- **创新性**：首次将NAS运行平台的资源约束纳入优化问题，使NAS能在嵌入式设备上本地执行，满足边缘端隐私保护需求。
- **实用性**：生成的模型极为紧凑（RAM<40 KiB），适合超低端MCU（如STM32F412），拓展了TinyML应用场景。
- **方法简单**：基于无导数优化，无需GPU，易于部署到常用嵌入式平台（Raspberry Pi系列）。
- **隐私优势**：可在网关设备上离线完成NAS，无需上传数据到云端。

## 8. 不足与局限

- **搜索开销大**：直接运行需数天，实际部署可能不可接受；虽提及代理数据集可加速，但未给出具体实验验证。
- **实验覆盖有限**：仅单数据集、单目标硬件，未展示跨场景泛化能力。
- **精度偏低**：与MCUNet的87.4%相比差距明显，搜索空间可能过于简化限制了性能上限。
- **缺乏消融与敏感性分析**：未探讨不同约束松弛程度、不同k/c组合的影响。
- **对比公平性存疑**：与Micronets和MCUNet的目标硬件不完全相同，且输入尺寸略有差异（MCUNet使用64×64×3），可能影响比较有效性。
- **实际部署细节未讨论**：未考虑功耗、温度、网络部署延迟等实际因素；未说明在512MB以下（如256MB）设备上的表现。

（完）
