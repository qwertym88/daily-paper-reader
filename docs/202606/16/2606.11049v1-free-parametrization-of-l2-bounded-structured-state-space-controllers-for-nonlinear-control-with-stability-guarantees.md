---
title: Free Parametrization of L_2-Bounded Structured State-Space Controllers for Nonlinear Control with Stability Guarantees
title_zh: 具有稳定性保证的非线性控制的L_2有界结构化状态空间控制器的自由参数化
authors: "Muhammad Zakwan, Leonardo Massai, Efe C. Balta, Giancarlo Ferrari-Trecate"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11049v1"
tags: ["query:hum-ctrl"]
score: 9.0
evidence: 非线性控制中LTI系统的自由参数化与稳定性保证
tldr: 针对非线性系统控制器设计中的稳定性与性能平衡难题，提出一种基于结构化状态空间模型（SSM）的自由参数化方法。通过L2-循环单元（L2RU）层设计，确保控制器满足预设L2增益，从而借助小增益定理或性能提升框架保证闭环稳定性，且不约束优化变量。该方法支持高效并行处理长序列，在移动机器人编队控制任务中验证了避障与稳定性的兼顾。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11049v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11049v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 584, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11049v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 668, \"height\": 527, \"label\": \"Figure\"}]"
motivation: 现有神经网络控制器虽表达力强但易因输入扰动导致闭环失稳，而显式参数约束方法引入额外计算开销。
method: 利用结构化状态空间模型（SSM）参数化LTI系统，提出L2-循环单元（L2RU）层，通过自由参数化强制实现指定L2增益。
result: L2RU控制器在移动机器人编队控制中实现碰撞与障碍物规避，同时保持闭环稳定性与性能。
conclusion: 所提自由参数化方法无需约束优化变量即可保证非线性系统稳定性，兼具并行计算效率，为安全控制提供新范式。
---

## 摘要
为非线性系统设计稳定控制策略同时优化复杂目标仍然是一个艰巨的挑战。神经网络（NNs）尽管具有强大的表达能力，但对微小输入扰动高度敏感，并且容易破坏闭环系统的稳定性。现有方法通常对控制器的参数施加显式约束以确保稳定性，但这通常会带来额外的计算开销。为了解决这个问题，我们利用最近提出的结构化状态空间模型（SSMs）来参数化非线性系统的离散时间控制策略。我们的关键贡献是一种新的自由参数化方法，用于具有指定L_2增益的线性时不变（LTI）系统。我们利用这一结果构建了L_2递归单元（L2RU），这是一种通过设计强制实现所需L_2界的SSM层。由此产生的架构可以通过小增益定理或所谓的性能提升框架来保证闭环稳定性，而无需依赖控制器的优化参数，从而实现对一般非线性目标的完全无约束优化。此外，所提出的参数化所引入的结构使得长输入序列的高效处理成为可能，因为通过并行扫描等算法可以高度并行化。我们在移动机器人的编队控制任务上展示了该方法的有效性，其中基于L2RU的控制器在保持稳定性和性能的同时确保了碰撞和障碍物避免。

## Abstract
Designing stabilizing control policies for nonlinear systems while optimizing complex objectives remains a formidable challenge. Neural networks (NNs), despite their expressive power, can be highly sensitive to small input perturbations and can easily destabilize the closed-loop system. Existing approaches often impose explicit constraints on the controller's parameters to ensure stability, but this typically leads to additional computational overhead.   To address this issue, we leverage recently proposed structured state-space models (SSMs) to parametrize discrete-time control policies for nonlinear systems. Our key contribution is a new free parametrization of linear time-invariant (LTI) systems with a prescribed L2 gain. We use this result to construct the L2-Recurrent Unit (L2RU), an SSM layer that enforces the desired L2 bound by design. The resulting architecture can be used to guarantee closed-loop stability via the small-gain theorem or the so-called performance-boosting framework, independently of the controller's optimization parameters, thereby enabling fully unconstrained optimization of general nonlinear objectives. Furthermore, the structure induced by the proposed parametrization enables the efficient processing of long input sequences, as it is highly parallelizable through algorithms such as parallel scan.   We demonstrate the effectiveness of this approach on a formation-control task for mobile robots, where the L2RU-based controller ensures collision and obstacle avoidance while maintaining stability and performance.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
在非线性系统的控制中，设计同时保证闭环稳定性和优化复杂性能目标的控制器极具挑战性。神经网络控制器虽然表达能力强，但对输入扰动敏感，容易导致闭环失稳。现有方法通过在控制器参数上施加显式约束来确保稳定性，但这会引入额外的计算开销（如投影、约束优化）。本文针对这一问题，提出一种基于结构化状态空间模型（SSM）的自由参数化方法，使得控制器具有预设的 \(L_2\) 增益，从而借助小增益定理或性能提升框架（performance-boosting）确保闭环稳定性，同时允许对任意非线性目标进行完全无约束优化。该方法还利用SSM的并行扫描能力，实现长序列的高效处理。

## 2. 方法论
- **核心思想**：利用结构化状态空间模型（SSM）参数化离散时间控制器，其中关键模块是 \(L_2\)-循环单元（L2RU），这是一个通过设计强制实现指定 \(L_2\) 增益的SSM层。将系统L2有界性与控制器参数解耦，稳定性由预设增益保证，优化过程无需约束。
- **关键技术细节**：
  - L2RU架构：由多个状态空间层（SSL）级联组成，每个SSL包括一个LTI系统和Lipschitz有界静态非线性函数，通过跳跃连接（skip connection）叠加。输入输出通过线性编码器/解码器处理。
  - 自由参数化 \( \kappa_\gamma \)（见Block 1）：给定增益 \(\gamma\)，自由参数包括复对角矩阵 \(A\)（利用 \(\lambda_j = \exp(-\exp(\mu_j) + i\exp(\theta_j))\) 确保稳定）、矩阵 \(B, C, D\) 通过缩放和归一化构造，满足有界实引理（从而保证L2增益）。
  - 整个L2RU的增益通过各层增益的乘积和归一化因子控制，使得整体增益等于预设 \(\gamma\)。
  - 利用并行扫描算法（parallel scan）高效计算前向递推，尤其在系统动态也可并行化时效果显著。
- **公式/算法流程**：控制器 \( \Sigma_\gamma^\omega \) 由L2RU实现，包含编码器、r个SSL、解码器。具体定义见公式(8)。参数 \( \omega \) 包括各层LTI系统参数和非线性函数参数，以及编码器解码器矩阵。

## 3. 实验设计
- **场景**：移动机器人编队控制（基于Robotarium平台的离散非线性轮式机器人模型），多agent（实验中用4个机器人）协同导航，需到达各自目标点并同时避免静态障碍物和相互碰撞。
- **数据集/benchmark**：没有使用公开数据集或标准benchmark。实验为自建仿真环境，初始状态随机采样20个初始条件（S=20）。
- **对比方法**：论文未与其他方法（如REN、port-Hamiltonian、LRU等）进行定量对比。仅展示了L2RU控制器自身的轨迹和碰撞避免效果。
- **损失函数**：包含目标误差、控制能量惩罚、障碍物避免惩罚、agent间碰撞避免惩罚。具体公式见第4节。
- **超参数**：L2RU：r=3层，每层维度 \(n_d=n_z=12\)，\(n_h=11\)，控制器预设增益 \(\gamma=1/(18.573+0.001)\)（plant的估计L2增益为18.573）。学习率1e-3，线性调度器，Adam优化器。

## 4. 资源与算力
论文正文中**未明确说明**所使用的GPU型号、数量、训练时长、内存等计算资源信息。元数据中也无提及。因此无法评估训练效率或资源需求。

## 5. 实验数量与充分性
- **实验数量**：文中主要展示了一个编队控制实验（4个agent），并提及附加实验在技术报告中。没有消融实验、参数敏感性分析或多任务对比。
- **充分性**：实验只定性展示了轨迹图和相对距离图（证明无碰撞和达到目标），但缺乏定量指标（如成功率、平均距离、对比其他方法）。没有进行统计显著性分析，也没有控制变量实验。因此实验充分性**不足**，未能严谨证明L2RU相比其他方法的优势或稳定性保证的实际价值。

## 6. 主要结论与发现
- 提出的自由参数化L2RU层能够在不约束优化参数的前提下，通过设计保证指定 \(L_2\) 增益，从而结合小增益定理确保闭环稳定性。
- 在移动机器人编队控制任务中，L2RU控制器成功实现避障和稳定性，且未因增益限制而显著损失表达能力。
- 该方法支持高效并行推理，适合长序列控制。

## 7. 优点
- **方法创新**：首次将SSM的自由参数化用于非线性控制，将稳定性保证与优化解耦，避免昂贵的约束优化或投影。
- **计算效率**：离散时间设计、复对角化A矩阵支持并行扫描，比NODE或REN更高效。
- **稳定性保证**：基于小增益定理提供严格稳定性认证，且适用于多种非线性系统（只要plant的L2界可知或可估计）。
- **灵活扩展**：可集成到性能提升框架或模型增强型强化学习中。

## 8. 不足与局限
- **plant L2界依赖**：需要精确或保守估计被控系统的L2增益，对于复杂非线性系统这本身就很困难，限制了实用范围。
- **实验验证薄弱**：仅在一个仿真场景下做了定性展示，缺乏与REN、LRU、port-Hamiltonian等方法的定量对比，无法证明性能优势或泛化能力。
- **未考虑实际部署问题**：如传感器噪声、模型失配、通信延迟等，也未讨论实时性约束。
- **未进行消融研究**：未分析层数、维度、增益值等超参数的影响。
- **未公开完整的训练细节**：如硬件资源、训练时长，影响可复现性。

（完）
