---
title: "RGB: RL Guided Whole-Body MPPI for Humanoid Control"
title_zh: RGB：基于强化学习引导的全身MPPI的人形机器人控制
authors: "Yunsoo Seo, Sol Choi, Euncheol Im, Myo Taeg Lim, Yisoo Lee"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25123v1"
tags: ["query:hum-ctrl"]
score: 9.0
evidence: 强化学习引导的全身MPPI用于人形控制
tldr: 人形机器人在接触丰富环境中需兼顾鲁棒与精确控制，但深度强化学习策略难以灵活添加新目标。本文提出RL引导全身模型预测路径积分（MPPI）框架，以预训练RL策略作为采样先验，通过模块化成本项指定任务，MPPI在线纠正RL先验满足目标。在MuJoCo中29自由度Unitree G1人形上实现平均280Hz稳定控制，相比纯RL基线，任务精度显著提升，且无需重新训练即可适应新反馈。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25123v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1837, \"height\": 687}, {\"url\": \"assets/figures/arxiv/2606.25123v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 1357}, {\"url\": \"assets/figures/arxiv/2606.25123v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 889, \"height\": 845}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25123v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 1189}]"
motivation: 人形机器人控制需兼顾鲁棒与精确，但RL策略与目标耦合紧密，难以扩展新反馈目标而不重训。
method: 提出RL引导MPPI框架，将预训练RL策略作为采样先验，通过模块化成本项指定任务目标，MPPI在线纠正RL先验以满足目标。
result: 在MuJoCo仿真中29自由度Unitree G1人形实现280Hz控制，比纯RL基线任务精度更高，有效纠正直行漂移。
conclusion: 无需重训即可灵活添加新目标，为全身控制提供可扩展框架。
---

## 摘要
人形机器人需要能够在接触密集环境中既稳健又精确的全身控制器。虽然深度强化学习（RL）实现了稳健的稳定性，但其行为与训练目标和命令接口紧密耦合，使得无需重新训练即可添加新的反馈目标变得困难。在本研究中，我们提出了一种基于强化学习引导的全身模型预测路径积分（MPPI）框架，该框架作为预训练RL策略之上的附加反馈控制器。我们并非将RL策略作为最终控制器，而是将其用作采样先验，使MPPI的展开偏向于动态可行的行为。任务目标通过模块化的MPPI成本项指定，MPPI通过在线持续纠正RL先验来闭合环路，以满足这些目标，而无需重新训练策略。在MuJoCo中对29自由度Unitree G1人形机器人的仿真展示了稳定的高速控制（平均280 Hz）。在相同命令接口下，所提方法在任务级精度上优于纯RL基线。这是通过纠正直线行走过程中的系统性漂移以及跟踪通过成本施加的额外全身参考信号来实现的。

## Abstract
Humanoid robots require whole-body controllers that are both robust and precise in contact-rich environments. While deep reinforcement learning (RL) achieves robust stability, its behavior is tightly coupled to the training objective and command interface, making it difficult to add new feedback objectives without retraining. In this study, we propose an RL guided whole-body model predictive path integral (MPPI) framework that acts as an add-on feedback controller on top of a pretrained RL policy. Instead of using RL policy as the final controller, we use it as a sampling prior that biases MPPI rollouts toward dynamically feasible behaviors. Task objectives are specified through modular MPPI cost terms, and MPPI closes the loop by continuously correcting the RL prior online to satisfy these objectives without retraining the policy. Simulations on a 29-DoF Unitree G1 humanoid in MuJoCo demonstrate stable high-rate control (average 280~Hz). The proposed method improves task-level precision over a pure RL baseline under the same command interface. This is achieved by correcting systematic drift during straight walking and tracking additional whole-body reference signals imposed through the cost.

---

## 论文详细总结（自动生成）

## 论文总结：RGB: RL Guided Whole-Body MPPI for Humanoid Control

### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：人形机器人在接触密集环境中需要兼具鲁棒性与精确性。深度强化学习（RL）策略训练后行为与目标和命令接口紧密耦合，添加新任务目标（如摆腿高度、骨盆高度调节）通常需要重新设计奖励并重新训练，缺乏灵活性。另一方面，纯模型预测控制（MPC）方法虽能显式优化多目标任务和约束，但在高自由度人形机器人上对采样分布和初始化敏感，容易产生不可行轨迹。
- **动机**：提出一种混合框架，将RL的鲁棒运动学能力与MPC的目标灵活性结合，使预训练的RL策略保持不变，MPPI作为附加反馈控制器，通过模块化成本项实现任务级扩展，无需重新训练。

### 2. 论文提出的方法论
- **核心思想**：利用预训练的RL策略作为MPPI的采样先验（sampling prior），使MPPI的随机展开（rollouts）集中在动态可行区域；任务目标通过MPPI的模块化成本项指定；MPPI在线利用物理引擎并行展开，通过重要性加权更新控制序列，持续纠正RL先验以降低成本。
- **关键技术细节**：
  - **RL策略作为采样先验**：RL策略输出标称关节位置参考 `q_RL`，作为MPPI采样均值。MPPI在均值上加高斯噪声生成N个候选控制序列。
  - **Knot参数化**：将控制序列用H个knot点表示，通过三次插值生成高分辨率序列，降低采样维度，保证时间平滑性。
  - **物理引擎展开**：使用MuJoCo（通过MJPC）对每个候选序列进行前向动力学仿真，计算累积成本（包括任务特定成本和软约束）。
  - **重要性加权更新**：根据每个rollout的成本计算权重，更新knot向量，插值后取第一个控制量作为期望关节位置，由底层PD控制器跟踪。
- **算法流程**（文字说明）：
  1. 初始化knot均值为0。
  2. 循环中：读取当前状态；若RL策略到更新时刻，则 `q_RL = π_RL(ot)` 更新knot均值。
  3. 若MPPI规划完成：对每个rollout采样knot噪声 `ϵ ~ N(0, νI)`，生成候选knot序列，插值得到控制序列，通过物理引擎展开得轨迹和成本，计算重要性权重，更新knot均值（加权和）。取插值后的第一个控制作为期望关节位置 `q_des_t`。
  4. 底层PD控制器以500 Hz跟踪 `q_des_t`。

### 3. 实验设计
- **仿真场景与环境**：MuJoCo仿真，使用29自由度Unitree G1人形模型。采用MJPC进行并行展开（CPU并行）。RL策略预训练于IsaacLab，使用PPO，训练为速度跟踪行走策略（输出12个下半身关节位置）。
- **基准方法**：纯RL基线（相同预训练策略，无MPPI修正）。
- **测试任务**：
  - **直行漂移抑制**：机器人以速度命令 (v_x=1, v_y=0, ψ_dot=0) 行走，评估横向位置和偏航漂移。MPPI成本项增加横向位置和偏航偏差惩罚。
  - **蹲起任务**：给定时变基座高度参考（0.71-0.78m），RL策略无此命令接口，MPPI成本项增加高度跟踪项。
- **对比方式**：与纯RL同一命令下的表现对比，使用RMSE（均方根误差）等指标。

### 4. 资源与算力
- 使用工作站：Intel Core i9-14900KF CPU（用于MPPI并行展开），32GB RAM，NVIDIA GeForce RTX 4070 Ti GPU（文中未说明GPU用于训练还是MPPI，RL策略预训练使用IsaacLab，未提及具体GPU型号和训练时长）。MPPI规划平均有效更新率约280 Hz（CPU仿真）。
- **算力信息不完整**：未报告RL策略训练的具体GPU型号、数量、耗时；也未提供MPPI仿真时GPU是否用于加速rollouts。

### 5. 实验数量与充分性
- 只进行了两组任务实验：直行漂移抑制和蹲起任务。每组提供了定量RMSE指标和定性轨迹图。未进行消融实验（如不同knot数、不同噪声方差、不同rollout数的影响）、未评估其他任务（如扰动恢复、变速度命令、不同地面情况）、未进行硬件实验。
- **充分性评价**：实验规模较小，仅两个简单场景，但展示了核心能力——纠正系统性漂移和添加新目标。缺乏系统消融和泛化性测试，公平性方面：与纯RL基线对比条件相同（同一预训练策略、相同命令），是合理的。但未与纯MPPI（无RL先验）或基于梯度的MPC对比，因此无法体现RL先验的具体增益。

### 6. 论文的主要结论与发现
- 所提RL引导MPPI框架在保持RL鲁棒行走能力的同时，通过MPPI在线修正，显著改善直行跟踪精度（横向位置RMSE从0.339m降至0.022m），且不降低前进速度跟踪能力。
- 在蹲起任务中，通过成本项成功实现了基座高度跟踪（RL策略无法直接控制），验证了无需重训即可扩展新目标的能力。
- 系统在仿真中实现平均280 Hz控制速率，具备实时性潜力。

### 7. 优点
- **模块化可扩展**：任务目标仅通过MPPI成本项添加，与RL策略解耦，无需修改或重训策略，即插即用。
- **样本效率高**：利用RL先验使MPPI采样集中在可行区域，减少无效rollout，提高优化收敛性。
- **隐式接触处理**：通过物理引擎展开隐式处理接触和动力学约束，无需手工设计接触惩罚项。
- **异步架构**：PL策略50Hz、MPPI 280Hz、PD 500Hz，分频设计允许各模块按需计算，易于实现。

### 8. 不足与局限
- **实验覆盖有限**：仅在仿真中测试两个基础任务，未评估动态扰动、变地形、复杂动作为（如跳跃、转弯），也未进行硬件实验，缺乏真实世界挑战。
- **依赖RL先验质量**：行为锚定在预训练策略上，若RL先验无法覆盖的新目标差异过大（如反向行走），可能难以修正。
- **未与纯MPPI对比**：未展示RL先验相对于随机采样或零先验的优势，无法明确量化RL引导带来的改进。
- **未做消融研究**：缺乏对knot数量、rollout数量、噪声方差等超参数的敏感性分析。
- **算力报告不完整**：未提供训练GPU详情和MPPI实际延迟分布，280Hz能否在更复杂硬件或真实机器人上维持存疑。

（完）
