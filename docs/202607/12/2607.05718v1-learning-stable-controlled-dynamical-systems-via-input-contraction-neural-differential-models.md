---
title: Learning Stable Controlled Dynamical Systems via Input-Contraction Neural Differential Models
title_zh: 通过输入收缩神经微分模型学习稳定的受控动力系统
authors: Syed Pouladi
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05718v1"
tags: ["query:hum-ctrl"]
score: 8.0
evidence: 用于稳定受控动力系统的输入收缩神经微分模型
tldr: 现有神经微分方程无法严格保证时变控制输入下系统的稳定性，本文提出输入收缩神经微分模型（ICNDM），通过输入依赖的收缩正则化实现增量指数收敛，同时学习非自主向量场和广义黎曼度量。在混沌振荡器和永磁同步电机上，ICNDM显著降低长期预测误差，对输入扰动具有强鲁棒性，为非自治系统学习提供了稳定性和泛化保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05718v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05718v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 835, \"height\": 466, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05718v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1071, \"height\": 312, \"label\": \"Table\"}]"
motivation: 现有方法处理控制输入时缺乏结构保证或仅限恒定输入，无法保证时变输入下系统的增量稳定性。
method: 提出ICNDM框架，通过输入编码器和参数化度量网络同时学习向量场和输入依赖的收缩度量，施加正则化实现指数收敛。
result: 在高度非线性混沌振荡器和PMSM实验数据上，ICNDM长期预测误差大幅降低，优于现有神经微分模型。
conclusion: ICNDM实现了时变控制输入下稳定动力系统的学习，具备输入-状态收缩性质，适用于控制与科学机器学习。
---

## 摘要
从观测数据中学习动力系统的连续时间表示已成为数据驱动控制和科学机器学习的基石。然而，现有的神经微分方程要么以启发式方式处理外部控制输入而不提供严格的结构保证，要么在恒定或消失输入的严格假设下强制执行稳定性。本文提出了输入收缩神经微分模型（ICNDM），这是一种新颖的深度学习框架，它无缝地结合了时变控制输入，同时通过输入依赖的收缩正则化确保增量指数收敛。通过利用嵌入的输入编码器和参数化度量网络，所提出的架构同时学习非自治神经向量场和广义黎曼收缩度量。我们推导了输入依赖收缩的充分条件，并在有界外部激励下正式建立了输入到状态的收缩性质。在高度非线性的混沌振荡器和永磁同步电机（PMSM）驱动系统的实验数据上的大量数值评估表明，与最先进的神经微分基准相比，ICNDM在长时域 rollout 误差上实现了显著降低，并且展现出对输入扰动的优越结构鲁棒性。

## Abstract
Learning continuous-time representations of dynamical systems from observation data has emerged as a cornerstone of data-driven control and scientific machine learning. However, existing neural differential equations either treat external control inputs heuristically without providing strict structural guarantees, or enforce stability properties under the restrictive assumption of constant or vanishing inputs. This paper proposes the Input-Contraction Neural Differential Model (ICNDM), a novel deep learning framework that seamlessly incorporates time-varying control inputs while ensuring incremental exponential convergence via input-dependent contraction regularization. By leveraging an embedded input encoder and a parameterized metric network, the proposed architecture learns both the non-autonomous neural vector fields and a generalized Riemannian contraction metric simultaneously. We derive sufficient conditions for input-dependent contraction and formally establish an input-to-state contraction property under bounded external excitations. Extensive numerical evaluations on highly nonlinear chaotic oscillators and experimental data from a Permanent Magnet Synchronous Motor (PMSM) drive system demonstrate that ICNDM yields substantial reductions in long-horizon rollout errors and exhibits superior structural robustness against input perturbations compared with state-of-the-art neural differential benchmarks.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有神经微分方程在处理受控动力系统（非自治系统）时，要么以启发式方式直接拼接控制输入，缺乏严格的结构稳定性保证；要么只能在恒定或消失输入的假设下保证收缩性（contraction），无法处理时变控制输入。这导致在长时域 rollout 中误差积累、状态发散，且对外部扰动极其敏感。
- **研究动机**：实际工程系统（协作机器人、自动驾驶、电力电子等）普遍受到时变控制作用，数据驱动建模需要在保证预测精度的同时，确保轨迹长时间稳定性和对输入扰动的鲁棒性。
- **整体含义**：本文填补了输入感知建模与结构稳定性之间的空白，提出**输入收缩神经微分模型（ICNDM）**，首次在非自治神经微分方程中同时实现严格增量指数收敛和输入到状态收缩性质，为数据驱动控制与科学机器学习提供了理论基础和实用框架。

## 2. 论文提出的方法论

### 核心思想
- 将**收缩理论**（Contraction Theory）推广到含时变输入的非自治系统：通过学习状态依赖的黎曼度量张量和神经向量场，施加正则化使得沿任意轨迹的微分距离指数收敛。
- 同时学习输入编码器、神经向量场和度量网络，三者联合优化，实现输入依赖的收缩保证。

### 关键技术细节
1. **模型架构**：
   - 状态方程：$\dot{x} = f_\theta(x, E_\phi(u))$，其中 $E_\phi$ 是输入编码器，将原始低维控制 $u$ 映射到高维潜在空间。
   - 度量网络 $M_\omega(x)$：通过 Cholesky 分解保证正定性：$M_\omega(x) = L_\omega(x)L_\omega(x)^T + \epsilon I$。
2. **收缩条件**：
   - 定义非自治收缩矩阵：$\Psi(x,u) = A_\theta^T M_\omega + M_\omega A_\theta + \dot{M}_\omega$，其中 $A_\theta$ 是状态雅可比，$\dot{M}_\omega$ 是度量沿轨迹的全导数。
   - 施加约束：$\Psi(x,u) \preceq -\beta M_\omega(x)$，保证指数收敛率 $\beta>0$。
3. **损失函数**（多目标优化）：
   - 预测损失 $L_{\text{pred}}$：1 步或多步轨迹 MSE。
   - 收缩损失 $L_c$：软谱屏障惩罚，促使 $\lambda_{\max}(\Psi + \beta M_\omega) \leq 0$。
   - 输入鲁棒损失 $L_u$：对输入雅可比 $\partial f_\theta / \partial u$ 的 Frobenius 范数惩罚。
   - 总损失：$L = L_{\text{pred}} + \lambda_c L_c + \lambda_u L_u$。
4. **理论保证**：
   - **定理1**：若收缩损失为0，则同一控制输入下任意两轨迹误差指数衰减：$\|x_1(t)-x_2(t)\| \leq \sqrt{\alpha_2/\alpha_1} e^{-\beta t/2} \|x_1(0)-x_2(0)\|$。
   - **定理2**（输入到状态收缩）：在 Lipschitz 假设下，不同输入下的轨迹误差有界：$\|x_1(t)-x_2(t)\| \leq \kappa e^{-\beta t/2}\|x_1(0)-x_2(0)\| + \frac{2\alpha_2 L_u}{\alpha_1 \beta} \sup_{0\le\tau\le t}\|u_1(\tau)-u_2(\tau)\|$。

## 3. 实验设计

- **数据集/场景**：
  1. **非线性混沌振荡器**（两个合成系统）：
     - Duffing 振荡器（受迫非线性弹簧系统）
     - Van der Pol 振荡器（自激振荡系统）
     均采用多频正弦输入产生丰富的非自治行为。
  2. **物理实验平台**：永磁同步电机（PMSM）驱动系统，状态变量为 $i_d, i_q$，控制输入为 $v_d, v_q, \omega_e$，具有强交叉耦合和非线性。
- **基准模型**（5个）：
  1. Standard Neural ODE [6]：输入简单拼接。
  2. Controlled Neural ODE [7]：基于输入路径的连续驱动。
  3. ICODE [1]：专门建模外部输入的结构。
  4. ControlSynth Neural ODE [2]：保证收敛的框架。
  5. Differential Neural Network (DNN) [3]：工业系统的结构稳定递归网络。
- **评价指标**：MSE（1步预测和200步长时域 rollout）。
- **实现细节**：PyTorch + torchdiffeq，AdamW 优化器，余弦退火学习率，训练 1000 个 epoch。

## 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量或训练总时长。仅提到使用 PyTorch 和 torchdiffeq 库，训练 1000 epoch。未提供硬件配置（如单卡/多卡、显存等）。
- 元数据中也无相关算力信息。

## 5. 实验数量与充分性

- **实验组数**：
  - 两组非线性混沌系统（Duffing 和 Van der Pol），每个系统对比了 5 个基线，报告了 1-step 和 200-step rollout 的 MSE（共 2×6=12 个数值）。
  - PMSM 实验：展示了 500-step 长时域 rollout 的定性曲线（图1）和不同输入噪声水平下的 MSE 曲线（图2）。
  - 鲁棒性分析：噪声标准差 $\sigma_u$ 从 0 到 0.2 变化，对比 4 个基线（ICODE, ControlSynth, Controlled NODE, Standard NODE），但未列出具体数值表。
- **充分性评估**：
  - **优点**：覆盖了合成数据和真实电机数据，验证了长时域稳定性和噪声鲁棒性，基线选择较全面。
  - **不足**：
    - 缺少**消融实验**（如去除 $L_c$、$L_u$ 或输入编码器的对比）。
    - 缺少对超参数 $\lambda_c, \lambda_u, \beta$ 的敏感性分析。
    - 仅在两个混沌系统和一个电机系统上评估，未涉及更高维系统或实际机器人控制场景。
    - 未提供置信区间或多次重复实验的统计结果（可能为单次随机种子）。
    - 噪声鲁棒性仅给出曲线，未给出定量 MSE 数值表。
  - **公平性**：所有模型在相同框架和优化器下训练，但未提及是否对基线进行了超参数调优以确保公平比较。

## 6. 论文的主要结论与发现

1. **ICNDM 在长时域 rollout 误差上显著优于所有基线**：在 Duffing 系统上，200-step MSE 仅为 1.02e-3，比最好的 ICODE（2.15e-2）低约 20 倍；在 Van der Pol 系统上类似（1.89e-3 vs 4.38e-2）。
2. **ICNDM 展现出对输入噪声的强鲁棒性**：随着噪声标准差增加，ICNDM 的跟踪误差增长最慢，而标准神经 ODE 迅速发散，验证了理论上的输入到状态收缩性质。
3. **在 PMSM 物理系统上**，ICNDM 的 500-step 轨迹与真实值高度吻合，而其他模型（特别是标准 NODE）随时间显著漂移。
4. **理论贡献**：首次为非自治神经微分方程提供了输入依赖的增量指数收敛保证和输入到状态收缩界，且该界可以通过训练中强制正则化实现。

## 7. 优点

- **方法创新性**：将收缩理论与输入感知建模有机结合，提出同时学习向量场和度量网络的联合优化框架，区别于以往分开学习或仅限自治系统的方法。
- **理论完备性**：严格推导了输入依赖收缩的充分条件，并给出了输入到状态收缩的显式界，具有可验证性。
- **正则化设计巧妙**：利用软谱屏障损失实现约束，避免硬约束导致的训练困难；输入雅可比正则化增强鲁棒性。
- **实验验证有力**：涵盖高度非线性的混沌系统和实际工业电机数据集，验证了方法的实际效果。
- **代码开源**（可预期）：基于 PyTorch 和 torchdiffeq，具有可复现性。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了三个系统（两个混沌 + 一个电机），缺乏对更高维、更复杂系统（如机器人、流体力学）的验证。
- **缺乏消融分析**：未分离不同损失项的贡献，未展示输入编码器或度量网络设计的作用。
- **统计可靠性缺失**：未报告多次运行的标准差或置信区间，可能结果存在随机波动。
- **超参数敏感性未知**：$\lambda_c, \lambda_u, \beta$ 的选择对性能影响未讨论，实际操作中可能需要调参。
- **训练复杂度**：联合优化度量网络和向量场可能比标准神经 ODE 更耗时，但文中未比较训练时间或收敛速度。
- **假设强度**：要求向量场对输入全局 Lipschitz 连续，且度量满足均匀正定界（$\alpha_1 I \preceq M \preceq \alpha_2 I$），实际中可能难以严格保证（虽通过正则化鼓励，但非严格强制）。
- **应用局限性**：方法面向开环识别，未考虑闭环控制场景或模型预测控制中的实时性约束；未提及对离散时间系统的扩展。

（完）
