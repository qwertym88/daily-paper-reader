---
title: Physics-enhanced reinforcement learning for real-time optimal control of dynamical systems
title_zh: 物理增强的强化学习用于动力系统的实时最优控制
authors: "Matteo Tomasetto, Nicolò Botteghi, Gabriele Bruni, Andrea Manzoni"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16177v1"
tags: ["query:hum-ctrl"]
score: 8.0
evidence: 物理增强强化学习桥接RL与传统最优控制
tldr: 强化学习在复杂动力系统控制中面临样本效率低、高维空间探索困难的问题。本文提出物理增强强化学习(PEARL)范式，利用动力学的可微性，通过actor-adjoint算法结合自动微分计算短时策略梯度，并利用神经网络近似伴随敏感性估计长期回报。在非定常流参数化导航任务中，PEARL显著减少环境交互次数，克服了长期梯度不稳定，性能超越SOTA算法。该方法实现了样本高效、跨场景泛化，并能扩展到高维状态动作空间，为实时最优控制提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16177v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1788, \"height\": 1007, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16177v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1848, \"height\": 1036, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16177v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1918, \"height\": 1918, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16177v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1846, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16177v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1789, \"height\": 1927, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16177v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1793, \"height\": 1908, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16177v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1800, \"height\": 1992, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16177v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 926, \"height\": 343, \"label\": \"Figure\"}]"
motivation: 现有强化学习方法在高维动力系统控制中样本效率低，难以实时优化。
method: 提出PEARL，利用动力学可微性，通过actor-adjoint算法结合自动微分与伴随敏感性，减少交互并稳定梯度。
result: 在非定常流导航中，PEARL以更少交互超越SOTA，泛化能力强，适用于高维系统。
conclusion: PEARL为高维动力系统实时最优控制提供了一种高效、可扩展的物理增强RL框架。
---

## 摘要
强化学习最近作为一种有前景的非线性和复杂动力系统的反馈控制策略出现。然而，强化学习算法样本效率低，需要与环境进行大量交互才能合成最优控制策略。因此，由于高维空间中探索-利用困境带来的维度灾难，强化学习的应用通常局限于稀疏的传感器和执行器。在本工作中，我们通过一种新颖的物理增强强化学习（PEARL）范式，将强化学习与传统动力系统最优控制相融合，该范式专门用于控制高维和参数化动力系统，利用其动力学的可微性。具体来说，PEARL采用了一种演员-伴随算法，该算法利用自动微分来在短视界上计算策略梯度，并通过神经网络近似未来回报的伴随敏感性，显著减少了环境交互次数，同时缓解了长期梯度不稳定性。通过非定常流中两个具有挑战性的参数化导航问题，我们展示了PEARL（i）有效利用可微环境，在性能上超越最先进的强化学习算法；（ii）由于物理引导的策略学习，样本效率高；（iii）在多个场景中具有泛化能力，这在处理参数化系统时至关重要；（iv）能够将强化学习扩展到高维状态和动作空间，而无需低维状态表示或多智能体策略。

## Abstract
Reinforcement learning (RL) has recently emerged as a promising feedback control strategy for nonlinear and complex dynamical systems. However, RL algorithms are sample inefficient and require a large number of interaction with the environment to synthesize optimal control strategies. Consequently, applications of RL are typically limited to sparse sensors and actuators due to the curse of dimensionality entailed by the exploration-exploitation dilemma in high-dimensional spaces. In this work, we bridge RL and traditional optimal control for dynamical system with a novel Physics-EnhAnced Reinforcement Learning (PEARL) paradigm tailored to the control of high-dimensional and parametric dynamical systems, exploiting the differentibility of their dynamics. Specifically, PEARL employs an actor-adjoint algorithm that leverages automatic differentiation to compute policy gradients over short horizons and adjoint-based sensitivities of future returns approximated via neural networks, significantly reducing the number of environment interactions, while mitigating long-term gradient instabilities. Through two challenging parametric navigation problems in unsteady flows, we show that PEARL (i) effectively exploits differentiable environments to outperform state-of-the-art RL algorithms, (ii) is sample efficient, thanks to the physics-guided policy learning, (iii) generalizes across multiple scenarios, which is crucial when dealing with parametric systems, and (iv) enables scaling RL to high-dimensional state and action spaces, without requiring low-dimensional state representations or multi-agent strategies.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：强化学习（RL）在复杂非线性动力系统的控制中面临样本效率低、高维状态/动作空间下的探索-利用困境，以及长期梯度不稳定性（梯度消失/爆炸）等挑战。传统最优控制（OC）虽能利用物理模型，但通常只能得到开环策略，难以实时自适应。
- **整体含义**：论文旨在桥接RL与OC，提出一种**物理增强强化学习（PEARL）**范式，通过利用环境的可微性，在短视界内用自动微分（AD）计算精确梯度，并借助神经网络近似长程伴随敏感性，从而在减少环境交互的同时实现实时闭环最优控制，并扩展到高维参数化动力系统。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：结合**短视界优化**与**伴随方法**，在可微环境中进行策略学习。将策略梯度拆分为短视界内的AD精确梯度 + 基于神经网络的终端伴随近似，以兼顾梯度稳定性与长期信用分配。
- **关键技术细节**：
  - **环境可微性假设**：系统动态由可微的 ODE/PDE 描述（如 \( \dot{y} = f(y, u, \mu) \)），离散后为 \( y_{k+1} = F(y_k, u_k, \mu) \)。
  - **Actor‑Adjoint 算法**：
    1. 将整个轨迹划分为长度为 \( h \) 的短视界（\( h \ll N_t \)）。
    2. 在每个短视界内，通过反向模式 AD 计算损失梯度 \( \nabla_\theta J_h \)，其中 \( J_h = \sum_{k=k_0}^{k_0+h-1} L(y_k, \pi(y_k,\mu;\theta),\mu) + \phi(y_{k_0+h}, \mu) \)。
    3. 核心创新：在短视界终端，用一个**伴随网络** \( \varphi(y_{k_0+h}, \mu; \bar{\vartheta}) \) 近似真实伴随变量（即价值梯度） \( \lambda_{k_0+h} = \partial \phi/\partial y_{k_0+h} + \varphi \)，而非像 SHAC 那样直接近似价值函数。
    4. 伴随网络通过 TD(λ) 目标进行训练，目标来自经典伴随方程 \( \bar{\lambda}_k = (\partial F/\partial y + \partial F/\partial u \cdot \partial \pi/\partial y)^\top [\partial L/\partial y_{k+1} + \gamma \bar{\lambda}_{k+1} + (1-\gamma)\varphi_{k+1}] \)，从而提供物理信息更丰富的梯度信号。
  - **算法流程**（文字说明）：
    1. 初始化策略网络 \( \pi \)、伴随在线网络 \( \varphi_\vartheta \) 和伴随目标网络 \( \varphi_{\bar{\vartheta}} \)。
    2. 每轮 episode 中，从初始状态开始执行策略，每 \( h \) 步为一个短视界。
    3. 对于每个短视界：
        - 用 AD 计算短视界损失 \( J_h \) 关于 \( \theta \) 的梯度，其中终端伴随由目标网络提供。
        - 更新策略参数 \( \theta \)（梯度下降）。
        - 收集该短视界内的状态-动作序列，计算 TD(λ) 目标并用 MSE 更新伴随在线网络。
        - 软更新伴随目标网络：\( \bar{\vartheta} \leftarrow \alpha \bar{\vartheta} + (1-\alpha) \vartheta \)。
    4. 重复直至收敛。

## 3. 实验设计

- **场景**：两个参数化导航问题，均在**非定常双涡流（Double Gyre）流场**中：
  - **Leader‑Follower 游戏**（低维）：单一跟随者（agent）需追踪被流场被动输运的领导者。状态为位置和流速，动作为控制速度。含密集奖励和稀疏奖励两种设置。
  - **Mean‑Field Leader‑Follower 游戏**（高维）：领导者在流场中运动，跟随者是定义在整个矩形域上的高斯密度（由扩散-平流 PDE 控制），动作是分布式的空间控制场。状态维度 \(N_y=2145\)，动作维度 \(N_u=4290\)。
- **Benchmark / 对比方法**：
  - 无模型 RL：PPO、TD3。
  - 基于 AD 的 RL：BPTT（完整视界）、截断 BPTT（短视界无修正）、SHAC（短视界+价值函数修正）。
  - 额外对比了 SHAC 的原始网络架构（无双编码）在平均场问题中。
- **评价指标**：训练和评估累积奖励曲线，以及可视化轨迹/密度分布。

## 4. 资源与算力

- **文中未明确说明**具体的GPU型号、数量、训练时长等信息。仅提到使用了可微环境（基于 PyTorch / JAX / FEniCS-adjoint 等框架），但未给出算力细节。因此无法评估硬件资源需求。

## 5. 实验数量与充分性

- **实验数量**：两个主要场景，每个场景包含多种基线对比（Leader‑Follower 含 7 种方法；Mean‑Field 含 5 种方法）。Leader‑Follower 问题测试了密集和稀疏两种奖励函数。
- **充分性**：
  - 对比了最先进的 RL 算法（PPO、TD3）和基于 AD 的方法（BPTT、截断BPTT、SHAC），且采用相同网络架构（双编码）和超参数设置，比较公平。
  - 在 Leader‑Follower 问题中提供了多组初始条件的可视化结果，展示泛化能力。
  - 在 Mean‑Field 问题中额外对比了 SHAC 原始架构（SHAC‑orig），证明了双编码结构的必要性。
  - 缺少对**短视界长度 \(h\)**、**折扣因子 \(\gamma\)**、**TD 参数 \(\lambda\)** 等超参数的消融或敏感性分析，但论文声称超参数选取与文献一致。
  - 实验覆盖了低维和高维、密集和稀疏奖励，整体较为充分。

## 6. 论文的主要结论与发现

- PEARL 在**两个导航任务**中均取得了优于所有对比方法的训练和评估奖励，尤其在稀疏奖励和高维场景下优势明显。
- PEARL 的**样本效率**远高于无模型方法（PPO、TD3），在 Leader‑Follower 中只需 1500 episode 即可收敛，而 PPO/TD3 在稀疏奖励下几乎无法学习。
- PEARL 成功将 RL 扩展到**高维状态/动作空间**（\(N_y=2145, N_u=4290\)），无需降维或多智能体分解，而 PPO/TD3 在此任务中因梯度爆炸而失效。
- 通过直接学习**价值梯度（伴随）**而非价值函数，PEARL 比 SHAC 能提供更准确的长期梯度信号，从而获得更好的策略性能。

## 7. 优点

- **方法创新**：将伴随灵敏度分析嵌入 RL 策略优化，首次提出 actor‑adjoint 架构，既解决了长期梯度不稳定性，又利用了物理先验，减少无意义探索。
- **样本高效**：短视界 AD + 伴随近似使策略梯度估计更准确，显著降低环境交互次数。
- **可扩展性**：直接处理高维分布式控制，无需降阶模型或多智能体分解。
- **泛化能力**：通过参数化策略和伴随网络，单策略可适应不同初始条件和环境参数。
- **实验设计严谨**：与多种基线方法在同一网络架构和超参数下进行比较，并提供了可视化分析。

## 8. 不足与局限

- **依赖环境可微性**：要求动力系统是确定性的且完全可微，对于非光滑或随机系统可能不适用。
- **短视界偏差依然存在**：虽然用伴随网络近似长期效应，但终端伴随的近似精度依赖于神经网络能力，可能引入偏差。
- **算力与可部署性**：论文未报告计算资源消耗，每步都需要通过完整可微求解器进行 AD，可能计算开销较大。
- **实验覆盖有限**：仅测试了双涡流场景中的导航问题，未在更复杂（如湍流、多相流）或真实机器人系统中验证。
- **缺乏超参数敏感性分析**：对短视界长度 \(h\)、软更新系数 \(\alpha\)、TD 参数 \(\lambda\) 等关键超参数的影响未深入探讨。
- **消融实验不足**：未单独消融“伴随网络”与“价值网络”的贡献，也未对比不同网络结构（如无双编码的单分支网络）的影响（仅在平均场问题中对 SHAC 做了单结构对比，但非系统消融）。

（完）
