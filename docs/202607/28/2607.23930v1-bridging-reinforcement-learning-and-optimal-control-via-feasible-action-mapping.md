---
title: Bridging Reinforcement Learning and Optimal Control via Feasible Action Mapping
title_zh: 通过可行动作映射桥接强化学习与最优控制
authors: "Stefan Richter, Alberto Giammarino, Guillem Torrente, Sam Blakeman, Peter Dürr"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23930v1"
tags: ["query:hum-ctrl"]
score: 8.0
evidence: 结合强化学习与最优控制处理约束动力学系统
tldr: 受约束动态系统控制需要兼顾任务效率与安全可行性。本文提出FAOC框架，通过计算高效的优化映射将强化学习动作转化为最优控制问题的可行参数集，严格保证约束满足。该方法无需专家设计的抽象动作空间，在机器人乒乓球任务中，FAOC在样本效率和闭环性能上均超越现有基线。它有效结合了最优控制的预测安全性与强化学习的灵活性，为约束控制提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23930v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1621, \"height\": 392, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1314, \"height\": 875, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1617, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1143, \"height\": 1814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1604, \"height\": 619, \"label\": \"Figure\"}]"
motivation: 现有方法难以同时保证约束系统的递归可行性与强化学习的探索效率，且RL动作空间常依赖专家设计。
method: 提出Feasible Action Mapping，将RL抽象动作实时映射为OCP可行参数集，通过优化求解确保约束严格满足。
result: 在机器人乒乓球模拟实验中，FAOC相比现有基线在样本效率和闭环性能上均取得显著提升。
conclusion: FAOC成功融合RL与OC，既保证约束可行性又保持学习灵活性，适用于复杂实时控制任务。
---

## 摘要
运行受约束的动力学系统需要控制器在强制执行递归可行性和安全约束的同时高效完成复杂任务。为应对这些相互冲突的需求，我们提出了最优控制的可行动作（FAOC），一种集成强化学习（RL）与最优控制（OC）的新型控制框架。关键贡献是一种计算高效、基于优化的映射算法，该算法将RL智能体的动作从静态抽象集转换为最优控制问题（OCP）的状态依赖可行参数集，从而严格保证动力学系统约束的满足。因此，FAOC有效结合了OC的可预测安全性与RL的灵活性。与先前工作不同，RL智能体的抽象动作空间无需专家或启发式设计，且OCP公式不会因RL无法保证可行性而受损。我们将该方法应用于机器人乒乓球实时运动规划，该任务包含了这些挑战。通过仿真实验，我们证明FAOC在样本效率和闭环性能方面均优于最先进的基线方法。

## Abstract
Operating constrained dynamical systems requires controllers to efficiently solve complex tasks while enforcing recursive feasibility and safety constraints. To address these competing requirements, we present Feasible Action for Optimal Control (FAOC), a novel control framework integrating Reinforcement Learning (RL) and Optimal Control (OC). The key contribution is a computationally efficient, optimization-based mapping algorithm that transforms the RL agent's action from a static abstract set into a state-dependent feasible parameter set of the Optimal Control Problem (OCP), guaranteeing strict satisfaction of the dynamical system's constraints. Thus, FAOC effectively combines the predictable safety of OC with the flexibility of RL. In contrast to prior work, the abstract action space of the RL agent does not require expert or heuristic design, and the OCP formulation is not compromised by the inability of RL to guarantee feasibility. We apply our approach to real-time motion planning for robot table tennis, which encapsulates these challenges. Via simulated experiments, we show that FAOC outperforms state-of-the-art baselines in both sample efficiency and closed-loop performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
受约束的动力学系统（如机器人）在运行时需要同时满足任务效率、安全约束和递归可行性。传统强化学习（RL）灵活但难以严格保证约束满足；最优控制（OC）能显式处理约束但计算复杂、难以应对长时域稀疏奖励问题。现有结合RL与OC的框架中，要么将OC参数化并让RL学习参数，但参数空间常依赖专家设计或启发式边界，且参数选择不当会导致OCP不可行。本文提出**Feasible Action for Optimal Control (FAOC)**，通过一个计算高效的优化映射算法，将RL的抽象动作（静态简单集）实时转换为OCP的状态依赖可行参数集，在保证约束严格满足的同时保持RL的探索灵活性，从而桥接RL和OC。

### 2. 论文提出的方法论
- **核心思想**：在RL与底层参数化OCP之间插入一个可逆映射层，将RL智能体输出的抽象动作（来自静态凸集 $\bar{\mathcal{A}}$，如超立方体 $[-1,1]^n$）映射到OCP的可行参数集 $\mathcal{P}(x)$，确保无论RL采样什么动作，OCP始终可行。
- **关键技术细节**：
  - **拓扑条件**：证明在满足一定条件下（如OCP约束凸、决策集紧致等），$\mathcal{P}(x)$ 是紧致、实心、凸的（引理1）；线性MPC加上全行秩终端约束即可满足（推论1）。
  - **通用径向映射算法（算法1）**：给定基集 $\mathcal{X}$ （抽象动作集）和 $\mathcal{Y}$ （$\mathcal{P}(x)$），分别计算内点 $x_c, y_c$，对于输入 $x$，沿方向 $d=x-x_c$ 求与 $\mathcal{X}$ 边界的缩放因子 $\alpha$，再通过方向变换 $\phi$ 得到 $d'$，求与 $\mathcal{Y}$ 边界的 $\beta$，输出 $y = y_c + \frac{\beta}{\alpha} d'$。映射是双射的（命题1）。
  - **方向变换**：
    - 二维面积匹配变换：通过匹配两凸集在角度上的累积面积分布函数，消除点聚集现象（命题2、3）。
    - 任意维线性变换：用椭球代理集（如最大体积内接椭球）构造线性变换矩阵 $\Phi$，近似几何扭曲（命题4）。
  - **隐式目标集的处理**：当 $\mathcal{P}(x)$ 由OCP约束隐式定义时，通过提升到决策变量空间计算内点（命题5-7）、shape矩阵（命题8）和边界缩放因子（式8），无需显式几何表示。结合离线计算的线性变换矩阵（例3）实现实时映射。
- **整体流程**：RL输出 → tanh映射为抽象动作 → 径向映射算法 → 得到OCP参数 $p$ → 求解OCP得到控制输入 → 施加于系统。

### 3. 实验设计
- **任务/场景**：机器人乒乓球，使用8自由度移动机械臂，要求实时运动规划（每31.25 Hz一次决策）。在仿真环境中进行训练和评估。
- **Benchmark**：与四种基线控制器对比：
  - `1Dpos`、`1Dvel`、`1Dacc`：基于Kiemel & Kröger（2021）的一维映射方法，RL只输出位置、速度或加速度之一。
  - `2Dsoft`：二维动作空间（位置-速度），但不保证可行性，OCP使用软终端代价。
  - FAOC为本方法。
- **RL算法与架构**：主要使用Soft Actor-Critic (SAC) 搭配 BRO-based 网络（基线实验），以及更强的SimBaV2（分布性critic、更大网络）进行对比。奖励为稀疏奖励（仅在回合结束时给出）。
- **评估指标**：Fraction Balls Returned（回球率）、Generated Topspin/Velocity/Backspin、Goal Distance。每次评估1000回合。每个方法训练5个随机种子。
- **实验分组**：
  1. 基线实验（SAC + BRONets）
  2. 算法与架构影响（SimBaV2）
  3. 低RL频率影响（SimBaV2 + 7.8125 Hz，原为31.25 Hz）

### 4. 资源与算力
论文中**未明确说明**所使用的GPU型号、数量及总训练时长。仅提及模拟环境在计算节点上运行，使用DAQP求解器，并调用了训练pipeline（感谢Sony AI平台团队）。图6中横坐标为环境步数（高达1.2e8），可推测训练计算量较大，但具体硬件规格未给出。

### 5. 实验数量与充分性
- **数量**：三个主要实验，每个实验包含5种方法 × 5个随机种子 = 25个训练实验，每个实验的评估基于1000个回合。
- **充分性**：实验覆盖了不同RL算法（SAC vs SimBaV2）、不同控制频率、多个对比方法。统计上使用了Mann-Whitney U检验，报告p值和显著性标记。评估指标全面（回球率、旋转、速度、精度）。实验设计较为充分且公平（所有方法共用相同RL训练环境和超参数）。
- **客观性**：FAOC取得了统计显著性优势，且未出现反例。不过实验仅在单一机器人乒乓球任务上进行，泛化性论证有限。

### 6. 论文的主要结论与发现
1. FAOC在所有实验中的样本效率和最终性能均优于所有基线，尤其是在低控制频率下优势更加明显。
2. 一维基线（1Dpos等）表现最差，说明可控制自由度不足限制性能。
3. 2Dsoft因含不可行动作导致探索效率下降，而FAOC通过状态依赖的可行映射避免了冗余探索。
4. 随着RL算法和数据效率提升（SimBaV2），FAOC仍然保持优势，说明其动作空间设计本身带来的益处是核心因素。
5. 在真实机器人上已实现与国际乒联职业选手比赛并获胜（引用[1]），但该部分实验细节不在本文中详述。

### 7. 优点
- **严格可行性保证**：任意RL动作经映射后均保证OCP可行，无需松弛变量或惩罚项。
- **无需人工设计动作空间**：抽象动作空间为简单固定凸集，OCP参数空间自动由状态决定。
- **可逆映射**：支持数据增强、专家演示回放等技术。
- **计算高效**：径向映射可解析求解（对多面体），隐式集处理中关键矩阵离线计算，在线成本低。
- **理论完整**：给出拓扑性质证明，并针对二维/高维提出了不同方向变换以减轻点聚集。
- **实验对比充分**：与多种基线、不同RL设置对比，统计验证显著差异。

### 8. 不足与局限
- **凸集假设**：映射算法理论上要求目标参数集为凸集。虽然作者说明在乒乓球任务中非凸性（如避障、非线性运动学）可由RL策略处理，局部OCP仍保持凸，但该方法对更一般的非凸OCP可能不直接适用。
- **单任务验证**：实验仅围绕机器人乒乓球这一特定任务，FAOC在更广泛的受约束控制问题（如自动驾驶、航空航天）上的泛化能力未测试。
- **计算资源未公开**：未提供GPU型号和训练时间，难以复现及评估计算成本。
- **离线预计算要求**：MCIS、MVIE等需离线计算，且对系统模型变化不鲁棒（重新计算代价大）。
- **缩放因子计算依赖有效内点**：内点选择不当可能影响边界缩放数值稳定性，尽管论文提出了鲁棒内点计算方法（命题6、7），但仍可能引入数值误差。
- **对比方法局限性**：1D基线本身较简单，2Dsoft的作者未提及调参过程（如权重 $w_s, W_t$ 的选择可能影响公平性），但论文表示FAOC无需调参。

（完）
