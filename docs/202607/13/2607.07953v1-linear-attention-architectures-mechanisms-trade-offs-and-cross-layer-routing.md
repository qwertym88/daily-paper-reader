---
title: "Linear Attention Architectures: Mechanisms, Trade-offs, and Cross-Layer Routing"
title_zh: 线性注意力架构：机制、权衡与跨层路由
authors: "Tommaso Cerruti, Tim Rieder, George Rowlands, Lingfeng Jin, Imanol Schlag"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07953v1"
tags: ["query:neural-arch"]
score: 9.0
evidence: 线性注意力架构的比较研究
tldr: 自注意力机制的二次复杂度限制了长序列处理。本文比较了四种线性注意力架构，用统一递归记忆符号描述其差异，并在350M参数15B tokens实验中评估。Kimi Delta Attention搭配Muon优化器取得最低验证损失，纯Gated DeltaNet训练吞吐最高。提出跨层值路由（CLVR）机制，通过将下层写值转发到上层，在DeltaNet和Gated DeltaNet上进一步降低损失。该工作为设计高效长序列模型提供了系统对比和新路由思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07953v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1463, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1369, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1696, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1698, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1023, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07953v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1453, \"height\": 790, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07953v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1663, \"height\": 1142, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1720, \"height\": 997, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 897, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1709, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1709, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1081, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1125, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07953v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1709, \"height\": 397, \"label\": \"Table\"}]"
motivation: 克服softmax注意力在处理长序列时的二次计算复杂度瓶颈。
method: 用统一递归记忆符号表达DeltaNet等四种线性注意力机制，并设计跨层值路由（CLVR）将下层写值转发到上层。
result: 350M模型15B tokens训练中，Kimi Delta Attention+Muon损失最低；纯Gated DeltaNet吞吐最高；CLVR使DeltaNet和Gated DeltaNet损失进一步降低。
conclusion: 线性注意力在长序列上更高效，跨层路由能提升性能但需权衡吞吐成本。
---

## 摘要
自注意力机制使每个标记能够从完整上下文中检索信息，但其序列长度的二次成本限制了长上下文下的训练与推理。本文对softmax注意力与四种近期提出的循环线性注意力架构进行了比较研究：DeltaNet、Gated DeltaNet、Kimi Delta Attention和Gated DeltaNet-2。我们以通用的循环记忆表示法描述这些机制，明确揭示了它们在表达能力、记忆衰减、擦除与写入控制、训练吞吐量及实现复杂度方面的差异。实验以350M参数模型为核心，训练15B token，涵盖优化器与学习率比较、混合与纯堆栈对比、序列长度运行时间测量、1.3B和3B参数下的更大规模DeltaNet运行，以及少量下游评估。报告的速度结果衡量训练吞吐量与迭代时间；未提供实证推理速度基准。在报告的350M参数、15B token扫描范围内，采用Muon优化器的Kimi Delta Attention达到了最低最终验证损失；使用AdamW训练的纯Gated DeltaNet堆栈具有最高归一化训练吞吐量；混合堆栈通常以吞吐量为代价改善损失；在我们评估的匹配架构设置中，Muon始终相对于AdamW降低了最终验证损失。我们为DeltaNet类记忆引入并评估了轻量级跨层路由机制。最自然的DeltaNet启发表述——将低层的Delta规则写入误差转发至下一层的值目标——未能在匹配基线之上带来改进。而将路由对齐到隐藏流并转发写入值，在我们报告的匹配运行中带来了适度改进：跨层值路由（CLVR）降低了DeltaNet和Gated DeltaNet的最终验证损失。

## Abstract
Self-attention lets each token retrieve information from the full context, but its quadratic cost in sequence length limits training and inference at long context. This paper presents a comparative study of softmax attention and four recent recurrent linear-attention architectures: DeltaNet, Gated DeltaNet, Kimi Delta Attention, and Gated DeltaNet-2. We express these mechanisms in a common recurrent-memory notation, making explicit how they differ in expressivity, memory decay, erase and write control, training throughput, and implementation complexity. Our experiments center on 350M-parameter models trained for 15B tokens, and include optimizer and learning-rate comparisons, hybrid-versus-pure stack comparisons, sequence-length runtime measurements, larger DeltaNet runs at 1.3B and 3B parameters, and a small set of downstream evaluations. The reported speed results measure training throughput and iteration time; we do not provide an empirical inference-speed benchmark. Within the reported 350M-parameter, 15B-token sweep, Kimi Delta Attention with Muon reaches the lowest final validation loss, a pure Gated DeltaNet stack trained with AdamW has the highest normalized training throughput, hybrid stacks generally improve loss at a throughput cost, and Muon consistently lowers final validation loss relative to AdamW in the matched architecture settings we evaluate. We introduce and evaluate lightweight cross-layer routing mechanisms for DeltaNet-style memories. The most natural DeltaNet-inspired formulation, forwarding a lower layer's delta-rule write error into the next layer's value target, does not improve over matched baselines. Routing into the aligned hidden stream and forwarding the write value instead yields a modest improvement in the matched runs we report: Cross-Layer Value Routing (CLVR) lowers final validation loss for both DeltaNet and Gated DeltaNet.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：标准Softmax自注意力机制在处理长序列时具有二次计算复杂度（O(T²)），导致训练和推理成本过高，限制了Transformer在长上下文场景下的可扩展性。
- **研究动机**：线性注意力通过将注意力权重分解为特征映射内积，可将复杂度降为线性（O(T)），但早期变体牺牲了大量精度。近期DeltaNet系列通过误差修正的delta规则和门控机制缩小了与Softmax注意力的性能差距，但不同变体在表达能力、记忆衰减、写入控制等方面各有权衡，缺乏系统性比较。
- **整体含义**：本文旨在对DeltaNet、Gated DeltaNet、Kimi Delta Attention、Gated DeltaNet-2这四种代表性线性注意力架构进行统一框架下的机理分析与实证对比，同时探索轻量级跨层信息路由方法以缓解深层信息稀释问题。最终目标是为长序列模型设计提供清晰的权衡图谱，而非宣布单一最佳架构。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **统一递归记忆表示法**：将所有线性注意力架构表达为对记忆矩阵W(ⅰ)的递归更新，共享读取规则y(ⅰ) = W(ⅰ)φ(q(ⅰ))，其中φ为特征映射。各变体的差异体现在记忆写入（delta规则）、衰减（标量/通道级）、擦除与写入门的解耦程度上。
- **DeltaNet**：写入残差r(ⅰ) = v(ⅰ) - W(ⅰ-1)κ(ⅰ)，更新为W(ⅰ) = W(ⅰ-1) + β(ⅰ)r(ⅰ)⊗κ(ⅰ)。仅写入当前值未被已有记忆预测的部分，减少键-值关联间的干扰。
- **Gated DeltaNet**：在DeltaNet基础上增加标量衰减门α(ⅰ)，先衰减旧记忆再计算残差：ŵ(ⅰ)_α = α(ⅰ)W(ⅰ-1)κ(ⅰ)，更新为W(ⅰ)=α(ⅰ)W(ⅰ-1)+β(ⅰ)r(ⅰ)_α⊗κ(ⅰ)。提供显式遗忘机制但衰减是全状态的。
- **Kimi Delta Attention**：将标量衰减替换为向量级（通道级）衰减D(ⅰ)_α=Diag(α(ⅰ))，更新为W(ⅰ)=W(ⅰ-1)D(ⅰ)_α+β(ⅰ)r(ⅰ)_α⊗κ(ⅰ)。可沿键特征维度差异化遗忘，控制粒度更细。
- **Gated DeltaNet-2**：进一步将标量beta门解耦为通道级擦除门b(ⅰ)和写入门w(ⅰ)，同时保留通道级衰减。擦除操作指向旧内存中应移除的部分，写入操作控制新值中应存储的通道。更新为W(ⅰ)=Ẇ(ⅰ-1)+r_GDN2⊗κ(ⅰ)，其中Ẇ(ⅰ-1)=W(ⅰ-1)D(ⅰ)_α，r_GDN2 = (w(ⅰ)⊙v(ⅰ)) - Ẇ(ⅰ-1)(b(ⅰ)⊙κ(ⅰ))。
- **跨层路由机制**：
  - **CLER (Cross-Layer Error Residuals)**：将下层delta规则写入误差r_l,t注入上层值目标：ṽ_l,t = v_l,t + Γ_l ρ(r_p(l),t)。但实验显示无效。
  - **CLVR (Cross-Layer Value Routing)**：将下层写入值v_l,t（而非误差）通过零初始化的投影矩阵P_l注入共享残差流：ε_l,t = P_l v_l,t，h_l,t ← h_l,t + ε_l,t。零初始化确保训练开始时与基线无偏差，投影可低秩分解。此方法保留线性时间结构，在DeltaNet和Gated DeltaNet上带来小幅验证损失降低。
- **混合堆栈 vs 纯堆栈**：混合堆栈每三个线性注意力层插入一个Softmax注意力层（2:1模式），以性能换取部分吞吐量；纯堆栈全部使用线性注意力。

## 3. 实验设计
- **数据集与Tokenizer**：使用FineWeb-Edu数据集（99/1/0训练/验证/测试划分），LLaMA2分词器。
- **基准与对比方法**：
  - 主要对比Softmax注意力、DeltaNet、Gated DeltaNet、Kimi Delta Attention、Gated DeltaNet-2五种架构。
  - 优化器对比：AdamW与Muon/NorMuon。
  - 学习率消融：从10⁻⁴到10⁻³范围扫描。
  - 混合堆栈 vs 纯堆栈对比。
  - 序列长度缩放实验：4k、16k、32k tokens测量迭代时间。
  - 更大规模：DeltaNet在1.3B参数/40B tokens和3B参数/60B tokens上的运行。
  - 下游评估：HellaSwag（准确率+归一化准确率）、PIQA、WinoGrande三个零样本任务。
  - 跨层路由消融：在DeltaNet和Gated DeltaNet上对比CLER、CLER-H（误差注入隐藏流）、CLVR（值注入隐藏流），以及注意力残差基线等。
- **评估指标**：验证损失（交叉熵，越低越好）、训练吞吐量（相对速度，归一化）、迭代时间（秒/迭代）。

## 4. 资源与算力
- **硬件**：单个GH200节点（NVIDIA Grace Hopper超级芯片），包含4个GPU，在CSCS Alps系统上运行。
- **训练时间与算力细节**：
  - 主对比：350M参数模型训练15B tokens，使用bf16精度，全局批次大小128，序列长度4096。
  - 较大规模：1.3B参数模型训练40B tokens，3B参数模型训练60B tokens，但未明确报告硬件节点数或总GPU小时数。
- **结论**：文中明确提到了单节点4 GPU的具体配置，但未给出总训练时长或能耗等更细粒度的算力度量。这是报告中的已知局限。

## 5. 实验数量与充分性
- **实验组数**：
  - 350M/15B主对比：包含Softmax、DeltaNet、Gated DeltaNet、Kimi Delta Attention、Gated DeltaNet-2分别在AdamW和Muon下、混合和纯堆栈的组合，共约16行结果（表2）。
  - 学习率消融：9种架构/优化器组合×5个学习率，共45个运行点（图4、5）。
  - 序列长度缩放：3种配置（Softmax、GDN混合、GDN纯）在3个长度下测量。
  - 更大规模DeltaNet：1.3B规模4个运行（含不同LR和CLER），3B规模3个运行。
  - 下游评估：多个检查点的零样本结果（表4、5）。
  - 跨层路由消融：CLER在4种设置下的对比（表6），CLER-H与CLVR在2种主机×3种规模下的对比（表7），以及额外控制实验（惊喜门控、拼接、输出注入等单种子结果）。
- **充分性与客观性评价**：
  - **优点**：控制变量严格（数据、分词器、序列长度、批次、精度、硬件固定），优化器、学习率、堆栈模式、架构均做了系统扫描。跨层路由设计包含负对照（CLER失效）和多种消融，逻辑清晰。
  - **不足**：所有比较均为单次运行（single run），未提供多次种子的均值与标准差，因此无法进行统计显著性检验。结论中的损失差（尤其是小于10⁻³的差异）应谨慎解读。下游评估仅覆盖三个任务，未测试长上下文或记忆密集任务。混合堆栈与纯堆栈的对比依赖于固定插入比例（1/3），未系统变化比例。更大规模路由对比缺失DeltaNet在1.3B/40B的报告。作者明确承认了这些局限。

## 6. 论文的主要结论与发现
1. **架构权衡非单维度排名**：Kimi Delta Attention + Muon在混合堆栈下取得了最低最终验证损失（2.273），但吞吐量相对较慢（70.9%）。纯Gated DeltaNet + AdamW在纯堆栈下取得最高归一化训练吞吐量（100%），但最终损失较高（2.433）。混合堆栈通常改善损失但降低吞吐。
2. **Muon优化器一致优于AdamW**：在所有匹配架构/堆栈设置中，Muon均降低了最终验证损失。但最佳学习率因架构和优化器而异（Muon偏好~3×10⁻⁴，AdamW线性注意力偏好~10⁻³）。
3. **序列长度缩放优势**：32k token时，Softmax迭代时间3.37s（相比4k增长+192%），Gated DeltaNet混合1.56s（+65%），纯堆栈0.96s（+8%）。纯堆栈几乎不受序列长度影响。
4. **跨层路由：CLVR有效但增益较小**：
   - 将下层写入误差注入值空间（CLER）无效。
   - 将写入值通过零初始化投影注入共享残差流（CLVR）在DeltaNet和Gated DeltaNet上均带来最终验证损失的小幅降低（350M/1B约-0.01，350M/15B约-0.002~-0.006，1.3B/40B的GDN约-0.002）。
   - 增益随训练延长和模型增大而缩小，但始终不反转。路由值优于路由误差。
5. **下游评估**：CLVR与基线在HellaSwag和PIQA上相当，WinoGrande上略有提高但不可靠，未见退化。
6. **注意力残差基线与CLVR对比**：注意力残差在DeltaNet上改善（-0.0101）但在Gated DeltaNet上略差（+0.0030），CLVR则更均匀。

## 7. 优点
- **统一的递归记忆视角**：将四种变体表示为同一形式，便于理解设计空间中的连续演变（从原始delta规则到通道级衰减再到解耦擦写）。分析部分清晰、有洞察力。
- **实验设计系统且公平**：固定数据、硬件、超参数框架，允许架构间可比；区分优化器-学习率交互，避免常见偏差；混合/纯堆栈对比突出实际部署权衡。
- **跨层路由研究严谨**：先证明CLER失败（否定结果），再诊断原因为空间不匹配，然后设计CLVR，并包含多个消融（路由值 vs 误差、零初始化、低秩投影、参数匹配控制、注意力残差基线）来归因效果。这种系统化消融提升了结果的可信度。
- **代码开源**：提供Megatron-LM实现和运行脚本，便于复现。

## 8. 不足与局限
- **统计显著性缺失**：所有结果来自单次运行，无标准偏差或置信区间。许多损失差（尤其是路由增益<0.002）可能处于噪声范围内。作者明确建议对小于10⁻³~10⁻²的差异应谨慎解读。
- **下游评估范围狭窄**：仅HellaSwag、PIQA、WinoGrande三个简短任务，未测试长上下文检索、多步推理或记忆密集场景（如key-value检索、passkey），而跨层路由的优势可能恰恰在这些场景中显现。
- **更大规模路由对比不完整**：CLVR在DeltaNet的1.3B/40B规模下未报告；Kimi Delta Attention和Gated DeltaNet-2主机上的路由实验完全缺失。文中保留为未来工作。
- **混合堆栈中Softmax比例固定**：未探索不同插入比例（如每2层或每4层一个Softmax）对损失与吞吐的影响。
- **无推理吞吐量基准**：仅测量训练迭代时间，未报告推理阶段的吞吐量或内存占用，这对部署决策至关重要。
- **Phi特征映射细节未深究**：线性注意力依赖特定的特征映射φ，文中未比较不同映射选择对性能的影响（如余弦相似度、随机特征等），仅使用现有架构的默认设置。
- **跨层路由增益随规模缩小**：1.3B/40B的GDN上CLVR增益仅-0.0019，而350M/15B上为-0.0059，趋势可能进一步收缩。是否在更大规模和更长训练后完全消失需要更严格验证。
- **实验结果记录标准与复现风险**：部分探索性变体（如惊喜门控路由、输出注入）被标记为“不完整记录”，未纳入主要结论。作者公开了运行脚本以减轻复现困难。

（完）
