---
title: Sharp Stability Threshold and Certification for Designing Stable Residual Architectures
title_zh: 设计稳定残差架构的尖锐稳定性阈值与认证
authors: "Hyemin Gu, Michael Tyrrell, Tuhin Sahai, Markos A. Katsoulakis"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14576v1"
tags: ["query:neural-arch"]
score: 9.0
evidence: 对残差架构稳定性的理论分析，为残差网络设计提供原则
tldr: 深度残差网络训练常因残差块速度场增长过快而不稳定。本文提出亚线性增长原则，要求速度场的输入幅度指数q≤1，通过经典ODE理论和最优控制分析证明q=1是稳定阈值。进一步提出建筑原语级别的指数算术认证方法，可将超临界Mamba块从q=5降至q=1，无需归一化层。实验验证满足q≤1的Mamba和PatchTST变体均稳定训练，为设计稳定残差架构提供了严格理论依据与高效认证工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14576v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14576v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1616, \"height\": 266, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14576v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1634, \"height\": 648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14576v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1633, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14576v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1633, \"height\": 638, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14576v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1634, \"height\": 636, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14576v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1647, \"height\": 884, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14576v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1514, \"height\": 961, \"label\": \"Table\"}]"
motivation: 深度残差架构训练不稳定，缺乏明确的稳定性阈值与高效认证方法。
method: 提出亚线性增长原则，要求残差块速度场满足输入幅度指数q≤1，并通过建筑原语指数算术实现自动认证。
result: 实验表明满足q≤1的Mamba和PatchTST变体训练稳定，超临界块可降阶至q=1。
conclusion: q≤1是残差架构稳定训练的必要充分条件，提供理论指导与实用认证工具。
---

## 摘要
我们提出深度残差架构的\emph{亚线性增长原理}——每个残差块速度场的输入幅度指数的一个尖锐稳定性阈值：$$\|v(x, t)\| \leq c\,\|x\|^q + b, \qquad q \in [0, 1].$$ 通过两个独立论证确立了阈值 $q = 1$。经典ODE理论给出 $q \le 1$ 时在 $[0, T]$ 上全局前向流，并在任何 $q > 1$ 时呈现发散速度场。通过Hamilton-Jacobi-Bellman方程的最优控制分析将此锐化为一个选择性陈述：训练最优解在可容许类边界上是bang-bang的，因此 $q > 1$ 时的最优解发散，而 $q \le 1$ 时的最优解通过构造是安全的。指数准则 $q \le 1$ 因此是稳定训练的必要充分条件。它阐明了确保训练和推理稳定性的架构配置，例如解释了层归一化的稳定作用。亚线性增长速度场构成\emph{正确的函数空间}，在该空间上前向动力学、伴随灵敏度和架构组合都得到了良好控制。在构建残差块的五种操作下的输入幅度指数算术能够高效地在架构原语级别认证 $q_k \le 1$，取代了在搜索稳定神经架构设计中的特设试错。一种无需参数的修改将超临界Mamba块从 $q = 5$ 降至 $q = 1$ 且不使用层归一化，证明了这一点。在Mamba和PatchTST上的实验证实，$q \le 1$ 变体训练稳定：准则在于输入幅度指数，而非是否存在归一化层。

## Abstract
We propose \emph{the sublinear-growth principle} for deep residual architectures -- a sharp stability threshold on the input-magnitude exponent of every residual block's velocity field: $$\|v(x, t)\| \leq c\,\|x\|^q + b, \qquad q \in [0, 1].$$ The threshold $q = 1$ is established via two independent arguments. Classical ODE theory gives a global forward flow on $[0, T]$ at $q \le 1$ and exhibits divergent velocity fields at any $q > 1$. The optimal-control analysis, via the Hamilton-Jacobi-Bellman equation, sharpens this to a selection statement: the training optimum is bang-bang on the boundary of the admissible class, so the optimum at $q > 1$ blows up while the optimum at $q \le 1$ is safe by construction. The exponent criterion $q \le 1$ is thereby a necessary and sufficient condition for stable training. It clarifies architectural placements that ensure the stability of training and inference, explaining, for instance, the stabilizing role of layer normalization. The sublinear-growth velocity fields form \emph{the right function space} on which forward dynamics, adjoint sensitivity, and architectural composition are all well-controlled. An arithmetic of input-magnitude exponents under the five operations that build residual blocks enables efficient certification of $q_k \le 1$ at the level of architectural primitives, in place of ad hoc trial and error in the search for stable neural architectural designs. A parameter-free modification reduces the supercritical Mamba block from $q = 5$ to $q = 1$ without layer normalization, demonstrating this point. Experiments on Mamba and PatchTST confirm that the $q \le 1$ variants train stably: the criterion is the input-magnitude exponent, not the presence of a normalization layer.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：深度残差架构（如Transformer、Mamba）在训练时经常因残差块（velocity field）增长过快而不稳定（梯度爆炸、激活值溢出），现有解决方法（如层归一化）主要依赖经验试错，缺乏一个统一、严格的稳定性判别准则。
- **研究动机**：作者试图从ODE连续动力学和最优控制（optimal control, OC）理论出发，为残差块的速度场提出一个**尖锐的稳定性阈值**，并基于此给出架构认证的数学工具，从而将“是否稳定”的判断从特设经验提升为理论指导。
- **整体含义**：提出**亚线性增长原则**——每个残差块的速度场必须满足输入幅度指数 \(q \le 1\)，其中 \(q = 1\) 是稳定性的临界阈值。这一原则统一解释了层归一化的作用，并能指导无归一化层的稳定架构设计。

## 2. 论文提出的方法论

### 核心思想
- 将残差网络视为前向欧拉离散化的ODE：\(X_{k+1} = X_k + \Delta t\, v(X_k; \theta_k)\)。要求速度场 \(v\) 满足 **亚线性增长**：\(\|v(x,t)\| \le c\|x\|^q + b\)，\(q \in [0,1]\)。
- **两个独立论证**：
  1. **经典ODE理论**：\(q \le 1\) 时ODE解全局存在；\(q > 1\) 时存在有限时间爆破。
  2. **最优控制（HJB）分析**：训练最优解是**bang-bang**的，即总是饱和在可容许类的边界上（\(\|v^*\| = c\|x\|^q + b\)）。因此若 \(q > 1\)，最优解必然进入爆破区域；若 \(q \le 1\)，则所有边界轨迹都安全。
- 由此得到**稳定性阈值**：\(q \le 1\) 是训练稳定的**充要条件**。

### 关键技术细节
- **输入幅度指数算术（Theorem 4.2）**：定义了五种操作下指数的变化规则：
  - 序列组合：\(q(B_1 \circ B_2) \le q_1 q_2\)
  - 并行和：\(q(B_1 + B_2) \le \max(q_1, q_2)\)
  - Hadamard积：\(q(B_1 \odot B_2) \le q_1 + q_2\)
  - 残差包裹：\(q(I + \Delta t B) \le \max(1, q(B))\)
  - 归一化包裹：\(q(\text{LN} \circ B) = 0\)
- **原语指数表（Table 1）**：常见原语的指数，如线性层 \(q=1\)，tanh \(q=0\)，层归一化 \(q=0\)，Mamba原生\(q=5\)等。
- **认证流程**：将候选残差块分解为原语 → 用算术计算块级 \(q\) → 若 \(q>1\) 则用归一化包裹或替换为Lipschitz原语降阶 → 一旦 \(q\le 1\) 即获得稳定性保证。

### 示例：Mamba的线性增长修改
- Mamba原生速度场通过选择性SSM中的乘法耦合（如 \(\bar{B}_t = \Delta t B_t\)、SiLU门控等）累积指数至 \(q=5\)。
- **修改**：
  - 将选择性参数分支的输入用 \(x / \|x\|\) 替换（L2归一化），使该分支 \(q=0\)。
  - 将SiLU门控改为sigmoid门控（移除 \(q=2\) 贡献）。
  - 残差流保持线性项（\(q=1\)）。
- 结果：无任何归一化层的Mamba块降至 \(q=1\)。

## 3. 实验设计

### 数据集与场景
- **数据集**：Weather（天气）和 ETTm1（电力变压器温度）。
- **任务**：多变量时间序列预测（TSF）。
- **设置**：输入窗长96，预测视界 \(H \in \{96, 192, 336, 720\}\)，残差深度 \(L \in \{3, 8\}\)。

### 骨干架构
- **Mamba**（选择性状态空间模型）——原生 \(q=5\)。
- **PatchTST**（基于自注意力）——所有原语指数均为 \(q=1\)。

### 对比方法
- 对**Mamba**：比较五种变体：
  - 基准：RMSNorm（\(q=0\)）
  - Pre-LN（\(q=0\)）、Peri-LN（\(q=0\)）
  - **线性增长修改**（无归一化，\(q=1\)）
  - **自由速度**（无归一化，保持原生 \(q=5\)）
- 对**PatchTST**：比较四种归一化变体（基准、Pre-LN、Peri-LN、自由速度），自由速度本身 \(q=1\) 无需修改。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量或训练时长。仅提到每个配置使用3或6个随机种子，模型规模为小模型（≤2M参数），但硬件资源信息缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - Mamba：288个种子级运行（64个配置单元×种子数，每个单元3或6种子）。
  - PatchTST：同样288个种子级运行。
  - 总实验数：576个独立训练。
- **充分性评估**：
  - 覆盖了两种不同骨干（Mamba和PatchTST）、两种深度、四个预测视界、多种归一化策略以及无归一化解耦探索。
  - 消融设计良好：通过对比“自由速度”Mamba（\(q=5\)）与线性修改Mamba（\(q=1\)）直接证明稳定性由指数决定而非归一化层；通过PatchTST的自由速度（\(q=1\)）进一步排除骨干差异。
  - 随机种子多次重复，报告了blow-up计数和平均MSE百分比偏差，统计可靠性较好。
- **客观公平性**：所有变体使用相同骨干、优化器、数据预处理，控制变量。但仅测试了浅层（\(L\le 8\)），未验证深层（如12、24层）是否仍符合结论。

## 6. 论文的主要结论与发现

1. **\(q=1\)是训练稳定的充要阈值**：任何残差块若 \(q>1\)，则存在有限时间爆破的风险，且最优控制下的最优解必然选择爆破轨迹；若 \(q\le 1\)，则前向与后向均稳定。
2. **准则独立于归一化层**：Mamba无归一化时 \(q=5\) 导致发散；通过无参数修改降为 \(q=1\) 后稳定；PatchTST无归一化时 \(q=1\) 也稳定。
3. **指数算术提供高效认证**：只需检查原语指数和组合规则，即可判定块级 \(q\)，无需实际训练试错。
4. **设计空间拓展**：指出了 \(q=0\) 和 \(q=1\) 之外的 \(q\in(0,1)\) 内部是未开发的设计区域，理论上可提供更优的前向界。

## 7. 优点

- **理论严密**：从ODE、OC、HJB三个角度交叉验证阈值，数学推导扎实。
- **可操作性强**：给出了原语指数表、组合算术和认证流程，工程师可直接用于架构设计或修改。
- **实验设计具有诊断价值**：通过修改Mamba将 \(q\) 直接降阶，清晰分离了“归一化层”与“指数”的影响；PatchTST进一步确认了低指数无需归一化。
- **结果清晰**：所有 \(q\le 1\) 变体零爆破，所有 \(q>1\) 变体大比例爆破，无歧义。
- **洞察深刻**：解释了为什么层归一化能稳定训练（因为它将速度场降至 \(q=0\)），并指出其他机制（如L2归一化、饱和激活、甚至线性修改）也可达到同样效果。

## 8. 不足与局限

- **深度范围有限**：实验仅测试 \(L=3,8\)，未验证更深层（如Transformer典型12~96层）中 \(q=1\) 是否仍能完全避免溢出（Remark 3.6指出 \(q=1\) 时前向界几何增长，数值上可能超出FP16/32范围）。
- **骨干种类少**：仅测试了Mamba和PatchTST，未覆盖视觉Transformer、扩散模型、其他SSM等。结论的泛化性需更多验证。
- **未探索 \(q\in(0,1)\) 内部**：论文指出 \(q\in(0,1)\) 原语缺失，但未提出或构造此类原语，也未实验验证其潜力。
- **理论假设与离散近似**：理论基于连续时间ODE，实际网络使用离散欧拉步，可能存在离散化误差。论文认为前向欧拉保守，但未定量分析。
- **算力与可复现性**：未报告硬件资源、训练时间，且仅针对小模型（≤2M参数），大模型场景未验证。
- **未涉及学习率、初始化等**：稳定性也受优化超参数影响，论文假设在可容许类内训练，但实际优化过程可能接近边界，此时初始化和学习率可能放大不稳定，论文未深入讨论。

（完）
