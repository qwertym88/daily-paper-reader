Title: Positional Attention-based Graph Neural Network for Learning Permutation Non-equivariant Wireless Policies

URL Source: https://arxiv.org/pdf/2607.12744v1

Published Time: Wed, 15 Jul 2026 11:59:00 GMT

Number of Pages: 15

Markdown Content:
> 1

# Positional Attention-based Graph Neural Network for Learning Permutation Non-equivariant Wireless Policies 

Baichuan Zhao, Chenyang Yang, Jianyu Zhao and Di Zhang 

ABSTRACT, INTRODUCTION AND CONCLUSION RE-MAIN UNREVISED. 

Abstract —Graph neural networks (GNNs) have emerged as a promising approach to learning wireless policies efficiently by leveraging topology prior and incorporating relational inductive biases. However, when the optimal policy is not permutation equivariant (PE), conventional GNNs suffer from mismatched inductive biases, leading to degraded performance or poor generalizability. This issue arises in wireless tasks with expected objectives, such as channel estimation and end-to-end (E2E) precoding, where the PE property of the optimal policy depends on the underlying channel distribution. In this paper, we propose a novel positional attention-based GNN to learn permutation non-equivariant policies efficiently. The core idea is to incorporate relative positions of vertices into the attention mechanism via an embedding function, enabling the GNNs to capture asymmetric relationships. Consequently, the proposed GNN can represent permutation non-equivariant functions, while retaining high learning efficiency and size generalizability through parameter sharing. We consider channel estimation and E2E precoding as case studies, and prove that their policies are PE to users but not to antennas under spatially correlated channels. We employ the proposed GNN to learn the policies, where the embedding function is designed based on the channel covariance matrix. Simulation results demonstrate that the proposed GNN outper-forms existing channel estimation and E2E precoding methods, requires fewer samples for training, and can be generalized to systems with different numbers of antennas and users. 

Index Terms —graph neural network, permutation equivariant, positional attention, channel estimation, end-to-end precoding 

I. I NTRODUCTION 

Graph neural networks (GNNs) have recently emerged as a powerful approach for learning policies efficiently in wireless communication systems. By introducing parameter sharing schemes, GNNs can harness the permutation equivariant (PE) property of wireless policies, thereby reducing the hypothesis space (i.e., the set of functions representable by the GNN) and training complexity [1]. Moreover, the parameter sharing scheme allows a well-trained GNN to infer on different problem sizes without retraining [2]. To improve the expres-sive power of GNNs, i.e., to enlarge their hypothesis space, attention-based GNNs have been proposed [3], [4], which have been applied to a wide range of wireless communication tasks [5]–[11] and achieved better performance. Despite the wide application of GNNs, existing GNN archi-tectures face an inherent trade-off between learning efficiency and performance when the optimal policy is permutation non-equivariant, i.e., its output is not equivariant to input  

> The authors are with the School of Electronics and Information Engineering, Beihang University, Beijing 100191, China (E-mail: {zhaobaichuan; cyyang; di zhang }@buaa.edu.cn).

permutations along some dimensions. On one hand, conven-tional GNNs adopt parameter sharing to improve learning efficiency, which, however, inherently enforces permutation equivariance, leading to a biased hypothesis space and de-graded performance. On the other hand, removing parameter sharing enlarges the hypothesis space but incurs higher training complexity and hinders generalization across varying system sizes. In wireless communication systems, this trade-off arises when the optimization objective of a wireless problem involves mathematical expectations, as to be shown later. For instance, in channel estimation, the widely used mean square error (MSE) metric contains an expectation over the true channel. Similarly, in precoding problems without accurate CSI, such as robust precoding [12] and end-to-end (E2E) precoding with implicit channel acquisition [13], precoders are designed by optimizing average performance metrics. In these cases, the optimal solution depends on the underlying distribution, but whether the resulting policy exhibits permutation property under specific distributions remains unexplored. As a result, when the policies are not PE, conventional GNN architectures may struggle to achieve satisfactory performance with low training complexity. 

A. Related Works 1) Attention-based GNNs for Wireless Communications: 

Attention mechanisms allow GNNs to adaptively adjust the importance of neighboring vertices, enabling them to focus on most relevant neighbors and better capture dependencies dur-ing aggregation [3]. Motivated by these advantages, attention-based GNNs has been used for various applications in wireless communications, such as precoding [5]–[8], power control [9] and power allocation [10], [11]. To better understand the limitations of existing attention mechanisms, we review how attention coefficients are typically computed and what information they depend on. In terms of computation, existing mechanisms adopt either the dot-product attention introduced in the Transformer architecture [14] or an FNN-based approach as in [15]. The dot-product attention mechanism was incorporated into GNNs to measure the similarity between vertex features during aggregation in [9], [16]. To enhance expressive power, the attention coef-ficient was extended from a scalar to a vector in [7], and the dot-product was replaced with a Hadamard product. For FNN-based attention, one well-known example is the Graph Attention Network (GAT) [3], which integrates the attention mechanism in [15] into a homogeneous GNN and has been used for precoding [6]. More recent works has extended the arXiv:2607.12744v1 [eess.SP] 14 Jul 2026 2

GAT to heterogeneous graphs for various wireless tasks, such as precoding [8] and power allocation [11]. From the perspective of input, most existing attention mech-anisms compute attention coefficients based solely on the hidden representations from the previous layer [3], [6]–[9], [11]. The GNN proposed in [16] is an exception, where both hidden representations and input features were used. In all cases, the inputs used to compute attentions remain equivariant under vertex permutations, and thus these attention mecha-nisms preserve the permutation properties of the underlying GNN architecture, as will be discussed later. Therefore, such mechanisms do not enable the model to represent permuta-tion non-equivariant functions, motivating the design of new attention mechanisms. 

2) Channel Estimation and E2E Precoding: In the fol-lowing, we briefly review existing methods used for channel estimation and E2E precoding, which serve as two case studies considered in this work. 

Channel Estimation: To improve estimation accuracy by ex-ploiting the sparsity of wireless channels, iterative algorithms such as orthogonal matching pursuit (OMP) [17], approximate message passing (AMP) [18], and sparse Bayesian learning (SBL) [19] were proposed, at the cost of increased computa-tional complexity. The algorithms were then used in [20]–[22]. To further enhance accuracy and reduce the computational burden, deep neural networks (DNNs) have been introduced for channel estimation. A line of work designed model-driven DNNs by unfolding iterative algorithms such as AMP and SBL [21], [23]. However, their performance is limited by the original algorithms and the computational complexity remains high. Alternatively, data-driven DNNs do not rely on mathe-matical models, making them more flexible for various tasks and faster at inference. In [24] and [25], convolutional neural networks (CNNs) were used to capture channel correlations, where angular-domain information was leveraged to enhance estimation accuracy. To improve scalability and generalizabil-ity, GNNs have recently been introduced. In [26], a GNN was used to estimate channels in reconfigurable intelligent surface (RIS)-aided systems from received sounding reference signals (SRSs). In [27], the full channel matrix was recovered from received SRSs of a subset of RIS element groups. To capture the spatial correlations among different RIS elements, each group was modeled as a vertex in a graph, and a GNN cascaded with a transformer was employed. Channel recovery was considered in [28], where pilots transmitted over partial time and frequency resources were treated as different tokens and fed into a Transformer. 

E2E Precoding: To improve system performance, several works have proposed to employ DNNs for optimizing precod-ing in an E2E manner. In frequency division duplex systems, this involves joint design of pilots, quantization feedback and precoding [29], [30]. In time division duplex (TDD) systems, downlink precoders are learned directly from uplink SRSs by leveraging channel reciprocity [31]–[33], which is also the focus or our work. Specifically, FNNs were employed in [31] and [32], while a Transformer that treats SRSs from different subframes as different tokens was adopted in [33] to capture temporal correlation. Although with improved performance, these DNNs can not be generalized to varying numbers of users, since their numbers of trainable parameters depend on that of users. Therefore, they can not be applied for practical wireless systems where the user number changes dynamically. To tackle the challenge, several works have employed GNNs for E2E precoding. In [13], both the precoder at base station (BS) and the phase shifts of RIS were jointly optimized, while in [34] and [35], hybrid analog and digital precoders were jointly optimized for mmWave systems. The GNNs designed in these works are PE to users, and can infer in scenarios with different numbers of users. 

Summary: Aforementioned works using GNNs for channel estimation, i.e., [26], [28], or E2E precoding, i.e., [13], [34], [35], did not examine whether the permutation properties of the GNNs are consistent with the policies. Specifically, in [28], the graph Transformer was followed by an FNN, and its learned policy is without permutation property. The GNNs in [13], [26], [34], [35] are PE to users, enabling inference across varying numbers of users. However, these works did not analyze whether the policies are PE to antennas, nor did they introduce parameter sharing along the antenna dimension. This may be because performance degradation was empirically observed after sharing parameters. Consequently, the number of trainable parameters grows with that of antennas, leading to high sample complexity for large antenna arrays and poor generalizability across antenna dimensions. 

B. Motivation and Contribution 

When learning permutation non-equivariant policies, exist-ing GNNs face a trade-off between learning efficiency and performance. For wireless problems involving expected objec-tives, such as channel estimation and E2E precoding, the per-mutation properties of the corresponding policies depend on the underlying channel distributions, whereas the dependency has not been investigated yet. Under certain distributions, these policies are not PE and can not be efficiently learned by existing GNNs. In this paper, we address this issue by proposing a novel positional attention-based GNN to learn permutation non-equivariant policies efficiently. We consider learning wireless policies in multi-user multi-input single-output (MU-MISO) systems, while the approach can be extended to other com-munication systems. Our main contributions are summarized as follows.  

> •

We formulate a class of wireless problems whose ob-jective involves an expectation, and analyze how the underlying probability distribution affects the PE property of the resulting policy. We identify the conditions under which the policy exhibits permutation property.  

> •

We propose a positional attention mechanism for GNNs to learn permutation non-equivariant policies. Unlike existing attention mechanisms, our method computes attention coefficients via an embedding function, which encodes the relative positions of vertices based on their indices. This design enables the GNN to better capture correlations induced by, e.g., the physical locations of entities in communication systems, and allows it to rep-resent permutation non-equivariant functions. 3 

> •

Using channel estimation and E2E precoding as case studies, we prove that the corresponding policies are PE to users but not to antennas in spatially correlated channels. Inspired by the iterative approximation of linear minimum mean square error (LMMSE) estimator, we de-sign the embedding function by resorting to the structure of channel covariance, and the resulting weight matrix of the GNN exhibits the same structure as the channel covariance matrix.  

> •

Extensive simulations show that the proposed GNN can achieve better performance than state-of-the-art counter-parts for channel estimation and E2E precoding. More-over, it requires much fewer samples to achieve the same performance than other methods, and can be generalized to different numbers of antennas and users. 

Notations: ai,j or [A]i,j is the element in the i-th row and the j-th column of A. I denotes the identity matrix. (·)T and 

(·)H denote transpose and conjugate transpose of matrices, respectively. || · || denotes Frobenius norm of matrices. ⊙

and ⊗ denote Hadamard product and Kronecker product of matrices, respectively. U(a, b ) denotes uniform distribution with range between a and b. CN (μ, σ ) denotes complex Gaussian distribution with mean μ and deviation σ. Px(x)

denotes the probability density function (PDF) of variable x,where the typewriter font x distinguishes the variable from its specific value x. 0 and 1 are matrices whose elements are all zeros and ones, respectively. Re (·) and Im (·) denote the real and imaginary parts of a complex number, respectively. a|| b

denotes the concatenation of vectors a and b.II. P ROBLEM FORMULATION 

Consider a MU-MISO system, where a BS equipped with N

antennas serves K single-antenna users. Since there often ex-ists randomness (say receiver noise) during decision-making, we consider a stochastic optimization problem involving math-ematical expectation [36]. Denote X ∈ CN ×K×Dx as the parameters measured for making a decision, and Y ∈ CN ×K×Dy as the optimization variable, where Dx and Dy represent the dimensions of vectors corresponding to each antenna-user pair. The first and second dimensions of X and Y correspond to antenna and user dimensions, respectively. The problem is formulated as 

P1 : max  

> Y

U (X, Y) = EZ|X[U ′(Y, Z)|X] (1a) 

s.t. c(X, Y)≤0, (1b) where U ′(·, ·) is a utility function, U (·, ·) is an average utility 

over a random variable Z ∈ CN ×K×Dz (e.g., the unknown channel state information) conditional on X, the conditional PDF is PZ|X(Z|X), and c(·, ·) is a constraint function. Denote the mapping from X to the optimal solution Y of problem P1 as Y = f (X), which is referred to as a policy .In the following, we aim to design a DNN to solve problem 

P1 by learning the policy. III. D ISTRIBUTION -DEPENDENT AND AUGMENTED 

POLICIES 

In this section, we analyze how the permutation property of the policy Y = f (X) is determined by the underlying data distribution. We show that, although the optimization problem is defined over unordered sets, the policy does not necessarily inherit permutation equivariance when the corresponding PDF itself is not permutation invariant (PI). Then, we construct an augmented policy by introducing additional features that encode positional information, and prove that the resulting policy is always PE regardless of the underlying distribution. 

A. Definition of Permutation Invariance and Equivariance 

To facilitate further analysis, we define the permutation properties of general functions with inputs T1, · · · , TS , where each input Ts ∈ CN ×K×Ds is a three-dimensional tensor whose first and second dimensions correspond to the antenna and user dimensions, respectively. Let ΠA ∈ RN ×N and 

ΠU ∈ RK×K denote the permutation matrices correspond-ing to antennas and users, respectively, and the PI and PE properties are defined as follows. 

Definition 1 (Permutation Invariance ). Afunction F (T1, . . . , TS ) is PI to antennas if 

F (T1, . . . , TS ) = F (T1ΠU, . . . , TS ΠU) and PI to users if F (T1, . . . , TS ) = F (ΠT

> A

T1, . . . , ΠT

> A

TS ).1

Definition 2 (Permutation Equivariance ). A tensor-valued function F(T1, . . . , TS ) ∈ CN ×K×Do is PE to antennas if 

ΠT

> A

F(T1, . . . , TS ) = F(ΠT

> A

T1, . . . , ΠT

> A

TS ) and PE to users if F(T1, . . . , TS )ΠU = F(T1ΠU, . . . , TS ΠU).

B. Distribution-dependent Policy 

In this subsection, we will investigate whether the policy 

Y = f (X) inherits PE property from the unordered nature of the antenna and user sets. While such equivariance has been established in many deterministic wireless optimization problems, it does not hold for the stochastic problem P1 .Specifically, we show that the permutation property of the policy depends on the permutation invariance of the underlying conditional distribution. Problem P1 involves two sets: the antenna set with N BS antennas and the user set with K users. Since these sets are inherently unordered, permuting their indices does not change the physical environment. For the policy Y = f (X), such permutations correspond to permuting the antenna and user dimensions of X and Y, which suggests that the policy may be PE to antennas and users, i.e., 

ΠT

> A

YΠ U = f



ΠT

> A

XΠ U



. (2) This intuition is valid for many deterministic wireless opti-mization problems, which can be regarded as a special case of (1) where no latent random variable is involved. In such cases, the optimization problem does not involve any underlying statistical distribution, and the objective and constraints are fully determined by the observed input X and the decision variable Y.For example, consider the classical sum-rate maximization precoding problem under a transmit power constraint, i.e., 

P2 : max  

> V

R (HDL , V) (3a) 

> 1The matrix–tensor multiplication applies standard matrix multiplication to each slice along the third dimension. 4

s.t. || V|| 2F − PDL ≤ 0, (3b) where HDL , V ∈ CN ×K denote the downlink channel matrix and precoding matrix, respectively, R(HDL , V) represents the achievable downlink sum rate, and PDL is the total transmit power budget. 2

In this problem, both the sum-rate function and the power constraint are explicit functions of HDL and V and do not involve any underlying distribution. In this case, permuting the rows or columns of HDL is equivalent to reordering the system entities, leaving the objective and constraint invariant [1]. Consequently, the optimal precoding policy V = f (HDL )

is PE. However, this intuition does not hold for the policy Y =

f (X) arising from the stochastic optimization problem P1 .Specifically, relabeling antennas or users corresponds to per-muting all context associated with these entities in the op-timization problem. In deterministic problems like P2 , the relevant context is fully captured by the measurements, so permuting X is equivalent to relabeling the entities. By contrast, in the stochastic problem P1 , its optimal solution 

Y is determined not only by the observed measurements X

but also by the conditional distribution PZ|X(Z|X). Therefore, unless the conditional distribution is PI, permuting X alone is not equivalent to relabeling the entities and the resulting policy is not PE. The following proposition formalizes the condition under which the policy Y = f (X) exhibits PE property. 

Proposition 1. Y = f (X) is PE to antennas or users if 

U ′(Y, Z), c(X, Y) and PZ|X(Z|X) in P1 are PI to antennas or users. Proof. See Appendix A. Proposition 1 indicates that the PE property of the policy 

Y = f (X) depends on the PI property of PZ|X(Z|X) due to the expectation in the objective function of P1 . We refer to such policies whose permutation property relies on the underlying distribution as distribution-dependent policies . In fact, the random variable may also appear in the constraints or in ways other than expectations (e.g., in probabilistic forms) [36], while we do not elaborate on these scenarios for the sake of clarity. By contrast, if neither the objectives nor the constraints of a wireless problem depend on unknown latent variables, such as the precoding problem P2 , then the permutation property of the resulting policy is fixed, which is referred to as a distribution-independent policy. In what follows, we provide a toy example of parameter estimation to show how the underlying distribution affects the permutation property of the policy. 

Example 1. Consider estimating z ∈ R2×1 from observation 

x ∈ R2×1, and the minimal MSE estimate is y = Ez|x[z|x].Denote the estimation policy as y = f (x). Suppose x

and z satisfy x = z + n, where n represents noise in-dependent of z. Denote x = [x1, x 2]T, z = [z1, z 2]T,

n = [n1, n 2]T, and assume that z ∼ N(0, diag( σ21 , σ 22 )) ,

n ∼ N(0 , σ 2

> n

I). Then, the resulting estimator is y = f (x) = 

> 2This problem will be revisited in a stochastic form in Section V-A later, where the detailed system model and expression are specified.

[ σ21 

> σ21+σ2
> n

x1, σ22 

> σ22+σ2
> n

x2]T. If the observations are permuted to 

x′ = Πx = [ x2, x 1]T, the corresponding optimal estimate will become y′ = f (x′) = [ σ21 

> σ21+σ2
> n

x2, σ22 

> σ22+σ2
> n

x1]T, which differs from Πy = [ σ22 

> σ22+σ2
> n

x2, σ21 

> σ21+σ2
> n

x1]T if σ1̸ = σ2. In other words, the policy exhibits PE property iff σ1 = σ2.C. Permutation Equivariant Policy Augmentation 

The policy Y = f (X) obtained from P1 is not always PE, while incorporating permutation equivariance into the policy structure is highly desirable when learning the policy using DNNs, since it introduces an inductive biases that help reduce the hypothesis space and improve learning efficiency. To this end, we construct an augmented policy by incorpo-rating additional features that encode the statistical distinction among entities, so that permutations can be explicitly repre-sented in the input space. To help understand, we consider a single-user setting and focus only on the antenna dimension, while the extension to the user dimension is straightforward. Without loss of generality, we set Dx = Dy = 1 so that the tensors X, Y, Z

reduce to vectors x, y, z ∈ CN ×1. Let Z = [ z1, . . . , zN ]T

denote the latent random variables and X = [ x1, . . . , xN ]T

denote the corresponding observations, with conditional PDF 

PZ|X(z|x).

1) Permutation Structure of Distribution Families: Con-sider a permutation of indices represented by a permutation matrix Π and its associated permutation mapping π(·), and the permuted variables are Zπ = [zπ(1) , . . . , zπ(N )]T and 

Xπ = [ xπ(1) , . . . , xπ(N )]T. After permuting the indices of the antennas, the conditional distribution becomes PZπ |Xπ (z|x).Since these conditional PDFs correspond to the same un-derlying statistical model under different index orderings, they satisfy 

PZ|X(z|x) = PZπ |Xπ (ΠTz|ΠTx), (4) which indicates that the permutation property is reflected in the relationship among different conditional PDFs associated with different index orderings. 

2) Positional Embedding and Augmented Distribution: To represent the permutation structure among different condi-tional PDFs using a single unified function, we introduce an additional vector p ∈ RN ×1 whose elements are associated with the antennas. This vector aims to encode the antenna indices, and we refer to it as a positional embedding vector .By incorporating p into the policy input, we construct an augmented PDF ˜P (z|x, p) defined as 

˜P (z|x, p) = 

(

PZ|X(z|x), p = ˜ p,PZπ |Xπ (z|x), p = ΠT ˜p, (5) where ˜p ∈ RN ×1 is a predetermined embedding vector associated with an initial index ordering. In 

˜P (z|x, p), the variable p takes values from the finite set 

{ΠT ˜p | Π is a permutation matrix }, each element of which corresponds to a permutation of antenna indices and specifies the conditional distribution associated with that ordering. The following proposition shows that such an augmented representation can always be constructed. 5

Proposition 2. The augmented conditional distribution 

˜P (z|x, p) is well defined if for any two permutations Π1 and 

Π2, ΠT 

> 1

p = ΠT 

> 2

p implies PZπ1 |Xπ1 (z|x) = PZπ2 |Xπ2 (z|x).

According to Proposition 2, the embedding vector is not unique. A simple sufficient construction is to assign distinct values to its elements, e.g., ˜p = [1 , . . . , N ]T.Combining (4) and (5), we obtain 

˜P (ΠTz | ΠTx, ΠTp) = ˜P (z|x, p), (6) which shows that the augmented conditional distribution 

˜P (z|x, p) is PI, regardless of the conditional PDF PZ|X(z|x).

3) Permutation Property of the Augmented Policy: Based on the positional embedding, we construct an augmented policy by concatenating the embedding with the original input vector. Specifically, define the augmented input as (x, p), and the augmented policy y = ˜f (x, p) is defined as the mapping from (x, p) to the optimal solution of the following problem, 

P3 : max  

> y

U (x, y, p) = E[U ′(y, z) | x, p] (7a) 

s.t. c(x, y)≤0, (7b) where the expectation is taken with respect to the augmented conditional distribution ˜P (z|x, p).When p = ˜p, P3 is equivalent to P1 in the sense that they share the same optimal solution. Moreover, the following proposition shows that introducing the positional embedding converts the original distribution-dependent policy into a distribution-independent policy. 

Proposition 3. y = ˜f (x, p) is PE to antennas or users if 

U ′(y, z) and c(x, y) in P1 are PI to antennas or users. Proof. The result follows directly from Proposition 1 since 

˜P (z|x, p) is permutation invariant. 

Remark 1. For the multi-user case, an additional positional embedding can be introduced to encode the user indices. 

IV. P OSITIONAL ATTENTION -BASED GNN In this section, we propose a positional attention mechanism and integrate it with GNN to learn the augmented policy. We begin by modelling the MU-MISO system as a heterogeneous graph and presenting an edge-GNN for learning policies on it. Then, we analyze the permutation property of the edge-GNN, and show that standard attention mechanisms preserve per-mutation equivariance. Finally, we incorporate the positional embedding vector into the attention coefficients, proposing a positional attention-based GNN whose permutation property is consistent with the augmented policy. 

A. Edge-GNN for Learning MU-MISO Policies 

We first introduce the graph representation and the basic edge-GNN architecture used to learn wireless policies. GNNs operate on graphs G = ( V, E), where V and E denote the sets of vertices and edges, respectively. By enforcing parameter sharing across vertices and edges of the same type, the number of trainable parameters of GNNs is independent of the graph size, which is a premise for generalizing the learned policy to different problem sizes. To support size generalization with respect to both the number of BS antennas and the number of users in the wireless policy Y = f (X), we construct a heterogeneous graph con-sisting of N antenna vertices A1, . . . , AN and K user vertices 

U1, . . . , UK . Since both the input features X ∈ CN ×K×Dx

and the output actions Y ∈ CN ×K×Dy are naturally indexed by antenna-user pairs, it is straightforward to assign features and actions to the edges connecting antenna and user vertices. Specifically, we denote the edge between An and Uk as (n, k ),and associate it with feature vector X[n, k, :] and action vector 

Y[n, k, :] , respectively. Consequently, the graph is a complete bipartite graph as shown in Fig. 1. A1 A2 A3 A4             

> U1U2
> 𝑁 t=4
> 𝐾 =2
> A: antenna
> U: user
> Feature: 𝐗 ->Hidden Output: 𝐃 (𝑙 )->Action: 𝐘
> Fig. 1. Heterogeneous graph for learning policies in MU-MISO systems.

Denote the hidden representation of edge (n, k ) in the l-th layer as d(l) 

> n,k

∈ RCl×1, where Cl is the dimension of the hidden representation and is a hyper-parameter. The hidden representations of all edges can be expressed as a tensor 

D(l) ∈ RN ×K×Cl with D(l)[n, k, :] = d(l)

> n,k

. Denote NL as the number of layers of the GNN. The input and output of the edge-GNN are d(0)  

> n,k

= [ Re (X[n, k, :]) T, Im (X[n, k, :]) T]T ∈

R2Dx×1 and d(NL) 

> n,k

= [ Re (Y[n, k, :]) T, Im (Y[n, k, :]) T]T ∈

R2Dy ×1, which consist of the real and imaginary parts of the features and actions, respectively. To improve expressive power, we incorporate an attention mechanism into the aggregation process and adopt vector-valued attention coefficients as in [7]. The update equation of the edge-GNN is given by 

d(l)

> n,k

=f (l) 

> C

d(l−1)  

> n,k

,

> N

X

> n′=1

ρ(l) 

> n,n ′,k

⊙ ˚d(l−1) 

> n′,k

| {z }

> aggregate along the antenna dimension

,

> K

X

> k′=1

ω(l) 

> k,k ′,n

⊙ ˚d(l−1) 

> n,k ′

| {z }

> aggregate along the user dimension

!

, (8) where ˚d(l−1)  

> n,k

= f (l) 

> P

(d(l−1)  

> n,k

) denotes the processed hidden representation, and f (l) 

> C

(·) and f (l) 

> P

(·) are respectively the combination and processing functions implemented by FNNs. The vectors ρ(l) 

> n,n ′,k

and ω(l) 

> k,k ′,n

denote attention coefficients along the antenna and user dimensions. As shown in (8), the same functions f (l) 

> C

(·) and f (l) 

> P

(·)

are used to update the hidden representations for all edges. Therefore, the number of free parameters of the edge-GNN is independent of N and K, which is necessary for generalization to other problem sizes. 6

B. Positional Attention Mechanism 

To learn the augmented policy constructed in the previous section, we first analyze the PE property of the edge-GNN, and then propose a positional attention mechanism to incorporate the positional embedding. As in Section III-C, we still focus on the antenna dimen-sion by considering a single-user scenario and omitting the subscript k to highlight the key idea of our design. Moreover, for notational simplicity, we set Cl = 1 so that the hidden representations reduce to scalars. Under these simplifications, the update equation in (8) becomes 

d(l) 

> n

= f (l)

> C



d(l−1)  

> n

,

> N

X

> n′=1

ρ(l) 

> n,n ′

˚d(l−1) 

> n′



. (9) 

1) Permutation Property of Attention-based GNN: Since the choices of f (l) 

> C

(·) and f (l) 

> P

(·) do not affect the permutation property of the GNN layer [37], we adopt f (l) 

> C

(x) = sum( ·)

and f (l) 

> P

(x) = x to further simplify the update equation in (9) and highlight the role of the attention coefficients, yielding 

d(l) 

> n

= d(l−1)  

> n

+

> N

X

> n′=1

ρ(l) 

> n,n ′

d(l−1)  

> n′

. (10) By stacking d(l) 

> n

, n = 1, . . . , N into a vector d(l) ≜

[d(l)1 , . . . , d (l) 

> N

]T, the update equation can be written in matrix form as 

d(l) = f (l)(d(l−1) , A(l)) ≜ (A(l) + IN )d(l−1) , (11) where A(l) ∈ RN ×N is the attention matrix with entries 

[A(l)]n,n ′ ≜ ρ(l) 

> n,n ′

. It can be easily verified that the update equation satisfies 

ΠTd(l) = f (l)(ΠTd(l−1) , ΠTA(l)Π). (12) For standard attention mechanisms, the attention coefficients are determined by the hidden representations from the previous layer d(l−1) or the input feature d(0) . As an example, the dot-product attention mechanism in [14] computes the coefficients as ρ(l) 

> n,n ′

= ρ(l)(d(l−1)  

> n

, d (l−1)  

> n′

) ≜ w(l) 

> Q

d(l−1)  

> n

· w(l) 

> K

d(l−1)  

> n′

/√Cl

with trainable parameters w(l) 

> Q

and w(l) 

> K

. The resulting attention matrix can be expressed as 

A(l)(d(l−1) ) ≜



ρ(l)(d(l−1) 1 , d (l−1) 1 ) · · · ρ(l)(d(l−1) 1 , d (l−1)  

> N

)

... . . . ...

ρ(l)(d(l−1)  

> N

, d (l−1) 1 ) · · · ρ(l)(d(l−1)  

> N

, d (l−1)  

> N

)

 .

(13) From (13), permuting the hidden representations to 

ΠTd(l−1) leads to a corresponding permutation of the atten-tion matrix, i.e., A(l)(ΠTd(l−1) ) = ΠTA(l)(d(l−1) )Π, and the updated output becomes 

d′(l)=f (l)(ΠTd(l−1) , A(l)(ΠTd(l−1) )) =f (l)(ΠTd(l−1) , ΠTA(l)(d(l−1) )Π) = ΠTd(l),

Therefore, the individual GNN layer and the complete map-ping learned by stacking NL such layers are PE to antennas. 

2) Injecting Positional Embeddings: Since the distribution-dependent policy may not exhibit PE property, employing the edge-GNN to learn it directly may introduce a mismatched inductive bias. To address this issue, we incorporate the positional embedding into the edge-GNN and employ it to learn the augmented policy y = ˜f (x, p) from P3 .A straightforward approach is to concatenate the posi-tional embedding vector ˜p with the input features, i.e., expanding d(0)  

> n

= [Re (xn)T, Im (xn)T]T to d(0)  

> n

=[Re (xn)T, Im (xn)T, ˜pn]T. This is similar to the positional en-coding used in Transformers [14]. However, this increases the feature dimensionality and makes the positional information difficult for subsequent layers to capture. Instead, we incorporate the positional embeddings into the attention mechanism so that positional information affects the aggregation at every layer. Since the augmented policy satisfies 

ΠTy = ˜f (ΠTx, ΠTp), each GNN layer should preserve the same permutation property. Specifically, with the attention matrix A(l)(˜ p), the l-th layer satisfies 

ΠTd(l) = f (l)(ΠTd(l−1) , A(l)(ΠT ˜p)) . (14) Comparing it with (12), the attention matrix induced by the positional embedding should satisfy 

A(l)(ΠT ˜p) = ΠTA(l)(˜ p)Π.

This requirement can be satisfied by constructing the attention coefficients based on pairwise positional embeddings. Specif-ically, we define 

ρ(l) 

> n,n ′

= f (l) 

> A

(EB(˜ pn, ˜pn′ )) , (15) where EB( ·, ·) is a deterministic pairwise embedding function that models the interaction between the positional embeddings of two antennas. In practice, the mapping from the antenna index n to its corresponding embedding value ˜pn is fixed and can be integrated into the embedding function EB( ·, ·). By setting 

˜pn = n without loss of generality, (15) can be simplified as 

ρ(l) 

> n,n ′

= f (l) 

> A

(EB( n, n ′)) .

To illustrate the role of EB( ·, ·), consider a typical scenario where antennas are arranged in structured arrays such as ULAs or UPAs. In such systems, the channel statistics associated with different antennas are determined by their spatial locations. Accordingly, the function EB( n, n ′) can be designed to reflect the relative spatial relationship of the n-th and the n′-th antennas, which facilitates the GNN to capture the underlying channel correlation structure. 

3) Update Equation: The positional attention mechanism was derived under the simplified single-user setting in (9). We now return to the general MU-MISO setting and provide the update equation of the positional attention-based GNN (referred to as PA-GNN ), which is 

d(l)

> n,k

=f (l)

> C



d(l−1)  

> n,k

,

> N

X

> n′=1

f (l) 

> A

(EB( n, n ′)) ⊙ ˚d(l−1)  

> n′,k

,

> K

X

> k′=1

ω(l) 

> k,k ′,n

⊙ ˚d(l−1) 

> n,k ′



, (16) 7

where the hidden representations are restored to vectors with dimension Cl > 1, and the output of f (l) 

> A

(·) has the same dimension. Here, the positional attention mechanism is applied only along the antenna dimension, since the two problems considered in the next section are PE with respect to users. Nonetheless, it can be extended to the user dimension if needed. The number of trainable parameters in PA-GNN is still independent of N . This is because f (l) 

> A

(·) is shared and produces all attention coefficients in A(l), whose input and output dimensions are fixed and do not scale with N .

Remark 2. The proposed positional attention mechanism is not limited to the bipartite graph illustrated in Fig. 1, and can be extended to other graph structures. For example, when applying GNNs to learn policies for MU-MIMO systems over more complex graph topologies [7], the relative positions of user antennas can also be leveraged. 

V. L EARNING CHANNEL ESTIMATION AND E2E PRECODING 

In this section, we focus on two practical distribution-dependent policies, i.e., the channel estimation policy and E2E precoding policy, and employ the proposed PA-GNN to learn them. We first prove that both policies are PE to users, but not to antennas when the channels are spatially correlated. Then, we design the embedding function of the PA-GNN for learning the two policies, by investigating the structure of the spatial channel correlation matrix to gain useful insights. 

A. Channel Estimation and E2E Precoding Problems 

Consider a TDD system, whose frame structure is depicted in Fig. 2. Each frame consists of two phases, i.e., the uplink phase with LUL subframes and the downlink phase with LDL 

subframes. The duration of each subframe is Ts.⋯ DL  ⋯UL  ⋯ UL                      

> uplink phase downlink phase
> (𝐿 UL subframes) (𝐿 DL subframes)
> 1⋯𝑄 1⋯𝑄 1⋯𝑄
> 𝑄 repeated SRSs
> 𝐿 SRS subframes
> 𝐇 UL
> 1⋯𝐇 UL
> 𝐿 SRS …𝐇 DL …
> Fig. 2. The frame structure of the TDD system.

1) Channel Estimation Problem: The first problem consid-ers uplink channel estimation. At the end of the uplink phase, LSRS subframes are used for sending SRSs, where each subframe consists of Q repeated SRS blocks to improve the signal-to-noise ratio (SNR) for channel acquisition. In each block, the users send orthogonal SRSs with length K to the BS simultaneously. Denote the channel vector from the k-th user to the BS in the l-th subframe for SRS transmission as hl 

> UL ,k

∈ CN ×1, l =1, · · · , L SRS , and denote the uplink channel matrix as HUL ∈

CN ×K×LSRS , where HUL [: , k, l ] ≜ hl

> UL ,k

. Since the SRSs among different users are orthogonal, the effective received SRSs at the BS in the frame are [34] 

R = HUL + N, (17) where N ∈ CN ×K×LSRS is white Gaussian noise with zero mean and variance σ2UL  

> PUL Q

, PUL is the uplink transmit power of each user, and σ2UL is the uplink noise power. In fact, R ∈

CN ×K×LSRS is the least squares (LS) estimate of HUL [38]. The uplink channels can be estimated from the effective received SRSs by minimizing the MSE, i.e., 

P4 : min 

> bHUL

EHUL |R

h

UMSE (HUL , bHUL )|R

i

, (18) where bHUL ∈ CN ×K×LSRS is the estimated uplink channel matrix, UMSE (HUL , bHUL ) ≜ || HUL − bHUL || 2F, and the expectation is taken over HUL given R.The channel estimation policy is the mapping from the effective SRSs to the estimated channels, denoted as bHUL =

fCE (R).

2) E2E Precoding Problem: The second problem under consideration is to optimize the downlink precoders in an E2E manner to maximize the downlink sum rate. According to the analyses in [34], the optimization of the precoders in the LDL downlink subframes can be decomposed into LDL sub-problems. Therefore, we optimize the precoder in a single downlink subframe, and the sum rate in this subframe is 

R(HDL , V) ≜

> K

X

> k=1

log 2 1 + |(hDL ,k )Hvk|2

P  

> j̸=k

|(hDL ,k )Hvj |2 + σ2DL 

!

,

(19) where hDL ,k ∈ CN ×1 is the channel vector from the BS to the 

k-th user, vk ∈ CN ×1 is the precoder for the k-th user, σ2DL 

is the downlink noise power, HDL = [ hDL ,1, · · · , hDL ,K ] ∈

CN ×K and V ≜ [v1, · · · , vK ] ∈ CN ×K are the downlink channel matrix and the precoding matrix, respectively. To enhance system performance, we optimize the downlink precoding matrix in an E2E manner. Since the downlink channel is unknown, the precoding matrix is optimized to maximize the average sum rate, i.e., 

P5 : max  

> V

EHDL |R [R (HDL , V) |R] (20a) 

s.t. || V|| 2F ≤ PDL , (20b) where the expectation is taken over HDL given R. In fact, 

P5 is the stochastic version of P2 after considering the randomness of the downlink channel. The E2E precoding policy is the mapping from the effective SRSs to the optimized precoder, denoted as V = fE2E (R).

B. Permutation Properties of fCE (·) and fE2E (·)

Problems P4 and P5 are special cases of P1 , where the associated objective and constraint functions, i.e., 

UMSE (HUL , bHUL ), R (HDL , V) and c(R, V) ≜ || V|| 2F −

PDL , are PI to both antennas and users. By Proposition 1, the induced policies fCE (·) and fE2E (·) are PE if PHUL |R(HUL |R)

and PHDL |R(HDL |R) are PI. In what follows, we investigate the PI properties of these distributions to users and antennas, assuming LSRS = 1 for simplicity. Then, the three-order tensors R, N and HUL degenerate into matrices. 8

1) PE Property to Users: Denote rk as the k-th col-umn of R, which represents the effective SRS of the k-th user. Under the assumption that different users are spatially separated and statistically identical [39], their channels are independent and identically distributed. Further considering that the receive noises N are white Gaussian, it is not hard to show that PHUL |R(HUL |R) = QKk=1 PhUL |r(hUL ,k |rk) and 

PHDL |R(HDL |R) = QKk=1 PhDL |r(hDL ,k |rk). Therefore, apply-ing any permutation matrix ΠU to the channel matrices and the received SRS merely reorders the factors in the product, which does not change its value due to the commutativity of multiplication. Hence, PHUL |R(HUL |R) and PHDL |R(HDL |R)

are PI to users. According to Proposition 1, the channel estimation policy 

fCE (·) and the E2E precoding policy fE2E (·) are PE to users. 

2) PE Property to Antennas: Different from the user di-mension, the channel coefficients among different antennas may be spatially correlated, and thus the conditional distri-butions can not be decoupled. For notational simplicity, we consider the single-user case since the number of users does not affect the permutation property to antennas. Then, HUL and R degenerate into vectors hUL and r, respectively, where the superscript k is omitted. The following proposition provides the conditions for the distributions being PI to antennas. 

Proposition 4. Consider the following two conditions: (a) PhUL (·) is PI to antennas. (b) PhDL |hUL (·|· ) is PI to antennas. If (a) holds, then PhUL |r(hUL |r) is PI to antennas. If both (a) and (b) hold, then PhDL |R(hDL |R) is PI to antennas. Proof. See in Appendix B. Proposition 4 indicates that the PI property of PhUL (·) is a premise for that of PhUL |r(hUL |r), P hDL |R(hDL |R). However, the condition does not hold in spatially correlated channels. To see this, we consider the covariance matrix C ≜

EhUL [hUL (hUL )H] of the channel vector. It is not hard to prove that if PhUL (hUL ) = PhUL (ΠT

> A

hUL ) holds for any permu-tation matrix ΠA, then all elements in hUL are identically distributed and equally correlated. As a result, C exhibits the structure given in Fig. 3(a) and C = ΠT

> A

CΠ A holds for any permutation matrices ΠA.(a)  (b)  

> Fig. 3. Visualization of (a) symmetric matrices with identical diagonal and off-diagonal elements, and (b) Toeplitz matrices.

We now analyze the structure of covariance matrix of spatially correlated channel, where the correlation is induced by the closely spaced antennas. Denote the steering vector of the antenna array equipped at the BS as a(ϕ) ∈ CN ×1, where 

ϕ is the angle of departure (AoD), then the channel covariance matrix is [40] 

C= Eϕ

a(ϕ)aH(ϕ) ≜ Eϕ

h eC(ϕ)

i

. (21) The values of elements ˜cn,n ′ (ϕ) in eC(ϕ) depend on the relative position of the n-th and the n′-th antennas, which is a function of the indices n and n′. This makes C a structural matrix. For example, if the BS is equipped with a ULA, then the steering vector is aULA (ϕ) ≜ [1 , · · · , e j2π(N −1) d sin ϕλ ]T,where d is the antenna spacing and λ is the wavelength, and 

˜cn,n ′ (ϕ) = ej2π(n−n′) d sin ϕλ depends on the relative position of the n-th and n′-th antennas (i.e., n − n′). As a result, eC(ϕ)

and C are Toeplitz matrices [40]. In Fig. 3(b), we visualize its structure for comparison, from which we can see that C

for ULAs does not exhibit the structure given in Fig. 3(a). Similarly, for uniform planar arrays, the corresponding channel covariance matrix is block-Toeplitz [41], and the elements within it depend on the relative positions of antenna pairs along horizontal and vertical directions. Only if d ≫ λ such that the channels between different antennas are approximately 

uncorrelated , the channel covariance matrix will degenerate into a scaled identity matrix with the structure in Fig. 3(a). According to Propositions 1 and 4, the conditions under which the channel estimation policy fCE (·) and E2E precoding policy fE2E (·) are PE are not satisfied in spatially correlated channels. Moreover, with similar derivations, they are also not guaranteed to be shift-equivariant to antennas in such scenarios. Therefore, GNNs and CNNs that are PE or shift equivariant to antennas introduce policy-mismatched inductive biases. As a validation and to help understand the analyses, we ex-amine the PE property of the well-known LMMSE estimator, which has a closed-form expression. From (17), the LMMSE channel estimate of a single user is [42] 

ˆhUL = W⋆r ≜ C(C + σ2UL 

PUL Q IN )−1r. (22) If the effective SRSs are permuted to r′ = ΠT

> A

r, then 

ˆh′ 

> UL

= W⋆ΠT

> A

r. If C has the structure given in Fig. 3(a), it can be proved from (22) that W⋆ will have the same structure. Then, according to Lemma 3 in [43], W⋆ΠT 

> A

= ΠT

> A

W⋆

holds and we have ˆh′ 

> UL

= ΠT 

> A

ˆhUL , which indicates that the LMMSE estimator is PE to antennas. On the contrary, if C does not have the structure in Fig. 3 (a), then W⋆ΠT

> A

̸ =

ΠT

> A

W⋆ and thus ˆh′

> UL

̸ = ΠT 

> A

ˆhUL , i.e., the LMMSE channel estimator is no longer PE to antennas due to spatial correlation. 

C. PA-GNN For Learning fCE (·) and fE2E (·)

We use the PA-GNN with the update equation in (16) to learn both the channel estimation and E2E precoding policies. For both tasks, the input of the PA-GNN is the effective SRSs, while the output is either the estimated channels for learning 

fCE (·) or the precoding matrix for learning fE2E (·). Moreover, an additional normalization step is applied for E2E precoding to satisfy the power constraint in (20b). The same embedding function EB( n, n ′) is used in both cases, while the attention coefficients ω(l) 

> k,k ′,n

for aggregating user vertices are different, since the channel estimation can be decoupled among users. 9

1) Embedding Function: Since the attention coefficients are intended to capture the correlations of hidden representations of antennas, we resort to the spatial channel covariance matrix to guide the design the embedding function. As analyzed in Section V-B, the structure of the covariance matrix comes from its reliance on the relative position of antenna pairs. To show how the covariance cn,n ′ depends on the positional information, we approximate (21) as 

cn,n ′

> (a)

≈ 1

M [Pϕ(ϕ1), · · · , P ϕ(ϕM )] 



˜cn,n ′ (ϕ1)

...

˜cn,n ′ (ϕM )

 , (23) where Pϕ(·) is the PDF of the AoDs, (a) is obtained by approximating the expectation by taking average on samples 

ϕm ≜ 2π(m−1)  

> M

, and M is the number of samples of ϕ

affecting the accuracy of the approximate. In (23), the coefficients in [Pϕ(ϕ1), · · · , P ϕ(ϕM )] reflect the unknown distribution of ϕ, while the positional information is reflected in [˜ cn,n ′ (ϕ1), · · · , ˜cn,n ′ (ϕM )] T. Hence, we design the embedding function as 

EB( n, n ′) = [˜ cn,n ′ (ϕ1), · · · , ˜cn,n ′ (ϕM )] . (24) Consequently, the resulting A(l) and the channel covariance matrix C exhibit the same structure as eC(ϕ). To see this, we assume ˜cn1,n ′ 

> 1

(ϕ) = ˜ cn2,n ′ 

> 2

(ϕ) in eC(ϕ). Then, we have 

cn1,n ′ 

> 1

= E[˜ cn1,n ′ 

> 1

(ϕ)] = E[˜ cn2,n ′ 

> 2

(ϕ)] = cn2,n ′ 

> 2

in C, and 

a(l)

> n1,n ′
> 1

= f (l) 

> A

(EB( n1, n ′

> 1

)) = f (l) 

> A

(EB( n2, n ′

> 2

)) = a(l)

> n2,n ′
> 2

in 

A(l).According to the definition of ˜cn,n ′ (ϕ), the specific form of the embedding function can be obtained by substituting the steering vectors into (21). For example, when ULA is deployed at the BS, the positional embedding vector is 

EB( n, n ′) = 

h

ej2π(n−n′) d sin ϕ1 

> λ

, · · · , e j2π(n−n′) d sin ϕMλ

iT

,

(25) which is a function of n−n′. Consequently, W(l)G is a Toeplitz matrix. Similarly, if a UPA is deployed, the corresponding embedding function can be derived from the UPA steering vector provided in [41]. 

2) Attention Coefficients for Aggregating User Vertices: 

Due to the orthogonality of the SRSs, the channel estimation problem in (18) can be decoupled among users, and thus the update of d(l) 

> n,k

does not require aggregating information from edges (n, k ′) connecting to other users. Therefore, we set 

ω(l) 

> k,k ′,n

as zero for learning channel estimation. The update equation for estimating the k-th user’s channel vector is 

d(l)

> n,k

=f (l)

> C

˚d(l−1)  

> n,k

,

> N

X

> n′=1

f (l) 

> A

(EB( n, n ′)) ⊙ ˚d(l−1) 

> n′,k



. (26) For learning the E2E precoding policy, we compute ω(l)

> k,k ′,n

in the same way as [7] to leverage channel correlation between users. The resulting update equation of the PA-GNN, which is PE to users, is 

d(l) 

> n,k

=f (l) 

> C

d(l−1)  

> n,k

,

> N

X

> n′=1

f (l) 

> A

(EB( n, n ′)) ⊙ ˚d(l−1)  

> n′,k

,

> K

X

> k′=1

f (l) 

> U

(d(l) 

> k

, d(l) 

> k′

) ⊙ ˚d(l)

> n,k ′

!

, (27) where f (l) 

> U

(d(l) 

> k

, d(l) 

> k′

) ≜ PNn=1 W(l) 

> Q

d(l) 

> n,k

⊙ W(l) 

> K

d(l) 

> n,k ′

/N .VI. S IMULATION RESULTS 

In this section, we evaluate the performance of the proposed PA-GNN for learning channel estimation and E2E precoding. 

A. Simulation Setup 

Simulation setups are as follows unless otherwise specified. According to the 3GPP standard [44], each frame consists of 10 subframes, each of which is with duration of Ts = 1 

ms. The numbers of uplink and downlink subframes are set as LUL = LDL = 5 [34]. The carrier frequency of the system is 3.5 GHz. The uplink and downlink SNRs are denoted as 

SNR UL ≜ PUL 

> σ2UL

and SNR DL ≜ PDL 

> σ2DL

, respectively. The channels of different users are independent, which are generated with the time-varying Saleh-Valenzuela channel model [45]. The BS is equipped with a ULA, and the channel vector of the k-th user in the l-th uplink subframe is 

hl 

> UL ,k

= 1

pNcl Nray 

> Ncl

X

> a=1
> Nray

X

> b=1

αa,b k ej2πlT sf a,b  

> d

aULA (ϕa,b k ),

(28) where Ncl and Nray are the numbers of clusters and scattering paths in each cluster, respectively, αa,b k and ϕa,b k are the complex channel gain and the AoD, respectively, f a,b  

> d

≜ 

> vk
> λ

cos( θa,b  

> d

) is the Doppler frequency shift, vk is the speed of the k-th user, and θa,b  

> d

is the movement direction relative to the angle of arrival of the k-th user. The settings of these parameters are given in Table I.                

> TABLE I SIMULATION PARAMETERS
> Parameters Values
> Ncl , N ray 2, 5
> αa,b kCN (0 ,1)
> ϕa,b kU( ¯ϕak−∆ϕ,¯ϕak+ ∆ ϕ)¯ϕakU(−π/ 2, π/ 2) ∆ϕπ/ 36
> vk30 km/h
> θa,b
> dU(−π, π )

According to the frame structure shown in Fig. 2 and the channel reciprocity, the channel vector of the k-th user in the 

l-th downlink subframe is obtained by replacing the index l

with l + LSRS in (28), i.e., hl 

> DL ,k

= hl+LSRS  

> UL ,k

.We generate a training set consisting of 100,000 samples, and the fine-tuned hyper-parameters of the proposed PA-GNN are listed in Table II. For channel estimation, each sample is a pair of R and HUL . The DNNs with input R are trained to minimize the MSE between its output (i.e., the channel estimate) and the label HUL via supervised learning . For E2E precoding, each sample is a realization of R. The DNNs with input R are trained to maximize the downlink sum rate via 

unsupervised learning , where the loss function is the negative sum rate. 10                                    

> TABLE II HYPER -PARAMETERS
> Hyper-Channel E2E parameters Estimation Precoding
> NL45
> Cl128 Hidden Layer Activation Leaky ReLU Output Layer Activation Linear
> √PDL D(NL)
> || D(NL)|| F
> f(l)
> P(·)-Linear
> f(l)
> C(·)Single-layer FNN
> f(l)
> A(·)Single-layer FNN ( M= 128 )Batch Size 256 Learning Rate 0.01 * The input dimension of f(l)
> C(·)and the output dimensions * of f(l)
> C(·)and f(l)
> A(·)are determined by Cl.

B. Channel Estimation 

For channel estimation, we consider K = 1 since the problem can be decoupled among different users, and set 

LSRS = Q = 1 to estimate the channel in a single subframe. We compare the PA-GNN with the following baselines.  

> •

LS: The channel estimator that regards R in (17) as the estimate of HUL . 

> •

LMMSE: The LMMSE estimator in (22), where C is obtained from (23) with M = 512 . 

> •

OMP: The channel estimation algorithm used in [20], where the dictionary contains N steering vectors uni-formly distributed in the angular domain.  

> •

Vanilla GNN: The GNN designed in [1] without atten-tion, which is PE to both users and antennas. 3 

> •

CNN with Attention: The DNN proposed in [25], which integrates CNN with an attention mechanism.  

> •

Dual CNN: The DNN proposed in [24], where two CNNs learning in spatial and angular domains, respectively, are cascaded together for channel estimation.  

> •

Transformer: The encoder part of the Transformer archi-tecture in [14]. The effective SRSs received by different antennas are regarded as different tokens. Positional en-coding is reserved to capture the spatial correlation.  

> •

Transformer (w/o p.e.) The only different to Trans-former is removing the positional encoding, and it is PE to antennas. The baseline DNNs, except for the Dual CNN, have the same numbers of hidden layers and the same hidden dimen-sions as the PA-GNN for a fair comparison. For the Dual CNN, each of the two CNNs consists of four hidden layers, with each layer containing 32 neurons, as in [25]. 

1) Performance versus System and Environment Parame-ters: For a fair comparison, we temporarily do not evaluate the performance of the Dual CNN, since it leverages information from angular domain while other DNNs only learn in spatial domain. The performance of DNNs when learning in angular domain will be compared later. In Fig 4, we compare the channel estimation accuracy of different methods under different uplink SNRs. It is shown that the performance of vanilla GNN and Transformer (w/o p.e.) nearly overlaps with that of LMMSE, whereas other learning-based channel estimators outperform non-learning methods.   

> 3The PE property of users is unnecessary when K= 1 .

The performance of vanilla GNN and Transformer (w/o p.e.) is inferior since they can only learn PE policies with respect to antennas, whereas the original channel estimation policy is not PE. According to our previous analysis, positional information should be incorporated so that the network can learn the corresponding augmented policy. With positional encoding, the Transformer can leverage antenna positional information and thus achieves improved performance. The proposed PA-GNN is with the lowest MSE, since its positional attention mechanism incorporates steering vectors as prior knowledge.      

> Fig. 4. MSE versus SNR UL ,N= 128 ,Ncl = 2 .

In Fig. 5, we show the impact of the number of antennas. It is shown that the MSEs of all methods decrease with more antennas, since more information can be used for estimating the channel coefficient of each antenna due to the spatial correlation. Again, the proposed PA-GNN achieves the lowest MSE among all methods, and the performance gain over other DNNs comes from the proper inductive bias.                

> Fig. 5. MSE versus N,SNR UL = 10 dB, Ncl = 2 .
> Fig. 6. MSE versus Ncl ,SNR UL = 10 dB, N= 128 .

In Fig. 6, we show the impact of the channel sparsity. We can see that the MSEs of all methods are lower when 11 

the number of clusters is small. This is because the spatial correlation is stronger in a sparser channel, which leads to a more accurate estimate. The PA-GNN achieves significant lower MSE than other methods. 

2) Performance of Learning in Angular Domain: In pre-vious simulations, the inputs of the DNNs are the effective received SRS R. As suggested in [24], [25], we can also learn the channel estimation policy in angular domain by inputting the discrete Fourier transform of R into the DNNs, and the channel estimate is then obtained by applying the inverse Fourier transform to their outputs. The estimation accuracies in spatial and angular domains are compared in Fig. 7, and the results of the vanilla GNN and Transformer (w/o p.e.) are not provided due to their inferior performance. 4 We can see that compared to learning in spatial domain, a higher accuracy can be achieved when learning in angular domain, and the gain is more obvious with larger N . When learning in angular domain, the proposed PA-GNN still achieves the best performance among all DNNs, and the Transformer can achieve nearly identical performance. To further demonstrate the advantage of the proposed PA-GNN, we consider a scenario where the BS is equipped with a UPA, and the channel estimation error achieved by the PA-GNN is reduced by 16% compare to the Transformer when N = 128 .The performance degradation of the Transformer stem from its positional encoding being designed for sequential data, which aligns with the structure of ULAs but not with UPAs.        

> Fig. 7. MSE versus Nwhen learning in different domains, SNR UL = 10
> dB, Ncl = 2 .

C. E2E Precoding 

For E2E precoding, we take the maximization of the sum rate of the second downlink subframe as an example. Unless otherwise specified, we set LSRS = Q = 2, and SNR UL = SNR DL = 10 dB. We compare the PA-GNN with the following baselines.  

> •

WMMSE (perf.): The precoding matrix is obtained by the WMMSE algorithm in [46] with perfect channel matrix. It serves as an unachievable performance upper bound for comparison.  

> •

WMMSE (outdate): The precoding matrix is obtained by the WMMSE algorithm [46] with the LMMSE esti-mate of the channel matrix HLSRS  

> UL

. 

> 4“(spatial)” and “(angle)” in the legends of Fig. 7 refer to learning in spatial and angular domains, respectively.
> •

GNN-2D: The GNN proposed in [7]. The only differ-ence from the PA-GNN is to use identical weights for aggregating antenna vertices, whose update equation can be obtained by removing f (l) 

> A

(EB( n, n ′)) in (27). The policy learned by the GNN-2D is PE to both antennas and users.  

> •

GNN-1D2D: The GNN proposed in [47], which cascades two GNNs for learning the E2E precoding policy. The former GNN is PE to users without introducing parameter sharing among antenna vertices, while the later GNN is the GNN-2D. The policy learned by the GNN-1D2D is only PE to users.  

> •

Transformer: The same Transformer as used in Section VI-B, with an extra normalization step to enforce the power constraint on the outputs. We regard the effective SRSs received by different antennas as different tokens and employ positional encoding along the antenna dimen-sion for comparison. 

1) Performance versus System and Environment Param-eters: In Fig. 8, we compare the sum rate achieved by different methods under different downlink SNRs. Only partial results of WMMSE (perf.) are provided, in order to show the performance difference among other methods when the down-link SNR is high. It can be observed that all learning-based methods outperform WMMSE (outdated). This is because the DNNs for E2E learning can predict channels implicitly by leveraging temporal correlation, and achieve lower MSE than the LMMSE channel estimator. Meanwhile, the performance of the WMMSE algorithm is very sensitive to the outdating of channels. Moreover, the proposed PA-GNN performs the best, and the gains over other DNNs grow as the downlink SNR increases.      

> Fig. 8. Sum rate versus SNR DL ,N= 16 ,K= 8 .

In Fig. 9 and Fig. 10, we provide the sum rates versus the numbers of users and antennas. We can see that the proposed PA-GNN achieves the best performance among all the methods except the unachievable upper bound. The GNN-1D2D and GNN-2D are inferior to the PA-GNN for different reasons. Specifically, GNN-1D2D does not share parameters among antenna vertices in its initial layers and thus has larger hypothesis space, leading to an overfitting for channel estimation [1]. GNN-2D, by contrast, enforces permutation equivariance along the antenna dimension without incorporat-ing positional information, and therefore cannot represent the statistical distinctions among antennas required by the E2E 12          

> Fig. 9. Sum rate versus K,N= 16 .
> Fig. 10. Sum rate versus N,K= 4 .

policy. The performance of the Transformer is poor when 

K and N are close. This is because, under such scenarios, the precoding policy is hard to learn due to severe multi-user interference, while the Transformer applying attention along the antenna dimension fails to capture the correlation among users.       

> Fig. 11. Sum rate versus LSRS ,N= 16 ,K= 8 .

In Fig. 11, we evaluate the sum rates versus the number of uplink subframes for transmitting SRSs. It can be observed that the performance of all learning-based methods increases with 

LSRS , since a more accurate channel matrix can be obtained with more historical information. Nonetheless, there is still a gap to the performance upper bound even if larger LSRS 

is considered (e.g., the performance of the PA-GNN is no more than 25 bps/Hz), because the future channels can not be predicted perfectly. 

2) Sample Complexity: In Fig. 12, we compare the sample complexities of the GNNs by evaluating their performance with different numbers of training samples. As can be seen, when the number of training samples is large (say more than 50,000), the sum rate achieved by the Transformer is the lowest, since it can not mitigate the multi-user interference effectively as mentioned before. The GNN-2D also performs poorly, since it can only learn 2D-PE functions, and thus its hypothesis space does not include the original E2E precoding policy without positional augmenta-tion. When the training samples are fewer, the performance of GNN-1D2D and Transformer degrades rapidly. This is because GNN-1D2D or Transformer does not introduce parameter sharing among antenna or user vertices, respectively, and thus has larger hypothesis space. As a result, more training samples are required to achieve a satisfactory performance [1].     

> Fig. 12. Sample complexity of the GNNs, N= 16 , K = 8 .

3) Size Generalizability: We evaluate the generalizabil-ity of the GNNs to the numbers of transmit antennas and users, by testing the GNNs in the scenarios “unseen” by the training samples. The performance is measured by the sum rate achieved on test samples from the new environment, normalized by the performance of a PA-GNN trained and evaluated within the new environment (with legend “Ref. Perf.”). In Table III, we compare the generalizability of the GNNs to the number of users, where the GNNs are trained with the samples in a scenario of K = 8 . The Transformer is not eval-uated, since we treat the effective SRSs of different antennas as distinct input tokens, and thus it is not generalizable to K.We can see that the PA-GNN achieves better generalization performance than GNN-1D2D and GNN-2D.                             

> TABLE III GENERALIZATION ABILITY TO K,N= 16 .
> K468 (Train) 10 12
> Ref. Perf. 14.7 18.1 20.2 21.4 22.0
> (bps/Hz) PA-GNN 90.3% 100.0% 100.0% 99.4% 98.1% GNN-1D2D 86.1% 92.8% 91.5% 89.9% 88.1%
> GNN-2D 53.0% 90.6% 90.6% 90.0% 89.0%

In Table IV, we compare the generalizability of the GNNs to the number of antennas, where the GNNs are trained with the samples in a scenario of N = 16 . The GNN-1D2D is not evaluated, since it is not generalizable to N . It is shown that both of the PA-GNN and GNN-2D can be well generalized except the case of N = K = 4 , and the PA-GNN performs slightly better. The generalization performance of the Transformer is worse. 13                              

> TABLE IV GENERALIZATION ABILITY TO N,K= 4 .
> N4816 (Train) 32 64
> Ref. Perf. 5.9 9.9 14.7 20.5 24.8
> (bps/Hz) PA-GNN 86.1% 91.9% 100.0% 93.2% 92.7% GNN-2D 85.8% 91.1% 95.1% 90.0% 90.7%
> Transformer 75.0% 87.2% 91.2% 87.0% 88.7%

4) Learned Attention Matrix: To understand the behavior of the PA-GNN for learning E2E precoding policy, we virtualize the attention matrix A(l) learned by the PA-GNN in the form of heat map, as shown in Fig. 13. (a)  (b)  

> 0
> 0.1
> Fig. 13. Attention matrices learned by the PA-GNN in the first layer (a) and the last layer (b).

As can be seen, in the first layer of the PA-GNN, the ele-ments near the diagonal are much larger, exhibiting a structure similar to that shown in Fig. 3(b). This indicates that the network mainly aggregates information from nearby antennas. Such a behavior is consistent with the spatial correlation induced by the antenna array geometry, where antennas that are physically close tend to experience similar propagation environments. Consequently, the positional embedding plays an important role in guiding the aggregation process in the early layers. In contrast, in the last layer, the off-diagonal elements of the learned attention matrix take similar values, resulting in a structure close to that shown in Fig. 3(a). This suggests that the dependence on positional embeddings weakens in deeper layers. This can be explained by the structure of the E2E precoding policy, which can be viewed as a composite of the channel estimation/prediction policy and the precoding policy. The latter, as discussed in P2 , is distribution-independent. As a result, the subsequent layers mainly learn the PE precoding policy and therefore do not require positional information. VII. C ONCLUSION 

In this paper, we proposed a PA-GNN for learning distribution-dependent policies in MU-MISO systems. We first analyzed how the mathematical expectation in the objective of wireless problems lead to policies whose permutation properties depend on the underlying distribution. Then, we proposed a novel attention mechanism where the attention coefficients are determined by the relative positions of vertices, allowing the GNN to represent permutation non-equivariant functions. We applied the PA-GNN to two tasks, i.e., channel estimation and E2E precoding, whose policies are not PE to antennas under spatially correlated channels, while the same idea can be extended to other dimensions, such as the subcarrier dimension in multi-carrier systems. Simulation results demonstrated the superiority of the PA-GNN over existing numerical and learning-based methods in terms of performance, and it requires fewer training samples than other learning-based counterparts. Furthermore, the PA-GNN can be generalized to systems with varying numbers of users and antennas. APPENDIX APROOF OF PROPOSITION 1We take the permutation to the antennas as an example for the proof. Since it has been proved in [48] that a policy is PE if the objectives and the constraints are PI, we only need to prove that U (X, Y) = EZ|X[U ′(Y, Z)|X] is PI to antennas. When X and Y are permuted along the antenna dimension by ΠA, we have 

U (ΠT

> A

X, ΠT

> A

Y)=EZ|X

h

U ′(ΠT

> A

Y, Z)|ΠT

> A

X

i

> (a)

=

Z

U ′(ΠT

> A

Y, Z)PZ|X(Z|ΠT

> A

X) dZ

> (b)

=

Z

U ′(ΠT

> A

Y, ΠT

> A

Z′)PZ|X(ΠT

> A

Z′|ΠT

> A

X) dΠT

> A

Z′

> (c)

=

Z

U ′(Y, Z′)PZ|X(Z′|X) dZ′ = U (X, Y),

where (b) is obtained by substituting Z′ ≜ ΠAZ, and (c) holds because U ′(Y, Z) and PZ|X(Z|X) are PI to antennas and the permutation matrices are orthogonal matrices. In the derivation above, the permutations ΠT

> A

X and ΠT

> A

Y

represent that the measurements and decisions associated with different antennas are permuted, rather than a mere reorder-ing of antenna indices. Consequently, the conditional PDF 

PZ|X(·|· ) used in step (a) is the same as that defined in (1). Otherwise, if the permutation is caused by index reordering, the conditional PDF will transform into a permuted version 

PZπ |Xπ (z|x) ≜ PZ|X(ΠT

> A

z|ΠT

> A

x) accordingly as in (4). In that case, the invariance of the utility always holds and does not rely on the PI properties of U ′(Y, Z) and PZ|X(Z|X), as the physical scenario is identical after reordering antenna indices. The proof for the permutation of users is similar. APPENDIX BPROOF OF PROPOSITION 4If PhDL |hUL (·|· ) is PI to antennas, according to Bayesian theorem, we have 

PhUL |r(ΠAhUL |ΠAr)= Pr|hUL (ΠAr|ΠAhUL )PhUL (ΠAhUL )

Pr(ΠAr)

> (a)

= Pn (ΠA(r − hUL )) PhUL (ΠAhUL )

R Pn (ΠA(r − hUL )) PhUL (ΠAhUL ) dhUL 

> (b)

= Pn (r − hUL ) PhUL (hUL )

R Pn (r − hUL ) PhUL (hUL ) dhUL 

= Pr|hUL (r|hUL )PhUL (hUL )

Pr(r) = PhUL |r(hUL |r), (29) 14 

where (a) holds due to (17), and (b) holds due to the assump-tion of PhUL (hUL ) = PhUL (ΠAhUL ).If PhDL |hUL (·|· ) is also PI to antennas, we have 

PhDL |r(ΠT

> A

hDL |ΠT

> A

r)=

Z

PhDL |hUL (ΠT

> A

hDL |hUL )PhUL |r(hUL |ΠT

> A

r) dhUL 

> (a)

=

Z

PhDL |hUL (ΠT

> A

hDL |ΠT

> A

h′

> UL

)

×PhUL |r(ΠT

> A

h′

> UL

|ΠT

> A

r) dΠT

> A

h′

> UL
> (b)

=

Z

PhDL |hUL (hDL |h′

> UL

)PhUL |r(h′

> UL

|r) dh′

> UL

=PhDL |r(hDL |r),

where (a) is obtained by substituting h′ 

> UL

≜ ΠT

> A

hUL , and (b) holds due to the PI assumption of PhDL |hUL (·|· ).REFERENCES [1] B. Zhao, J. Guo, and C. Yang, “Understanding the performance of learn-ing precoding policies with graph and convolutional neural networks,” 

IEEE Trans. Commun. , vol. 72, no. 9, pp. 5657–5673, Apr. 2024. [2] Y. Lu, Y. Li, R. Zhang, W. Chen, B. Ai, and Others, “Graph neural networks for wireless networks: Graph representation, architecture and evaluation,” IEEE Wireless Commun. , vol. 32, no. 1, pp. 150–156, Oct. 2024. [3] P. Velickovic, G. Cucurull, A. Casanova, A. Romero, P. Lio et al. ,“Graph attention networks,” stat , vol. 1050, no. 20, pp. 10–48 550, Oct. 2017. [4] V. P. Dwivedi and X. Bresson, “A generalization of transformer networks to graphs,” arXiv:2012.09699 , 2020. [5] Z. Huang, Z. Wang, and S. Chen, “Sub-6GHz assisted mmwave hybrid beamforming with heterogeneous graph neural network,” IEEE Trans. Commun. , vol. 72, no. 11, pp. 6917–6928, May 2024. [6] J. Yang, J. Xu, Y. Zhang, H. Zheng, and T. Zhang, “Graph attention network-based precoding for reconfigurable intelligent surfaces aided wireless communication systems,” IEEE Trans. Veh. Technol. , vol. 73, no. 6, pp. 9098–9102, Jan. 2024. [7] S. Liu, J. Guo, and C. Yang, “Multidimensional graph neural networks for wireless communications,” IEEE Trans. Wireless Commun. , vol. 23, no. 4, pp. 3057–3073, Aug. 2024. [8] J. Guo and C. Yang, “Recursive GNNs for learning precoding policies with size-generalizability,” IEEE Trans. Mach. Learn. Commun. Netw. ,vol. 2, pp. 1558–1579, Oct. 2024. [9] S. Mishra, L. Salaun, H. Yang, and C. S. Chen, “Graph neural network aided power control in partially connected cell-free massive MIMO,” 

IEEE Trans. Wireless Commun. , vol. 23, no. 9, pp. 12 412–12 423, Apr. 2024. [10] H. Hu, Z. Xie, H. Shi, B. Liu, H. Zhao, and G. Gui, “Unsupervised power allocation based on combination of edge aggregated graph attention network with deep unfolded WMMSE,” IEEE Trans. Veh. Technol. ,vol. 73, no. 11, pp. 17 359–17 372, July 2024. [11] Z. Wang, M. Bennis, and Y. Zhou, “Graph attention-based MADRL for access control and resource allocation in wireless networked control systems,” IEEE Transactions on Wireless Communications , vol. 23, no. 11, pp. 16 076–16 090, Aug. 2024. [12] J. Shi, W. Wang, X. Yi, X. Gao, and G. Y. Li, “Deep learning-based robust precoding for massive mimo,” IEEE Trans. Commun. , vol. 69, no. 11, pp. 7429–7443, Aug. 2021. [13] T. Jiang, H. V. Cheng, and W. Yu, “Learning to reflect and to beamform for intelligent reflecting surface with implicit channel estimation,” IEEE J. Sel. Areas Commun. , vol. 39, no. 7, pp. 1931–1945, May 2021. [14] A. Vaswani, “Attention is all you need,” NeurIPS , 2017. [15] D. Bahdanau, K. Cho, and Y. Bengio, “Neural machine translation by jointly learning to align and translate,” arXiv:1409.0473 , 2014. [16] J. Guo and C. Yang, “A model-based GNN for learning precoding,” 

IEEE Trans. Wireless Commun. , vol. 23, no. 7, pp. 6983–6999, Dec. 2023. [17] J. Lee, G.-T. Gil, and Y. H. Lee, “Channel estimation via orthogonal matching pursuit for hybrid MIMO systems in millimeter wave com-munications,” IEEE Trans. Commun. , vol. 64, no. 6, pp. 2370–2386, Apr. 2016. [18] D. L. Donoho, A. Maleki, and A. Montanari, “Message-passing algo-rithms for compressed sensing,” PNAS , 2009. [19] D. P. Wipf and B. D. Rao, “Sparse bayesian learning for basis selection,” 

IEEE Trans. Signal Process. , vol. 52, no. 8, pp. 2153–2164, Aug. 2004. [20] M. Liu, T. Lin, Y. Zhu, and Y.-J. A. Zhang, “Sparse channel estima-tion for IRS-assisted millimeter wave MIMO OFDM systems,” IEEE Transactions on Communications , vol. 72, no. 10, pp. 6553–6568, May 2024. [21] C. Ruan, Z. Zhang, H. Jiang, H. Zhang, J. Dang, and L. Wu, “Simplified learned approximate message passing network for beamspace channel estimation in mmWave massive MIMO systems,” IEEE Trans. Wireless Commun. , vol. 23, no. 5, pp. 5142–5156, Oct. 2023. [22] H. Yang, A. Zhang, Y. Sun, J. Li, and P. Liu, “Regularized sparse bayesian learning based channel estimation for RIS-assisted wireless communication system,” IEEE Commun. Lett. , vol. 28, no. 6, pp. 1412– 1416, Mar. 2024. [23] L. Lv, S. Wu, Y. Su, C. Jiang, and L. Kuang, “Sparse bayesian learning unfolding network for efficient DoA estimation in low SNR,” IEEE Trans. Veh. Technol. (Early Access) , Apr. 2025. [24] P. Jiang, C.-K. Wen, S. Jin, and G. Y. Li, “Dual CNN-based channel estimation for MIMO-OFDM systems,” IEEE Trans. Commun. , vol. 69, no. 9, pp. 5859–5872, June 2021. [25] J. Gao, M. Hu, C. Zhong, G. Y. Li, and Z. Zhang, “An attention-aided deep learning framework for massive MIMO channel estimation,” IEEE Trans. Wireless Commun. , vol. 21, no. 3, pp. 1823–1835, Aug. 2021. [26] M. Ye, X. Liang, C. Pan, Y. Xu, M. Jiang et al. , “GNN-based channel estimation for intelligent reflecting surface aided multiuser systems relying on user locations,” IEEE Wireless Commun. Lett. , vol. 13, no. 8, pp. 2110–2114, May 2024. [27] S. Singh, A. Trivedi, and D. Saxena, “Channel estimation for intelligent reflecting surface aided communication via graph transformer,” IEEE Trans. Green Commun. Netw. , vol. 8, no. 2, pp. 756–766, Dec. 2023. [28] F. Liu, P. Jiang, J. Zhang, W. Wang, C.-K. Wen et al. , “PD-CEViT: A novel pilot pattern design and channel estimation network for OFDM systems,” IEEE Trans. Commun. (Early Access) , Nov. 2024. [29] F. Sohrabi, K. M. Attiah, and W. Yu, “Deep learning for distributed channel feedback and multiuser precoding in FDD massive MIMO,” 

IEEE Trans. Wireless Commun. , vol. 20, no. 7, pp. 4044–4057, Feb. 2021. [30] Y. Guo, W. Chen, J. Xu, L. Li, and B. Ai, “Deep joint CSI feedback and multiuser precoding for MIMO OFDM systems,” IEEE Trans. Veh. Technol. , vol. 74, no. 1, pp. 1730–1735, Aug. 2024. [31] Y. Chen, W. Xia, J. Zhang, and Y. Zhu, “Joint learning of channel estimation and beamforming for cell-free massive MIMO systems,” 

IEEE Wireless Communications Letters , vol. 13, no. 5, pp. 1359–1363, Feb. 2024. [32] J. Park, F. Sohrabi, A. Ghosh, and J. G. Andrews, “End-to-end deep learning for TDD MIMO systems in the 6G upper midbands,” IEEE Trans. Wireless Commun. , vol. 24, no. 3, pp. 2110–2125, Dec. 2024. [33] H. Jiang, M. Cui, D. W. K. Ng, and L. Dai, “Accurate channel prediction based on transformer: Making mobility negligible,” IEEE J. Sel. Areas Commun. , vol. 40, no. 9, pp. 2717–2732, July 2022. [34] R. Wang, C. Yang, S. Han, J. Wu, S. Han et al. , “Learning end-to-end hybrid precoding for multi-user mmWave mobile system with GNNs,” 

IEEE Trans. Mach. Learn. Commun. Netw. , pp. 978–993, June 2024. [35] Z. Linfu, P. Zhiwen, and M. El-Hajjar, “Graph neural network aided beamforming for holographic millimeter wave MIMO systems,” IEEE Trans. Veh. Technol. (Early Access) , Feb. 2025. [36] P. Kall, S. W. Wallace, and P. Kall, Stochastic programming . Springer, 1994, vol. 5. [37] J. Gilmer, S. S. Schoenholz, P. F. Riley, O. Vinyals, and G. E. Dahl, “Neural message passing for quantum chemistry,” ICML , 2017. [38] J. R. Hampton, Introduction to MIMO communications . Cambridge university press, 2013. [39] D. Gesbert, M. Kountouris, R. W. Heath, C.-B. Chae, and T. Salzer, “Shifting the MIMO paradigm,” IEEE Signal Process. Mag. , vol. 24, no. 5, pp. 36–46, Sep. 2007. [40] Y.-C. Liang and F. P. S. Chin, “Downlink channel covariance matrix (DCCM) estimation and its applications in wireless DS-CDMA sys-tems,” IEEE J. Sel. Areas Commun. , vol. 19, no. 2, pp. 222–232, Feb. 2001. [41] Y.-M. Chen, J.-H. Lee, and C.-C. Yeh, “Two-dimensional angle-of-arrival estimation for uniform planar arrays with sensor position errors,” 

IEE Proc. , 1993. [42] S. M. Kay, “Fundamentals of statistical signal processing: Estimation theory,” 1993. 15 

[43] M. Zaheer, S. Kottur, S. Ravanbakhsh, B. Poczos, R. R. Salakhutdinov 

et al. , “Deep sets,” NeurIPS , 2017. [44] 3GPP, “NR: Physical channels and modulation (release 15),” 3GPP, Tech. Rep. TS 38.211, 2006. [45] A. A. Saleh and R. Valenzuela, “A statistical model for indoor multipath propagation,” IEEE J. Sel. Areas Commun. , vol. 5, no. 2, pp. 128–137, Feb. 1987. [46] Q. Shi, M. Razaviyayn, Z.-Q. Luo, and C. He, “An iteratively weighted MMSE approach to distributed sum-utility maximization for a MIMO interfering broadcast channel,” IEEE Trans. Signal Process. , vol. 59, no. 9, pp. 4331–4340, Apr. 2011. [47] B. Zhao and C. Yang, “Learning beamforming for RIS-aided systems with permutation equivariant graph neural networks,” IEEE VTC-Spring ,2023. [48] M. Eisen and A. Ribeiro, “Optimal wireless resource allocation with random edge graph neural networks,” IEEE Trans. Signal Process. ,vol. 68, pp. 2977–2991, Apr. 2020.