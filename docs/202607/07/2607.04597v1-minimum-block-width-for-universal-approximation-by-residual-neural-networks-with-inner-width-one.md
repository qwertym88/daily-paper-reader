---
title: Minimum Block Width for Universal Approximation by Residual Neural Networks with Inner Width One
title_zh: 内宽度为1的残差神经网络实现通用逼近的最小块宽度
authors: "Qi Zhou, Xuan Zhou, Xiao-Song Yang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04597v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 残差网络通用逼近的理论研究
tldr: 残差神经网络在内部宽度为1时，通用逼近所需的最小块宽度是关键问题。针对LeakyReLU等激活函数，理论分析得出Lp逼近的最小块宽为输入输出维度的最大值；均匀逼近的上界由输入输出维度的组合确定。同时证明块宽小于该最小值时无法逼近任意目标函数。该结果揭示了残差网络结构复杂度的理论边界，为网络设计提供依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04597v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1030, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04597v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 433, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04597v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1060, \"height\": 517, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04597v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1361, \"height\": 1029, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04597v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1324, \"height\": 1670, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04597v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1264, \"height\": 417, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04597v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1398, \"height\": 951, \"label\": \"Table\"}]"
motivation: 探究内宽为1的残差网络实现通用逼近所需的最小块宽度。
method: 理论分析结合构造性证明，建立块宽度的上下界。
result: "Lp逼近最小块宽为max{dx,dy}，均匀逼近上界为min{dx+dy, max{2dx+1,dy}}，且小于max{dx,dy}时无法逼近。"
conclusion: 确定了残差网络以最小复杂度逼近任意函数的关键条件。
---

## 摘要
本文研究了残差神经网络的通用逼近性质，并得到了一些新结果。对于输入和输出维度$d_x$和$d_y$，以及LeakyReLU、ReLU、类ReLU激活函数，建立了块宽度的上下界。为了在任意紧致域上实现$L^p$逼近（$1 \leq p < +\infty$），我们证明了当内部宽度为1时，精确的最小块宽度为$\max\{d_x, d_y\}$。此外，我们证明了在残差分支内部宽度为1的约束下，块宽度为$\min\{d_x + d_y, \max\{2d_x + 1, d_y\}\}$的残差神经网络可以在任意紧致域上实现一致逼近。另外，对于任意激活函数族，我们证明了块宽度小于$\max\{d_x, d_y\}$的残差神经网络无法逼近所有目标函数，无论是在$L^p$意义下还是一致意义下，无论内部宽度如何。

## Abstract
In this paper, we study the universal approximation property of residual neural networks, and obtain some new results. For input and output dimensions $d_x$ and $d_y$, and LeakyReLU, ReLU, ReLU-like activation functions, the upper and lower bounds of the block width are established. To achieve $L^p$ approximation $(1\leq p <+\infty)$ on any compact domain, we show that the exact minimum block width is $\max\{d_x,d_y\}$ when the inner width is 1. Furthermore, we show that residual neural networks with block width $\min\{d_x+d_y, \max\{2d_x+1,d_y\}\}$ can achieve uniform approximation on any compact domain under the constraint that each residual branch has inner width 1. Besides, for any activation function family, we prove that residual neural networks with block width less than $\max\{d_x, d_y\}$ cannot approximate all target functions, both in the $L^p$ sense and the uniform sense, regardless of inner width.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：残差神经网络（ResNets）在通用逼近理论中，两个关键结构参数——块宽度（block width）和内部宽度（inner width）——对逼近能力的影响。本文聚焦于最严格的内部宽度为1的情况，探究：
  - 内部宽度为1时，块宽度需多大才能实现 $L^p$ 逼近和一致逼近？
  - 是否存在一个下界，当块宽度低于该下界时，无论内部宽度多大，ResNets 都无法实现通用逼近？
- **背景**：
  - 现有结果：ODENet 和 ResNet 在 $d_x \ge d_y$ 条件下有上界结果（如 $d_x + d_y$、$2\max\{d_x,d_y\}+1$ 等），但缺乏内宽为1时的下界和精确最小宽度。
  - 本文填补了内部宽度固定为1时块宽度必要性和充分性的理论空白。
  - 揭示了块宽度和内部宽度在逼近能力中的不同角色：块宽度提供残差变换的**环境维度**，内部宽度控制局部非线性更新的**复杂度**。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  1. 将 ResNet 分解为：初始线性层（提升维度）→ 残差块序列（每个残差块 = identity + 一个内宽为1的小网络）→ 最终线性层（投影到输出空间）。
  2. 利用仿射变换、坐标非线性映射（如LeakyReLU、ABS、ReLU-like）的逼近能力，证明由内宽为1的残差块组合可以近似任意连续函数。
  3. 对 $L^p$ 逼近：构造上界，并利用拓扑/维数论证明下界。
  4. 对一致逼近：通过嵌入到更高维空间并利用微分同胚逼近定理（C2-微分同胚可通过残差块组合逼近），得到上界；下界同 $L^p$ 情况。
- **关键技术**：
  - 定理1：任何仿射变换 $T_{W,B} \in \text{Aff}_{w,w}$ 可由内宽为1的残差块组合进行紧逼近（基于SVD和PLU分解）。
  - 定理2：对LeakyReLU、ReLU、类ReLU激活函数，分段线性函数 $f_a(x)$（$a<0$ 对应于ABS）可由内宽为1的残差块组合逼近。
  - 定理3（$L^p$上界）：$w_{\min}^p \le \max\{d_x,d_y\}$。证明：MLP（含LeakyReLU和ABS）可逼近任意 $L^p$ 函数，且MLP的每一层可被残差块模拟。
  - 定理4（下界）：$w_{\min}^p \ge \max\{d_x,d_y\}$，一致逼近下界相同。证明：若 $w < d_x$，构造函数 $f^*(x)=\|x\|$ 无法逼近（因为存在退化方向）；若 $w < d_y$，构造多项式曲线无法被低维仿射子空间包含。
  - 定理5（精确结果）：$L^p$ 下最小块宽度 $w_{\min}^p = \max\{d_x,d_y\}$。
  - 定理7（一致逼近界限）：$\max\{d_x,d_y\} \le w_{\min}^{\sup} \le \min\{d_x+d_y, \max\{2d_x+1,d_y\}\}$。
  - 下界对任意激活函数族和任意内宽成立。
- **公式/算法**：无特定算法流程，主要是构造性证明。

## 3. 实验设计

- 该论文为**纯理论工作**，未设计系统实验集群。
- 仅在附录C中提供了一个**数值示例**：
  - 目标函数：一个分段线性映射 $f: \mathbb{R}^2 \to \mathbb{R}^2$（由式(7)定义）。
  - 网络架构：块宽3、内宽1、5个残差块，使用ReLU激活。
  - 训练：梯度下降算法（Adam），达到逼近误差 $<3\times10^{-5}$（目标函数在 $[-2,2]^2$ 上的 sup 误差）。
  - 目的：展示宽度为3的ResNet可以逼近一个之前被认为MLP宽度3无法逼近的函数（克服MLP的拓扑障碍）。
- **Benchmark**：无标准数据集，仅作为理论的反例验证。
- **对比**：无其他方法对比。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长等算力信息。
- 数值示例规模极小（5个残差块，总参数量约几十），推测在普通CPU/GPU即可完成，非大规模实验。

## 5. 实验数量与充分性

- 仅有一组数值示例，不构成系统性实验。
- 实验充分性：作为理论论文，该实验只是辅助说明理论结论，不追求实验覆盖；主要贡献在数学证明。
- 客观性：理论证明严格，示例仅用于直观展示，不足以验证所有情况，但符合理论论文惯例。

## 6. 论文的主要结论与发现

- **$L^p$ 通用逼近的最小块宽度**：当内部宽度固定为1时，对LeakyReLU、ReLU、类ReLU激活函数，最小块宽度恰好为 $\max\{d_x,d_y\}$（定理5）。
- **一致逼近的块宽度界限**：$\max\{d_x,d_y\} \le w_{\min}^{\sup} \le \min\{d_x+d_y, \max\{2d_x+1,d_y\}\}$（定理7）。
- **下界普适性**：对于任意激活函数族，只要块宽度 < $\max\{d_x,d_y\}$，ResNets 无法实现通用逼近（无论内宽多大）。
- **内宽为1的保持性**：即使残差分支极窄（内宽1），只要块宽度足够，ResNets 仍保持通用逼近性质。
- **与MLP的本质区别**：ResNets 允许非嵌入（自交）图像，因此窄宽度的逼近能力比MLP更强（数值示例显示宽度3可逼近MLP宽度3无法逼近的函数）。

## 7. 优点

- **理论完整性**：同时给出上界和下界，确定 $L^p$ 的精确最小值，接近完全刻画。
- **激活函数覆盖广**：结果适用于LeakyReLU、ReLU以及众多类ReLU函数（ELU、CELU、SELU、Softplus、GELU、SiLU、Mish、ReLU6、Softshrink、HardSigmoid、HardTanh、HardSwish），具有实际指导意义。
- **结构新颖**：揭示内部宽度和块宽度分离的角色，为极简ResNet设计提供理论依据。
- **与现有工作对比**：改进了以往结果（如文献[22]的 $2\max\{d_x,d_y\}+1$，文献[1]的 $d_x+d_y$），去除了 $d_x\ge d_y$ 的限制。
- **一般性下界**：不依赖激活函数具体形式，仅依赖维度，鲁棒性强。
- **数值示例**：虽简单，但清晰说明理论结论，并展示ResNet与MLP的本质差别。

## 8. 不足与局限

- **实验覆盖极弱**：仅有1个数值例子，缺乏大规模验证或对不同维度的系统性测试。
- **一致逼近的下界未完全闭合**：上界 $\min\{d_x+d_y, 2d_x+1\}$ 可能不是紧的（特别在 $d_x$ 为偶数、$d_y=2d_x-1$ 时，文献[9]指出有改进空间），作者自己也承认这一点。
- **深度（残差块数量）的刻画缺失**：论文只讨论宽度，未给出达到指定精度所需的深度估计，实际应用仍需要深度复杂度分析。
- **激活函数限制**：下界不依赖激活函数，但上界依赖类ReLU或LeakyReLU；对于其他激活（如sigmoid、tanh）是否成立未讨论。
- **应用限制**：理论结果针对连续函数/$L^p$ 空间，未涉及离散数据或泛化问题；实际训练中优化困难可能使理论逼近不可达。
- **数值示例的公平性**：对比MLP时使用的函数是特定构造，MLP宽度3的不可逼近性是已知的，但ResNet的成功依赖特定的残差结构和非全局微分同胚，可能不具有普遍性。
- **算力/资源信息缺失**：不能提供实用的训练成本参考。

（完）
