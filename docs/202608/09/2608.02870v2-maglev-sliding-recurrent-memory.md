---
title: "Maglev: Sliding Recurrent Memory"
title_zh: "Maglev: 滑动循环记忆"
authors: "Bo Liu, Qiang Liu"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02870v2"
tags: ["query:neural-arch"]
score: 8.0
evidence: 提出固定大小记忆的循环Transformer新架构，面向高效序列建模
tldr: 针对固定大小记忆的循环Transformer难以兼顾长程依赖与训练并行化的挑战，提出Maglev架构。它由利用全注意力的预填充器Q生成记忆目标，以及仅用滑动窗口注意力与循环K/V注入的解码器P组成，并通过一致性损失对齐二者。实验显示Maglev在验证损失和下游基准上优于滑动窗口与潜在循环Transformer基线，且共享参数可降低内存。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02870v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1135, \"height\": 403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02870v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 477, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.02870v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 437, \"label\": \"Table\"}]"
motivation: 现有滑动窗口和潜在循环Transformer在处理长程依赖时效果有限，且训练并行化存在困难。
method: 提出双模型结构，Q用全注意力生成记忆目标，P用滑动窗口注意力加循环K/V注入，并用一致性损失训练。
result: 在验证损失和下游预训练基准上均优于滑动窗口和潜在循环Transformer基线，共享参数保留大部分增益。
conclusion: Maglev以固定大小记忆实现高效长程建模，兼顾训练并行性，为循环Transformer提供新思路。
---

## 摘要
我们引入了\ours{}，一种具有固定大小记忆的循环Transformer架构，它推广了滑动窗口注意力，同时在训练期间保持可并行化。\ours{}由两个耦合模型组成：一个预填充器$Q$，它利用完全注意力（在实践中，我们对$Q$使用交替的全注意力和滑动窗口注意力，因为这会产生更强的性能。基本要求是$Q$比$P$更具表达力，并且能够访问完整历史记录）来生成记忆目标$m'_t$，以及一个解码器$P$，它仅使用滑动窗口注意力和循环K/V注入来生成用于下一个令牌预测的解码器记忆$m_t$。我们使用记忆一致性损失训练\ours{}，该损失将$m_t$与$m'_t$对齐，从而允许推理单独使用$P$。经验上，\ours{}在验证损失和下游预训练基准上优于滑动窗口和潜在循环Transformer基线。此外，在$P$和$Q$之间共享参数减少了参数内存，同时保留了大部分收益。

## Abstract
We introduce \ours{}, a recurrent Transformer architecture with fixed-size memory that generalizes sliding-window attention while remaining parallelizable during training. \ours{} consists of two coupled models: a prefiller $Q$, which leverages full attention\footnote{In practice, we use interleaved full and sliding-window attention for $Q$, as this yields stronger performance. The essential requirement is that $Q$ be more expressive than $P$, with access to the full history.} to produce memory targets $m'_t$, and a decoder $P$, which uses only sliding-window attention and recurrent K/V injection to produce decoder memories $m_t$ for next-token prediction. We train \ours{} with a memory consistency loss that aligns $m_t$ with $m'_t$, allowing inference to use $P$ alone. Empirically, \ours{} improves validation loss and downstream pretraining benchmarks over sliding-window and latent recurrent transformer baselines. Moreover, sharing parameters between $P$ and $Q$ reduces parameter memory while preserving most of the gains.