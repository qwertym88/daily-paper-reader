---
title: Learning to Control Coupled-Dynamics Environments with Joint Markov Decision Processes
title_zh: 联合马尔可夫决策过程下耦合动态环境的学习控制
authors: "Ege C. Kaya, Aliasghar Pourghani, Mahsa Ghasemi, Vijay Gupta, Abolfazl Hashemi"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.22765v1"
tags: ["query:rl-control"]
score: 9.0
evidence: 开发最优控制方法，定义非参数分布贝尔曼最优性算子
tldr: 针对耦合动力学环境中反事实结果间的依赖关系，本文提出联合马尔可夫决策过程的最优控制方法。定义非参数分布Bellman最优算子，证明在边际MDP唯一最优策略下迭代收敛到最优联合回报分布；对前两矩建立更弱条件下的收敛。还提供神经逼近的采样目标，为JMDP最优控制奠定基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1667, \"height\": 718, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22765v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1476, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22765v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1676, \"height\": 732, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22765v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1549, \"height\": 468, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22765v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1667, \"height\": 492, \"label\": \"Figure\"}]"
motivation: 普通MDP只建模各动作的边际分布，忽略耦合动力学中反事实结果间的依赖，导致控制次优。
method: 定义JMDP的非参数分布Bellman最优算子，推导前两矩收敛条件并给出神经逼近的采样目标。
result: 证明边际MDP唯一最优策略时分布迭代按Wasserstein距离收敛，矩收敛条件更弱。
conclusion: 为耦合动力学环境提供可收敛的最优控制框架，支持神经网络近似求解。
---

## 摘要
耦合动态环境暴露了在共同的外生随机性实现下，若干可能反事实动作所对应的一步结果。普通马尔可夫决策过程形式允许人们推理每个动作的边际分布，但丢弃了这些反事实结果之间的相关性。联合马尔可夫决策过程（JMDP）形式保留了这种相关性。先前的工作建立了该形式并解决了JMDP中的固定策略联合矩评估问题。本文开发了最优控制方法。我们为JMDP定义了一个非参数分布贝尔曼最优性算子，并证明当诱导的边际MDP具有唯一最优策略时，其迭代在Wasserstein距离下收敛到最优联合回报分布。对于前两个矩，我们在更弱条件下建立了收敛性，该条件允许若干均值最优动作，只要它们的平局解决方案共享一个二阶矩不动点。我们还为神经近似推导了采样目标。

## Abstract
Coupled-dynamics environments expose the one-step outcomes that would follow from several possible counterfactual actions under a common realization of exogenous randomness. The ordinary Markov decision process formalism allows one to reason about the marginal law of each action but discards dependence across these counterfactual outcomes. The Joint Markov decision process (JMDP) formalism preserves that dependence. Prior work established the formalism and solved the fixed-policy joint moment evaluation problem in JMDPs. This paper develops optimal-control methods. We define a nonparametric distributional Bellman optimality operator for JMDPs, and prove that when the induced marginal MDP has a unique optimal policy, its iterates converge in Wasserstein distance to the optimal joint return law. For the first two moments, we establish convergence under a weaker condition that permits several mean-optimal actions as long as their tie resolutions share a second-moment fixed point. We also derive sampled targets for neural approximation.

---

## 论文详细总结（自动生成）

## 联合马尔可夫决策过程下耦合动态环境的学习控制：论文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：在经典马尔可夫决策过程（MDP）中，智能体每次只能观测到所执行动作的单一采样结果，这一形式化只保留了各动作回报的边际分布，却丢失了在同一外生随机性实现下、不同反事实动作结果之间的**依赖关系**。
- **问题背景**：在**耦合动态环境**中，不同动作在相同状态下的转移和回报受共享随机变量影响（如共同的风向扰动），此时边际化分析无法刻画动作间回报的协方差、优势概率等跨动作对比性统计量。
- **研究缺口**：虽然已有工作提出联合马尔可夫决策过程（JMDP）形式化框架，并解决了固定策略下的联合矩评估问题，但**JMDP的最优控制问题尚未解决**——如何在保留反事实依赖的前提下设计收敛的最优控制算法，是本文的核心研究问题。
- **整体含义**：本文将JMDP从固定策略评估推进到最优控制层面，在理论上给出了分布级和矩级收敛条件，并在实践上提供了适用于神经网络逼近的采样目标，为耦合动态环境下的安全决策（如交通分配、风险感知控制）奠定了方法基础。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在JMDP中，动作‑回报向量 ($Z(s,a))_{a \in A}$ 的联合分布是核心研究对象。控制目标不是简单地最大化期望，而是在保持均值最优的同时保留跨动作依赖信息。
- **关键技术细节**：
  - **配置（Configuration）**：定义配置 $h = ((s_i, a_i))_{i=1}^d \in H_d = (S \times A)^d$，记录 $d$ 维联合回报向量中每个坐标对应的状态‑动作对，用于对反事实联合回报分布进行索引。
  - **非参数分布Bellman最优性算子**：定义算子 $T_G \eta := T^{G(\eta)} \eta$，其中 $G(\eta)$ 为基于当前均值估计的贪心选择器。该算子在有唯一最优策略时收敛到最优联合回报分布。
  - **定理1（唯一最优策略收敛）**：当诱导的边际MDP具有唯一最优策略且所有状态的动作间隔 $\Delta^* > 0$ 时，分布迭代 $D_p(\eta_k, \eta_H^{\pi^*}) \to 0$（Wasserstein距离）。证明思路：均值迭代收敛后，贪心选择器稳定为最优策略，此后更新退化为固定策略的 $\gamma$-收缩映射。
  - **命题2（更弱的矩收敛条件）**：允许存在多个均值最优动作，但要求所有最优选择器对应同一二阶矩不动点（**条件2**），则一阶矩和二阶矩同时收敛。证明通过误差分解和 $\gamma^2$-收缩性完成。
  - **采样训练目标**：
    - 均值目标：$y_\mu = r + \gamma \bar{\mu}_\theta(s', g_{\bar{\theta}}(s'))$
    - 二阶矩目标：$y_\Sigma = r\tilde{r} + \gamma r\mu_{\bar{\theta}}(\tilde{s}', g_{\bar{\theta}}(\tilde{s}')) + \gamma \tilde{r}\mu_{\bar{\theta}}(s', g_{\bar{\theta}}(s')) + \gamma^2 \Sigma_{\bar{\theta},\bar{\psi}}(s', g_{\bar{\theta}}(s'), \tilde{s}', g_{\bar{\theta}}(\tilde{s}'))$，其中交叉项同时依赖两个后继状态‑动作对。
    - 总体损失函数结合了均值MSE和二阶矩MSE（带权重 $\lambda$）。

### 3. 实验设计：使用了哪些数据集 / 场景、benchmark、对比方法
| 实验场景 | 环境描述 | benchmark/对比方法 |
|---------|---------|------------------|
| **7.1 精确表格分布控制（耦合奖励链）** | 5个非终止状态+终止状态的链式环境，两种动作，共享伯努利变量决定奖励向量，动作0唯一最优 | 普通值迭代、固定策略分布评估、精确二阶矩评估；另有两个状态的手工JMDP验证平局切换行为 |
| **7.2 随机风网格世界（矩控制）** | 5×5网格，四动作，$\gamma=0.95$，共享阵风扰动所有动作的转移 | 普通值迭代（均值）、固定策略JMDP矩评估（协方差）、8000条蒙特卡洛轨迹（96步horizon） |
| **7.3 反相关路径选择环境** | 单步决策：一条安全路线+两条风险山脊路线，共享风导致山脊路线负协方差 | 全协方差均值‑方差（MV）、对角MV基线、CVaR单路选择、优势概率（superiority）准则 |
| **7.4 耦合倒立摆（神经逼近）** | 连续状态（角度‑角速度），三动作，共享扰动，固定稳定器策略，horizon=10 | 联合矩神经网络 vs 对角基线和洗牌基线；联合粒子模型 vs 洗牌粒子模型；用能量距离评估联合分布 |

### 4. 资源与算力
- **论文未明确说明**所使用GPU型号、数量、训练时长等具体计算资源信息。
- 从实验设置可推断：7.4节的神经逼近任务使用了规模较小的网络（两层96单元隐藏层），训练20,000个minibatch、每batch128个状态、每个状态8条回报向量，属于轻量级实验，可在单块中端GPU上短时间完成。但论文未披露实际硬件和运行时间。

### 5. 实验数量与充分性
- **实验组数**：共4个主要实验场景，覆盖表格精确计算（7.1）、有限状态动态规划（7.2）、单步组合决策优化（7.3）和连续状态神经逼近（7.4）。
- **对比充分性**：
  - 每个实验都设置了与理论匹配的参考基线（MDP值迭代、固定策略评估、蒙特卡洛滚动、对角/洗牌消融）。
  - 7.4节同时对比了联合 vs 洗牌、联合 vs 对角线，有效隔离了耦合信息的贡献。
  - **充分性评估**：实验覆盖了从理论验证到实际神经逼近的完整链条，数量适中，设计合理；但未在大规模或高维状态/动作空间上验证，也缺少真实世界数据集或标准RL基准环境（如Atari、MuJoCo等）。

### 6. 论文的主要结论与发现
- 在边际MDP具有**唯一最优策略**的条件下，非参数分布Bellman最优性算子的迭代在Wasserstein距离下收敛到最优联合回报分布。
- 在**所有最优选择器共享同一二阶矩不动点**这一更弱条件下，一阶矩和二阶矩可以收敛，放宽了对唯一性的要求。
- 采样训练目标能够保留交叉矩中对两个后继状态‑动作对的依赖，使神经网络能从耦合采样表恢复联合矩和联合分布。
- 在路径选择实验中发现：仅使用边际方差（对角MV）或单路规则（CVaR、优势概率）无法捕捉风险分散收益，而利用负协方差的全MV分配能有效规避灾难性损失。
- 在神经逼近实验中，基于联合数据训练的模型在协方差、相关性及分布距离指标上均优于对角基线和洗牌基线，说明耦合信息可以通过采样表被神经网络有效学习。

### 7. 优点
- **理论深度与完整性**：在分布级（定理1）和矩级（命题2）分别给出严格的收敛证明，清晰刻画了收敛所需的条件层次。
- **较弱的矩收敛条件**：允许最优平局存在，仅要求共同二阶不动点，比唯一最优策略假设更贴近实际。
- **采样目标设计合理**：交叉矩同时依赖两个后继状态‑动作对，避免了对角近似或局部依赖近似带来的信息损失。
- **实验设计严谨且多元**：从精确表格验证（无近似误差）到连续状态神经逼近，由简至繁；每个场景均有对应理论或直观的参考对照。
- **与经典问题建立联系**：将JMDP控制与Markowitz组合优化（均值‑方差）、能源距离等成熟工具结合，拓展了理论的应用价值。

### 8. 不足与局限
- **条件可验证性受限**：
  - 条件1要求已知动作间隔 $\Delta^* > 0$，条件2要求跨选择器的共同不动点，但论文未给出如何在实际问题中验证或估计这些条件的实用方法。
  - 当存在多个最优策略但二阶矩不动点不同（如方差或协方差不一致）时，矩迭代可能发散，论文未能给出失败场景的合理替代方案。
- **实验覆盖有限**：
  - 实验环境都较为简单（链、5×5网格、单步决策、低维倒立摆），未在标准大规模RL基准（如Atari、DeepMind Control Suite）上验证可扩展性。
  - 未测试较大动作空间（$|A| > 4$）或高维连续状态下的计算开销与数值稳定性。
- **神经逼近的评估深度不足**：
  - 7.4只用了MAE和能量距离做评估，未比较在真实控制任务（策略优化）中耦合先验带来的最终任务表现差异。
  - 粒子模型（32个向量）的容量有限，对长期回报维度更高的场景（长horizon、多动作）是否适用未知。
- **计算资源信息缺失**：
  - 论文未报告训练所需的GPU型号、数量、训练耗时和内存占用，影响可复现性评估。
- **未讨论耦合模型的实际来源**：论文假设耦合结果表 $J(\cdot|s)$ 可直接采样或已知，但在真实系统中建立这种耦合模型本身可能非常困难，论文对此实践路径未作说明。

（完）
