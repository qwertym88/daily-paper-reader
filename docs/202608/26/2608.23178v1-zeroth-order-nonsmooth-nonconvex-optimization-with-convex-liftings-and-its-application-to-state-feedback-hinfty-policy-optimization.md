---
title: Zeroth-Order Nonsmooth Nonconvex Optimization with Convex Liftings and Its Application to State-Feedback $H_\infty$ Policy Optimization
title_zh: 具有凸提升的零阶非光滑非凸优化及其在状态反馈$H_\infty$策略优化中的应用
authors: "Xuhao Wang, Yujie Tang"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23178v1"
tags: ["query:rl-control"]
score: 9.0
evidence: 利用凸提升的零阶非光滑非凸优化用于策略优化
tldr: "状态反馈H∞策略优化构成非凸非光滑问题，但其隐藏凸性可由凸提升框架刻画。本文提出零阶近端点算法：外环构造强凸子问题，内环仅用函数评估近似求解，无需显式投影即可保持可行性。以概率不低于1-δ返回ε最优解，函数评估复杂度为O~ d ε^{-3}。应用于离散时间H∞策略优化，复杂度为O~(n_u n_x ε^{-3})。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.23178v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1516, \"height\": 801, \"label\": \"Table\"}]"
motivation: 非凸非光滑策略优化难以直接求解，但凸提升揭示隐藏凸性，需开发仅用函数评估的零阶方法。
method: 提出零阶近端点算法，外环构建强凸子问题，内环用函数评估近似求解，无需投影保持可行性。
result: "以概率1-δ返回ε最优解，复杂度O~ d ε^{-3}；对H∞策略优化为O~(n_u n_x ε^{-3})。"
conclusion: 验证了理论假设对离散时间状态反馈H∞策略优化成立，给出实用复杂度保证。
---

## 摘要
直接策略优化在强化学习与控制中被广泛使用，但通常会导致非凸优化问题。对于状态反馈$H_\infty$控制，策略目标函数也是非光滑的，尽管其拥有一个良性景观，其隐藏的凸性可以通过最近发展的扩展凸提升框架揭示。受隐藏凸优化最新进展的启发，我们研究具有凸提升的非光滑非凸问题的零阶优化。我们提出一种零阶近端点算法：非精确近端点外层循环构造强凸子问题，而内层循环仅使用函数评估来近似求解每个子问题。以至少$1-δ$的概率，我们提出的算法使用$\widetilde{O}\left(dε^{-3}\right)$次函数评估返回一个$ε$-最优解，同时所有迭代点在没有显式投影的情况下保持可行性。最后，我们验证了支撑我们分析的假设适用于离散时间状态反馈$H_\infty$策略优化，从而为达到规定的目标值差距提供了$\widetilde{O}\left(n_u n_xε^{-3}\right)$的预言机复杂度，其中$n_u\times n_x$是要优化的反馈增益的维度。

## Abstract
Direct policy optimization is widely used in reinforcement learning and control, but generally leads to nonconvex optimization problems. For state-feedback $H_\infty$ control, the policy objective is also nonsmooth, despite possessing a benign landscape whose hidden convexity can be revealed by the recently developed extended convex lifting framework. Motivated by recent advances in hidden convex optimization, we study zeroth-order optimization of nonsmooth, nonconvex problems admitting a convex lifting. We propose a zeroth-order proximal point algorithm: An inexact proximal-point outer loop constructs strongly convex subproblems, while an inner loop approximately solves each subproblem using only function evaluations. With probability at least $1-δ$, our proposed algorithm returns an $ε$-optimal solution using $\widetilde{O}\left(dε^{-3}\right)$ function evaluations, while all iterates remain feasible without explicit projection. Finally, we verify that the assumptions underlying our analysis hold for discrete-time state-feedback $H_\infty$ policy optimization, yielding an oracle complexity of $\widetilde{O}\left(n_u n_xε^{-3}\right)$ for attaining a prescribed objective value gap, where $n_u\times n_x$ is the dimension of the feedback gain to be optimized over.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **背景**：直接策略优化（Direct Policy Optimization）是强化学习和控制中广泛使用的方法，但在很多控制问题中（如状态反馈 $H_\infty$ 控制）会导致非凸、非光滑的优化问题，难以从理论上保证收敛到全局最优。
- **已有进展**：近期提出的扩展凸提升（Extended Convex Lifting, ECL）框架揭示了此类问题存在“隐藏凸性”——虽然原问题非凸，但通过引入额外变量（如 Lyapunov 变量）可以将问题的 epigraph 双射到凸集，从而使非凸目标具有良好景观。此前已有工作利用该框架证明了 Clarke 稳定点即全局最优，但缺乏关于目标值间隙（objective value gap）的收敛率与复杂度保证。
- **核心问题**：针对具有凸提升的非光滑非凸优化问题（以状态反馈 $H_\infty$ 策略优化为典型例子），能否设计一种仅使用零阶信息（函数值）的算法，并给出高概率意义下关于全局目标值间隙的收敛率与预言机复杂度？
- **整体含义**：文章给出肯定回答，提出零阶近端点算法，以 $\widetilde{O}(dε^{-3})$ 次函数评估返回 $ε$-最优解，且所有迭代点自动保持在可行域内（无需投影）。将此结果应用于离散时间状态反馈 $H_\infty$ 策略优化，得到 $\widetilde{O}(n_u n_x ε^{-3})$ 的复杂度，为模型无关的策略优化提供了理论根基。

## 2. 方法论

- **核心思想**：利用凸提升的隐藏凸性，采用近端点法的思想：外层循环构造带二次正则项（即 $m\Vert x-x_0\Vert^2$）的强凸子问题；内层循环仅用函数值构造梯度估计，近似求解该子问题。由于目标函数弱凸且域开放非凸，直接投影不可行，因此通过精心设置步长与输出加权平均方式，保证所有中间迭代以高概率留在可行域内。
- **算法结构**（Algorithm 1）：
  - 外层循环 $n=1,\dots,N$：令 $x^{(n)} \leftarrow \text{ZOGD}(x^{(n-1)}, m, \nu^{(n)}, T^{(n)}, t_0^{(n)})$；
  - 子程序 $\text{ZOGD}$：
    - 步长为 $\eta_t = \frac{3}{m(t+t_0)}$；
    - 迭代 $x_t = x_{t-1} - \eta_t\big(G(x_{t-1}; z_t,\nu) + 2m(x_{t-1}-x_0)\big)$，其中 $z_t \sim U(S^{d-1})$，$G$ 为中心差分梯度估计：$G(x; z, \nu) = \frac{d\left(f(x+\nu z)-f(x-\nu z)\right)}{2\nu} z$；
    - 输出加权平均 $x_T = \frac{2}{T(T+2t_0-1)}\sum_{t=1}^{T}(t+t_0-1)x_{t-1}$。
- **关键技术细节**：
  - 利用凸提升假设（Assumption 1）与弱凸、Lipschitz 等半全局正则性假设（Assumption 2）构造强凸子问题；
  - 引入偏移参数 $t_0$ 使步长足够小，从而保证中间迭代不越出可行域（$x_t\in C\subseteq \mathcal{D}_\nu$），避免因域边界导致梯度估计未定义；
  - 使用随机零阶梯度估计的次高斯性质、Ville 不等式、广义 Freedman 不等式等高概率工具，对内外循环分别给出失败概率上界；
  - 对状态反馈 $H_\infty$ 控制，显式构造凸提升：通过有界实引理和 Schur 补将问题转化为 LMI 形式的凸集，并验证 Lipschitz、弱凸等性质，从而满足算法假设。
- **复杂度结论**：当 $N=\Theta(1/ε)$ 时，函数评估次数为 $O\left(dε^{-3}\ln\frac{1}{εδ}\right)$；应用于 $H_\infty$ 时维度 $d=n_u n_x$，得到 $O\left(n_u n_x ε^{-3}\ln\frac{1}{εδ}\right)$。

## 3. 实验设计

- **本文为纯理论论文，没有进行数值实验或仿真**。因此不存在数据集、benchmark 或对照算法。
- 所谓的“实验”主要是对理论假设的验证：作者在论文第 3.3 节和附录 B 中逐步证明了状态反馈 $H_\infty$ 策略优化问题满足凸提升假设（Assumption 1）与技技术假设（Assumption 2），并给出了完整的证明（包括有界实引理、LMI 等价性、紧性、Lipschitz 连续性、弱凸性等）。
- 与相关工作的比较是在理论上进行的：与 Goldstein 次梯度方法 [30]、导数自由策略优化 [31]、次梯度方法 [32] 在收敛率、复杂度、对目标值间隙 vs. 平稳性、以及概率保证等方面进行了讨论，但未进行数值对比。

**总结**：实验设计中只有理论验证，没有实际数值实验。

## 4. 资源与算力

- 论文未提及任何 GPU、CPU、训练时长或计算资源的使用情况。
- 这是因为本文属于纯理论推导与证明，通常不需要大规模计算资源。

## 5. 实验数量与充分性

- 由于是理论论文，不存在多组实验或消融实验。
- 论证充分性体现在：
  - 严格证明了所提算法在凸提升假设下的收敛性与复杂度；
  - 完整验证了 $H_\infty$ 策略优化问题满足所有假设（见附录 B）；
  - 分析了与现有理论结果（[30,31,32,43]）的优劣。
- 从理论角度而言，证明结构是严谨的，覆盖了非光滑、非凸、无投影、高概率可行等关键难点。但缺少数值示例来展示实际有效性和参数选择的敏感性。

## 6. 主要结论与发现

- 提出了针对具有凸提升的非光滑非凸问题的零阶近端点算法，该算法仅需函数评估即可实现高概率的全局收敛。
- 取得的主要复杂度结果为：$\widetilde{O}(dε^{-3})$ 次函数评估即可达到 $ε$-最优解（成功概率 $1-δ$）。
- 所有迭代点保持可行，无需显式投影，这是对开放非凸域内零阶优化的一项重要进展。
- 将该框架成功应用于离散时间状态反馈 $H_\infty$ 策略优化，第一次给出了关于目标值间隙（而非平稳性）的预言机复杂度 $\widetilde{O}(n_u n_x ε^{-3})$。
- 相比已有方法，本文的复杂度关于 $ε$ 的依赖优于 [32] 的 $O(ε^{-4})$，且不假设中间迭代点事先在域内；概率保证可任意接近 1，优于 [31] 的固定成功概率。

## 7. 优点

- **理论贡献扎实**：将凸提升（ECL）与近端点框架相结合，为具有隐藏凸性的非光滑非凸策略优化提供了统一的分析途径。
- **算法设计细致**：通过步长偏移 $t_0$ 和加权平均，在没有投影的情况下保持迭代点在开放域内，这是在控制类策略优化中极具实际意义的技术。
- **复杂度结果强**：高概率意义下的全局最优性保证，且复杂度几乎最优（除 $O(d)$ 因子外匹配现有最好的一阶非光滑隐藏凸优化复杂度）。
- **落地性强**：验证了状态反馈 $H_\infty$ 策略优化问题满足全部假设，填补了该问题在目标值间隙复杂度方面的空白。
- **与现有工作对比清晰**：明确指出了与 [30,31,32] 在收敛目标（平稳性 vs. 全局最优）、概率保证、迭代可行性等方面的差异。

## 8. 不足与局限

- **实验缺失**：没有数值实验或仿真验证，无法展示算法在实际问题中的表现、参数敏感性或收敛行为。
- **假设较强**：要求目标函数在相关子水平集上为全局 Lipschitz、弱凸，且凸提升双射及其逆为局部 Lipschitz。虽然对 $H_\infty$ 问题已验证，但推广到其他控制问题（如输出反馈、结构化 $H_\infty$）可能需要额外工作。
- **零阶预言机为精确值假设**：本文假设可查询精确函数值，未考虑噪声或样本估计误差。虽然提及了可从 [31] 引入样本估计，但未给出组合后的复杂度分析。
- **复杂度中的常数与对数项不够明确**：定理中的 $O$ 符号隐藏了较大的绝对常数、对 $\ln d$ 等高次对数项，实际参数选择（如 $t_0, T(n)$）的明确值虽给出，但并未进行数值调优。
- **未讨论实际部署问题**：如步长与平滑半径的选择、循环长度 $T(n)$ 的预设计、终止条件等工程细节需要更多实验指导。

（完）
