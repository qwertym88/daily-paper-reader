---
title: "Antagonistic Control: Foundations, Scalability and Nonlinearity"
title_zh: 对抗控制：基础、可扩展性与非线性
authors: "Sribalaji C. Anand, André M. H. Teixeira"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22204v1"
tags: ["query:rl-control"]
score: 8.0
evidence: 面向鲁棒控制问题的半定规划求解
tldr: 研究约束控制输入的最坏情况影响：输入在受限输出下最大化其他输出的平均成本，统一涵盖H∞范数与输出增益。对线性系统，单约束时给出精确SDP，多约束给出上界SDP，并基于零点和相对度判定无界性且可闭环修正。对正系统，提供复杂度随状态维度线性增长的SDP与LP；对多项式系统给出SOS程序，数值示例验证了有效性。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.22204v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1781, \"height\": 419, \"label\": \"Table\"}]"
motivation: 对抗控制与安全评估需要量化受限输入对输出的最坏影响，现有H∞范数等指标较零散，缺乏统一且可扩展的框架。
method: 针对线性时不变系统，利用SDP精确/上界求解最坏成本；对正系统构造线性规模SDP与LP；对多项式系统采用SOS程序。
result: 得到单约束精确SDP与多约束上界；系统零点/相对度导致无界时闭环可修正；正系统复杂度线性。
conclusion: 为鲁棒控制和攻击检测提供统一框架，同时揭示结构限制并给出可扩展数值方法。
---

## 摘要
本文研究受约束控制输入的最坏情况影响：一个输入试图最大化某些输出的平均代价（以 $L_2$ 或 $L_1$ 范数度量），同时保持在其他输出方面的有界性。该问题模板涵盖了诸如 $H_\infty$ 范数和输出到输出增益等经典度量，并出现在对抗控制、安全评估和鲁棒控制中。对于线性时不变系统，我们在单一约束下提供精确的半定规划（SDP），在多约束下提供计算上界的 SDP。我们基于系统零点和相对阶推导了最坏情况代价无界的充分条件，并给出了一种消除无界性的构造性闭环修正。从安全角度来看，无界值揭示了在检测某些攻击输入时的结构性局限。对于正系统，我们提供了复杂度随状态维度线性增长的可扩展公式：针对二次代价的可扩展 SDP 和针对线性代价的精确线性规划。结果通过平方和规划扩展到非线性多项式系统。我们通过数值示例说明了这些结果。

## Abstract
This paper studies the worst-case impact of constrained control inputs: an input seeks to maximize the average cost of some outputs, measured in the $L_2$ or $L_1$ norm, while remaining bounded in terms of other outputs. This problem template subsumes classical metrics such as the $H_\infty$ norm and the output-to-output gain, and arises in adversarial control, security assessment, and robust control. For linear time-invariant systems, we provide an exact semi-definite program (SDP) when there is a single constraint, and SDPs computing upper bounds when there are multiple constraints. We derive sufficient conditions, in terms of system zeros and relative degrees, under which the worst-case cost is unbounded, together with a constructive closed-loop modification that removes the unboundedness. From a security standpoint, unbounded values reveal structural limitations in detecting certain attack inputs. For positive systems, we provide scalable formulations whose complexity grows linearly in the state dimension: a scalable SDP for quadratic costs, and an exact linear program for linear costs. The results extend to nonlinear polynomial systems via a sum-of-squares program. We illustrate the results with numerical examples.

---

## 论文详细总结（自动生成）

## 一、论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在网络安全与鲁棒控制领域，需要量化一个“对抗性输入”在满足某些隐蔽性约束（如不触发报警、能量有限）的条件下，能对系统性能造成的最坏影响。经典的 $H_\infty$ 范数、输出到输出 $\ell_2$ 增益等问题均可视为该问题的特例，但缺乏一个统一的、适用于多约束情形的分析框架。

- **核心问题**：考虑连续时间动态系统（1），输入 $u$ 在满足一组输出约束 $\int_0^\infty \ell(y_j,R_j)dt \le \delta_j$ 的前提下，最大化另一组输出的平均代价 $\frac{1}{|\mathcal{P}|}\sum_{i\in\mathcal{P}}\int_0^\infty \ell(y_i,Q_i)dt$，即问题（2）。这一“对抗控制”问题涵盖 $H_\infty$ 范数、输出到输出增益、$H_\infty/H_-$ 指数比等经典指标，可应用于攻击影响评估、安全资源分配、鲁棒控制器设计等场景。

**整体含义**：论文期望为不同系统类别（LTI、正 LTI、多项式系统）、不同代价函数（二次型、线性）、不同输入信号空间（$L_{2e}$、$L_2^0$、$L_2^{0+}$、$L_1^+$）和不同约束数量提供一套统一的分析方法，并揭示问题的结构性质（如无界性条件）及补救手段。

## 二、论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **统一分析流程**（贯穿全文的 pipeline）：先对原始最坏情况问题做 Lagrange 对偶化，再利用（环）耗散性理论（(cyclo-)dissipativity）将无穷维约束转化为有限维矩阵不等式，最终得到 SDP（半定规划）、LP（线性规划）或 SOS（平方和）程序。

- **核心技术——耗散性条件**：核心思路是寻找一个存储函数（storage function），使得供给率（supply rate）在所有输入下非负，从而对偶问题可行。例如在定理 5 中，供给率定义为 $s(u,t)=\gamma y_2^\top R_2 y_2 - y_1^\top Q_1 y_1$，通过耗散不等式等效为 LMI（6）。

- **单约束情形（定理 5）**：当 $N=2$（一个目标输出、一个约束输出）、$u\in L_{2e}$ 时，给出精确的 SDP（6）：
  $$\min_{\gamma,P} \gamma\delta_2 \quad \text{s.t.}\quad \begin{bmatrix}A^\top P+PA & PB\\B^\top P & 0\end{bmatrix}+\Gamma \preceq 0,\ P\succeq 0,\ \gamma\ge 0,$$
  其中 $\Gamma = \begin{bmatrix}C_1^\top\\D_1^\top\end{bmatrix}Q_1\begin{bmatrix}C_1&D_1\end{bmatrix}-\gamma\begin{bmatrix}C_2^\top\\D_2^\top\end{bmatrix}R_2\begin{bmatrix}C_2&D_2\end{bmatrix}$。强对偶性通过零输入 $u=0$ 满足严格可行性来保证。

- **无界性条件**：
  - 定理 5(b)：若 $\Sigma_2$（约束输出对应子系统）有不稳定有限零点不是 $\Sigma_1$ 的零点，则问题无界。
  - 定理 6：基于相对度给出另一个充分条件——若 $r_{\min}^1 < r_{\min}^2$（目标输出相对度小于约束输出相对度），则 SDP 不可行（问题无界）。证明通过频域不等式和传递函数高频渐近行为完成。
  - 定理 10(b)：多约束情形下，若 $\Sigma_m$ 有不稳定零点不是 $\Sigma_p$ 的零点，则无界。
  - 定理 12(b)：$u\in L_2^0$ 时，若 $\Sigma_m$ 有虚轴上的零点不是 $\Sigma_p$ 的零点，则无界。

- **闭环修正（定理 8）**：当 $r_{\min}^1 < r_{\min}^2$ 时，通过对相对度不足的输出施加积分器（实际可用泄漏积分器）来提高其相对度到 $r_{\min}^2$，从而使得修改后的闭环系统满足 $r_{\min}^1 \ge r_{\min}^2$，消除无界性。

- **多约束上界（定理 10、12）**：通过引入多个 Lagrange 乘子 $\gamma_j$ 和对偶化，将原问题转化为求上界的 SDP（15）和（27）。定理 12 利用 $L_2^0$ 情形下的严格可行性（$u=0$ 满足约束）证明了对偶约束的等价性，而定理 10 中 $L_{2e}$ 情形只能给出充分条件（上界）。

- **正系统可扩展公式**：
  - 定理 15（二次代价）：将存储函数 $P$ 限制为对角矩阵，配合 $\Gamma$ 的非负约束，利用 Rantzer 关于正系统的 KYP 引理[26]，把 SDP 参数数从 $O(n^2)$ 降至 $O(n)$，适合大规模系统。
  - 定理 17（线性代价）：利用正系统的耗散性结果[9]，将无界约束量化问题转化为精确的 LP（40）。

- **多项式系统扩展（定理 19）**：对多项式系统（42），将对偶条件通过 SOS 程序（44）求解，其中存储函数 $S(x)$ 取 SOS 多项式，保证约束为 SOS 条件。

## 三、实验设计：使用的数据集 / 场景、benchmark 与方法对比

- **实验场景与数值示例**（本文无基准数据集，采用控制领域典型数值算例验证）：
  - 示例 2：四水箱过程（quadruple-tank process），检验 $L_2^0$ 与 $L_{2e}$ 信号空间下的最优值差异，揭示结构脆弱性。
  - 示例 7：两个 SISO 传递函数 $G_1(s)$、$G_2(s)$，验证定理 6 相对度导致的无界性。
  - 示例 9：在示例 7 基础上应用定理 8 的闭环修正（泄漏积分），重新求解 SDP 得有限值 28.2017。
  - 示例 11：$A=\begin{bmatrix}0&1\\0&\epsilon\end{bmatrix}$ 的二阶系统，验证定理 10（多约束上界 SDP）得 36。
  - 示例 13：同一系统在 $u\in L_2^0$ 下用定理 12 求解得 26。
  - 示例 16：三阶正系统，验证定理 15（可扩展 SDP）得 46.67。
  - 示例 18：二阶正系统，验证定理 17（精确 LP）得 20。
  - 示例 20：带交叉项/二次项的多项式系统，验证定理 19（SOS 程序）——8 次存储函数得 0.2507，26 次得 0.2501。

- **benchmark 与对比方法**：本文为理论性论文，没有使用标准 benchmark 数据集，也没有与现有方法做显式数值对比；对比主要体现在与经典指标（$H_\infty$ 范数、输出到输出增益）的关系说明上，以及 $L_{2e}$ 与 $L_2^0$ 两种信号空间下结果的差异。

## 四、资源与算力

- **论文未明确说明使用的算力**：全文没有提及 GPU 型号、数量、训练时长等计算资源信息。
- 文中仅提到使用 **YALMIP** 作为建模工具、**MOSEK 11** 作为 SDP 求解器（示例 2），且示例均为小规模低维系统（阶数 2~3），单次求解在普通工作站上即可完成，无需大规模并行计算资源。

## 五、实验数量与充分性：是否充分、客观、公平

- **实验数量**：全文共 8 个数值示例（示例 1 为动机性描述），覆盖了：
  - 单约束 LTI（示例 2、7、9）；
  - 多约束 LTI——$L_{2e}$（示例 11）与 $L_2^0$（示例 13）；
  - 正系统——二次代价（示例 16）与线性代价（示例 18）；
  - 多项式系统（示例 20）。
- **充分性评估**：
  - **优点**：每个定理都有对应的数值示例验证，覆盖了论文声称的全部系统类别与约束场景。
  - **不足**：实验规模偏小（状态维度 ≤ 3），没有大规模（如 $n=100$ 以上）正系统的可扩展性验证；没有消融实验；没有与现有竞争方法的数值对比；示例中个别数值的理论最优性未做严格验证（如多约束上界与真实最优值的 gap 未量化）。

总体上，实验验证了理论的正确性，但作为实证研究而言覆盖度和对比性有限。

## 六、论文的主要结论与发现

1. **统一框架**：问题（2）是一个统一模板，可涵盖 $H_\infty$ 范数、输出到输出增益等经典指标，并且通过统一的“对偶化 + 耗散性”流程可在各类系统上求解。
2. **单约束精确解**：LTI 系统、单约束、$u\in L_{2e}$ 情形下，最坏代价可精确计算（定理 5），且强对偶性成立。
3. **多约束上界**：多约束情形下给出 SDP 上界（定理 10、12），这是文献中此前缺乏的一般性计算方法。
4. **无界性刻画**：最坏代价的无界性可由系统的不稳定（或虚轴）有限零点、以及目标输出相对度小于约束输出相对度来判定（定理 5b、6、10b、12b）。
5. **构造性补救**：通过积分器修正可提升相对度、消除无界性（定理 8），为系统层面的安全加固提供思路。
6. **可扩展性**：正系统下通过对角存储函数将 SDP 复杂度从 $O(n^2)$ 降至 $O(n)$（定理 15）；线性代价下可化为精确 LP（定理 17）。
7. **非线性扩展**：SOS 程序为多项式系统的对抗控制问题提供上层界（定理 19）。
8. **安全性启示**：$L_2^0$ 有限而 $L_{2e}$ 无界的现象（示例 2）说明频域/渐近分析可能遗漏结构性安全漏洞，需注意输入信号空间的建模假设。

## 七、优点

- **理论深度强**：建立了完整的对偶性、耗散性、KYP 引理和正系统理论的衔接，分别在精确性、可扩展性、非线性扩展三个维度上给出了系统性结果。
- **统一性**：一个问题模板覆盖多个经典指标，将看似不同的安全/鲁棒指标纳入同一分析框架，视角新颖且实用。
- **无界性分析与构造性补救**：不仅指出结构缺陷（零点、相对度），还提供修复方法（定理 8），对实际系统设计具有直接指导意义。
- **可扩展性对比明确**：明确将对角线存储函数与全矩阵存储函数做对比，突出 $O(n)$ 与 $O(n^2)$ 参数量的差别，并论证了正系统假设的合理性（正输入不触发平凡异常检测）。
- **充分交代了强对偶性的适用条件**：对强弱对偶的使用边界做了清晰区分（单约束精确 vs 多约束上界；$L_2^0$ 等价 vs $L_{2e}$ 上界），避免误导读者。

## 八、不足与局限

- **多约束情形仅为上界而非精确值**：当 $|\mathcal{M}|>1$ 时，对偶化与耗散性条件可能引入对偶间隙，论文没有给出上界与真实最优值之间的紧度分析。
- **无界性条件是充分非必要的**：定理 5(b)、6、10(b)、12(b) 只给出无界的充分条件，未刻画完整边界；满足条件之外的系统也可能无界，反之亦然。
- **正系统假设限制了适用范围**：定理 15 和 17 依赖内部正性（Metzler 矩阵、非负输入输出），对一般物理系统不一定成立；对角 $P$ 的约束可能引入额外保守性。
- **闭环修正的额外代价未量化**：定理 8 中引入积分器会改变系统动态（可能带来相移、稳定性或瞬态响应问题），论文未分析这些副作用。
- **SOS 程序的规模与紧度**：多项式系统的 SOS 上界随存储函数次数升高而收紧，但计算代价增长显著，示例中 26 次多项式已属于较高计算负担；且仅给出数值验证，没有讨论求解失败的鲁棒性。
- **数值实验覆盖有限**：没有大规模算例，没有与其他安全评估方法的对比，也没有讨论求解时间与数值稳定性。
- **未涉及控制器综合**：论文主要关注分析和量化，后续的“设计最小化最坏代价的控制器”作为未来工作提出，尚未给出合成方法。
- **某些假设较强**：例如定理 12 要求无直馈（$D_i=0$）、可控性（Assumption 4）、稳定性（Assumption 3），限制了工程应用范围。

（完）
