---
title: "Core-KAN: Continuous Vision Kernels with Kolmogorov-Arnold Networks"
title_zh: Core-KAN：基于Kolmogorov-Arnold网络的连续视觉核
authors: "Lan Guo, Mengling Li, Haoran Li, Jun Shen, Yuanbo Jiang, Qingguo Zhou, Binbin Yong"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19817v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 面向视觉网络的新型连续卷积核设计
tldr: 传统卷积核固定在离散网格上，难以适应异构局部结构。现有自适应算子常将几何尺度变化与内容相关滤波耦合，且逐位置生成核导致高计算成本。Core-KAN提出连续相对尺度KAN卷积，通过轻量尺度控制器预测局部尺度，用KAN生成连续坐标函数的深度核基，再按尺度插值并独立混合，实现尺度自适应与内容滤波解耦。在三个视觉任务上以极小代价超越强基线，提供高效通用的连续尺度自适应卷积框架。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.19817v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 817, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19817v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1793, \"height\": 980, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19817v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 638, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19817v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1830, \"height\": 609, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19817v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 864, \"height\": 534, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.19817v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 422, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.19817v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1847, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.19817v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1847, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.19817v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 327, \"label\": \"Table\"}]"
motivation: 现有自适应卷积算子将尺度变化与内容滤波耦合，且逐位置生成核计算开销大；需解耦几何尺度适应与内容依赖滤波并避免高成本。
method: Core-KAN将输入映射至紧凑潜基空间，用轻量尺度控制器预测相对尺度，以KAN生成连续坐标函数的核基，按尺度插值并独立混合实现低秩动态卷积。
result: 在三个代表性视觉任务上，Core-KAN以极小的参数量和计算开销持续超越强卷积基线与动态卷积基线。
conclusion: Core-KAN提供高效通用的连续尺度自适应卷积框架，可无缝集成层级视觉骨干，兼具灵活性与效率。
---

## 摘要
传统的卷积核通常定义在固定的离散网格上，限制了其适应异构局部结构的能力。现有的自适应算子提高了灵活性，但常常将几何尺度变化与内容相关滤波耦合在一起，同时因逐位置内核生成而产生高昂的计算成本。为了将几何尺度适应与内容相关滤波解耦，同时避免昂贵的逐位置内核生成，我们提出了连续相对尺度KAN（Core-KAN），这是一种相对尺度条件的连续卷积算子。Core-KAN将输入特征映射到一个紧凑的潜在基空间，并使用轻量级尺度控制器来预测相对于指数移动平均参考的局部尺度。基于KAN的生成器将深度卷积核基表示为连续坐标函数，使算子能够在任意分辨率下合成空间滤波器，而不是局限于固定网格。它不是在每个位置合成独立的内核，而是构建一个紧凑的尺度条件内核响应库，并根据预测的局部尺度图进行插值。独立的混合控制器进一步基于局部内容组合插值后的基响应，明确地将几何尺度适应与内容相关滤波解耦。结合轻量级逐点投影，该设计形成了一种低秩动态卷积，能够随内核大小高效扩展，并易于集成到分层视觉骨干网络中。在三个代表性视觉任务上的实验表明，Core-KAN持续优于强卷积和动态内核基线，且仅需极少的参数和计算开销，为连续、尺度自适应卷积提供了一个高效通用的框架。

## Abstract
Conventional convolutional kernels are typically defined on fixed discrete grids, limiting their ability to accommodate heterogeneous local structures. Existing adaptive operators improve flexibility but often couple geometric scale variation with content-dependent filtering, while incurring high computational cost from per-location kernel generation. To decouple geometric scale adaptation from content-dependent filtering while avoiding expensive per-location kernel generation, we propose Continuous Relative-scale KAN (Core-KAN), a relative-scale-conditioned continuous convolution operator. Core-KAN maps input features into a compact latent basis space and uses a lightweight scale controller to predict local scales relative to an exponential moving average reference. A KAN-based generator represents depth-wise kernel bases as continuous coordinate functions, allowing the operator to synthesize spatial filters at arbitrary resolutions rather than being confined to a fixed lattice. Instead of synthesizing independent kernels at every location, it constructs a compact bank of scale-conditioned kernel responses and interpolates them according to the predicted local scale map. An independent mixing controller further combines the interpolated basis responses based on local content, explicitly decoupling geometric scale adaptation from content-dependent filtering. Together with lightweight pointwise projections, this design forms a low-rank dynamic convolution that scales efficiently with kernel size and integrates readily into hierarchical vision backbones. Experiments across three representative vision tasks show Core-KAN consistently outperforms strong convolutional and dynamic-kernel baselines with only marginal parameter and computational overhead, offering an efficient, general framework for continuous, scale-adaptive convolution.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：传统卷积核定义在固定离散网格上，所有空间位置共享同一套核权重，难以适应纹理、边界、细长结构等异构局部结构。提高适应性的直接方案（逐位置生成核）会弱化参数共享且开销巨大。
- **现有方法的张力**：条件/动态卷积通过路由或调制实现内容自适应，可变形卷积通过偏移采样点实现几何适应，连续核方法则有坐标到权重的映射能力，但往往存在以下问题：
  - 几何尺度变化与内容相关滤波彼此耦合在单一路由信号中；
  - 绝对尺度预测在不同特征阶段和优化过程中漂移，缺乏校准；
  - 逐位置核合成计算成本高。
- **核心洞察**：空间自适应的两个关键决策应被分离——**几何尺度**（决定核的锐利/宽窄/形状）和**内容组合**（决定潜在滤波模式的混合比例）。
- **论文定位**：提出一种高效、通用、即插即用的连续尺度自适应卷积算子，可无缝集成到层级视觉骨干中，在 ImageNet-1K、COCO、ADE20K 三个任务上取得一致的性能提升。

## 2. 方法论

- **整体架构**：Core-KAN 由四个主要组件构成：
  1. **输入/输出低秩投影**：1×1 卷积将输入映射到紧凑的 N 维基空间，自适应计算被限制在低秩基空间中；
  2. **尺度控制器（Scale Controller）**：从原始输入预测密集局部尺度场，并用**指数移动平均（EMA）参考**将绝对尺度归一化为相对尺度；
  3. **KAN 连续核生成器**：共享 KAN 将连续二维坐标映射到 N 个标量核场，输出随尺度变化的深度核基；
  4. **混合控制器（Mixing Controller）**：独立从内容预测 N 个基上的 simplex 分布权重，完成内容相关的基组合。

- **关键技术细节**：
  - **相对尺度归一化**：每个算子维护一个非可学习的 EMA 参考 μ_α（training 迭代更新、inference 固定），预测尺度除以该参考得到相对尺度 s(p)，避免绝对尺度跨层/跨迭代漂移。
  - **支持采样与插值（Support-and-Interpolate）**：在预测区间 [α_min, α_max] 内均匀取 M 个尺度支持，仅对 M 个支持处采样连续核场并做 depthwise 卷积得到响应库 R；每个空间位置根据其局部尺度线性插值相邻两个支持响应。
  - **响应空间插值等价于核空间插值**：由于卷积对核权重是线性的，响应间插值严格等价于对核进行分段线性插值，无需物化位置特定的核张量。
  - **解耦机制**：尺度分支改变核空间轮廓，混合分支改变基的贡献权重，两者由完全独立的控制器产生，实现几何尺度适应与内容依赖组成的显式解耦。
  - **低秩动态卷积解释**：有效位置相关核 = W_out × diag(m(p)) × K̂(s(p)) × W_in，空间自适应被约束在 N 维紧凑基空间中。

## 3. 实验设计

- **数据集与任务**：
  - **ImageNet-1K**：图像分类（300 epochs，batch size 4096，单 crop 224×224 验证）；
  - **COCO val2017**：目标检测 + 实例分割（Mask R-CNN + FPN，标准 1× 即 12 epochs 和 3× 即 36 epochs 两种训练计划）；
  - **ADE20K**：语义分割（160K iterations，ResNet-50 骨架）。
- **对比方法**：ResNet-50（基线）、DY-Conv、ODConv、SCConv、RefConv、PartialNet、KernelWarehouse、FDConv 等主流动态/高效卷积方法。
- **公平性声明**：在下游基准中，所有重训的骨干共享相同的数据管线、训练调度、任务头和评估协议。ImageNet 表格中注明不同方法训练配方有差异，仅供参照比较。

## 4. 资源与算力

- **论文正文未披露 GPU 型号、数量或具体训练时长**。仅提供了训练配置（ImageNet 300 epochs、batch size 4096、AdamW、初始学习率 4×10⁻³、20 epochs warm-up、cosine 衰减；COCO 1×/3× 计划；ADE20K 160K iterations）和模型参数量，未给出实际的硬件资源信息。

## 5. 实验数量与充分性

- **主要实验结果**：三类任务共 4 张主结果表（ImageNet 分类 1 张、COCO 检测/分割 2 张（1× 和 3×）、ADE20K 语义分割 1 张），每张表均与 5~8 种强基线方法对比并覆盖多个细粒度指标（如 COCO 中按物体尺度 S/M/L 拆分评测）。
- **附加分析实验**：
  - 核轨迹可视化（图 4）：显示核基在不同相对尺度下的连续演化轨迹和 PCA 结构；
  - 插值保真度分析（图 5）：不同支持数 M 对直接连续核评估的相对输出误差。
- **充分性评价**：实验覆盖了分类、检测、实例分割、语义分割四大视觉任务，对比方法较新（涵盖 2025-2026 年成果），结论一致性较强。但**主文未包含显式的控制变量消融表**（如替换 KAN 为 MLP、去掉相对归一化、不同 N/M 取值的系统消融等），这类消融被放在附录（附录 B.1-B.4），正文提取文本未见详细数据。整体实验设计较充分，公平性声明到位，但消融深度在正文中体现不足。

## 6. 主要结论与发现

- **性能提升**：Core-KAN 在三个任务上以极小的参数开销超越所有对比方法：
  - ImageNet-1K：81.45% top-1 / 95.68% top-5，仅 26.61M 参数；相对 ResNet-50 提升 3.84%，相对 KernelWarehouse 提升 0.49% 且参数少 73.92%；
  - COCO 1×：43.0 AP_box / 39.5 AP_mask；3×：46.2 AP_box / 42.2 AP_mask，多个指标排名第一；
  - ADE20K：44.19% mIoU / 54.38% mAcc，超越最强对比 FDConv 1.59% mIoU 且参数更少。
- **连续核行为**：KAN 核场产生的是有意义的连续轨迹（不同基有不同的尺度演化模式），而非离散查询表。
- **插值近似有效**：M=8 的支持数在保真度和计算开销之间取得良好平衡，增加支持数可持续降低近似误差。

## 7. 优点

- **解耦设计**：将几何尺度适应与内容依赖组合分离为两个独立控制器，增强了可解释性，避免单一路由信号混淆两种功能。
- **高效性**：支持-插值方案避免了逐位置核合成；计算限制在 N=16 维低秩基空间，参数开销极小（相对 ResNet-50 仅增 4.11%）。
- **理论等价性**：响应空间插值与核空间插值的严格等价关系（式 17-20）使近似误差可控制，且 M 可以平滑调节精度/效率。
- **相对尺度归一化**：EMA 参考机制解决了绝对尺度预测跨层/跨迭代不稳定的问题，使尺度条件更鲁棒。
- **通用即插即用**：可无缝集成到标准 ResNet 等层级骨干中，不依赖特定任务头。

## 8. 不足与局限

- **响应库成本随 M 线性增长**：论文明确承认这是剩余局限，支持数增大时计算开销随之增加，降低该成本而不损失插值保真度是后续方向。
- **实验骨干单一**：主实验仅以 ResNet-50 为骨干，未在更大或更新的骨干（如 ConvNeXt、Swin Transformer）上验证通用性，Plug-and-play 的断言支撑稍显不足。
- **正文消融不完整**：关键设计决策（如 KAN vs MLP、EMA 相对归一化的增益、双控制器解耦的定量收益）的消融实验被放在附录，正文中缺少这些证据会削弱说服力。
- **训练配方差异**：ImageNet 对比表中不同方法采用了不同的训练配方，论文也声明仅供参照，横向比较的严格公平性因此有限。
- **硬件资源未披露**：读者难以评估训练成本的可复现性。
- **近似插值误差**：响应插值是对直接逐位置连续评估的分段线性近似，在快速变化的尺度区域仍存在精度损失（虽可接受）。

（完）
