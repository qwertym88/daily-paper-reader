Title: Computationally efficient safe exploration in reinforcement learning

URL Source: https://arxiv.org/pdf/2609.22919v1

Published Time: Tue, 22 Sep 2026 00:47:13 GMT

Number of Pages: 8

Markdown Content:
# Computationally efficient safe exploration in reinforcement learning 

Shreeram Murali, Shankar A. Deka, and Dominik Baumann 

Abstract — Reinforcement learning in real-life applications requires safety guarantees during exploration. Typical rein-forcement learning algorithms do not provide such guarantees, and many modifications that do rely on Gaussian processes (GPs), which have a large computational cost. We propose a computationally lightweight algorithm based on the Nadaraya-Watson estimator that safely explores and optimizes constrained Markov decision processes (MDPs). Our algorithm, C OLS AFE -MDP, uses an estimator that scales in constant-time with bounds on the estimates, a significant improvement from its GP-based counterparts that scale cubically with the number of data points. We then evaluate its performance in a grid-based environment and on observational Martian terrain data. 

I. INTRODUCTION Suppose a planetary rover on the surface of Mars is tasked with collecting geological samples in an unfamiliar landscape. The rover must navigate terrain in which the scientific value of each location and the safety of the terrain are unknown a priori . Furthermore, the rover operates under strict computational resource constraints: onboard processing is limited, and decisions are made in real-time. This example captures the core challenge we address in this paper: how can an agent safely explore an unknown environment and learn a near-optimal policy with limited computational resources? Standard reinforcement learning (RL) has been widely successful in recent years, especially in structured envi-ronments such as video and board games [1]. However, typical RL algorithms often disregard safety entirely in favour of trial-and-error exploration. This can be potentially catastrophic in applications such as autonomous driving, robotics, and space exploration, where people and property are involved [2], [3]. It is therefore crucial that safety is maintained not just after convergence, but throughout exploration. A natural formalism for including safety constraints into RL is through constrained Markov decision processes (CMDPs) [4], [5]. Here, the agent must satisfy a set of con-straints while maximizing some cumulative reward. While this is a strong framework, most existing work provides theoretical guarantees only after policy convergence [6] or ensure zero constraint violation only in expectation [7]. Neither is sufficient when even a single safety violation can lead to damage. Ensuring safety with high probability at every step during learning is a substantially harder problem.  

> Supported by the Research Council of Finland flagship programme: the Finnish Center for Artificial Intelligence (FCAI) and the Tandem Industry Academia Seed funding from the Finnish Research Impact Foundation. S. Murali and D. Baumann are with the Cyber-physical Systems Group; S. A. Deka is with the Nonlinear Systems and Control Group, Aalto Univer-sity, Espoo, Finland (Email:

firstname.lastname@aalto.fi ). 

The safe Bayesian optimization literature addresses this problem. SAFE OPT [8] and its extensions [9], [10] use Gaussian processes to construct confidence intervals over unknown constraint functions and only evaluate inputs that are safe with high probability. However, S AFE OPT is essen-tially a bandit algorithm: it can evaluate any input it certifies as safe, regardless of which inputs it evaluated before. In an MDP, the agent can only reach states through the environment dynamics, so a safe state is useful only if a safe path leads to it and away from it. Prior work [5], [11] extends GP-based safe exploration to this setting by accounting for reachability and returnability, and SNO-MDP [12] further provides near-optimality guarantees. All of these methods, however, rely on GP inference, whose posterior updates scale cubically with the number of observations [13]. This limits them to small state spaces or short horizons. 

1) Contributions: Therefore, in this paper, we propose COLS AFE -MDP, an algorithm for safe exploration and near-optimal policy learning in constrained MDPs that is both computationally lightweight and theoretically grounded. Our contributions are as follows:  

> •

We introduce a recursive formulation of the Nadaraya-Watson estimator [14], [15] for CMDPs that exploits the finite state space and compact kernel support to achieve constant-time updates per observation, a significant im-provement from the cubic scaling of GP-based methods.  

> •

We derive confidence intervals for the estimator and use them to construct pessimistic and optimistic safe sets.  

> •

On the basis of these, we prove that C OLS AFE -MDP does not violate the safety constraint with high probabil-ity and converges to a near-optimal policy in finite time. Furthermore, our theoretical guarantees require only Lipschitz continuity of the unknown functions (with a known constant), a milder assumption than [12].  

> •

We evaluate C OLS AFE -MDP on synthetic grid worlds and on real Martian terrain data. The algorithm dis-covers nearly three times as many safe states as the GP-based baseline while running at a fraction of the wall-clock time, all without a single safety violation. 

2) Related work: There are many approaches to incorpo-rating safety in RL [16]. Model-based approaches associate safety with state constraints to guarantee stability [10], [17], whereas model-free approach such as policy opti-mization [18] and Lyapunov-based methods [19] guarantee constraint satisfaction only upon convergence or in expec-tation. Recent policy gradient methods for CMDPs have strengthened these guarantees, with feasibility guarantees   

> Accepted final version. To appear in the Proceedings of the 65th IEEE Conference on Decision and Control .©2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works. arXiv:2609.22919v1 [cs.LG] 19 Sep 2026

throughout learning [20]. These methods target large or continuous state-action spaces with parametrized policies, an adjacent problem setting to the one we consider in our work. When the environment is unknown, GP-based confi-dence intervals offer a principled way to certify safety. SAFE OPT [8] introduced this in the bandit setting, and [5] extended this to finite MDPs. Later, [11] and [12] unified this into SNO-MDP, a two-phase algorithm that first explores safely and then optimizes the reward, with provable guarantees on safety and optimality. However, these methods still suffer from the cubic computational complexity of GPs. To overcome this, more recent approaches have used nonparametric methods, such as the Nadaraya-Watson estimator [14], [15], to replace GPs in S AFE OPT [21], [22]. In [22], the authors implement a version of the estimator that scales linearly in time. However, it is possible to improve this to sublinear computational complexity with some pre-processing and the use of sophisticated data structures [23], [24]. Moreover, these approaches and improvements are not targeted for MDP settings. In our work, we present a version suited to CMDPs that is recursive and scales with O(1) in time. II. P ROBLEM SETTING 

We consider an infinite-horizon CMDP as the tuple 

M = ⟨S , A, f, r, g, γ ⟩,

where S is a finite set of states {s}, A is a finite set of actions 

{a}, f : S × A → S is a known state transition function, r :

S → [0 , R max ] is the reward function, g : S → R is the safety constraint function, and γ ∈ (0 , 1) is a discount factor. In our setting, both the reward function r and constraint function g

are unknown a priori . At every time step t ∈ N, the agent must be in a ‘safe’ state defined as g(st) ≥ h, where h ∈ R

is the safety threshold. The agent’s behaviour is specified by a policy π : S → A ,which defines the action to take in each state. The value of a policy represents the expected discounted cumulative reward from a certain state st. We denote this value function as V π

> M

.Our objective is then to find the policy π that maximizes: V π 

> M

= E

" ∞X 

> τ=0

γτ r(st+τ ) st

#

,

subject to: g(st+τ ) ≥ h, ∀τ ∈ [0 , ∞).

Our problem is centred around estimating the unknown reward and safety functions r and g from data. We assume to receive noisy measurements of r and g after every experiment with a certain policy. Thus, we introduce an assumption about the nature of noise in our measurements. For notational convenience, we define a vector m(st) = [ r(st) g(st)] ⊤

that stacks the unknown reward and safety functions. We denote its components as m0(s) = r(s) and m1(s) = g(s).

Assumption 1: At every t, we receive measurements 

˜m(st) of the reward and safety functions as ˜m(st) = m(st)+ 

ωt, where ωt is a zero-mean noise vector whose components are each conditionally σ-sub-Gaussian. That is, we consider ωt to be a two-dimensional real process adapted to the natural filtration (Ft)t≥0

defined by the measurement equation, where Ft−1 =

σ(s1: t, a 1: t−1, ˜m1: t−1) represents the σ-algebra generated by all states visited up to and including time t, together with the actions taken and measurements received up to time t − 1.Since r and g are unknown a priori , the agent cannot certify any state as safe at t = 0 , and its first action would be unconstrained. Thus, we require the following assumption. 

Assumption 2: We assume the agent starts its exploration in an initial set of states S0 ⊆ S that is known to be safe, i.e., g(s) ≥ h for all s ∈ S0.To overcome the difficulties associated with estimating the safety of the environment based on measurements, we invoke regularity assumptions on the unknown reward and safety functions r and g.

Assumption 3: The functions r and g are Lipschitz-continuous with a known Lipschitz constant L < ∞.Invoking Lipschitz continuity to provide guarantees is very common in the control and safe learning literature [25], [26], [22]. Such an assumption is closely tied with invoking an upper bound on the norm in a reproducing kernel Hilbert space [9]. However, unlike [12], we do not explicitly assume a bounded RKHS norm. Although these assumptions often go hand-in-hand [27], the Lipschitz constant of a function can be more intuitive to estimate based on data [28], [29]. III. S AFE EXPLORATION ALGORITHM 

In this section, we introduce our proposed algorithm for safe and optimal policy learning. 

A. Nadaraya-Watson estimator 

The Nadaraya-Watson estimator is a nonparametric kernel regressor that produces the conditional expectation of arandom variable [14], [15]. We use the standard form of this estimator to estimate m as 

ˆmt(s) := 

> t

X 

> τ=1

Kλ(s, s τ )

κt(s) ˜m(sτ ), (1) where 

κt(s) := 

> t

X 

> τ=1

Kλ(s, s τ ),Kλ(s, s τ ) := K

 ∥s − sτ ∥

λ



. (2) Here, K is the kernel function, λ is the bandwidth pa-rameter, and ˜m(s) are the measurements of the reward and constraint functions. In (2) and in the remainder of this paper, we use ∥ · ∥ to denote the L2 norm. To derive guarantees on the estimate produced by (1), we now introduce an assumption on the nature of the kernel K.

Assumption 4: There exists a constant 0 < χ K < ∞ such that the kernel K : Rd → R satisfies χK ≤ K(v) ≤ 1 for all ∥v∥ ≤ 1, and K(v) = 0 for all ∥v∥ > 1.Since the kernel function is user-defined, this assumption can be satisfied either by explicitly selecting a kernel that matches these conditions, for instance the box kernel, or by formulating one in line with this assumption.   

> Accepted final version. To appear in the Proceedings of the 65th IEEE Conference on Decision and Control .©2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works.

B. Confidence intervals 

Next, for safety and optimality guarantees, we require bounds on how close each component of our estimate ˆmi(s)

is to the true mi(s).

Lemma 1: Let Assumptions 1–4 hold. For every δ ∈

(0 , 1) , with probability at least 1 − δ,

| ˆmi(s) − mi(s)| ≤ βt(s)

simultaneously for all i ∈ { 0, 1}, all time steps t ≥ 1, and all states s ∈ S . The confidence bound width βt(s) is defined as 

βt(s) = 



+∞, if κt(s) = 0 ,Lλ + σκt(s) εt(s) otherwise, where 

εt(s) = 

s

2 ln 

 2|S| 

δ

p1 + νt(s)

p1 + νt(s),νt(s) = 

> t

X 

> τ=1

K2

> λ

(s, s τ ).

The proof of this lemma builds upon [22, Lem. 1]. The confidence bound βt(s) holds uniformly over both compo-nents, all time steps, and the finite state space S via a union bound. We collect our proofs in Section VI. 

C. Updating the estimator in constant time 

In its naive form, the estimator in (1) scales with O(t) in time since it iterates over t points. Nevertheless, a specific quirk in our problem setting of constrained MDPs allows us to implement a recursive variant of the Nadaraya-Watson estimator that scales in constant-time, i.e., O(1) .In our setting, we make use of two properties to imple-ment a recursive grid-based approach. First, since the agent operates on a finite set of states S (e.g., grids on a map), we know exactly what all the possible query points s will be. Second, the kernel function drops to exactly 0 after a certain distance controlled by the bandwidth parameter λ.This discretized setting allows us to avoid recalculating the sum in (1) over all past t observations. Instead, we can trade storage for speed by maintaining sufficient statistics for each state s in our grid S. For the estimator in (1), these are 

θt := 

> t

X 

> τ=1

Kλ(s, s τ ) ˜ m(sτ ), and κt from (2). When a new observation (st+1 , m t+1 ) arrives, we only update the statistics θt(s) and κt(s) for states within the compact kernel’s bandwidth (i.e., where ∥s − st+1 ∥ ≤ λ) as 

θt+1 (s) ← θt(s) + Kλ(s, s t+1 )mt+1 ,κt+1 (s) ← κt(s) + Kλ(s, s t+1 ). (3) With this, we can update our estimate as 

ˆm(s) = θt+1 (s)

κt+1 (s) . (4) For the confidence intervals, the only data-dependent term is νt(s), which can be recursively updated as 

νt+1 (s) ← νt(s) + K2

> λ

(s, s t+1 ). (5) 

D. Pessimistic and optimistic safe sets 

With valid confidence intervals established, we now de-fine two kinds of predicted safe spaces as inferred by our estimator: the predicted pessimistic and optimistic safe sets. These notions are similar to those in [12] and are inspired by [5], [11], [30]. The pessimistic safe set consists of states that satisfy our estimates of the safety constraint with high probability. The optimistic safe set contains any state that could potentially be safe. Furthermore, we filter this set to consider those states that are also reachable and returnable. The safety is evaluated on the basis of the bounds inferred by our estimator. At any step t, the Nadaraya-Watson estimator provides a valid confidence interval for the safety function g(s) at state s, denoted as Qt(s) := [ ˆ m1(s) − βt(s), ˆm1(s) + βt(s)] .To ensure that our safe set does not shrink over time, we intersect these intervals sequentially. We define Ct(s) =

Qt(s) ∩ Ct−1(s), initializing with C0(s) = [ h, ∞) for all known safe states s ∈ S0, and C0(s) = ( −∞ , ∞) for all others. The tightened lower and upper bounds are denoted by lt(s) := min Ct(s) and ut(s) := max Ct(s). Rather than relying on computationally expensive GP posteriors, we construct these spaces using the recursive confidence bounds established in Lemma 1. Both safe spaces are initialized with the known safe seed from Assumption 2, X − 

> 0

= X +0 = S0.

1) Predicted pessimistic safe set: We now define two sets for the probabilistic safety guarantee. First, we have S− 

> t

,the set of states that satisfy the safety constraint h with high probability. By using the Lipschitz continuity (Assumption 3) of the safety function, we can certify states that have not yet been directly sampled but are sufficiently close to known safe states as 

S− 

> t

= {s ∈ S | ∃ s′ ∈ X − 

> t−1

: lt(s′) − L · ∥ s − s′∥ ≥ h}.

However, certifying a state as safe is insufficient if the agent cannot safely navigate to it or return from it. We enforce topological safety using reachability and returnability operators. Reachability ensures the agent can enter a newly certified state from its current safe set, while returnability ensures it can leave again. Let Rreach (X) be the set of states reachable from a set X in one step. Let Rret (X, ¯X) denote the states in X from which the agent can transition back to a target set ¯X in one step. An n-step returnability operator is defined recursively as Rn

> ret

(X, ¯X) = Rret (X, R n−1 

> ret

(X, ¯X)) .The limit as n → ∞ , denoted ¯Rret (X, ¯X), yields all states that can eventually return to ¯X through X.With this, the predicted pessimistic safe space X − 

> t

is thus defined as the subset of S− 

> t

as 

X − 

> t

= {s ∈ S− 

> t

| s ∈ Rreach (X −

> t−1

) ∩ ¯Rret (S− 

> t

, X −

> t−1

)}. (6) Therefore, X − 

> t

is the subset of S− 

> t

consisting of states that are reachable from the previous safe space X − 

> t−1

and from   

> Accepted final version. To appear in the Proceedings of the 65th IEEE Conference on Decision and Control .©2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works.

Algorithm 1 COLS AFE -MDP  

> 1:

Input: State space S, transition function f , safe seed S0,Lipschitz constant L, bandwidth λ, optimality bound ¯β,safety threshold h, desired confidence δ 

> 2:

Initialize (θ0, κ 0, ν 0) ← 0; C0(s) ← [h, ∞) ∀s ∈ S0 

> 3:

repeat  

> 4:

Update predicted safe spaces X − 

> t

and X + 

> t
> 5:

Update set of potential expanders Gt with (8)  

> 6:

Set Tt ← G t if Gt̸ = ∅, else X − 

> t
> 7:

Select target state ξ ← arg max 

> s∈T t

wt(s) 

> 8:

Go to ξ (via shortest path within X − 

> t

), receive ˜m(st) 

> 9:

Update ˆm(st) using ( θt, κ t, ν t) 

> 10:

until max  

> s∈X −
> t

wt(s) ≤ 2 ¯β 

> 11:

loop  

> 12:

Update optimistic reward with (9)  

> 13:

Compute optimal value function J∗(st) over X − 

> t
> 14:

Execute action at := arg max  

> a

E[J∗(st)]  

> 15:

end loop 

which the agent can return to it. By restricting the policy to actions that transition exclusively within X − 

> t

, the algorithm theoretically guarantees that the agent will not violate the safety threshold h.

2) Predicted optimistic safe set: To guide exploration, we must also identify states that have the potential to be safe. Using the upper bound of our confidence intervals, we define an optimistic safe set S+ 

> t

as 

S+ 

> t

= {s ∈ S | ∃ s′ ∈ X + 

> t−1

: ut(s′) − L · ∥ s − s′∥ ≥ h}.

States outside of S+ 

> t

are deemed unsafe with high probability and are discarded from the search space. Thus, the predicted optimistic safe space X + 

> t

is 

X + 

> t

= {s ∈ S+ 

> t

| s ∈ Rreach (X +

> t−1

) ∩ ¯Rret (S+ 

> t

, X +

> t−1

)}. (7) In other words, X + 

> t

contains any state that could po-tentially be safe. The agent’s goal during the exploration phase is to select paths within the pessimistic set X − 

> t

that gather observations to systematically convert elements of the optimistic set X + 

> t

into certified safe states. 

E. The algorithm: COLS AFE -MDP We now introduce our proposed algorithm, C OLS AFE -MDP, that efficiently achieves a near-optimal policy in finite time while guaranteeing safety. Algorithm 1 displays the pseudo-code overview. We split the algorithm into two phases. The first phase is dedicated entirely to the exploration of safety. To efficiently expand the pessimistic safe space X − 

> t

, the agent identifies a set of expanders Gt, defined as 

Gt = {s ∈ X − 

> t

| ∃ a ∈ A : f (s, a ) ∈ X + 

> t

\ X − 

> t

}. (8) These are states within X − 

> t

from which safety can potentially be extended to currently uncertified states via Assumption 3. We greedily select the state ξ ∈ Gt with the largest confi-dence interval width wt(s) = ut(s) − lt(s). This difference is the measure of uncertainty about g at s, so we sample where our knowledge of the safety function is the weakest. If 

Gt is empty, we default to reducing the highest uncertainty remaining within the currently certified safe space. While traversing to the target ξ, we receive new measurements 

˜m(st) of the reward and safety functions and update the local statistics (θt, κ t, ν t). This exploration phase terminates when the maximum uncertainty across the entire pessimistic safe space drops below a user-defined optimality bound ¯β.In the second phase, we focus on exploiting the reward. Operating exclusively within the certified safe space X − 

> t

, we define an optimistic reward function for a pair (s, a ) based on the upper confidence bound for the reward component 

m0 at the successor state s′ := f (s, a ) as 

Ut(s, a ) = min 



Rmax , u rt (s′)



, (9) where urt (s) := ˆm0,t (s) + βt(s) is the upper confidence bound on the reward from Lemma 1. We then compute the optimal value function J∗(st) using the Bellman equation and execute the action at that maxi-mizes its expected value [31]. By updating the estimator with reward measurements while executing this optimistic policy, the agent reduces reward uncertainty. In the next section, we show that this converges to a near-optimal policy without breaching the safety threshold. 

F. Safety and optimality 

The safety of C OLS AFE -MDP rests on the pessimistic safe set X − 

> t

. By construction, we restrict the agent’s transi-tions to states within X − 

> t

, which are certified safe through the confidence bounds from Lemma 1. The following theorem formalizes this. 

Theorem 1: Under Assumptions 1–4, and following Al-gorithm 1, g(st) ≥ h for all t ≥ 0 with probability at least 

1 − δ.The proof proceeds by induction on the safe set, using Lemma 1 and Assumption 3 to certify that each newly added state satisfies g(s) ≥ h.Next, we prove that the algorithm converges to a near-optimal policy. This requires two steps. First, we must show that βt(s) can be driven below any target threshold ¯β > Lλ 

with a finite number of measurements. Under Assumption 3, the residual bias Lλ does not vanish with more data for a fixed bandwidth λ.

Lemma 2: Under Assumptions 1–4 and for any ¯β > Lλ ,there exists a finite N ¯β ∈ N, such that an accumulation of 

N ≥ N ¯β local measurements sτ satisfying ∥s − sτ ∥ ≤ λ

guarantees βt(s) ≤ ¯β.Intuitively, the noise contribution to βt(s) scales as 

˜O(1 /√N ) and vanishes with increasing data, while the bias term Lλ remains constant. Thus, for any margin ¯β > Lλ ,we prove that sufficiently many local observations will drive the total bound below ¯β. We use this result on shrinking uncertainty to show an upper-bound on the exploration phase. 

Corollary 1: Under Assumptions 1–4, a chosen ¯β > Lλ ,and N ¯β ∈ N as in Lemma 2, the safety exploration phase ter-minates after at most t∗ ≤ D|S| N ¯β environment transitions,   

> Accepted final version. To appear in the Proceedings of the 65th IEEE Conference on Decision and Control .©2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works.

0.0 0.5 1.0

Time step 

2.50 2.75 3.00 3.25 3.50 

> Best reward
> ColSafe-MDP SNO-MDP with ES2

0 1 2

Elapsed time (s) 

0200 400 600 800 

> |X  −
> t |
> ColSafe-MDP SNO-MDP with ES2

100 200 300 

Max iterations 

10 1

10 2

10 3    

> Wall time (s)
> SNO-MDP wo. ES2 ColSafe-MDP Fig. 1. Performance comparison of C OLS AFE -MDP against the SNO-MDP baseline in the synthetic grid environment. The leftmost panel shows the best seen reward against a normalized time step count. The dashes at the bottom indicate a transition from Phase 1 to Phase 2. The middle plot shows the size of the certified safe set against wall-clock time. The rightmost plot shows, on a logarithmic scale, the computational time taken by the two algorithms. From these figures, we see that while maintaining the same performance as the baseline, COLS AFE -MDP expands the safe set faster at a substantially lower computational cost.

where D is the maximum shortest-path distance between any two states in X − 

> t

over all t.We show that targeting maximum uncertainty forces it to drop below 2 ¯β in at most |S| N ¯β measurements, requiring no more than D|S| N ¯β transitions. With the exploration phase now finite, we prove the near-optimality of the resulting policy. 

Theorem 2: For a chosen ¯β > Lλ , by Corollary 1, the exploration phase terminates in t∗ ≤ D|S| N ¯β steps. With probability at least 1 − δ and a policy πt executed for all 

t ≥ t∗,

V πt (st) ≥ V ∗(st) − ϵ, (10) where ϵ = 2 ¯β

1 − γ and V ∗(s) is the value function corre-sponding to the true optimal policy restricted to the safe space. 

Remark 1: The bound t∗ ≤ D|S| N ¯β is a worst-case estimate assuming each state requires N ¯β independent visits. In practice, convergence is faster because each observation also reduces uncertainty at neighbouring states within the kernel’s support. IV. E VALUATION 

We evaluate the performance of C OLS AFE -MDP against the baseline SNO-MDP from [12] in two settings. First, we use a synthetic grid-environment to study C OLS AFE -MDP’s reward gain, explorativeness, and computational efficiency. Second, we conduct simulations over data from Martian digital imagery to assign safety function values based on the terrain slope. 

A. Synthetic environment 

Our synthetic environment, identical to [12], is a discrete grid-world MDP framework consistent with Section II. The state space S consists of the cells of a two-dimensional rectangular grid, where each cell represents a distinct lo-cation the agent can occupy. The action space A =

{stay, up, right, down, left } contains five deterministic ac-tions, with transitions that move the agent to the adjacent grid cell in the chosen direction or keep it in place. Each state s ∈ S is associated with a scalar reward value r(s) and a scalar safety value g(s), both of which are unknown to the agent and must be estimated online from noisy observations. In our experiments, we use a 20 × 20 square grid where the reward and safety values for each cell were randomly generated. For C OLS AFE -MDP, we chose a box kernel with bandwidth λ = 1 , which corresponds to a compact kernel that weighs two neighbouring states in each direction equally. We set L = 0 .08 , σ = 0 .04 , and δ = 0 .05 . For the baseline algorithm, we chose a Gaussian kernel with length scale ℓ =2 for the GP that infers reward and ℓ = 1 for the GP that infers safety. We set the confidence interval parameters to 

βt = 4 .11. For both methods, we set the discount factor 

γ = 0 .99 .The baseline method has an early-stopping condition (ES2 in [12]) where the safe set expansion is terminated if the optimal policy for the MDP heads for the inside of X − 

> t

.Figure 1 summarizes this comparison. We observe, in the rightmost panel, that our computational improvement over the baseline is substantial. The wall-clock time stays nearly flat as the iteration count grows from 50 to 300 while the GP-baseline grows with O(n3). The middle panel shows that although we require more iterations, C OLS AFE -MDP discovers safe states faster than SNO-MDP with ES2. Fi-nally, the leftmost panel shows that this comes at no cost to the learnt policy; both methods reach comparable best-seen reward. The corresponding reward and safety exploration on the environment is visualized in Figure 3. There were no safety violations for both C OLS AFE -MDP and the baseline algorithm.     

> 1Here, βtis from [12], not the bound from Lemma 1. To successfully reproduce the results of SNO-MDP, we had to choose hyperparameters different from those reported in [12].
> Accepted final version. To appear in the Proceedings of the 65th IEEE Conference on Decision and Control .©2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works.

B. Mars surface exploration −0.65 

> −0.60
> −0.55
> −0.50
> −0.45
> −0.40
> −0.35
> −0.30
> g(s) =  − tan( α)

Fig. 2. Safety heatmap of the Martian 

40 × 40 -grid considered in our experi-ments. Unsafe regions are marked with a dashed line. 

In this setup, we con-sider the example of aplanetary rover explor-ing an unknown envi-ronment in pursuit of visiting different states to collect samples while avoiding unsafe terrain. Here, we used publicly available data from the High-resolution Imaging Science (HiRISE) cam-era that was part of the Mars Reconnaissance Or-biter [32]. More specifically, we considered a 40 × 40 

grid from a digital terrain model around a crater-impacted hydrothermal zone located at roughly 31 ◦S and 202 ◦E2.The safety of each state is defined by its slope α, computed as g(s) = − tan α. We selected the threshold h based on 

α = 29 ◦. This is depicted in Figure 2, where the unsafe states comprise one-sixth of the grid. We generated the rewards randomly and set the hyperparameters identically to the experiments in Section IV-A. This setup is in line with [12]. The results from these experiments are presented in Ta-ble I. C OLS AFE -MDP certifies nearly three times as many safe states against the baseline. Consequently, with more of the terrain available to safely exploit, the learnt policy attains higher mean reward. The computational gap is widest during exploration, where C OLS AFE -MDP completes the phase in 0.79 s against 10 .73 s , and remains substantial in the exploitation phase. We see that C OLS AFE -MDP discovers nearly three times as many safe states whilst achieving higher reward, zero violations, and at faster runtime. Notably, the baseline incurs one safety constraint violation while expanding its safe set, whereas C OLS AFE -MDP incurs none in either phase. These results are consistent with the synthetic experiments and further confirm that C OLS AFE -MDP scales favourably to the case of a rover navigating an unknown planetary landscape. 

TABLE I MARS TERRAIN EXPERIMENT RESULTS .

For each method, the first row reports the exploration phase and the second the exploitation phase. |X −| is the number of certified safe states, ¯r the mean reward of the learnt policy, ‘Viol.’ the number of safety constraint violations, and ‘Time’ the wall-clock time in seconds. 

Method |X −| ¯r Viol. Time (s) COLS AFE -MDP – 0.561 0 0.79 1979 0.981 0 35.04 

SNO-MDP-ES2 [12] – 0.556 1 10.73 674 0.567 0 75.85 

> 2

Link: Stratigraphy of Potential Crater Hydrothermal System, HiRISE, Lunar and Planetary Lab, University of Arizona. 0 5(c) ColSafe-MDP 02468

0 5(d) SNO-MDP with ES2 

024Reward Explore Exploit Start End 

0 5(a) ColSafe-MDP 02468

0 5(b) SNO-MDP with ES2 

02Safety Explore Exploit Start End 

Fig. 3. Visualizations of the path taken by the agent on the environment, overlaid with reward and safety function values. Top: safety g(s); bottom: reward r(s). The paths indicate the extent of exploration during the training phase, any potential safety violations, and the final state. The agent visits more safe states with our algorithm, and both methods converge to a similar final position that maximizes reward within safe limits. 

V. C ONCLUSION 

In this paper, we present a safe exploration strategy in reinforcement learning that outperforms its counterparts on performance, computational efficiency, and explorativeness under an arguably milder set of assumptions. We adopt the principles behind the SNO-MDP algorithm [12] and replace the GP estimates with the Nadaraya-Watson estimator. We then update these estimates in O(1) time recursively—a significant improvement over the O(n3) computational com-plexity of GPs. To facilitate the usage of the new estimator in a safe learning setting, we derive safety guarantees on the estimate and near-optimality guarantees on the derived policy that hold with high probability. This overall improvement comes at the cost of sample efficiency. Although outbalanced by the computational improvement, C OLS AFE -MDP has a more conservative safe-set expansion strategy that consumes more iterations. For future work, it would be relevant to consider exten-sions that further tighten our bounds, such as by using local Lipschitz constants to reduce the estimator’s bias, or by using an adaptive bandwidth parameter λ. We also inherit the same set of assumptions as baseline methods [8], [33]. Future extensions could weaken these assumptions. For example, approaches to logically constrained decision making use scenario optimization, which requires fewer assumptions on function smoothness and noise distributions [34]. Other extensions would include expanding efficiency and efficacy to higher dimensional settings.   

> Accepted final version. To appear in the Proceedings of the 65th IEEE Conference on Decision and Control .©2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works.

VI. P ROOFS 

In this section, we prove the safety and optimality guar-antees: Lemma 1, Theorem 1, Lemma 2, Corollary 1, and Theorem 2. 

A. Proof of Lemma 1 Proof: We build upon the point-wise Nadaraya-Watson martingale bounds derived in [22]. By Assumption 1, the super-martingale inequality [22, Cor. 2, Lem. 1] yields that for any fixed state s ∈ S , fixed component i ∈ { 0, 1}, and failure probability δ′ ∈ (0 , 1) , the following bound holds uniformly over all t ≥ 1 with probability at least 1 − δ′:

| ˆmi,t (s) − mi(s)| (11) 

≤ Lλ + σκt(s)

s

2 ln 

 1

δ′

p1 + νt(s)

p1 + νt(s).

Now, let Es,i denote the event that the point-wise con-fidence bound fails at state s for component i. Applying Boole’s inequality, we have, 

P

 [

> s∈S

[

> i∈{ 0,1}

Es,i 

 ≤ X

> s∈S

X

> i∈{ 0,1}

P(Es,i ).

We assign a uniform pointwise failure probability δ′ := δ

2|S| 

to each pair (s, i ). Substituting δ′ into the logarithmic term of (11), we obtain 

ln 

 1

δ′

p1 + νt(s)



= ln 

 2|S|

δ

p1 + νt(s)



.

Substituting this into (11) yields the exact definition of βt(s)

from Lemma 1, concluding the proof that | ˆmi(s)−mi(s)| ≤ 

βt(s) holds jointly for all i ∈ { 0, 1} and all s ∈ S with probability at least 1 − δ.

B. Proof of Theorem 1 Proof: Let E denote the event on which | ˆmi(s) −

mi(s)| ≤ βt(s) holds simultaneously for all i ∈ { 0, 1},

t ≥ 1, and s ∈ S . By Lemma 1, P(E) ≥ 1 − δ. We show that on E, safety holds for all t ≥ 0 by induction. By Assumption 2, the initial set S0 is safe, with g(s) ≥ h

for all s ∈ X − 

> 0

. For the inductive step, assume that all states 

s ∈ X − 

> t−1

are safe. At step t, any new certified safe state s ∈X − 

> t

\ X − 

> t−1

must satisfy the pessimistic expansion condition from (6): 

lt(s′) − L∥s − s′∥ ≥ h. 

On E, g(s′) ∈ Qτ (s′) for every τ ≥ 1. Since Ct(s′) = 

Qt(s′)∩Ct−1(s′), it follows that g(s′) ∈ Ct(s′) for all t, and hence g(s′) ≥ lt(s′) := min Ct(s′). Then, by Assumption 3, 

g(s) ≥ g(s′) − L∥s − s′∥ ≥ lt(s′) − L∥s − s′∥ ≥ h. 

Thus, on E, X − 

> t

contains exclusively safe states for every 

t. Since the agent’s transitions are restricted to X − 

> t

, we conclude that g(st) ≥ h for all t ≥ 0 jointly with probability at least 1 − δ.

C. Proof of Lemma 2 Proof: From Lemma 1 we have bounds as 

βt(s) = Lλ + σκt(s)

s

2 ln 

 2|S| 

δ

p1 + νt(s)

p1 + νt(s).

Suppose we have N local measurements within the band-width λ of state s ∈ S . By Assumption 4, the kernel is bounded such that 0 < χ K ≤ Kλ(·) ≤ 1. Consequently, 

κt(s) ≥ N χ K , and νt(s) = X K2

> λ

(·) ≤ N · (1) 2 = N .Substituting these limits, we obtain a conservative upper bound function: 

βt(s) ≤ Lλ + σ√1 + NN χ K

s

2 ln 

 2|S| 

δ

√1 + N

| {z }  

> := f(N)

.

As N → ∞ , the data-dependent noise term scales as 

˜O(√N ln N /N ). Because this term vanishes asymptotically, the bound tightly converges to the inherent estimator bias: 

lim   

> N→∞

f (N ) = Lλ .By our hypothesis we choose ¯β > Lλ . By the formal definition of a limit, there must exist a finite integer N ¯β

such that for all sample counts N ≥ N ¯β , the estimation noise evaluates to strictly less than or equal to the margin 

¯β − Lλ . Thus, for any N ≥ N ¯β , βt(s) ≤ f (N ) ≤ ¯β, thereby concluding the proof. 

D. Proof of Corollary 1 Proof: Let nt(s) be the number of local measurements at state s up to time t. By Lemma 2, nt(s) ≥ N ¯β implies 

wt(s) ≤ 2 ¯β.At each selection the agent picks ξt = arg max 

> s∈T t

wt(s),where Tt = Gt if Gt̸ = ∅ and Tt = X − 

> t

otherwise. Suppose exploration is still running after T = |S| N ¯β + 1 target selections. Since it has not terminated, wt(ξt) > 2 ¯β for every 

t ≤ T , so by the contrapositive of Lemma 2, nt(ξt) < N ¯β

at every selection. Each selection of a target ξt yields at least one measure-ment at ξt, so nt(s) increases by at least one every time s is targeted. Consequently, any state can be targeted at most N ¯β

times, after which its count satisfies nt(s) ≥ N ¯β , and it can no longer be selected. With |S| states, this bounds the total number of selections by |S| N ¯β , contradicting the assumption that T = |S| N ¯β + 1 selections occurred. Hence exploration terminates within |S| N ¯β target selections. If D is the largest shortest-path distance between any two states in X − 

> t

, each target selection costs at most D

transitions, so exploration terminates after at most t∗ ≤

D|S| N ¯β state transitions. 

E. Proof of Theorem 2 Proof: When t ≥ t∗ (i.e., the second phase of the algorithm), we operate exclusively over X − 

> t

where the un-certainty wt(s) ≤ 2 ¯β. For a deterministic state transition   

> Accepted final version. To appear in the Proceedings of the 65th IEEE Conference on Decision and Control .©2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works.

s′ = f (s, a ), our optimistic reward Ut(s, a ) = urt (s′)

satisfies, by Lemma 1 applied to m0,

Ut(s, a ) − r(s′) ≤ 2βt(s′) ≤ 2 ¯β. (12) Let V πUt denote the value of a policy π under the op-timistic rewards. By the generalized induced inequality in [35, Lem. 5], the value difference for any policy in X − 

> t

is bounded by 

V πUt (s) − V π (s) ≤ max s,a |Ut(s, a ) − r(f (s, a )) |

1 − γ

≤ 2 ¯β

1 − γ . (13) Let π∗ be the true optimal safe policy restricted to the safe and reachable states, and πt be the agent’s greedy optimistic policy. By Lemma 4 in [12], the optimistic value of πt upper bounds the true optimal value as 

V πt 

> Ut

(s) ≥ V π∗ 

> Ut

(s) ≥ V π∗

(s) = V ∗(s). (14) Substituting (13) in (14) gives us 

V πt (s) ≥ V πt 

> Ut

(s) − ϵ ≥ V ∗(s) − ϵ, (15) where ϵ = 2 ¯β

1 − γ . This proves that the policy executed for all t ≥ t∗ is ϵ-optimal. REFERENCES [1] D. Silver, A. Huang, C. J. Maddison, A. Guez, L. Sifre, G. van den Driessche, J. Schrittwieser, I. Antonoglou, V. Panneershelvam, M. Lanctot, S. Dieleman, D. Grewe, J. Nham, N. Kalchbrenner, I. Sutskever, T. Lillicrap, M. Leach, K. Kavukcuoglu, T. Graepel, and D. Hassabis, “Mastering the game of Go with deep neural networks and tree search,” Nature , 2016. [2] M. Fazel, R. Ge, S. Kakade, and M. Mesbahi, “Global convergence of policy gradient methods for the linear quadratic regulator,” in Pro-ceedings of the 35th International Conference on Machine Learning ,2018. [3] R. Koppejan and S. Whiteson, “Neuroevolutionary reinforcement learning for generalized control of simulated helicopters,” Evolution-ary Intelligence , 2011. [4] E. Altman, Constrained Markov Decision Processes: Stochastic Mod-eling , 1st ed. Boca Raton: Routledge, 2021. [5] M. Turchetta, F. Berkenkamp, and A. Krause, “Safe exploration in finite Markov decision processes with Gaussian processes,” in Pro-ceedings of the 30th International Conference on Neural Information Processing Systems , 2016. [6] D. Ding, C.-Y. Wei, K. Zhang, and A. Ribeiro, “Last-iterate convergent policy gradient primal-dual methods for constrained MDPs,” in Pro-ceedings of the 37th International Conference on Neural Information Processing Systems , 2023. [7] K. C. Kalagarla, R. Jain, and P. Nuzzo, “A safe Bayesian learning algorithm for constrained MDPs with bounded constraint violation,” in Proceedings of the 28th International Conference on Artificial Intelligence and Statistics , 2025. [8] Y. Sui, A. Gotovos, J. Burdick, and A. Krause, “Safe exploration for optimization with Gaussian processes,” in Proceedings of the 32nd International Conference on Machine Learning , 2015. [9] C. Fiedler, J. Menn, L. Kreisk¨ other, and S. Trimpe, “On safety in safe Bayesian optimization,” 2024, arXiv:2403.12948 [cs]. [10] F. Berkenkamp, M. Turchetta, A. P. Schoellig, and A. Krause, “Safe model-based reinforcement learning with stability guarantees,” in Pro-ceedings of the 31st International Conference on Neural Information Processing Systems , 2017. [11] A. Wachi, Y. Sui, Y. Yue, and M. Ono, “Safe exploration and optimiza-tion of constrained MDPs using Gaussian processes,” in Proceedings of the 32nd AAAI Conference on Artificial Intelligence , 2018. [12] A. Wachi and Y. Sui, “Safe reinforcement learning in constrained Markov decision processes,” in Proceedings of the 37th International Conference on Machine Learning , 2020. [13] C. E. Rasmussen and C. K. I. Williams, Gaussian Processes for Machine Learning , 3rd ed., ser. Adaptive Computation and Machine Learning. Cambridge, MA, USA: MIT Press, 2008. [14] E. A. Nadaraya, “On estimating regression,” Theory of Probability and Its Applications , 1964. [15] G. S. Watson, “Smooth regression analysis,” Sankhy¯ a: The Indian Journal of Statistics, Series A (1961-2002) , 1964. [16] J. Garc´ ıa and F. Fern´ andez, “A comprehensive survey on safe rein-forcement learning,” Journal of Machine Learning Research , 2015. [17] J. Fisac, A. Bajcsy, S. Herbert, D. Fridovich-Keil, S. Wang, C. Tomlin, and A. Dragan, “Probabilistically safe robot planning with confidence-based human predictions,” in Proceedings of Robotics: Science and Systems XIV , 2018. [18] J. Achiam, D. Held, A. Tamar, and P. Abbeel, “Constrained policy optimization,” in Proceedings of the 34th International Conference on Machine Learning , 2017. [19] Y. Chow, O. Nachum, E. Duenez-Guzman, and M. Ghavamzadeh, “A Lyapunov-based approach to safe reinforcement learning,” in Pro-ceedings of the 32nd International Conference on Neural Information Processing Systems , 2018. [20] T. Ni and M. Kamgarpour, “A safe exploration approach to constrained Markov decision processes,” in Proceedings of the 28th International Conference on Artificial Intelligence and Statistics , 2025. [21] D. Baumann, K. Kowalczyk, K. Tiels, and P. Wachel, “A computation-ally lightweight safe learning algorithm,” in Proceedings of the IEEE Conference on Decision and Control , 2023. [22] D. Baumann, K. Kowalczyk, C. R. Rojas, K. Tiels, and P. Wachel, “Safety and optimality in learning-based control at low computational cost,” IEEE Transactions on Automatic Control , 2025. [23] N. Rao and V. Protopopescu, “On PAC learning of functions with smoothness properties using feedforward sigmoidal networks,” Pro-ceedings of the IEEE , 1996. [24] S. Murali, C. R. Rojas, and D. Baumann, “Computationally lightweight classifiers with frequentist bounds on predictions,” in Pro-ceedings of the 29th International Conference on Artificial Intelligence and Statistics , 2026. [25] S. Magureanu, R. Combes, and A. Proutiere, “Lipschitz bandits: regret lower bound and optimal algorithms,” in Proceedings of the Conference on Learning Theory , 2014. [26] L. Brunke, M. Greeff, A. W. Hall, Z. Yuan, S. Zhou, J. Panerati, and A. P. Schoellig, “Safe learning in robotics: from learning-based control to safe reinforcement learning,” Annual Review of Control, Robotics, and Autonomous Systems , 2022. [27] A. Tokmak, K. G. Krishnan, T. B. Sch¨ on, and D. Baumann, “Safe exploration in reproducing kernel Hilbert spaces,” in Proceedings of the 28th International Conference on Artificial Intelligence and Statistics , 2025. [28] R. G. Strongin, “On the convergence of an algorithm for finding a global extremum,” Engineering Cybernetics , 1973. [29] J. W. Huang, S. Roberts, and J.-P. Calliess, “On the sample complexity of Lipschitz constant estimation,” Transactions on Machine Learning Research , 2023. [30] M. Turchetta, F. Berkenkamp, and A. Krause, “Safe exploration for interactive machine learning,” in Proceedings of the 33rd International Conference on Neural Information Processing Systems , 2019. [31] R. S. Sutton and A. G. Barto, Reinforcement Learning: An Introduc-tion . Cambridge, MA, USA: A Bradford Book, MIT Press, 2018. [32] A. S. McEwen, E. M. Eliason, J. W. Bergstrom, N. T. Bridges, C. J. Hansen, W. A. Delamere, J. A. Grant, V. C. Gulick, K. E. Herkenhoff, L. Keszthelyi, R. L. Kirk, M. T. Mellon, S. W. Squyres, N. Thomas, and C. M. Weitz, “Mars Reconnaissance Orbiter’s High Resolution Imaging Science Experiment (HiRISE),” Journal of Geophysical Re-search: Planets , 2007. [33] S. R. Chowdhury and A. Gopalan, “On kernelized multi-armed ban-dits,” in Proceedings of the 34th International Conference on Machine Learning , 2017. [34] A. Tokmak, T. Karvonen, T. B. Sch¨ on, and D. Baumann, “Safe learning-based control via function-based uncertainty quantification,” 2026, arXiv:2604.01173 [eess.SY]. [35] A. L. Strehl and M. L. Littman, “An analysis of model-based interval estimation for Markov decision processes,” Journal of Computer and System Sciences , 2008.