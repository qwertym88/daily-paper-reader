Title: Rethinking Vision Architectures with Gated Linear Attention and KAN

URL Source: https://arxiv.org/pdf/2609.22506v1

Published Time: Tue, 22 Sep 2026 00:26:08 GMT

Number of Pages: 17

Markdown Content:
# Rethinking Vision Architectures with Gated Linear Attention and KAN 

Ali MEHIZEL *, Oussama KHALDI ‡

> *

Department of Applied Statistics, ENSSEA, Kolea, Tipaza, Algeria, 

std-mehizel.ali@enssea.edu.dz 

> ‡

Department of Computer Science, Universit´ e Paris Cit´ e, Paris, France, 

oussama.khaldi@etu.u-paris.fr 

Abstract 

Vision Transformers allocate most parameters to multi-layer perceptrons (MLPs) for channel mixing, while token interactions usually rely on quadratic multi-head self-attention (MHSA). Linear attention reduces sequence complexity to O(N ), but remains coupled with the same fixed-activation MLP as softmax Transformers. Kolmogorov–Arnold Networks (KANs) instead place learnable univariate maps on edges, yet prior vision KANs keep MHSA or omit attention entirely. We introduce LKAT (Linear Kolmogorov–Arnold Trans-former), an isotropic ViT encoder that couples chunkwise Gated Linear Attention (GLA) with a two-layer KAN feed-forward, and we provide an I/O-aware fused RBF–KAN ker-nel for the radial-basis grid maps. Under a shared DeiT-style recipe we compare LKAT with ViT, ViT-5, and MLP-Mixer. LKAT-B exceeds ViT-B/16, ViT-5-B, and Mixer-B/16 on ImageNet-100. Tiny/Small/Base LKAT variants scale consistently on CIFAR-10/100, and ImageNet-100 pretraining transfers to CIFAR fine-tuning. The results support gated lin-ear attention and KAN-based radial-basis functions as complementary inductive biases for mid-scale visual representation learning. Code: https://github.com/mehizelali/ linear-kan-transformer .

Keywords: Kolmogorov–Arnold Networks, Gated Linear Attention, RBF, Vision Architecture, Representation Learning 

# 1 Introduction 

Visual representation learning long relied on convolutional residual networks (He et al. 2016); modernized ConvNets such as ConvNeXt (Zhuang Liu et al. 2022) show that carefully re-designed convolutions remain competitive with Transformers on ImageNet and dense predic-tion. Vision Transformers instead treat images as patch sequences under an isotropic encoder (Dosovitskiy et al. 2021; Touvron, Cord, Douze, et al. 2021), building on the multi-head self-attention (MHSA) Transformer (Vaswani et al. 2017). Subsequent supervised recipes such as DeiT III (Touvron, Cord, and J´ egou 2022) strengthen plain ViT training, while ViT-5 (F. Wang et al. 2026) modernizes the canonical Attention–FFN stack (normalization, activations, positional encoding, gating, and learnable tokens) without abandoning the flat topology. Register tokens further mitigate high-norm artifact tokens in ViT feature maps (Darcet et al. 2024). Hierarchical variants such as Swin and PVT (Ze Liu et al. 2021; W. Wang et al. 2021) reintroduce multi-scale structure for dense tasks, while MLP-Mixer and gMLP (Tolstikhin et al. 2021; H. Liu et al. 2021) replace softmax self-attention with static MLP operators over tokens and channels. We retain a flat ViT-style topology and instead redesign the two block operators: attention and channel mixing. 1arXiv:2609.22506v1 [cs.CV] 18 Sep 2026 Linear attention. Linear attention replaces the softmax similarity with a kernel feature map, yielding O(N ) sequence cost and a recurrent form (Katharopoulos et al. 2020). This recurrent view is formally equivalent to classical fast weight programmers, where additive outer prod-ucts of keys and values write into a finite matrix memory; delta-rule updates further allow the mapping to be corrected over time (Schlag, Irie, and Schmidhuber 2021). Chunkwise gated mechanisms such as RetNet and Gated Linear Attention (GLA) (Sun et al. 2024; S. Yang, B. Wang, et al. 2024) combine inter-chunk recurrence with intra-chunk parallelism for hardware-efficient training. Gated Linear Attention in particular adds a data-dependent forget gate on the recurrent state, improving expressivity over plain linear attention while remaining sub-quadratic. Orthogonal to approximate or linear attention, I/O-aware exact attention kernels such as FlashAttention (Dao et al. 2022) show that memory traffic governs wall-clock cost. These methods typically keep the standard two-layer MLP for channel mixing. 

Kolmogorov–Arnold Networks. Kolmogorov–Arnold Networks (KANs) (Ziming Liu et al. 2025) place learnable univariate functions on edges rather than fixed activations on nodes, mo-tivated by the Kolmogorov–Arnold representation theorem. Spline-parameterized KANs are GPU-heavy; FastKAN (Li 2024) approximates edge maps with Gaussian RBFs on a fixed grid, enabling fused evaluation. Alternative bases (Chebyshev, matrix B-splines) exist (Sidharth and Gokul 2024; Coffman and Chen 2025), but we adopt RBF maps for fused GPU evaluation. 

KAN in vision. Convolutional KANs (Bodner et al. 2024) replace fixed CNN kernels with learnable spline-based univariate maps, improving parameter efficiency on Fashion-MNIST, but remain outside the Transformer block and do not address attention. Within ViT-style en-coders, prior KAN stacks leave a clear gap relative to Gated Linear Attention. Hyb-KAN ViT (Dey et al. 2025) swaps the ViT MLP for Wavelet-KAN (and Efficient-KAN in the head) but retains softmax MHSA; its wavelet path is less GPU-native than fused GEMMs. KAT (X. Yang and X. Wang 2024) replaces MLPs with Group-Rational KAN (GR-KAN) and CUDA rational kernels, yet keeps MHSA and uses a hybrid KAN–MLP construction (shared group bases plus a dense aggregate), not a pure edge-wise KAN substitute. FlashKAT (Raffel and Chen 2025) shows that FLOPs parity with MLPs does not imply wall-clock parity: KAT can remain or-ders of magnitude slower in training due to memory stalls in the GR-KAN backward pass, motivating I/O-aware fused kernels. Vision KAN (ViK) (Z. Yang et al. 2026) instead removes pairwise attention and uses MultiPatch-RBFKAN as an attention-free sequence mixer; KAN-based radial-basis functions under linear attention are left unexplored. Taken together, prior art either (i) injects KANs into CNN convolutions rather than Trans-former blocks (Bodner et al. 2024), (ii) pairs KAN/Wav-KAN channel mixing with quadratic softmax attention (Dey et al. 2025; X. Yang and X. Wang 2024), (iii) CUDA-accelerates a hybrid GR-KAN feed-forward while retaining MHSA, yet still exposing severe memory bottlenecks without careful kernels (Raffel and Chen 2025), (iv) uses KAN-based radial-basis functions to replace attention rather than to rethink the feed-forward path under linear attention (Z. Yang et al. 2026), or (v) removes attention via static MLP sequence operators (Tolstikhin et al. 2021; H. Liu et al. 2021) without learnable univariate edge functions. The joint substitution of gated linear attention for MHSA and KAN-based radial-basis functions for the MLP (two comple-mentary redesigns of the Transformer block) has remained largely unexamined. 

Our approach. We propose LKAT (Linear Kolmogorov–Arnold Transformer): an isotropic ViT backbone that couples chunkwise Gated Linear Attention with KAN-based radial-basis functions. We build the attention path on the Flash Linear Attention (FLA) library (S. Yang and Y. Zhang 2024). Register tokens and an end-of-sequence class token keep the layout com-patible with causal recurrence; absolute positions are added to all tokens, and 2D rotary po-sition embeddings (RoPE) (Su et al. 2024) are applied to patch Q/K after per-head RMSNorm 2(B. Zhang and Sennrich 2019). Motivated by FlashAttention’s I/O-aware design (Dao et al. 2022) and FlashKAT’s analysis of KAN memory stalls, radial-basis grid maps are realized as I/O-aware fused Triton kernels (Tillet, Kung, and Cox 2019) rather than unfused operators. We evaluate Tiny, Small, Base, and Large variants against ViT, ViT-5, and MLP-Mixer under a shared recipe. ImageNet-100 is a standard mid-scale setting for studying ViT-family models from scratch and for ablations (Y. Liu et al. 2021; Zhu, Tang, and Han 2021); we adopt it to isolate the Gated Linear Attention and RBF–KAN substitutions before scaling further. Our contributions are threefold: • We introduce LKAT, a ViT architecture that replaces multi-head self-attention with gated linear attention and the MLP with a two-layer Kolmogorov–Arnold network. • We provide an I/O-aware Kolmogorov–Arnold network using Triton fused kernels for the radial-basis maps. • We evaluate the Linear Kolmogorov–Arnold Transformer on mid-scale classification against ViT, ViT-5, and MLP-Mixer (CIFAR-10/100 and ImageNet-100), and report CIFAR fine-tuning from ImageNet-100 checkpoints. 

# 2 Method 

This section recalls MHSA, linear attention, Gated Linear Attention, and RBF–KAN, then de-fines the LKAT encoder and the shared from-scratch training recipe. 

2.1 Preliminaries 

We review the attention operators and Kolmogorov–Arnold constructions that LKAT substi-tutes into the ViT block. 

Multi-head self-attention (MHSA). Given tokens X ∈ RN ×d, a single attention head com-putes queries, keys, and values Q = XW Q, K = XW K , V = XW V and 

Asoftmax (Q, K, V ) = softmax 

 QK ⊤

√dh



V, (1) with head dimension dh (Vaswani et al. 2017). Multi-head self-attention concatenates H heads and applies an output projection. The QK ⊤ term costs O(N 2dh), which is the quadratic bottle-neck of ViT-style encoders. 

Linear attention. Linear attention removes the softmax and factorizes the similarity with a feature map ϕ (Katharopoulos et al. 2020): 

A(Q, K, V ) = ϕ(Q)  ϕ(K)⊤V 

ϕ(Q) ϕ(K)⊤1 . (2) Associativity yields an equivalent recurrent form with a matrix state St ∈ Rdh×dh :

St = St−1 + ϕ(kt)v⊤ 

> t

, ot = S⊤ 

> t

ϕ(qt), (3) reducing sequence cost from O(N 2) to O(N ).3Gated Linear Attention (GLA). Gated Linear Attention (S. Yang, B. Wang, et al. 2024) aug-ments the recurrent update with a data-dependent forget gate Gt ∈ (0 , 1) dh×dh (often parame-terized as a low-rank / elementwise gate): 

St = Gt ⊙ St−1 + ktv⊤ 

> t

, ot = S⊤ 

> t

qt. (4) For training, Gated Linear Attention uses a chunkwise-parallel algorithm: the length-N sequence is split into chunks of size B; intra-chunk computation is parallel, while inter-chunk state is propagated serially. This preserves linear complexity while remaining hardware-efficient relative to naive recurrence. 

Kolmogorov–Arnold representation theorem. The Kolmogorov–Arnold representation the-orem states that any multivariate continuous function f defined on a bounded domain can be expressed as a finite composition of continuous univariate functions and addition. In particu-lar, for a continuous f : [0 , 1] n → R there exist univariate maps ϕq,p and Φq such that 

f (x1, . . . , x n) = 

> 2n+1

X

> q=1

Φq



> n

X

> p=1

ϕq,p (xp)

 . (5) Equation (5) is the classical depth-2 form: an inner layer of univariate transforms of each co-ordinate, followed by outer univariate maps of their sums. Whereas MLPs are typically moti-vated by the universal approximation theorem (fixed nonlinearities on nodes), KANs (Ziming Liu et al. 2025) take Eq. (5) as an architectural prior and generalize it to deeper compositions of learnable univariate maps. 

Kolmogorov–Arnold Networks (KANs). A modern KAN layer implements a fully connected graph in which each edge carries a trainable univariate function ϕq,p and each node merely sums incoming activations (no node-wise nonlinearity). Stacking such layers yields 

KAN( x) =  ΦL ◦ · · · ◦ Φ1

(x), (6) where each Φℓ is a matrix of univariate edge functions. In the original construction (Ziming Liu et al. 2025), every ϕq,p is a B-spline (optionally with a residual basis), which confers local control and interpretability but incurs recursive basis evaluation and grid-adaptation overhead that are poorly matched to dense GPU GEMMs. We therefore follow FastKAN (Li 2024) and replace B-splines with Gaussian radial bases; related alternatives include rational bases in KAT (X. Yang and X. Wang 2024). 

KAN-based radial-basis functions. Following FastKAN, we approximate each univariate edge map by a Gaussian radial basis expansion on a fixed grid {cg}Gg=1 with bandwidth d > 0.For a scalar activation x, the fused grid-linear evaluation is 

y =

> G

X

> g=1

wg exp −

 x − cg

d

2!

, (7) i.e., a weighted sum over the radial bases (Algorithms 1–2). This preserves the Kolmogorov– Arnold edge-function viewpoint while avoiding de Boor-style spline recursion. Motivated by the I/O-aware design of FlashLinearAttention (S. Yang, B. Wang, et al. 2024), we implement Eq. (7) as a fused Triton kernel rather than an unfused PyTorch map that materializes an in-termediate [. . . , D, G ] RBF tensor in HBM. See Algorithm 1 for the forward pass (Algorithm 2 in Appendix 7 describes the backward pass). Autotuning selects BLOCK ∈ { 256 , . . . , 4096 }.4In LKAT, a two-layer KAN-based radial-basis function network replaces the MLP; block-level pre-norm LayerNorm keeps activations within the effective support of the radial grid. 

Algorithm 1 Fused RBF–grid linear forward 

Input: flattened x ∈ RN in HBM centers c ∈ RG, weights w ∈ RG in HBM 

inv ← 1/d ; block size BLOCK 

Output: y ∈ RN in HBM 1: for each program p in parallel do 

2: Load tile x[i], i ∈ Block( p) (HBM →on-chip) 3: acc ← 0 (registers) 

4: for g = 1 to G do 

5: t ← (x[i] − cg) · inv 

6: acc ← acc + wg · exp( −t2)

7: end for 

8: Store y[i] ← acc to HBM 9: end for 

Algorithm 1 evaluates Eq. (7) by tiling the length-N feature axis (batch, tokens, and chan-nels) into blocks of size BLOCK (lines 1–2). Each program loads a tile of x from HBM into on-chip memory and accumulates the weighted Gaussian bases in registers (lines 3–7), writ-ing only the scalar outputs y[i] back to HBM (line 8). Relative to an unfused composition of broadcast, elementwise exponentiation, and reduction (which allocates an intermediate tensor of shape [. . . , D, G ] in HBM), this non-materializing schedule keeps the RBF expansion on chip and reduces HBM traffic to a single read of x and a single write of y. The shared centers c and weights w are reused across programs; with G ≪ N , the serial basis loop is inexpensive and tile-level parallelism maintains high GPU occupancy. Latency and peak-memory comparisons with unfused PyTorch are reported in Figs. 7–8 (Appendix 7). 

2.2 Linear Kolmogorov–Arnold Transformer 

We introduce LKAT (Linear Kolmogorov–Arnold Transformer), a vision backbone that retains the isotropic ViT topology (Dosovitskiy et al. 2021) while replacing multi-head self-attention with causal gated linear attention (S. Yang, B. Wang, et al. 2024; S. Yang and Y. Zhang 2024) and the feed-forward MLP with a two-layer Kolmogorov–Arnold network whose univariate bases are radial basis functions evaluated by I/O-aware fused Triton kernels (Fig. 1; Algorithm 1). 

Figure 1: LKAT architecture. Patch embedding feeds N pre-norm blocks (LayerNorm then Gated Linear Attention with per-head RMSNorm on Q/K; LayerNorm then KAN-based radial-basis functions), each with a residual skip. A linear head reads the final class token. 5Given an image I ∈ RH×W ×3, we extract non-overlapping patches of size P ×P and linearly project them to D-dimensional tokens. The embedded patches are concatenated with a small set of learnable register tokens (Darcet et al. 2024) and a class token placed at the sequence end, remaining compatible with causal recurrence. Absolute positional embeddings are added to the full sequence, while rotary position embeddings (2D RoPE) (Su et al. 2024) are applied only to patch query/key vectors after per-head RMS normalization (B. Zhang and Sennrich 2019). Each encoder block follows a pre-normalization residual design with LayerNorm preced-ing both the Gated Linear Attention module and the KAN block (Fig. 1): 1. Gated Linear Attention: independent linear projections produce queries, keys, values, and gates; short convolutions act on the Q/K/V paths; LayerNorm is applied on the value stream prior to the output projection. Attention follows Eq. (4) in place of MHSA (Eq. (1)). 2. KAN-based radial-basis functions: a two-layer KAN with edge maps of the form Eq. (7), replacing the standard MLP while retaining the edge-wise univariate inductive bias of Eqs. (5)–(6). Figure 2 shows representative learned edge maps ϕp,q from ImageNet-100 LKAT-B (early, mid, and late blocks). The maps remain localized on the radial grid and diversify with depth; the full block-wise set for both KAN layers is in Appendix 7.        

> (a) block 0, fc1 (b) block 5, fc1 (c) block 11 , fc1 (d) block 11 , fc2

Figure 2: Learned RBF–KAN edge functions ϕp,q (x) for ImageNet-100 LKAT-B (representative blocks; full set in Figs. 9–10). Classification is performed from the final LayerNormed class token. Unless otherwise stated, depth, width, and patch size are matched to the corresponding ViT and ViT-5 baselines, yielding comparable parameter counts (Table 2), so that accuracy differences can be attributed to the Gated Linear Attention and KAN-based radial-basis function substitutions rather than to capacity mismatch. 

2.3 Training Protocol 

We train all models under one DeiT-style recipe so that accuracy gaps track the Gated Linear Attention and RBF–KAN substitutions rather than optimizer changes. All from-scratch runs share the recipe in Table 1. Following the DeiT-style pipeline (Touvron, Cord, Douze, et al. 2021), we use RandAugment (Cubuk et al. 2020), Mixup (H. Zhang et al. 2018), CutMix (Yun et al. 2019), Random Erasing (Zhong et al. 2020), and label smoothing (Szegedy et al. 2016). 6Table 1: From-scratch training recipe. Setting Value 

Model defaults 

Resolution / grid / bases 224 2 / G=4 / RBF Registers / RoPE 4 / 2D RoPE ( 2dv1 )

Optimization 

Optimizer AdamW Peak LR / weight decay 3×10 −4 / 0

Warmup / min. LR 5 epochs / 10 −5

Global batch 256 /512 

Train augmentation (DeiT) 

Spatial bicubic resize → RandomCrop (P =4 , reflect) → HFlip RandAugment rand-m9-mstd0.5-inc1 

Random Erasing p=0 .25 , scale (0 .02 , 0.1) 

Mixup / CutMix α=0 .8 / 1.0

Mixup prob. / switch 1.0 / 0.5 (batch mode) Label smoothing 0.1

Val / test 

Pipeline bicubic resize to input size /0.875 → CenterCrop → normalize 

Schedule 

Epochs CIFAR-10: 100 ; CIFAR-100: 200 ; ImageNet100: 300 

CIFAR fine-tuning from ImageNet-100 follows the appendix protocol (Table 6). We com-pare LKAT-T/S/B/L against ViT-5, ViT, and MLP-Mixer under this shared setting. Full ImageNet-1K pretraining is left to future work. 

# 3 Experiments 

Unless otherwise stated, we train from scratch with Table 1 and fine-tune with Table 6, re-porting best validation Top-1 accuracy (%). Baselines are ViT , ViT-5 (F. Wang et al. 2026), and 

MLP-Mixer ; LKAT-T/S/B/L denote our encoders with gated linear attention and KAN-based radial-basis functions, at parameter counts comparable to the matched ViT / ViT-5 scales (Ta-ble 2). We restrict the comparison to flat ViT-style backbones and omit hierarchical models (e.g., Swin, PVT) (Ze Liu et al. 2021; W. Wang et al. 2021) and modernized ConvNets (Zhuang Liu et al. 2022) to isolate the effect of the block substitutions. 

3.1 Image classification 

We evaluate from-scratch Acc@1 on CIFAR-10/100 and ImageNet-100 under the shared recipe. Table 2 reports Top-1 Acc@1; Table 3 lists LKAT width, heads, and depth. 7Table 2: From-scratch Top-1 Acc@1 (%). Params / GFLOPs at 224 2.                                                                                                    

> Model Params GFLOPs CIFAR-10 CIFAR-100 ImageNet100
> LKAT (our)
> LKAT-T/16 6.25 2.40 82.24 62.00 69.74 LKAT-S/16 23.99 9.42 85.57 67.62 76.66 LKAT-B/16 93.99 37.34 88.43 70.25 80.14 LKAT-L/16 329.47 132.14 90.85 72.10 83.16
> rbfKAT (our)
> rbfKAT-T/16 5.51 2.15 80.96 61.97 68.92 rbfKAT-S/16 21.64 8.48 85.51 69.53 75.84 rbfKAT-B/16 85.75 33.70 89.45 70.41 79.32
> ViT
> ViT-T/16 5.54 2.15 82.41 68.33 76.38 ViT-S/16 21.70 8.48 87.60 71.80 80.76 ViT-B/16 85.88 33.70 78.85 67.13 78.72 ViT-L/16 304.33 119.40 84.20 70.40 71.62
> ViT-5
> ViT-5-T 5.54 2.19 78.46 58.28 60.26 ViT-5-S 21.69 8.65 82.24 64.84 72.52 ViT-5-B 85.85 34.38 85.70 68.28 77.46 ViT-5-L 304.00 125.60 87.40 70.50 78.90
> MLP-Mixer
> Mixer-T/16 4.16 1.62 80.80 65.40 70.80 Mixer-S/16 15.44 6.36 87.10 68.83 76.50 Mixer-B/16 59.19 25.20 88.33 69.86 78.92 Mixer-L/16 208.20 89.20 89.17 71.20 78.08

Table 3: LKAT variants at 224 2. Params (M) and GFLOPs for one forward pass.                          

> Model Params GFLOPs Dim Heads Depth LKAT-T 6.25 2.40 192 312 LKAT-S 23.99 9.42 384 612 LKAT-B 93.99 37.34 768 12 12 LKAT-L 329.47 132.14 1024 16 24

LKAT-T/S/B/L scale monotonically with capacity on CIFAR-10/100 and ImageNet-100. In contrast, ViT does not: under the shared recipe, ViT-S/16 exceeds ViT-B/16 on both CIFAR-10 and ImageNet-100, so increasing width/depth fails to improve Top-1 (Base underperforms Small). On CIFAR-10, rbfKAT-B leads Acc@1, exceeding Mixer-L/16 and LKAT-B. On CIFAR-100, LKAT-L leads Acc@1; among Base models, rbfKAT-B exceeds LKAT-B, Mixer-B/16, and ViT-B/16. ViT-5-B surpasses ViT-B/16 at matched width, whereas ViT-5-T/S trail their ViT counterparts. On ImageNet-100, LKAT-L leads Acc@1; among Base models, LKAT-B exceeds rbfKAT-B, Mixer-B/16, ViT-B/16, and ViT-5-B, while ViT-S/16 exceeds Base and Large ViT de-spite fewer parameters. Figure 3 plots Top-1 against GFLOPs on ImageNet-100 (CIFAR coun-terparts in Figs. 5–6). 8Figure 3: ImageNet-100 Top-1 Acc@1 versus GFLOPs ( 224 2). 

# 4 Channel-mixing variants 

We ablate two controls of the RBF–KAN path on ImageNet-100 at fixed LKAT-B width: an additive gated residual on the RBF–KAN output, and the radial grid size G (Fig. 4, Table 4). The default layer applies a double weighted sum over radial bases, then input channels, with 

φg as in Eq. (7). The gated variant adds an elementwise residual γ ⊙ σ(γ):

zp =

> G

X

> g=1

wp,g φg(xp), (8) 

f = W z, (9) 

γ = Wγ x + bγ , (10) 

y = f + γ ⊙ σ(γ). (11) 

Figure 4: Gated residual around the RBF–KAN block (Eqs. (8)–(11)). 9Variant Params (M) GFLOPs Acc@1 

Gated residual 

LKAT-B 93.99 37.34 80.14 + gate 149.96 60.10 82.02 

RBF grid size GG=4 (default) 93.99 37.34 80.14 

G=6 93.29 37.34 81.08 

G=8 93.29 37.34 81.44 

Table 4: LKAT-B ImageNet-100 ablations ( 224 2, 300 epochs). Bold denotes the best Acc@1 in each block. The gated residual improves ImageNet-100 Acc@1 over default LKAT-B (Table 4) at a larger parameter budget, indicating that the additive γ ⊙ σ(γ) branch remains beneficial once Gated Linear Attention handles sequence mixing. Increasing the radial grid from G=4 to G=6 and 

G=8 likewise raises Acc@1 above the default LKAT-B entry in Table 2, consistent with under-resolved univariate bases under the compact default grid; denser grids trade expressivity for additional basis parameters and fused RBF work. 

# 5 CIFAR fine-tuning 

We initialize from ImageNet-100 checkpoints and fine-tune the full backbone. CIFAR inputs are bilinearly upsampled to 224 2 by default ( 384 2 for higher-resolution RB). Table 5 reports CIFAR transfer from the ImageNet-100 LKAT-B checkpoint. Table 5: CIFAR fine-tuning of ImageNet-100 LKAT-B. Setting CIFAR-10 CIFAR-100 

RA, 224 2 93.01 78.20 

RB, 224 2 93.62 78.28 

RB, 384 2 95.45 78.63 

LKAT-B fine-tuning under RA/RB at 224 2 improves substantially over from-scratch CIFAR training, and RB at 384 2 yields a further gain on both CIFAR-10 and CIFAR-100 (Table 5). 

# 6 Discussion 

LKAT-T/S/B/L exhibit monotonic capacity scaling on CIFAR and ImageNet-100 (Table 2), whereas ViT plateaus or declines from Small to Base under the same recipe (ViT-S/16 out-performs ViT-B/16 on CIFAR-10 and ImageNet-100), indicating a mid-scale optimization / capacity mismatch for plain MHSA–MLP stacks. LKAT-B remains competitive against ViT-B, ViT-5-B, and Mixer-B. Ablations show that a gated KAN residual and a denser RBF grid both improve ImageNet-100 Acc@1 relative to the default block (Table 4). Fine-tuning from ImageNet-100 yields substantial Acc@1 improvements over from-scratch training (Table 5), in-dicating that the joint gated-linear-attention and KAN-based radial-basis representation trans-fers within classification. These results motivate full ImageNet-1K pretraining under the same block design, following the common ablation-then-scale pattern on ImageNet-100 (Zhu, Tang, and Han 2021). 10 7 Conclusion 

We presented LKAT, an isotropic vision encoder that replaces MHSA with chunkwise Gated Linear Attention and the MLP with a two-layer KAN-based radial-basis function network, under a shared DeiT-style training recipe. The design isolates complementary inductive biases for attention and channel mixing without hierarchical stages or dense-prediction heads. Empirically, LKAT-T/S/B/L scale monotonically on CIFAR-10/100 and ImageNet-100 (Ta-ble 2), while ViT does not: ViT-S/16 exceeds ViT-B/16 on CIFAR-10 and ImageNet-100 under the shared recipe. On ImageNet-100, LKAT-B exceeds rbfKAT-B, ViT-B/16, ViT-5-B, and Mixer-B/16; LKAT-L leads the column. On CIFAR-10, rbfKAT-B leads Acc@1. ImageNet-100 pretrain-ing transfers effectively: RA/RBat 224 2 and RB at 384 2 yield substantial Acc@1 improvements over from-scratch CIFAR training (Table 5). These results indicate that gated linear attention and KAN-based radial-basis functions are effective substitutes for the canonical ViT block for mid-scale visual representation learning, with more reliable capacity scaling than softmax MHSA under the same DeiT-style recipe. Future work includes full ImageNet-1K pretraining under the same block design (Zhu, Tang, and Han 2021) and broader transfer studies. 

# Acknowledgements 

We thank Ayoub Asri, Ahmed Ala Eddine Benali, and Lizhong Chen for their support, and AI GRID for providing computational resources, including NVIDIA RTX 5090 GPUs, which enabled the from-scratch training, ImageNet-100 pretraining, and CIFAR fine-tuning experi-ments reported in this work. 11 Appendix 

This appendix reports the CIFAR fine-tuning recipes, data preprocessing, CIFAR scaling curves, fused RBF–grid kernels, and learned edge-function visualizations. 

Fine-tuning recipe 

Table 6 details CIFAR fine-tuning from the ImageNet-100 LKAT-B checkpoint under RA and 

RB.Table 6: CIFAR fine-tuning recipe ( RA vs. RB). Setting RA RB

Shared 

Init ImageNet-100 pretrained encoder Optimizer / peak LR AdamW / 10 −4

Resolution / aug. 224 2/384 2 / DeiT Mixup / CutMix 0.8 / 1.0

Differing 

Weight decay 0 0.05 

Drop path 0 0.1

KAN LayerNorm on off CIFAR-10 Acc@1 93 .01 93 .62 

Data preprocessing 

Let S denote the target resolution ( S=224 from scratch and for 224 2 fine-tuning; S=384 for higher-resolution RB). Training applies bicubic resize to S, random crop ( S, padding 4, re-flect), horizontal flip, RandAugment ( rand-m9-mstd0.5-inc1 ), normalization, and ran-dom erasing ( p=0 .25 , scale (0 .02 , 0.1) ), then batch Mixup/CutMix ( α=0 .8/1.0, probability 1.0,switch 0.5, batch mode) with label smoothing 0.1. Validation/test use bicubic resize to S/ 0.875 ,center crop to S, and the same normalization. 12 CIFAR scaling curves 

Figures 5–6 extend the ImageNet-100 scaling plot in the main text. 

Figure 5: CIFAR-10 Top-1 Acc@1 versus GFLOPs ( 224 2). 

Figure 6: CIFAR-100 Top-1 Acc@1 versus GFLOPs ( 224 2). 

Fused RBF–grid linear kernels 

The forward kernel is given in Algorithm 1. We describe the fused backward pass below and report wall-clock latency and peak allocated memory against an unfused PyTorch baseline (Figures 7–8), in the same spirit as the FlashLinearAttention microbenchmarks of (S. Yang, B. Wang, et al. 2024). Our reference implementation uses Triton (Tillet, Kung, and Cox 2019). 

Backpropagation. Write the scalar map of Eq. (7) as 

y =

> G

X

> g=1

wg φg(x), φg(x) = exp −

 x − cg

d

2!

. (12) Differentiating w.r.t. the input and the basis weights yields 

∂y ∂x =

> G

X

> g=1

wg φg(x)



−2 x − cg

d2



, (13) 

∂y ∂w g

= φg(x). (14) Given an upstream gradient dy = ∂L 

> ∂y

, the local gradients are 

dx = d y · ∂y ∂x , (15) 

dwg =

> N

X

> i=1

dyi φg(xi). (16) The fused backward kernel (Algorithm 2) recomputes {φg} on the fly, accumulates Eq. (13) in registers for dx, and aggregates Eq. (16) with atomic adds over the tiled feature axis. 13 Algorithm 2 Fused RBF–grid linear backward 

Require: x, c, w, d as in Alg. 1; upstream dy ∈ RN

Ensure: dx ∈ RN , dw ∈ RG

1: inv ← 1/d ; inv 2 ← 1/d 2; dw ← 0

2: for each program p in parallel do 

3: Load tile x[i], dy[i]

4: jac ← 0

5: for g = 1 to G do 

6: diff ← x[i] − cg

7: r ← exp   − (diff · inv) 2

8: jac ← jac wg r (−2 · diff · inv 2)

9: AtomicAdd(d wg, P 

> i

dy[i] · r)

10: end for 

11: Store dx[i] ← dy[i] · jac 

12: end for 

Figure 7: RBF–grid linear latency (ms, log scale) versus channel width D. (a) Forward; (b) back-ward. Unfused PyTorch versus fused Triton across group sizes G ∈ { 4, 8, 16 , 32 }.14 Figure 8: Peak allocated GPU memory (MiB, log scale) for the RBF–grid linear map versus channel width D. (a) Forward; (b) forward+backward. Unfused PyTorch versus fused Triton at G ∈ { 4, 16 }.

Learned RBF–KAN edge functions 

Figures 9–10 extend Figure 2 to all twelve encoder blocks for both KAN layers (fc1, fc2) of ImageNet-100 LKAT-B. Across depth, the learned univariate maps ϕp,q remain localized on the radial grid and continue to change shape, rather than collapsing to a shared fixed nonlinearity.           

> 012345
> 678910 11

Figure 9: Learned RBF–KAN edge functions ϕp,q (x) for ImageNet-100 LKAT-B, first KAN layer (fc1), blocks 0–11 (panel labels).           

> 012345
> 678910 11

Figure 10: Learned RBF–KAN edge functions ϕp,q (x) for ImageNet-100 LKAT-B, second KAN layer (fc2), blocks 0–11 (panel labels). 15 References 

Bodner, Alexander Dylan et al. (2024). “Convolutional Kolmogorov-Arnold Networks”. In: 

arXiv:2406.13155 .Coffman, Cale and Lizhong Chen (2025). “MatrixKAN: Parallelized Kolmogorov-Arnold Network”. In: arXiv:2502.07176 .Cubuk, Ekin D. et al. (2020). “RandAugment: Practical Automated Data Augmentation with a Reduced Search Space”. In: Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition Workshops , pp. 702–703. Dao, Tri et al. (2022). “FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness”. In: Advances in Neural Information Processing Systems . Vol. 35, pp. 16344–16359. Darcet, Timoth´ ee et al. (2024). “Vision Transformers Need Registers”. In: arXiv:2309.16588 .Dey, Sainath et al. (2025). “Hyb-KAN ViT: Hybrid Kolmogorov-Arnold Networks Augmented Vision Transformer”. In: arXiv:2505.04740 .Dosovitskiy, Alexey et al. (2021). “An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale”. In: International Conference on Learning Representations .He, Kaiming et al. (2016). “Deep Residual Learning for Image Recognition”. In: Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition , pp. 770–778. Katharopoulos, Angelos et al. (2020). “Transformers are RNNs: Fast Autoregressive Transformers with Linear Attention”. In: Proceedings of the 37th International Conference on Machine Learning . PMLR, pp. 5156–5165. Li, Ziyao (2024). “Kolmogorov-Arnold Networks are Radial Basis Function Networks”. In: 

arXiv:2405.06721 .Liu, Hanxiao et al. (2021). “Pay Attention to MLPs”. In: Advances in Neural Information Processing Systems . Vol. 34. Liu, Yahui et al. (2021). “Efficient Training of Visual Transformers with Small Datasets”. In: 

Advances in Neural Information Processing Systems . Vol. 34. Liu, Ze et al. (2021). “Swin Transformer: Hierarchical Vision Transformer Using Shifted Windows”. In: Proceedings of the IEEE/CVF International Conference on Computer Vision ,pp. 10012–10022. Liu, Zhuang et al. (2022). “A ConvNet for the 2020s”. In: Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition , pp. 11976–11986. Liu, Ziming et al. (2025). “KAN: Kolmogorov-Arnold Networks”. In: International Conference on Learning Representations .Raffel, Matthew and Lizhong Chen (2025). “FlashKAT: Understanding and Addressing Performance Bottlenecks in the Kolmogorov-Arnold Transformer”. In: arXiv:2505.13813 .Schlag, Imanol, Kazuki Irie, and J ¨ urgen Schmidhuber (2021). “Linear Transformers Are Secretly Fast Weight Programmers”. In: Proceedings of the 38th International Conference on Machine Learning . PMLR, pp. 9355–9366. Sidharth, S. S. and R. Gokul (2024). “Chebyshev Polynomial-Based Kolmogorov-Arnold Networks: An Efficient Architecture for Nonlinear Function Approximation”. In: 

arXiv:2405.07200 .Su, Jianlin et al. (2024). “RoFormer: Enhanced Transformer with Rotary Position Embedding”. In: Neurocomputing 568. arXiv:2104.09864 (2021), p. 127063. Sun, Yutao et al. (2024). “Retentive Network: A Successor to Transformer for Large Language Models”. In: International Conference on Learning Representations (ICLR) .Szegedy, Christian et al. (2016). “Rethinking the Inception Architecture for Computer Vision”. In: Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition ,pp. 2818–2826. 16 Tillet, Philippe, H. T. Kung, and David Cox (2019). “Triton: An Intermediate Language and Compiler for Tiled Neural Network Computations”. In: Proceedings of the 3rd ACM SIGPLAN International Workshop on Machine Learning and Programming Languages ,pp. 10–19. Tolstikhin, Ilya O. et al. (2021). “MLP-Mixer: An All-MLP Architecture for Vision”. In: 

Advances in Neural Information Processing Systems . Vol. 34, pp. 24261–24272. Touvron, Hugo, Matthieu Cord, Matthijs Douze, et al. (2021). “Training Data-Efficient Image Transformers & Distillation Through Attention”. In: International Conference on Machine Learning . PMLR, pp. 10347–10357. Touvron, Hugo, Matthieu Cord, and Herv´ e J´ egou (2022). “DeiT III: Revenge of the ViT”. In: 

arXiv:2204.07118 .Vaswani, Ashish et al. (2017). “Attention Is All You Need”. In: Advances in Neural Information Processing Systems . Vol. 30. Wang, Feng et al. (2026). “ViT-5: Vision Transformers for The Mid-2020s”. In: arXiv:2602.08071 .Wang, Wenhai et al. (2021). “Pyramid Vision Transformer: A Versatile Backbone for Dense Prediction without Convolutions”. In: Proceedings of the IEEE/CVF International Conference on Computer Vision , pp. 568–578. Yang, Songlin, Bailin Wang, et al. (2024). “Gated Linear Attention Transformers with Hardware-Efficient Training”. In: arXiv:2312.06635 .Yang, Songlin and Yu Zhang (2024). FLA: A Triton-Based Library for Hardware-Efficient Implementations of Linear Attention Mechanism .Yang, Xingyi and Xinchao Wang (2024). “Kolmogorov-Arnold Transformer”. In: 

arXiv:2409.10594 .Yang, Zhuoqin et al. (2026). “Vision KAN: Towards an Attention-Free Backbone for Vision with Kolmogorov-Arnold Networks”. In: arXiv:2601.21541 .Yun, Sangdoo et al. (2019). “CutMix: Regularization Strategy to Train Strong Classifiers with Localizable Features”. In: Proceedings of the IEEE/CVF International Conference on Computer Vision , pp. 6023–6032. Zhang, Biao and Rico Sennrich (2019). “Root Mean Square Layer Normalization”. In: 

Advances in Neural Information Processing Systems . Vol. 32. Zhang, Hongyi et al. (2018). “mixup: Beyond Empirical Risk Minimization”. In: International Conference on Learning Representations .Zhong, Zhun et al. (2020). “Random Erasing Data Augmentation”. In: Proceedings of the AAAI Conference on Artificial Intelligence . Vol. 34. 07, pp. 13001–13008. Zhu, Mingjian, Yehui Tang, and Kai Han (2021). “Vision Transformer Pruning”. In: 

arXiv:2104.08500 .17