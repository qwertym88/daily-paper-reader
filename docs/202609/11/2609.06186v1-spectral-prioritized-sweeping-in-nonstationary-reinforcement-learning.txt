Title: Spectral Prioritized Sweeping in Nonstationary Reinforcement Learning

URL Source: https://arxiv.org/pdf/2609.06186v1

Published Time: Wed, 09 Sep 2026 01:04:44 GMT

Number of Pages: 41

Markdown Content:
# Spectral Prioritized Sweeping in Non-stationary Reinforcement Learning 

Hung Pham 

SOICT, Hanoi University of Science and Technology 

hungpg230036@sis.hust.edu.vn 

Tuan Dam 

SOICT, Hanoi University of Science and Technology 

tuandq@soict.hust.edu.vn 

## Abstract 

Prioritized Sweeping (PS) accelerates model-based reinforcement learning by selecting backups according to Bellman residual magnitude. In nonstationary reward settings, however, the canonical priority score is shortsighted: after a localized reward shift, residuals propagate only through realized backups, so bottlenecked or topologically distant state estimates may remain static under a limited replanning budget. We introduce the Graph Topology Augmentation 

framework, which employ the graph’s resolvent and its diffusion semantic, to augment the inquired signal. Our application, Graph Topology Augmentation for Prioritized Sweeping (GTA-PS), or which the alias Spectral Prioritized Sweeping (SPS) might be more universal, provides a drop-in ordering score for the setting of fixed dynamics and changing state rewards. GTA-PS uses a smootherized policy, inducing a transition chain, with its in- and out-Laplacian. The standard priority key is augmented with a mixing of regularized Laplacian inverses diffusing the residual magnitude. Furthermore, the topology contribution is annealed by a scheduler based on the Second Largest Eigenvalue Modulus (SLEM), allowing its scale to adapt to the chain’s mixing regime. We prove that the forward potential coincides with geometric discounted residual propagation and show that GTA-PS gives active priority instantly to all states. Tabular experiments on FourRooms and GARNET domains demonstrate improved replanning efficiency over standard PS under both exact DP and Dyna-style host planners. 

## 1 Introduction 

Model-based reinforcement learning (MBRL) improves general behavior by using a known or learned transition model for planning [60, Ch. 8]. The agent benefits most when planning effort can be directed to the states that matter most, especially within a brittle planning budget. [59, 30]. 

Prioritized Sweeping (PS, or Standard-PS as to distinguish from our introduced variants) addresses this by maintaining a queue of states keyed by Bellman or Temporal-Difference residual magnitude [42, 45]. For each planning step, the state with the largest priority has its value estimate updated. After that, predecessors’ value estimates often increase in magnitude; their refreshed priorities have higher chance to be queried next. This effect encourages information propagation backward through the predecessor support graph. Although conceptually simple, the resulting sweep can improve update efficiency by several orders of magnitude compared to uniform random backups [68]. We focus our study in the case of nonstationary Markov Decision Process (MDP), where the transition dynamics are fixed but the state reward changes over time [44]. This setting appears whenever the          

> 40th Conference on Neural Information Processing Systems (NeurIPS 2026). arXiv:2609.06186v1 [cs.LG] 5 Sep 2026 rt(sr) changes
> initial: (sr) > 0
> (a) Local reward shift
> reactive predecessor wavefront
> blocked: pPS (s) = 0
> (b) Standard PS after K= 4 backups
> =(IPt)1
> dense: (s) > 0 before the wave arrives
> (c) GTA-PS topology priority

Figure 1: A localized reward shift creates sparse residual at sr . Standard PS is shortsighted: priority of distant states receives no information hints. GTA-PS adds a topology potential on the policy-induced graph, assigning priority to upstream states before predecessor pushes reach them. navigation structure is comparatively stable but the task objective changes, as in goal-conditioned robotics [46], dynamic pricing and operations settings [5], and sequential recommendation problems [70]. The standard approach using residual priority is shortsighted and reactive , as most state order remain stale until the Bellman wave has propagated to them through concrete realized backups. Under the budget of K updates between reward shifts, some topologically distant states may never be reached, regardless of how sophisticated the host planner is [60]. In the nonstationary setting of our focus, this creates a further hindsight effect: by the time the residual signal reaches distant regions, the reward information that triggered it may already be stale. The resulting value errors can then feed back into the planning–action loop, causing the host to allocate future updates according to outdated priorities, compound into a circular degradation of both planning quality and subsequent data collection [15, 33]. We propose to address the former hindrance – credit propagation – by utilizing the model as as-signment guidance. The Graph Topology Augmentation (GTA) framework capture preferences of the agent’s navigation field using spectral operators. From that, we derive rich representations and signal preconditioning that can be used to augment available score-based decisions, such as credit assignment, or bandit identification. Graph Topology Augmentation for Prioritized Sweeping 

(GTA-PS) is an implementation of the GTA ideology for our nonstationary setting study, that augment the residual-keyed priority with an additive mixed terms constructed from directed Laplacian inverses. The method is broadly applicable to orthogonal techniques, such as functional approximation, partial observability, and model uncertainty. 

Our contributions. 

• We formalize priority-based planning as a host-agnostic queue-management problem, mak-ing the priority definition a modular design choice. The revised planning module plugs seamlessly into both exact tabular (DP) and Dyna-style planners without modifying the host. • We introduce Graph Topology Augmentation (GTA), a general RL framework that applies spectral operators on policy-induced chains to produce topology-aware planning signals. • We present GTA-PS (with the alias SPS), a concrete GTA instantiation for nonstationary rewards. It augments residual priority with a directed regularized-Laplacian potential and a fixed forward/backward weight aggregation together with a SLEM-driven βt scheduler that adapts topology gain to the chain’s mixing regime. • We prove key properties of the topology signal, including its numerical stability, the geomet-ric discount and implicit importance sampling effects, plus SLEM adaptive capabilities. • We present a succint experimental evaluation, variating chain statistics across environments and planner architectures, with surgical ablations isolating each cause’s effectiveness. 

## 2 Related Work 

In model-based planning, three lines of work have been established to provide richer planning guidance beyond raw residual magnitude, aiding prioritized sweeping decision. Successor-based 

2signals spread value information over the state graph rather than waiting for purely local error cascades [22, 58]. Learned abstractions compress the planning space, utilizing the generalization capabilities of supervised learning, as in VaST ’s abstract tabular model [20]. Graph-structural heuristics uses auxiliary graph representation, as in TVI/FTVI , which exploit the strongly connected components decomposition of the MDP [21]. Together these demonstrate that good planning requires more than raw residuals and that model knowledge is a powerful source of leadership. Spectral graph theory has a long history of applications in RL representation learning. Proto-value functions [38, 39] use undirected Laplacian eigenvectors as smooth value-approximation bases [55, 7]. The same eigenvectors underpin option discovery [36] and its deep analogues via the 

successor representation [37] and cover-time options [32]. Scalable computational methods include 

Krylov-subspace methods such as Arnoldi and GMRES [27, 48], Graph-drawing objectives with unique solutions [66], and recent exact recovery methods [28]. Recent concurrent work on effective resistance has proposed it as an intrinsic reward guiding agents toward structurally accessible goals [14]. In these works, spectral operators mainly serve as offline representation or filtering bases rather than as a direct mechanism for shaping online backup selection. Our proposed Graph Topology Augmentation framework brings analytical Markov-chain tools into an RL planning module. It builds on the Laplacian-potential intuition from electrical networks [24], treating the policy-induced chain as the Laplacian kernel [17] and its Green’s function as the potential operator [18]. Eigenvalues of the directed chain [34] is utilized as a scheduling signal. Additionally, several analytical tools such as biorthogonal decomposition [29], additive reversibility decomposition [12], and classical spectral graph bounds [19] are investigated to further enrich the framework. 

## 3 Preliminary 

Markov Decision Processes. We consider a finite discounted MDP 

M = ( S, A, p, r t, γ ),

with |S| < ∞, |A| < ∞, fixed discount γ ∈ (0 , 1) , and a fixed transition kernel p(s′ | s, a ). The reward is a bounded, state-based vector rt : S → R that may change over time . Non-station is introduced in the modeling process as demand permutations captured by the reward signal, while the navigation skeleton is asserted to be static. 

Priority-based planning. A planning sweep can only be realistically implemented on a serial computer as a sequence of individual backups. Such updates poses to be more effective when performed in-place, so that later backups in the same sweep can immediately use earlier updates [60, Ch.4]. Hence, one can go even further, by applying an ordering on backups, so that the overlapping effect is most utilized. Such order can be maintained programmatically in a priority queue, ensuring the queried element always hold the largest weight. The particular landscape of priority-based planning is more sophisticated, and we dedicate to describe them thoroughly in [B.3, D.5]. 

Reward setting. Let {τm}m≥0 denote the reward-shift times and let Nshift (n) be the number of shifts up to step n. The reward is piecewise stationary on each interval [τm, τ m+1 ) and may shift exogenously at the times τm with maximum magnitude ∆r .Sparse or localized reward changes are the main regime of interest. Let es ∈ R|S| denote the standard basis vector associated with state s, i.e., 

(es)s′ =

1, s′ = s, 

0, s′̸ = s. 

A rank-one reward shift at time t is a localized perturbation of the form δrt = ∆ r,t esr,t where 

sr,t ∈ S is the affected state and ∆r,t is the shift amplitude. A shift batch aggregates multiple localized reward shifts occurring at the same time step: 

δrt =

> mt

X

> i=1

∆(i) 

> r,t

e(i) 

> sr,t

, mt ≤ mmax ≪ |S| .

This holds in almost all logistical reward design settings. 3Table 1: Core notation used.                            

> Symbol Meaning
> M= ( S,A, p, r t, γ )Finite discounted tabular MDP with stable transition skeleton and piecewise-stationary reward
> π, π PS Host policy and topology policy, respectively
> PPS
> t(e.g., Pt)Topology-policy-induced transition matrix
> Lout
> t=I−PtOut-Laplacian; governs forward diffusion
> Lin
> t=Din
> t−(Pt)⊤In-Laplacian; governs backward attribution
> fout
> t,fin
> tForward and backward residual potentials
> SLEM( Pt) ( e.g., SLEM) Second largest eigenvalue modulus of the topology chain
> K, T shift Fixed planning budget per host step and host step budget per reward shift respectively

Agent setting. The host planner can vary: exact asynchronous value iteration or a Dyna-style planning loop. In both cases the planner maintains a value estimate, requiring efficient backup ordering. Such individual backup is denoted as a planning step , planned before the host decides to take a committal action (denoted as a host step ). In our paper, the number of planning step is fixed to 

K per host step for fair comparisons. Our setting deliberately limits the use of prior knowledge, so that the agent is designed under minimal assumptions. The information available at initialization is either an oracle tabular model in the DP setting, or an immature learned model in the learning setting. Details of component settings in each host algorithm is discussed in B.1. 

Residuals as inferred belief. Residual (in short for value estimation residual ) measures the discrepancy between the planner’s current estimate and new, implied one by latest evidence. Formally, 

ξt(s) := |T tVt(s) − Vt(s)| ,

where TtVt(s) is the value estimate inferred from the current reward and transition information. Prior-itized sweeping treats residual magnitude as a belief-correction score: states with larger discrepancies are backed up first. The important caution is that the inferred estimation should be both unbiased and 

robust to variance, which limits the residual utilizing only one-step samples. 

Paper-wise scope. In our theoretical interest, we lean on the ideal ground of an oracle model and tabular value function as the main analytical regime. For general usage (specifically in the Dyna deployment), the efficiency of our proposal is verified empirically [Sec. 6]. 

## 4 Graph Topology Augmentation for Prioritized Sweeping 

4.1 Graph Topology Augmentation framework Policy-induced random walk. For any policy μ(a | s), the induced transition matrix 

P μ = X

> a

diag( μ(·, a )) P a, P μ(s, s ′) = X

> a

μ(a | s) p(s′ | s, a ),

defines a random walk operator on the state graph. GTA treats P μ as the soft adjacency matrix of the MDP that captures both the transitional rule and the navigational preference of the policy. 

Directed topology operators. The row-stochastic matrix Pt defines a directed state graph. GTA-PS uses two regularized directed Laplacian systems: 

Lout  

> t

= I − Pt, Lin  

> t

= Din  

> t

− P ⊤ 

> t

, Din  

> t

= diag( P ⊤ 

> t

1).

The out-potential measures how much residual a walk starting from s expects to encounter under the topology chain. The in-potential applies the analogous construction on the edge-reversed flow. For a residual vector ξt, define the diffused signal 

f out  

> t

= ( Lout  

> t

+ ηI )−1|ξt|, f in  

> t

= ( Lin  

> t

+ ηI )−1|ξt|, η > 0.

where the regularizer ηI makes both systems invertible A.2.1. 4The forward potential has a particularly grounded interpretation. At the canonical regularization 

η⋆ = 1−γγ . Since Lout  

> t

= I − Pt, (Lout  

> t

+ η⋆I)−1 = γ(I − γP t)−1. Therefore 

f out  

> t

= γ(I − γP t)−1ξt = γ X

> k≥0

(γP t)kξt,

⇒f out  

> t

(s) = γ EPt

X

> k≥0

γkξt(Xk) | X0 = s

 .

Thus the diagonal regularizer is not an arbitrary feature: η effectively controls the geometric discount horizon. With η = η⋆, it equals exactly the reward discount rate γ. Some additional diffusers design also hold various forms of effective horizon, as discussed further in C.1. 

Second Largest Eigenvalue Modulus. For a general stochastic matrix P , the eigenvalues are ordered by modulus as 1 = λ1 ≥ | λ2| ≥ · · · ≥ | λn|. As the operator is applied iteratively, the Second Largest Eigenvalue Modulus SLEM( P ) = |λ2| governs the asymptotic contraction rate of the chain toward its stationary distribution [34]. When SLEM( Pt) is close to 1, the chain has slow modes that can cause long delays in reward propagation – often occurs in bottlenecks [57]. This reveals the potential of annealing learning process with SLEM, pays as a minor compute overhead. 

Graph Topology Augmentation framework As canonical planning techniques uses mixing tech-niques or representational kernels to artificially transmit the reactive signal, our GTA framework instead takes a topological-driven approach – informing decision processes by preconditioning signal with topological operator. The framework facilitates four roles: • proactive priority signal for guiding the algorithm 4.2; • detector of high-impact states and bottlenecks E.2; • scheduler for modulating the topology gain 4.2; • diagnostic tool for understanding the planning geometry A.2. 

4.2 The GTA-PS instantiation. Topology policy. As to guide the priority credit process, we design the topology policy πPS  

> t

,separated but dependent on the host’s policy. For the convergence behavior of standard VI to hold [Lemma A.2.4], the choice of πPS  

> t

is not critically constrained, as long as it is reasonably aligned with the host’s policy and ensures infinite-visit exploration [Lemma 5.1.1]. We propose a soft policy based on the softmax function. Define 

Qt(s, a ) = rt(s) + γ X

> s′∈S

P at (s, s ′)Vt(s′)

as the one-step action-value estimate on (s, a ). Define the centered action score 

At(s, a ) = Qt(s, a ) − max  

> b∈A

Qt(s, b ).

We feed this as logits to construct the Boltzmann policy over action advantages with global unit-normalizer temperature constant b > 0:

πPS  

> t

(a | s) = exp( b A t(s, a )) 

P 

> a′

exp( b A t(s, a ′)) , Pt = X

> a

diag( πPS  

> t

(·, a )) P a.

This keeps the ordering of action preferences but softens the policy, ensuring infinite explorations requirement in 5.1 Finite b standardizes the logits scale-free. 

Augmented priority score. Standard PS uses the Bellman residual pPS  

> t

(s) = |ξt(s)| as its queue key. GTA-PS keeps this residual key and adds a structure-aware correction. The module produces a topology-imbued signal: 

f out  

> t

= ( Lout  

> t

+ ηI )−1 |ξt| , f in  

> t

= ( Lin  

> t

+ ηI )−1 |ξt| ,

5f out  

> t

(s) quantifies the forward topological influence of the residual at s, and f in  

> t

(s) quantifies the backward influence from predecessors via the edge-reversed graph. These potentials are the standard graph-theoretic solutions to (Lout  

> t

+ ηI )f out  

> t

= ξt and (Lin  

> t

+ ηI )f in  

> t

= ξt.The topology signal is: 

τ t ≜ (1 − αt) f in + αt f out 

where αt ≡ α ∈ (0 , 1) is held fixed in deployed variants, balancing forward and backward informa-tion. GTA-PS replaces the queue key with: 

priority t(s) = |ξt(s)| + βt τt(s).ξt(s) is the reactive base, while τt(s) is the proactive topology correction. 

Two-queue architecture with top-q frontier. Because τ t = Gξ t is dense (nonzero everywhere when ξt̸ = 0), inserting it directly into the residual heap would crowd the queue with low-priority states, adding an excessive O(|S| ) cost multiplier. The simple solution is to limit the queue capacity by q. Additionally, the implementation uses two heaps: Qξ over the nonzero residual set and Hq over a filtered top-q frontier extracted from the dense Qτ . This decouples the two signals : the residual heap guarantees that states with large Bellman error remain eligible at all times, while the topology frontier is retained only as a fallback once the residual queue is empty. Setting qt = |S| removes the frontier truncation but does not alter the residual gate. 

SLEM-based β scheduler Should we anneal αt βt to react fairly to the MDP flow and reward non-station? As αt should intuitively be hold constant for a fair credit assignment (and empirically so C.2), we introduce variations in βt.In our called GTA-PS β variant, we use a SLEM-driven linear scheduler, 

βt = β1(1 − SLEM( Pt) + β2),

which amplifies topology gain when 1 − SLEM( Pt) + β2 > 0 and dampens it otherwise. This sched-uler adaptively reduce topology gain in slowly mixing regimes, preventing long-horizon propagation from overexploiting stale or noisy residual signals in early stages. The intuition for these design choices is further expanded in Appendix C. 

## 5 Theoretical Analysis of GTA-PS 

General identities and convergence statements below use the standing assumptions from Section 4. Results about blocked states and cumulative tracking additionally require bounded, sparse, ergodic reward shifts, as made explicit in the theorem statement and Appendix A.1. All full proofs are given in Appendix A.3. 

5.1 Convergence-guarantee of GTA-PS on DP 

As to ensure GTA-PS is not worse than standard PS, Value Iteration should retain converge to the optimal policy under stationary MDP condition – value estimation can always recover if the shifts are sparse enough. We state central properties that govern this, providing the decay factor γ < 1.

Lemma 5.1.1 (Convergence under infinite state visits) . Let T be the Bellman optimality operator, a 

γ-contraction in ∥·∥ ∞. Suppose an asynchronous backup scheme selects a sequence of states {sk}k≥0

such that every state s ∈ S is selected infinitely often. Then Vk → V ∗ in ∥·∥ ∞, independently of the backup-ordering rule. 

We claim that GTA-PS maintains lifelong exploration: 

Proposition 5.1 (GTA-PS satisfies the infinite-visit condition) . Under GTA-PS β with β2 > 0,

β1(1 + β2) < η ∗, and random tie-breaking among equal-priority states in the queue, then every state 

s ∈ S is selected for backup infinitely often. Consequently, GTA and GTA-PS β converges to V ∗ in 

∥·∥ ∞.

6Algorithm 1 Pseudocode for GTA-PS computational workloads 

Input: value estimate V , reward r, model { ¯P a}a∈A , predecessor map Pred , backup budget K, refresh period M , frontier size q

Parameters: topology temperature b, mix α ∈ [0 , 1] , regularizer η > 0, scheduler parameters β1 > 0, β 2 ≥ 0

Convention: ¯P a = P a in exact DP and ¯P a = bP a in Dyna. 

A. Priority refresh procedure REFRESH KEYS (V, r, { ¯P a})Build πPS (a|s) ∝ exp( bA (s, a )) 

P ← P 

> a

diag( πPS (·, a )) ¯P a

Lout ← I − PDin ← diag( P ⊤1)

Lin ← Din − P ⊤

ξ(s) ← | (Tr, ¯P V )( s) − V (s)|, ∀s ∈ S 

Solve (Lout + ηI )f out = ξ

Solve (Lin + ηI )f in = ξτ ← (1 − α)|f out | + α|f in |

σ ← SLEM( P )

β ← β1(1 − σ + β2)

Fq ← TOP Q(τ, q )

Qξ ← HEAP s:ξ(s)>0[ξ(s) + βτ (s)] 

Hq ← HEAP s∈F q [ξ(s) + βτ (s)] 

return (P, ξ, τ, β, Fq , Qξ , Hq )

end procedure B. Budgeted prioritized planning procedure GTA-PS( V, r, { ¯P a}, K )

(P, ξ, τ, β, Fq , Qξ , Hq ) ← REFRESH KEYS (V, r, { ¯P a})

for k = 1 , . . . , K do 

s ← POP MAX (Qξ , Hq )

if s = ∅ then break end if 

Remove s from both heaps 

vold ← V (s)

V (s) ← (Tr, ¯P V )( s)∆V ← V (s) − vold 

for u ∈ { s} ∪ Pred( s) do 

ξ(u) ← | (Tr, ¯P V )( u) − V (u)|

UPDATE (Qξ , u, ξ (u) + βτ (u)) 

if u ∈ F q then 

UPDATE (Hq , u, ξ (u) + βτ (u)) 

end if end for if REWARD SHIFTED () or k mod M = 0 then 

(P, ξ, τ, β, Fq , Qξ , Hq ) ← REFRESH KEYS (V, r, { ¯P a})

end if end for return V

end procedure 

5.2 Operator Theory and Spectral Geometry 

We provide analytical tools for controlling the resolvent interaction. 

Lemma 5.2.1 (Green’s function spectral representation) . Given that the kernel Pt is diagonalizable (Cor. 1) with a biorthogonal eigendecomposition Pt = P 

> k

λkvku⊤ 

> k

and u⊤ 

> j

vk = δjk . Then for any 

η > 0, the forward potential has the spectral expansion 

f out t = X

> k

u⊤ 

> k

|ξt|

1 − λk + η vk,

so slow modes are amplified by at most 1/η , and the first non-Perron mode k = 2 is the SLEM-associated topology-discriminating component. 

Proposition 5.2 (Doob h-transform and implicit importance sampling) . Fix a single-site shift at sr

and define the Green’s predecessor entry ψ(s) ≜ Gout η⋆,t (s, s r ) > 0. Then 

˜Pt(s, s ′) ≜ γP t(s, s ′) ψ(s′)

ψ(s)

is the Doob h-transform of the γ-killed topology chain. Along any path (X0, . . . , X k), the associated change-of-measure weight telescopes as ψ(Xk)/ψ (X0). In this sense, ranking states by ψ(s) is an implicit importance-sampling rule over propagation paths toward the shifted reward site. 

Proposition 5.3 (Dense support and PS barrier) . Assume a single-site reward shift at sr and let 

BK ≜ {s ∈ S : h∗(s; sr ) > K }

be the blocked set beyond the K-backup predecessor wavefront. Then: (i) τt(s) > 0 for every state 

s; and (ii ) after at most K reactive PS backups, every blocked state satisfies pPS  

> t

(s) = 0 while 

τt(s) > 0. This reveals the reactive barrier, alongside bottleneck hindrance effect formally. 

75.3 Nonstationary Performance Analysis 

Finally, we present cumulative, inter-episode superiority of GTA-PS β over standard PS. We apply the assumption 4 that the evolving topology kernel remains close to a frozen reference kernel ¯P :

∥Pt − ¯P ∥∞ ≤ εpol .

Lemma 5.3.1 (Lipschitz priority mass advantage) . For a fixed single-site episode-start residual field 

ζ = ∆ r esr , define the proactive reward shift vector: 

τ (P, ζ ) ≜ γ(I − γP )−1ζ. 

Let τ(q+1) (P, ζ ) denote the (q + 1) -st largest entry of τ (P, ζ ), using the conservative convention that ties at the cutoff are excluded from the top-q set. Define 

adv( P, ζ ) ≜ 1

γ

X  

> s∈B K(sr)

 τ (P, ζ )( s) − τ(q+1) (P, ζ )

> +

as the excess priority mass, within the K-neighborhood of the reward-shift site, above the top-q

cutoff. Then, adv( P, ζ ) is uniformly Lipschitz in P :

|adv( P, ζ ) − adv( P ′, ζ )| ≤ L ∥P − P ′∥∞ , L ≜ 2|S| γ∆max 

(1 − γ)2 .

Proposition 5.4 (Cumulative ergodic advantage) . Under additional assumptions 3 and 5 

1

N

> N

X

> t=1

adv t(Pt, ζ t) ≥ ¯A − Lε pol + o(1) a.s. ,

for some ¯A > 0.

Theorem 5.1 (Cumulative tracking advantage of GTA-PS over reactive PS) . Under additional assumptions 2, with budget q = K. Assume also that |B sel ,t K | ≥ 1 at positive μR-frequency. Define 

err GTA  

> t

≜ τ(q+1) ,t /γ , TE GTA (N ) ≜ PNt=1 err GTA  

> t

, and 

TE PS (N ) ≜

> N

X

> t=1

X

> s∗∈B sel ,t K

Φt(s∗),

where Φt ≜ (I − γP t)−1ξt. Then the cumulative tracking error of GTA-PS is strictly smaller than reactive PS by at least N ( ¯A − Lε pol ) + o(N ) almost surely. In particular, if Lε pol < ¯A then 

TE PS (N ) − TE GTA (N ) → +∞ almost surely. 

Significance. Thm. 5.1 is the centerpiece of our analysis: it provides the first cumulative guarantee for topology-augmented planning, showing that GTA-PS’s advantage over reactive PS grows without bound as the number of episodes increases. 

## 6 Experiments 

We evaluate GTA-PS on tabular GARNET [4] and Minigrid FourRooms [16] under repeated nonsta-tionary reward shifts, applying an oracle DP host and a Dyna host with a fixed planning budget K per host step. Full environment setup, reward protocol, hyperparameters, and computational details are described to Appendix D.1–D.7. 

6.1 Recovery and planning efficiency 

The main empirical result is that GTA-PS improves both planning accuracy, reward collection and shift recovery under the same planning budget. In the Dyna setting of Fig. 2, both GTA variants reduce value error faster than Standard-PS across all tested state sizes and budgets, with the gain most visible immediately after each shift when reactive propagation is still local. Cumulative performance also holds. Fig. 3 shows that the per-episode error gap accumulates into a persistent advantage (Fig. 8), matching the interpretation of Thm. 5.1. These results support the practical claim of greater sample efficiency under limited replanning. 8Figure 2: Repeated reward-shift recovery under a Dyna host. Each panel reports value-error traces after multiple reward shifts for a fixed planning budget K and state size |S| . Solid lines denote mean 

L1 error and dashed lines denote mean L∞ error. Across all tested sizes and budgets, both GTA variants recover faster than Standard-PS, with GTA-PS β typically matching or slightly improving over the fixed-β variant. 

Figure 3: Cumulative tracking ablation under repeated reward shifts in GARNET. GTA-PS β lowers the per-episode terminal error, reduces cumulative tracking error TE( N ), and produces a steadily increasing cumulative advantage over Standard-PS. This is the theorem-facing empirical evidence for persistent advantage under frequent reward shifts. 

6.2 Robustness and Recommendations 

Ablations on fixed, varied α = αt and β = βt in Appendix D.8 show that moderate β improves the agreement between the topology field and the realized pop order, while oversized β sharply degrades. This is consistent with the convergence logic of Prop. 5.1: the topology term is most useful as a bias that surfaces blocked states early and not as a replacement for residual-driven prioritization. Complementary α sweeps also favor small backward mixing. In practice we therefore tune GTA-PS with small α and moderate β.Extended stochasticity, transport, and bottleneck audits are investigated to Appendix D.8. The common lesson is environment adaptability: GTA-PS remains effective in stochastic GARNET instances and in bottlenecked FourRooms layouts, but a custom, highly adversarial transport can render topological guidance clueless. The best of both worlds is to keep topology guidance as an adaptive bias rather than a hard replacement for residual priority. 97 Conclusion 

Our GTA framework utilizes graph-theoretic foundations to craft representation operators that capture entities of interest. By leveraging these tools, GTA therefore can capture subtle patterns of connectivity, influence, and community structure that are not visible from simple degree counts or path-based metrics. On top of this spectral foundation, GTA-PS employs learned models to map beliefs in both the model and the policy into a rich structural priority score. Within the technique, we provide intensive theoretical analysis and surgical experiments to reveals its profound ground. We hope to further extend the framework to other settings, as well as combine it with orthogonal advances in representation learning, exploration, and scalable planning. 10 References 

[1] David Abel. simple_rl: Reinforcement learning in Python, 2019. URL https://github.com/ david-abel/simple_rl .[2] Takuya Akiba, Shotaro Sano, Toshihiko Yanase, Takeru Ohta, and Masanori Koyama. Optuna: A next-generation hyperparameter optimization framework. In Proceedings of the 25th ACM SIGKDD International Conference on Knowledge Discovery & Data Mining , pages 2623– 2631, 2019. doi: 10.1145/3292500.3330701. URL https://doi.org/10.1145/3292500. 3330701 .[3] Marcin Andrychowicz, Filip Wolski, Alex Ray, Jonas Schneider, Rachel Fong, Peter Welin-der, Bob McGrew, Josh Tobin, Pieter Abbeel, and Wojciech Zaremba. Hindsight experi-ence replay. In Advances in Neural Information Processing Systems , volume 30. Curran As-sociates, Inc., 2017. URL https://papers.nips.cc/paper_files/paper/2017/file/ 453fadbd8a1a3af50a9df4df899537b5-Paper.pdf .[4] T. W. Archibald, K. I. M. McKinnon, and L. C. Thomas. On the generation of Markov decision processes. Journal of the Operational Research Society , 46(3):354–361, 1995. doi: 10.1057/jors.1995.50. [5] Yossi Aviv and Amit Pazgal. A partially observed Markov decision process for dynamic pricing. 

Management Science , 51(9):1400–1416, 2005. doi: 10.1287/mnsc.1050.0393. [6] Andrew G. Barto, Steven J. Bradtke, and Satinder P. Singh. Learning to act using real-time dy-namic programming. Artificial Intelligence , 72(1–2):81–138, 1995. doi: 10.1016/0004-3702(94) 00011-O. [7] Mikhail Belkin and Partha Niyogi. Laplacian eigenmaps for dimensionality reduction and data representation. Neural Computation , 15(6):1373–1396, 2003. doi: 10.1162/ 089976603321780317. [8] Richard Bellman. Dynamic Programming . Princeton University Press, 1957. [9] Abraham Berman and Robert J. Plemmons. Nonnegative Matrices in the Mathematical Sciences ,volume 9 of Classics in Applied Mathematics . SIAM, reprint edition, 1994. doi: 10.1137/1. 9781611971262. [10] Dimitri P. Bertsekas and John N. Tsitsiklis. Parallel and Distributed Computation: Numerical Methods . Prentice Hall, Englewood Cliffs, NJ, 1989. ISBN 978-0-13-648700-5. [11] Rajendra Bhatia. Matrix Analysis , volume 169 of Graduate Texts in Mathematics . Springer, 1997. doi: 10.1007/978-1-4612-0653-8. [12] Joris Bierkens. Non-reversible metropolis-hastings. Statistics and Computing , 26(6):1213–1228, 2016. [13] Blai Bonet and Héctor Geffner. Labeled RTDP: Improving the convergence of real-time dynamic programming. In Proceedings of the 13th International Conference on Automated Planning and Scheduling (ICAPS) , pages 12–21. AAAI Press, 2003. [14] Jatin Chauhan, Shivam Bhardwaj, Aditya Saibewar, Aditya Ramesh, Sadbhavana Babar, and Manohar Kaul. Graph-theoretic intrinsic reward: Guiding RL with effective resis-tance. In International Conference on Learning Representations (ICLR) , 2026. URL 

https://openreview.net/forum?id=W8bKDPf1Ko .[15] Wang Chi Cheung, David Simchi-Levi, and Ruihao Zhu. Reinforcement learning for non-stationary Markov decision processes: The blessing of (more) optimism. In Proceedings of the 37th International Conference on Machine Learning (ICML) , volume 119 of Proceedings of Machine Learning Research , pages 1843–1854. PMLR, 2020. URL https://proceedings. mlr.press/v119/cheung20a.html .11 [16] Maxime Chevalier-Boisvert, Bolun Dai, Mark Towers, Rodrigo Perez-Vicente, Lucas Willems, Salem Lahlou, Suman Pal, Pablo Samuel Castro, and Doina Precup. Minigrid & Miniworld: Modular & customizable reinforcement learning environments for goal-oriented tasks. In 

Advances in Neural Information Processing Systems (NeurIPS) , volume 36, 2023. URL https: //arxiv.org/abs/2306.13831 .[17] Fan Chung. Laplacians and the Cheeger inequality for directed graphs. Annals of Combinatorics ,9(1):1–19, 2005. doi: 10.1007/s00026-005-0237-z. [18] Fan Chung and Shing-Tung Yau. Discrete Green’s functions. Journal of Combinatorial Theory, Series A , 91(1-2):191–214, 2000. doi: 10.1006/jcta.2000.3094. [19] Fan R. K. Chung. Spectral Graph Theory . Number 92 in CBMS Regional Conference Series in Mathematics. American Mathematical Society, 1997. [20] Dane Corneil, Wulfram Gerstner, and Johanni Brea. Efficient model-based deep reinforcement learning with variational state tabulation. In Proceedings of the 35th International Conference on Machine Learning (ICML) , pages 1049–1058. PMLR, 2018. URL https://arxiv.org/ abs/1802.04325 .[21] Peng Dai, Mausam, Daniel S. Weld, and Judy Goldsmith. Topological value iteration algorithms. 

Journal of Artificial Intelligence Research , 42:181–209, 2011. doi: 10.1613/jair.3390. URL 

https://arxiv.org/abs/1401.3910 .[22] Peter Dayan. Improving generalization for temporal difference learning: The successor repre-sentation. Neural Computation , 5(4):613–624, 1993. doi: 10.1162/neco.1993.5.4.613. [23] Thomas G. Dietterich. Hierarchical reinforcement learning with the MAXQ value function decomposition. Journal of Artificial Intelligence Research , 13:227–303, 2000. doi: 10.1613/ jair.639. [24] Peter G. Doyle and J. Laurie Snell. Random Walks and Electric Networks , volume 22 of 

Carus Mathematical Monographs . Mathematical Association of America, 1984. URL https: //arxiv.org/abs/math/0001057 .[25] Stanley C. Eisenstat, Howard C. Elman, and Martin H. Schultz. Variational iterative methods for nonsymmetric systems of linear equations. SIAM Journal on Numerical Analysis , 20(2): 345–357, 1983. doi: 10.1137/0720023. [26] Miroslav Fiedler. Algebraic connectivity of graphs. Czechoslovak Mathematical Journal , 23 (98):298–305, 1973. [27] Gene H. Golub and Charles F. Van Loan. Matrix Computations . Johns Hopkins University Press, 4th edition, 2013. [28] Diego Gomez, Michael Bowling, and Marlos C. Machado. Proper Laplacian representation learning. CoRR , 2023. URL https://arxiv.org/abs/2310.10833 .[29] Roger A. Horn and Charles R. Johnson. Matrix Analysis . Cambridge University Press, 1985. [30] Michael Janner, Justin Fu, Marvin Zhang, and Sergey Levine. When to trust your model: Model-based policy optimization. In Advances in Neural Information Processing Systems (NeurIPS) , volume 32, 2019. URL https://arxiv.org/abs/1906.08253 .[31] Jiaming Ji, Borong Zhang, Jiayi Zhou, Xuehai Pan, Weidong Huang, Ruiyang Sun, Yiran Geng, Yifan Zhong, Juntao Dai, and Yaodong Yang. Safety-gymnasium: A unified safe reinforcement learning benchmark. In Advances in Neural Information Processing Systems (NeurIPS) , 2023. URL https://arxiv.org/abs/2310.12567 .[32] Yuu Jinnai, Jee Won Park, David Abel, and George Konidaris. Discovering options for ex-ploration by minimizing cover time. In Proceedings of the 36th International Conference on Machine Learning (ICML) , pages 3130–3139. PMLR, 2019. 12 [33] Khimya Khetarpal, Matthew Riemer, Irina Rish, and Doina Precup. Towards continual rein-forcement learning: A review and perspectives. Journal of Artificial Intelligence Research , 75: 477–524, 2022. doi: 10.1613/jair.1.13673. [34] David A. Levin and Yuval Peres. Markov Chains and Mixing Times . American Mathematical Society, 2nd edition, 2017. [35] David A. Levin, Yuval Peres, and Elizabeth L. Wilmer. Markov Chains and Mixing Times .American Mathematical Society, 2009. [36] Marlos C. Machado, Marc G. Bellemare, and Michael Bowling. A Laplacian framework for option discovery in reinforcement learning. In Proceedings of the 34th International Conference on Machine Learning (ICML) , pages 2295–2304. PMLR, 2017. URL https: //proceedings.mlr.press/v70/machado17a.html .[37] Marlos C. Machado, Clemens Rosenbaum, Xiaoxiao Guo, Miao Liu, Gerald Tesauro, and Murray Campbell. Eigenoption discovery through the deep successor representation. In 

International Conference on Learning Representations (ICLR) , 2018. URL https://arxiv. org/abs/1710.11089 .[38] Sridhar Mahadevan. Proto-value functions: Developmental reinforcement learning. In Proceed-ings of the 22nd International Conference on Machine Learning (ICML) , pages 553–560, 2005. doi: 10.1145/1102351.1102421. [39] Sridhar Mahadevan and Mauro Maggioni. Proto-value functions: A Laplacian framework for learning representation and control in Markov decision processes. Journal of Machine Learning Research , 8:2169–2231, 2007. [40] J. A. Meijerink and H. A. van der Vorst. An iterative solution method for linear systems of which the coefficient matrix is a symmetric M -matrix. Mathematics of Computation , 31(137): 148–162, 1977. doi: 10.1090/S0025-5718-1977-0438681-4. [41] Ravi Montenegro and Prasad Tetali. Mathematical aspects of mixing times in Markov chains. 

Foundations and Trends in Theoretical Computer Science , 1(3):237–354, 2006. doi: 10.1561/ 0400000003. [42] Andrew W. Moore and Christopher G. Atkeson. Prioritized sweeping: Reinforcement learning with less data and less time. Machine Learning , 13(1):103–130, 1993. doi: 10.1007/BF00993104. [43] Nuitka Team. Nuitka: The python compiler, 2024. URL https://nuitka.net/ .[44] Sindhu Padakandla, Prabuchandran K. J, and Shalabh Bhatnagar. Reinforcement learning algorithm for non-stationary environments. Applied Intelligence , 50:3590–3606, 2020. doi: 10.1007/s10489-020-01758-5. [45] Jing Peng and Ronald J. Williams. Efficient learning and planning within the Dyna framework. 

Adaptive Behavior , 1(4):437–454, 1993. doi: 10.1177/105971239300100403. [46] Matthias Plappert, Marcin Andrychowicz, Alex Ray, Bob McGrew, Bowen Baker, Glenn Powell, Jonas Schneider, Josh Tobin, Maciek Chociej, Peter Welinder, Vikash Kumar, and Wojciech Zaremba. Multi-goal reinforcement learning: Challenging robotics environments and request for research. arXiv preprint arXiv:1802.09464 , 2018. URL https://arxiv.org/abs/1802. 09464 .[47] Martin L. Puterman. Markov Decision Processes: Discrete Stochastic Dynamic Programming .John Wiley & Sons, 1994. doi: 10.1002/9780470316887. [48] Youcef Saad and Martin H. Schultz. GMRES: A generalized minimal residual algorithm for solving nonsymmetric linear systems. SIAM Journal on Scientific and Statistical Computing , 7 (3):856–869, 1986. doi: 10.1137/0907058. [49] Aliaksei Sandryhaila and José MF Moura. Discrete signal processing on graphs. IEEE transactions on signal processing , 61(7):1644–1656, 2013. 13 [50] Tom Schaul, John Quan, Ioannis Antonoglou, and David Silver. Prioritized experience replay. In International Conference on Learning Representations (ICLR) , 2016. URL https://arxiv. org/abs/1511.05952 .[51] Julian Schrittwieser, Ioannis Antonoglou, Thomas Hubert, Karen Simonyan, Laurent Sifre, Simon Schmitt, Arthur Guez, Edward Lockhart, Demis Hassabis, Thore Graepel, Timothy Lillicrap, and David Silver. Mastering Atari, Go, chess and shogi by planning with a learned model. Nature , 588:604–609, 2020. doi: 10.1038/s41586-020-03051-4. [52] Guy Shani, David Heckerman, and Ronen I. Brafman. An MDP-based recommender system. 

Journal of Machine Learning Research , 6:1265–1295, 2005. URL https://jmlr.org/ papers/v6/shani05a.html .[53] Jack Sherman and Winifred J. Morrison. Adjustment of an inverse matrix corresponding to a change in one element of a given matrix. Annals of Mathematical Statistics , 21(1):124–127, 1950. doi: 10.1214/aoms/1177729893. [54] Jonathan Richard Shewchuk. An introduction to the conjugate gradient method without the agonizing pain. Technical report, School of Computer Science, Carnegie Mellon Uni-versity, Pittsburgh, PA, August 1994. URL https://www.cs.cmu.edu/~quake-papers/ painless-conjugate-gradient.pdf .[55] David I. Shuman, Sunil K. Narang, Pascal Frossard, Antonio Ortega, and Pierre Vandergheynst. The emerging field of signal processing on graphs. IEEE Signal Processing Magazine , 30(3): 83–98, 2013. doi: 10.1109/MSP.2012.2235192. [56] David Silver, Richard S. Sutton, and Martin Müller. Sample-based learning and search with permanent and transient memories. In Proceedings of the 25th International Conference on Machine Learning (ICML) , pages 968–975. ACM, 2008. doi: 10.1145/1390156.1390278. [57] Alistair Sinclair. Improved bounds for mixing rates of markov chains and multicommodity flow. 

Combinatorics, Probability and Computing , 1(4):351–370, 1992. [58] Kimberly L. Stachenfeld, Matthew M. Botvinick, and Samuel J. Gershman. The hippocampus as a predictive map. Nature Neuroscience , 20(11):1643–1653, 2017. doi: 10.1038/nn.4650. [59] Richard S. Sutton. Integrated architectures for learning, planning, and reacting based on approximating dynamic programming. In Proceedings of the Seventh International Conference on Machine Learning (ICML) , pages 216–224. Morgan Kaufmann, 1990. [60] Richard S. Sutton and Andrew G. Barto. Reinforcement Learning: An Introduction . MIT Press, 2nd edition, 2018. URL http://incompleteideas.net/book/the-book-2nd.html .[61] Richard S. Sutton, Doina Precup, and Satinder Singh. Between MDPs and semi-MDPs: A framework for temporal abstraction in reinforcement learning. Artificial Intelligence , 112(1–2): 181–211, 1999. doi: 10.1016/S0004-3702(99)00052-1. [62] Emanuel Todorov, Tom Erez, and Yuval Tassa. MuJoCo: A physics engine for model-based control. In 2012 IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) ,pages 5026–5033. IEEE, 2012. doi: 10.1109/IROS.2012.6386109. [63] Mark Towers, Ariel Kwiatkowski, Jordan Terry, John U. Balis, Gianluca De Cola, Tristan Deleu, Manuel Goulão, Andreas Kallinteris, Markus Krimmel, Arjun KG, Rodrigo Perez-Higueras, Andrea Pierrot, Sander Schulhoff, Jun Jet Tan, Hannah Tan, and Omar G. Younis. Gymnasium: A standard interface for reinforcement learning environments. arXiv preprint arXiv:2407.17032 ,2024. URL https://arxiv.org/abs/2407.17032 .[64] John N. Tsitsiklis. Asynchronous stochastic approximation and Q-learning. Machine Learning ,16(3):185–202, 1994. doi: 10.1007/BF00993306. [65] Harm Van Seijen and Rich Sutton. Planning by prioritized sweeping with small back-ups. In Sanjoy Dasgupta and David McAllester, editors, Proceedings of the 30th Inter-national Conference on Machine Learning , volume 28 of Proceedings of Machine Learn-ing Research , pages 361–369, Atlanta, Georgia, USA, 17–19 Jun 2013. PMLR. URL 

https://proceedings.mlr.press/v28/vanseijen13.html .14 [66] Kaixin Wang, Kuangqi Zhou, Qixin Zhang, Jie Shao, Bryan Hooi, and Jiashi Feng. Towards better Laplacian representation in reinforcement learning with generalized graph drawing. In Proceedings of the 38th International Conference on Machine Learning (ICML) , pages 10946–10957. PMLR, 2021. URL https://arxiv.org/abs/2107.05545 .[67] Wikipedia contributors. Laplacian matrix — Wikipedia, The Free Encyclopedia, 2026. URL 

https://en.wikipedia.org/wiki/Laplacian_matrix .[68] David Wingate and Kevin D. Seppi. Prioritization methods for accelerating MDP solvers. 

Journal of Machine Learning Research , 6:851–881, 2005. URL https://jmlr.org/papers/ v6/wingate05a.html .[69] Wolfgang Woess. Random Walks on Infinite Graphs and Groups , volume 138 of Cambridge Tracts in Mathematics . Cambridge University Press, Cambridge, UK, 2000. ISBN 978-0-521-55292-9. [70] Lixin Zou, Long Xia, Zhuoye Ding, Jiaxing Song, Weidong Liu, and Dawei Yin. Reinforcement learning to optimize long-term user engagement in recommender systems. In Proceedings of the 25th ACM SIGKDD International Conference on Knowledge Discovery & Data Mining ,pages 2810–2818, 2019. doi: 10.1145/3292500.3330668. URL https://dl.acm.org/doi/ 10.1145/3292500.3330668 .15 Appendix of “Graph Topology Augmentation for Prioritized Sweeping in Non-stationary Reinforcement Learning” 

Table of Contents Appendix A. Proofs of Theoretical Results 16 Appendix B. Algorithms and Implementation Details 25 Appendix C. Intuitions, Design Decisions, Limitations and Future Directions 31 Appendix D. Environments, Hyperparameters, and Experimental Protocol 34 Appendix E. Miscellaneous Supplement 39 

# Appendix A. Proofs of Theoretical Results 

# A.1 Analytical assumptions 

First we address some stronger behaviorial assumptions or workarounds applied to ensure robustness throughout our analytics. 

Assumption 1 (Communicating MDP) . The MDP M = ( S, A, p, R, γ ) has finite state and action spaces and is communicating : for any two states s, s ′ ∈ S , there exists a stationary policy under which s′ is reachable from s with positive probability. Equivalently, every ε-soft stationary policy induces an irreducible transition matrix on S. Since the Boltzmann policy design in 4.2 assigns nonzero probability to every action, the induced transition matrix Pt is irreducible for all t.

Assumption 2 (Bounded reward signal) . Rewards may be stochastic and may change exogenously over time, but their absolute value do not exceed ∆max . This keeps residuals, value targets, and topology scores finite. 

Assumption 3 (Ergodic reward shifts) . The sequence {(sr,t , ∆r,t )}t≥1 is stationary-ergodic under a measure μR, with mean shift amplitude ¯∆r ≜ EμR [∆ r,t ] > 0 and cumulative total variation 

BN ≜ PNt=1 ∆r,t = N ¯∆r (1 + o(1)) a.s. 

Assumption 4 (Slow topology drift for cumulative tracking) . For the cumulative-tracking analysis, there exist a reference kernel ¯P and a constant εpol ≥ 0 such that throughout the analysis window, 

∥Pt − ¯P ∥∞ ≤ εpol .

This is the frozen-kernel / slow-drift envelope used to compare the evolving process against a fixed-kernel ergodic reference. 

Assumption 5 (Frozen-kernel positive local advantage) . Under the frozen reference kernel ¯P from Assumption 4, the reward-shift process has positive probability of placing strictly above-cutoff topology mass inside the K-neighborhood of the shift site: 

μR

 ∃s ∈ B K (sr ) : τ ( ¯P , ζ )( s) > τ (q+1) ( ¯P , ζ ) > 0.

Corollary 1 (Well-definedness of the kernel Pt). The existence of a unique stationary distribution 

μPS  

> t

requires the policy-induced kernel Pt to be irreducible and aperiodic. When this condition is not met exactly, it can be enforced by adding an auxiliary bridge edge with arbitrarily small probability 

ε > 0. Likewise, the Green’s-function expansion in (5.2.1) is simplest when Pt is diagonalizable, which can be enforced by an arbitrarily small generic perturbation of one transition entry. 

# A.2 Supporting Lemmas 

Lemma A.2.1 (Invertible regularized Laplacian) . The shifted systems Lout t + ηI and Lin t + ηI are invertible for every η > 0.

16 Proof. Since Pt is row-stochastic, P  

> j̸=i

pij = 1 − pii . Hence Lout  

> t

+ ηI = (1 + η)I − Pt is strictly row diagonally dominant, with diagonal gap η > 0.Similarly, for ci = ( P ⊤ 

> t

1)i, the matrix Lin  

> t

+ ηI = Din  

> t

+ ηI − P ⊤ 

> t

has diagonal gap 

(ci + η − pii ) − (ci − pii ) = η > 0.

Both matrices are therefore nonsingular by the Levy–Desplanques theorem. 

Lemma A.2.2 (Resolvent norm bounds) . (Lout t + ηI )−1 

> ∞

≤ 1/η and (Lin t + ηI )−1 

> ∞

≤ 1/η .Proof. Both shifted matrices have the same useful structure. Let 

At ∈ { Lout t + ηI, L in t + ηI }.

From the row-surplus computation in Lemma A.2.1, At is a strictly row diagonally dominant Z-matrix with surplus η. Hence At is an invertible M -matrix, so A−1 

> t

≥ 0 entrywise. Moreover, both directed Laplacians annihilate the all-ones vector: 

Lout t 1 = 0 , Lin t 1 = Din t 1 − P ⊤ 

> t

1 = 0 .

Therefore 

At1 = η1, A−1 

> t

1 = 1

η 1.

Since A−1 

> t

is entrywise nonnegative, its ℓ∞ operator norm is the maximum row sum: 

A−1  

> t∞

= A−1 

> t

1 ∞ = 1

η .

Applying this to At = Lout t + ηI and At = Lin t + ηI gives the two desired bounds. 

Lemma A.2.3 (Geometric discount horizon) . Let Pt be row-stochastic, γ ∈ (0 , 1) , and η⋆ =(1 − γ)/γ . Then 

(Lout  

> t

+ η⋆I)−1 = γ(I − γP t)−1 = γ X

> k≥0

(γP t)k.

Thus, for any zt ≥ 0,

 (Lout  

> t

+ η⋆I)−1zt

(s) = γ EPt

X

> k≥0

γkzt(Xk) | X0 = s

 .

Proof. Since Lout  

> t

= I − Pt,

Lout  

> t

+ η⋆I = 1

γ (I − γP t).

The inverse identity follows. Since ∥γP t∥∞ < 1, the Neumann series applies. Finally, (P kt zt)( s) = 

EPt [zt(Xk) | X0 = s].

Corollary 2 (Exact topology-priority sorter) . At η = η⋆, taking zt = |ξt| in Lemma A.2.3 gives 

τt(s) =  (Lout  

> t

+ η⋆I)−1|ξt|(s) = γ EPt

X

> k≥0

γk|ξt(Xk)| | X0 = s

 .

Hence the additive score τt ranks states by expected discounted future Bellman residual, reflecting the proactive amount of future Bellman updating that will be applied through the backup chain. 

Lemma A.2.4. The Bellman optimality operator T V (s) = rt(s) + γ max a

P 

> s′

p(s′|s, a )V (s′) is a 

γ-contraction in ∥·∥ ∞, independent of the topology policy πPS  

> t

.

17 Proof. Fix V, V ′ : S → R and a state s ∈ S . Let 

as ∈ arg max 

> a

X

> s′

p(s′|s, a )V (s′), a′ 

> s

∈ arg max 

> a

X

> s′

p(s′|s, a )V ′(s′).

Using as as a feasible action for the maximization that defines T V ′(s),

(T V )( s) − (T V ′)( s) = γ max 

> a

X

> s′

p(s′|s, a )V (s′) − γ max 

> a

X

> s′

p(s′|s, a )V ′(s′)

≤ γ X

> s′

p(s′|s, a s) V (s′) − V ′(s′)

≤ γ ∥V − V ′∥∞

X

> s′

p(s′|s, a s) = γ ∥V − V ′∥∞ .

Exchanging the roles of V and V ′ gives 

(T V ′)( s) − (T V )( s) ≤ γ ∥V − V ′∥∞ .

Hence 

|(T V )( s) − (T V ′)( s)| ≤ γ ∥V − V ′∥∞ .

Taking the supremum over s ∈ S yields 

∥T V − T V ′∥∞ ≤ γ ∥V − V ′∥∞ .

The formula for T depends only on p and rt, so this contraction property is independent of the topology policy πPS  

> t

.

# A.3 Proofs of Theoretical Results 

Proof of Lemma 5.1.1 

Proof. Let 

Ek ≜ ∥Vk − V ∗∥∞ .

At backup step k, only the selected state sk is updated: 

Vk+1 (sk) = ( T Vk)( sk), Vk+1 (s) = Vk(s) for s̸ = sk.

Since V ∗ = T V ∗ and T is a γ-contraction by Lemma A.2.4, 

|Vk+1 (sk) − V ∗(sk)| = |(T Vk)( sk) − (T V ∗)( sk)| ≤ γE k.

For every s̸ = sk, the error is unchanged: 

|Vk+1 (s) − V ∗(s)| = |Vk(s) − V ∗(s)| ≤ Ek.

Hence Ek+1 ≤ Ek for every k, so the error sequence is nonincreasing. Now define a sequence of cover times. Let N0 = 0 . Since every state is selected infinitely often and 

S is finite, there exists a finite time N1 > N 0 such that every state appears at least once among 

sN0 , s N0+1 , . . . , s N1−1.

Inductively, once Nm is defined, there exists Nm+1 > N m such that every state appears at least once among 

sNm , s Nm+1 , . . . , s Nm+1 −1.

Fix m ≥ 0 and a state s ∈ S . Let js ∈ { Nm, . . . , N m+1 − 1} be the last time in this block at which state s is updated. Immediately after that update, 

|Vjs+1 (s) − V ∗(s)| ≤ γE js ≤ γE Nm ,

because Ek is nonincreasing. Since s is not updated again between times js + 1 and Nm+1 , its value stays fixed on that interval, so 

VNm+1 (s) − V ∗(s) = |Vjs+1 (s) − V ∗(s)| ≤ γE Nm .

This bound holds for every state s, therefore 

ENm+1 ≤ γE Nm .

Iterating gives 

ENm ≤ γmE0 −−−−→ 

> m→∞

0.

Finally, if Nm ≤ k < N m+1 , then Ek ≤ ENm because the error sequence is nonincreasing. Hence 

Ek → 0 as k → ∞ , i.e. 

Vk → V ∗ in ∥·∥ ∞ .

18 Proof of Proposition 5.1 

Proof. Assume that no further reward shifts occur, so the Bellman operator T is fixed. 

Queue mechanism We restate the notations in B.4.1. Let 

ξt(s) ≜ (T Vt)( s) − Vt(s), It ≜ {s ∈ S : |ξt(s)| > 0}.

For convenience, It is asserted to be non-empty, as Assumption 1 and 4.2 ensure infinite exploration. The implementation maintains the residual queue exactly over the nonzero reactive residuals, 

Keys( Qξ (t)) = It,

and a separate topology queue Qτ (t). Backups are selected by the combined priority 

st ∈ arg max 

> s∈I t

{| ξt(s)| + βtτt(s)} .

The topology score is computed from the full residual field through the outgoing and incoming directed Laplacian resolvents: 

f out t ≜ (Lout t + η∗I)−1|ξt|, f in t ≜ (Lin t + η∗I)−1|ξt|.

The realized topology signal is the in–out mixture 

τt(s) = (1 − αt)|f out t (s)| + αt|f in t (s)|, αt ∈ [0 , 1] . (1) 

Topology signal bound By Assumption 2, the reward signal is uniformly bounded; hence the Bellman targets and residuals remain finite. Moreover, by Lemma A.2.2, 

(Lout t + η∗I)−1 

> ∞

≤ 1

η∗ , (Lin t + η∗I)−1 

> ∞

≤ 1

η∗ .

Therefore, 

0 ≤ τt(s) ≤ 1

η∗ ∥ξt∥∞ < ∞. (2) The mixing-time modulation is 

βt = β1(1 − SLEM( Pt) + β2).

Since SLEM( Pt) ≤ 1 also gives 1 − SLEM + β2 ≥ β2, we derive the tighter lower bound 

βt ≥ β1β2 > 0, which ensures βtτt(s) ≥ 0 for every s. Combining this with (2) gives 

0 ≤ βtτt(s) ≤ Bβ

η∗ ∥ξt∥∞ ≜ Cτ ∥ξt∥∞. (3) 

Infinite-visit property. Let 

F ≜ {s ∈ S : s is selected only finitely often }, U ≜ S \ F. 

If F = ∅, every state is selected infinitely often and Lemma 5.1.1 gives convergence directly. Suppose F̸ = ∅. Choose N such that no state in F is selected after time N . Then 

Vt(f ) = VN (f ), ∀f ∈ F, t ≥ N. 

However, the residuals of states in F may still change because their Bellman targets depend on the values of states in U .Regard F as fixed boundary data and define the restricted Bellman operator on U by 

(TF W )( u) ≜ (T V )( u), u ∈ U, 

where V = W ⊕ VN |F . Updates to u ∈ U see the frozen boundary values VN |F , so TF acts as a fixed γ-contraction on RU for all t ≥ N . Every state in U is selected infinitely often by definition, so asynchronous value iteration on this restricted system converges by Lemma 5.1.1: 

max  

> u∈U

|ξt(u)| → 0.

19 Now suppose, toward a contradiction, that the finitely-selected set carries persistent residual: 

lim sup 

> t→∞

max   

> f∈F

|ξt(f )| = δ > 0.

Choose 

ε < (1 − Cτ )δ

4 .

Then there are arbitrarily large times t ≥ N such that 

MF (t) ≜ max   

> f∈F

|ξt(f )| ≥ δ

2 , MU (t) ≜ max  

> u∈U

|ξt(u)|ε. 

Let ft ∈ arg max f ∈F |ξt(f )|, so ft ∈ I t. Since MF (t) ≥ δ/ 2 > ε > M U (t), the F -states dominate the sup-norm: ∥ξt∥∞ = MF (t). Therefore, for every u ∈ U , (3) gives 

|ξt(u)| + βtτt(u) ≤ MU (t) + Cτ MF (t)

ε + Cτ MF (t)

MF (t)= |ξt(ft)|≤ | ξt(ft)| + βtτt(ft),

where the third line uses ε < (1 − Cτ )MF (t), which holds because MF (t) ≥ δ/ 2 and ε < 

(1 − Cτ )δ/ 4. Hence no state in U attains the arg max of the combined priority over It. Because 

ft ∈ F ∩ I t, the feasible set contains at least one F -state, so the selected backup st must lie in 

F ∩ I t ⊆ F — whether st = ft or another F -state reached by random tie-breaking. This contradicts the choice of N , which stipulates that no state in F is selected after time N . Therefore, 

lim sup 

> t→∞

max   

> f∈F

|ξt(f )| = 0 .

Together with max u∈U |ξt(u)| → 0, this gives 

∥ξt∥∞ → 0.

where the hypothesis β1(1 + β2) < η ∗ gives Cτ < 1.

Convergence to V ∗. Using V ∗ = T V ∗ and the γ-contractivity of T ,

∥Vt − V ∗∥∞ ≤ ∥ Vt − T Vt∥∞ + ∥T Vt − T V ∗∥∞

≤ ∥ ξt∥∞ + γ∥Vt − V ∗∥∞.

Rearranging, 

(1 − γ)∥Vt − V ∗∥∞ ≤ ∥ ξt∥∞ → 0.

Therefore GTA-PS β converges deterministically to V ∗ in the no-shift regime. 

Proof of Lemma 5.2.1 

Proof. The biorthogonal decomposition gives 

Pt = X

> k

λkvku⊤ 

> k

, u⊤ 

> j

vk = δjk .

Let 

Mt ≜ Lout t + ηI = (1 + η)I − Pt.

For each eigenpair (λk, v k) of Pt,

Mtvk =  (1 + η)I − Pt

vk = (1 + η − λk)vk = (1 − λk + η)vk.

Since Pt is stochastic, every eigenvalue satisfies |λk| ≤ 1. Therefore 

Re(1 − λk + η) = 1 − Re( λk) + η ≥ η > 0,

so 1 − λk + η̸ = 0 for every k. Thus Mt is invertible on every eigendirection and 

M −1 

> t

vk = 11 − λk + η vk.

20 Because {vk, u k} form a biorthogonal basis, every vector admits the expansion 

ξt = X

> k

(u⊤ 

> k

ξt) vk.

Applying M −1 

> t

termwise gives 

f out t = M −1 

> t

ξt

= X

> k

(u⊤ 

> k

ξt) M −1 

> t

vk

= X

> k

u⊤ 

> k

ξt

1 − λk + η vk.

This is the claimed spectral expansion. For the amplification bound, each modal coefficient is multiplied by (1 − λk + η)−1, and 

|1 − λk + η| ≥ Re(1 − λk + η) ≥ η. 

Hence 1

|1 − λk + η| ≤ 1

η ,

so no mode is amplified by more than 1/η . The same argument applies to periodic chains: even if a nontrivial eigenvalue lies on the unit circle, the positive regularization parameter η keeps the denominator away from zero. The final sentence of the theorem now follows directly from the spectral ordering: k = 1 is the Perron mode, while k = 2 is the first non-Perron mode associated with the SLEM. 

Proof for Proposition 5.2 

Proof. Set P ⋆t ≜ γP t. By the Neumann series, ψ(s) = GP ⋆t (s, s r ). Positivity ψ(s) > 0 follows from Assumption 1: for every s there exists m ≥ 1 with P mt (s, s r ) > 0, so GP ⋆t (s, s r ) ≥ γmP mt (s, s r ) >

0. The identity η∗ = (1 − γ)/γ gives Lout t + η∗I = 1 

> γ

(I − γP t), hence (Lout t + η∗I)−1 =

γ(I − γP t)−1, confirming ψ(s) = Gout η∗,t (s, s r ).

Harmonicity. The resolvent identity Q(I − Q)−1 = ( I − Q)−1 − I with Q = γP t gives 

(P ⋆t ψ)( s) = γγP t(I − γP t)−1 

> s,s r

= γ(I − γP t)−1 − I 

> s,s r

= ψ(s) − γδ s,s r , (4) so P ⋆t ψ = ψ pointwise on S \ { sr }.

Row sums of ˜Pt. From (4), 

X

> s′

˜Pt(s, s ′) = (P ⋆t ψ)( s)

ψ(s) = 1 − γ δ s,s r

ψ(s) .

Hence ˜Pt is stochastic on S \ { sr } and has row-sum defect γ/ψ (sr ) at sr, which is the standard Doob absorption at the target site [69, Sec. 7]. 

Telescoping and importance sampling. For any P ⋆t -positive path (X0, . . . , X k),

d˜P

dP

(X0, . . . , X k) =

> k−1

Y

> j=0

˜Pt(Xj , X j+1 )

P ⋆t (Xj , X j+1 ) =

> k−1

Y

> j=0

ψ(Xj+1 )

ψ(Xj ) = ψ(Xk)

ψ(X0) .

The initial factor ψ(X0)−1 is the exact importance weight for the path-measure change. Since 

ψ(s) = GP ⋆t (s, s r ) is the discounted occupation mass at sr, ranking by ψ(s) concentrates backups on the states with maximal ˜Pt-path weight toward the reward-shift site. 21 Proof for Proposition 5.3 

Proof. (i). At episode start after a single-site reward shift, 

ξt = ∆ r esr .

By Cor. 2, 

τt = γ(I − γP t)−1ξt = γ∆r

> ∞

X

> k=0

(γP t)kesr .

Evaluating the s-th component gives 

τt(s) = γ∆r

> ∞

X

> k=0

γk[P kt ]s,s r .

Every term in this series is nonnegative. Because Pt is finite and irreducible, for every pair (s, s r )

there exists some k = k(s, s r ) such that [P kt ]s,s r > 0. Therefore at least one term in the series is strictly positive, and hence 

τt(s) > 0 for every s ∈ S .

This proves dense support. 

(ii). Fix s ∈ B K , by single-site reward shift assumption, for s ∈ B K

h∗(s; sr ) > K. 

By definition of the backward predecessor distance, there is no path 

s = s0, s 1, . . . , s ℓ = sr

with ℓ ≤ K and Pt(si+1 , s i) > 0 for all i. Reactive PS propagates nonzero priority from the shifted reward state sr only through predecessor pushes, one predecessor edge per backup. Therefore after at most K reactive backups, the residual wavefront can reach only states at backward predecessor distance at most K from sr . Since s lies outside that K-step predecessor set, its reactive PS priority remains zero: 

pPS (s) = 0 .

By part (i), the GTA-PS topology signal still satisfies τt(s) > 0. Hence GTA-PS assigns nonzero topology priority to every blocked state while reactive PS does not. This proves part (ii). 

Remark 1 (Blocked-set non-emptiness) . Under Assumption 4a, define the backward directed eccen-tricity of the shifted reward state by 

D∗(sr ) ≜ max  

> s∈S

h∗(s; sr ).

Then the blocked set BK = {s : h∗(s; sr ) > K } is non-empty whenever K < D ∗(sr ). In bottlenecked environments, D∗(sr ) is large relative to the backup budget, so blocked states exist for the budgets where GTA-PS is intended to help. For periodic chains, the same structural claim still holds because it depends only on the support graph. Independently, the regularized resolvent remains well-defined because η⋆ = (1 − γ)/γ > 0.

Proof for Lemma 5.3.1 and Proposition 5.4 

Proof. We separate the argument into the fixed-kernel ergodic average and the drift perturbation. 

Step 1: fixed-kernel ergodic average. For a fixed stochastic kernel P , Assumption 3 makes the reward-shift descriptor Zt = ( sr,t , ∆r,t ), and hence ζt = ∆ r,t esr,t , stationary-ergodic. 

ζ 7 −→ adv t(P, ζ )

is measurable and integrable, then Birkhoff’s ergodic theorem gives 

1

N

> N

X

> t=1

adv t(P, ζ t) a.s.

−−−−−→ 

> N→∞

¯A(P ) ≜ EμR [adv t(P, ζ )] .

22 Because the state space is finite and τ (P, ζ ) ≜ γ(I − γP )−1ζ is obtained by a bounded linear map applied to the bounded random vector ζ, the functional adv( P, ζ ) is measurable and integrable. The positive-frequency assumption |B sel ,t K | ≥ 1 implies that 

μR

 adv t(P, ζ t) > 0 > 0

under the frozen-kernel process. Since each summand in the definition of adv is nonnegative, and 

E[X] ≥ E[X 1X> 0] > 0 whenever X ≥ 0 and P (X > 0) > 0, it follows that 

¯A(P ) > 0.

Step 2: decomposition around a reference kernel. By Assumption 4, there exists a frozen reference kernel ¯P such that ∥Pt − ¯P ∥∞ ≤ εpol for all t. Then 

adv t(Pt, ζ t) = adv t( ¯P , ζ t) + 



adv t(Pt, ζ t) − adv t( ¯P , ζ t)



.

Summing over t and dividing by N yields 

1

N

> N

X

> t=1

adv t(Pt, ζ t) = 1

N

> N

X

> t=1

adv t( ¯P , ζ t) + 1

N

> N

X

> t=1



adv t(Pt, ζ t) − adv t( ¯P , ζ t)



.

The first term converges almost surely to ¯A( ¯P ) by Step 1; write the convergence error as eN ≜

> 1
> N

P 

> t

adv t( ¯P , ζ t) − ¯A( ¯P ) = o(1) a.s. 

Step 3: drift error bound. For two kernels P, P ′, the resolvent identity gives 

(I − γP )−1 − (I − γP ′)−1 = ( I − γP )−1 γ(P − P ′) ( I − γP ′)−1,

and therefore, recalling τ (P, ζ ) = γ(I − γP )−1ζ,

τ (P, ζ ) − τ (P ′, ζ ) = γ2(I − γP )−1(P − P ′)( I − γP ′)−1ζ. 

Since ∥P ∥∞ = 1 implies ∥(I − γP )−1∥∞ ≤ 1/(1 − γ) via the Neumann series, 

∥τ (P, ζ ) − τ (P ′, ζ )∥∞ ≤ γ2

(1 − γ)2 ∥P − P ′∥∞ ∥ζ∥∞ .

The order-statistic map x 7 → x(q+1) is 1-Lipschitz in ∥·∥ ∞ (since |x(k) − y(k)| ≤ ∥ x − y∥∞ for any rank k, by a standard sorting argument), so 

τ(q+1) (P, ζ ) − τ(q+1) (P ′, ζ ) ≤ ∥ τ (P, ζ ) − τ (P ′, ζ )∥∞ .

Because x 7 → (x)+ is also 1-Lipschitz, and recalling adv( P, ζ ) = 1

> γ

P  

> s∈B K(sr)

 τ (P, ζ )( s) −

τ(q+1) (P, ζ )

> +

,

|adv( P, ζ ) − adv( P ′, ζ )|≤ 1

γ

X  

> s∈B K(sr)



|τ (P, ζ )( s) − τ (P ′, ζ )( s)| + τ(q+1) (P, ζ ) − τ(q+1) (P ′, ζ )



≤ 2|B K (sr )|

γ ∥τ (P, ζ ) − τ (P ′, ζ )∥∞

≤ 2|B K (sr )| γ

(1 − γ)2 ∥ζ∥∞ ∥P − P ′∥∞ .

Since ∥ζt∥∞ = ∆ r,t ≤ ∆max (the maximum absolute reward perturbation across states at time t)and |B K (sr )| ≤ |S| , the functional is uniformly Lipschitz in P with constant 

L ≜ 2|S| γ∆max 

(1 − γ)2 .

Therefore 

adv t(Pt, ζ t) − adv t( ¯P , ζ t) ≤ L Pt − ¯P ∞ ≤ Lε pol 

23 throughout the drift envelope. Hence 

1

N

> N

X

> t=1



adv t(Pt, ζ t) − adv t( ¯P , ζ t)



≥ − Lε pol .

Combining with Step 2 gives 

1

N

> N

X

> t=1

adv t(Pt, ζ t) ≥ ¯A( ¯P ) − Lε pol + eN = ¯A( ¯P ) − Lε pol + o(1) a.s. Renaming ¯A( ¯P ) as ¯A yields the displayed conclusion. 

Proof of Theorem 5.1 

Proof. Part (i): per-episode GTA-PS residual. By Cor. 2, 

err GTA  

> t

= τ(q+1) ,t 

γ = Φ (q+1) ,t ≤ ∥ξt∥∞

1 − γ ≤ ∆r,t 

1 − γ .

The first equality is the definition of err GTA  

> t

. The second is the exact-sorter identity. For the first inequality, 

Φt = ( I − γP t)−1ξt =

> ∞

X

> k=0

(γP t)kξt,

so 

∥Φt∥∞ ≤

> ∞

X

> k=0

γk ∥ξt∥∞ = 11 − γ ∥ξt∥∞ .

Since Φ(q+1) ,t is one coordinate of Φt, this implies Φ(q+1) ,t ≤ ∥ ξt∥∞/(1 − γ). At episode start, under the single-shift assumption: ξt = ∆ r,t esr . Hence ∥ξt∥∞ = ∆ r,t and the final inequality follows. 

Part (ii): cumulative advantage reduction. By Prop. 5.3(ii), reactive PS leaves every blocked state 

s∗ ∈ B sel ,t K unresolved after the q = K within-episode backups. By Cor. 2, its discounted future residual there is Φt(s∗) = τt(s∗)/γ . Under the conservative cutoff convention, GTA-PS reduces the unresolved residual on that state to the cutoff level τ(q+1) ,t /γ . Hence each selected blocked state contributes (τt(s∗) − τ(q+1) ,t )/γ to the reactive-PS minus GTA-PS gap, and summing over all such states gives 

TE PS (N ) − TE GTA (N ) ≥

> N

X

> t=1

X

> s∗∈B sel ,t K

τt(s∗) − τ(q+1) ,t 

γ .

The states s∗ ∈ B sel ,t K are precisely those whose τt-value exceeds the (q+1) -th order statistic τ(q+1) ,t ,so each summand is nonnegative and the (·)+ in the definition of adv t is tight; therefore the right-hand side equals PNt=1 adv t(Pt, ζ t). By Prop. 5.4, multiplying its a.s. bound (1 /N ) P 

> t

adv t ≥

¯A − Lε pol + o(1) through by N ,

> N

X

> t=1

adv t(Pt, ζ t) ≥ N ( ¯A − Lε pol ) + o(N ) a.s. This proves the theorem’s displayed lower bound. If in addition Lε pol < ¯A, then the right-hand side tends to +∞, so 

TE PS (N ) − TE GTA (N ) → +∞

almost surely. 24 Appendix B. Algorithms and Implementation Details 

# B.1 Algorithmic settings for MDP observability 

As to aid the integration of GTA-PS onto DP and Dyna, we can not ignore what each of them is built for in the first place. DP can only be chosen when the full oracle distributional setting is exposed. The host agent’s belief cannot exceed that of exact planning model, as every intel: the known transition kernel, the current reward distribution, and the ideal Bellman residual can be traced at ease. The host does not need exploratory action selection onto the MDP to collect data. The one actual committal process – being Policy Improvement – serves as the host step. In the sample-based Dyna setting, because of 3, the host agent’s belief must be empirical: it can only be formed through observed experience, by typically maintaining a learned model plus the current reward estimate. The available information is therefore local and noisy, best managed by one-step TD residuals rather than other more sample consuming ones. The host step is action execution under an exploratory move, as this leads for subsequent learning process. 

Note. In Dyna, the host policy should be an ε-greedy one, so that new experience continues to update the sample model and not get stuck in stale knowledge. 

# B.2 Computational methods 

B.2.1 Construction of Lout t , Lin t .

Given per-action matrices P a and topology policy πPS  

> t

, construct Pt = P 

> a

diag( πPS  

> t

(·, a )) P a in 

O(|S| · |A| · deg) . Then Lout t = I − Pt and Lin t = Din t − P ⊤ 

> t

where Din t = diag( P ⊤ 

> t

1). Both are stored in CSR format. 

B.2.2 Resolvent computation. 

Lout t = I − Pt is singular (null vector 1). Adding ηI shifts all eigenvalues by η, so Re( λk(Lout t +

ηI )) ≥ η > 0 for any ergodic chain. The inverse is thus well-defined without any symmetry requirement. The forward and backward potentials are residual diffusions: 

f out t = ( Lout t + ηI )−1ξt, f in t = ( Lin t + ηI )−1ξt.

For small problems ( |S| ≤ 300 in the reference implementation), the sparse systems are factored once with scipy.sparse.linalg.splu ; repeated resolvent evaluations then reduce to triangular solves through lu.solve(rhs) . This is the path used by the tabular FourRooms and small GARNET experiments. For larger problems, a realistic sparse iterative path is restarted GMRES with an ILU preconditioner [48, 40]. The SciPy path calls spla.spilu with fill_factor=10 and drop_tol=1e-4 , then calls spla.gmres with restart=20 , maxiter=200 , and atol=1e-8 . If ILU fails because of a singular pivot, or if GMRES stalls, the implementation falls back to spla.spsolve or a fresh LU factorization. Reward shifts are especially cheap in the LU regime because the operator is unchanged: if δrt is the new reward perturbation, the update is f out t ← f out t + ( Lout t + ηI )−1δrt and analogously for f in t . A full column cache g(·, s ) would reduce repeated reward-shift queries to O(|supp( δr)| · |S| ), but at 

O(|S| 2) memory cost; the reference path instead reuses sparse factors and solves on demand. 

B.2.3 SLEM computation via Arnoldi. 

The SLEM is recomputed from the current topology kernel Pt using the Arnoldi process underly-ing scipy.sparse.linalg.eigs . Each matrix-vector product costs O(supp( Pt)) , so a k-vector Krylov run costs O(k supp( Pt) + k2|S| ). In practice we use k = 15 and pass the dominant eigen-vector from the previous recomputation as a warm start. The non-unit eigenvalue with the largest modulus is taken as the SLEM estimate. Recomputing every Tarn = ⌈p|S|⌉ policy updates is usually sufficient; between those checkpoints, the scheduler reuses the last estimate. 25 Algorithm 2 Resolvent Solve Path  

> 1:

Psp ← sp.csr_array (Pt) 

> 2:

Aout ← sp.eye (n, format="csr" ) · (1 + η) − Psp  

> 3:

Ain ← in_laplacian (Pt) + sp.eye (n, format="csr" ) · η 

> 4:

if n ≤ 300 then  

> 5:

lu_out ← spla.splu (Aout .tocsc() ) 

> 6:

lu_in ← spla.splu (Ain .tocsc() ) 

> 7:

f out t ← lu_out.solve (ξt) 

> 8:

end if  

> 9:

if n > 300 then  

> 10:

ilu ← spla.spilu (Aout .tocsc() , fill_factor = 10 , drop_tol = 10 −4) 

> 11:

M ← spla.LinearOperator (( n, n ), ilu.solve ) 

> 12:

f out  

> t

, info ← spla.gmres (Aout , ξ t, M, restart = 20 , maxiter = 200 , atol = 10 −4) 

> 13:

if info ̸ = 0 then  

> 14:

f out  

> t

← spla.spsolve (Aout .tocsc() , ξ t) 

> 15:

end if  

> 16:

end if  

> 17:

f in t is solved analogously with Ain 

Algorithm 3 SLEM Update  

> 1:

Psp ← sp.csr_array (Pt); Psp .eliminate_zeros()  

> 2:

λ, V ← spla.eigs (Psp , k = 16 , which="LM" , v 0 = vprev ) 

> 3:

mi ← | λi| for all returned eigenvalues  

> 4:

discard the eigenvalue with |λi − 1| ≤ 10 −4 

> 5:

λ2 ← arg max i mi over the remaining set  

> 6:

SLEM( Pt) ← | λ2| 

> 7:

vprev ← Re( V [: , 0]) for the next warm start  

> 8:

recompute only if t mod Tarn = 0 

B.2.4 Topology signal assembly. 

Once ξt is available, the computational chain is: solve the forward and backward resolvents, mix them with the fixed coefficient α, filter the dense signal to a top-qt frontier, and rekey the residual queue with the aggregate topology score. In the exact-model regime ξt(s) = ( T Vt)( s) − Vt(s); in sample-based Dyna this can be replaced by the most recent TD(0) error. The reference implementation materializes the dense frontier with np.argpartition and keeps the active residual and topology-only candidates in separate heaps ( heapq.heapify ), which avoids sorting all states at every backup. 

Algorithm 4 Topology Assembly  

> 1:

ξt(s) ← (T Vt)( s) − Vt(s) 

> 2:

f out  

> t

← solve_out (Pt, ξ t)) ; f in  

> t

← solve_in (Pt, ξ t) 

> 3:

τ t ← (1 − α)|f out  

> t

| + α|f in  

> t

| 

> 4:

βt ← β or β1((1 − SLEM( Pt)) + β2) 

> 5:

Iq ← np.argpartition (τ t, n − qt)[ n − qt :]  

> 6:

Qξ ← heapq.heapify {(s, |ξt(s)| + βtτt(s)) : |ξt(s)| > 0} 

> 7:

Hq ← heapq.heapify {(s, β tτt(s)) : s ∈ Iq , }

B.2.5 Two-queue priority with top-q filtering. 

Because τ t is dense, inserting it directly into the residual heap would crowd the queue. The implementation uses two persistent structures: Qξ over the active residual set and Qτ over all states keyed by τt(s). After each R EFRESH TAU , a filtered frontier Fq = TOP Q(Qτ , q t) is materialized (via argpartition, O(|S| )) and stored as a small heap Hq keyed by the aggregate score ξ[s] + βtτ [s].The backup candidate set is Ct = Keys( Qξ ) ∪ F q , with selection by arg max s∈C t {| ξt(s)| + βtτt(s)}.Setting qt = |S| recovers the unfiltered algorithm. 26 B.2.6 Computation chain. 

The safest way to read the deployed module is as a directed acyclic computation graph. At a fixed host step, the topology-policy state determines the induced topology kernel and its two linear systems: 

πPS  

> t

−→ Pt −→ (Lout t , L in t ).

From that point the computation splits into three downstream branches. The spectral branch is 

Pt −→ SLEM( Pt) −→ βt.

The dense topology branch is 

(ξt, L out t , L in t ) −→ (f out t , f in t ) −→ τ t −→ Q τ −→ F q −→ H q .

The residual branch is simply 

ξt −→ Q ξ .

These branches meet only at the comparison stage through the score |ξt(s)| + βtτt(s) used by POP AND PRIORITIZE to compare the head of Qξ against the head of Hq . In particular, βt does not enter the resolvent solves, τ t does not enter Arnoldi, and top-qt filtering is applied only after the dense topology field has already been formed. 

B.2.7 Per-step cost summary. 

Procedure Cost GTA POLICY UPDATE O(|A| ) per backed-up state TOPOLOGY UPDATE (per policy step) O(|S| ) (LU tri-solves + SM) REFRESH TAU (per shift / lazy) O(|S| + qt)

REWARD UPDATE (per shift) O(|S| + qt)

POP AND PRIORITIZE (per backup) O(log |S| + log qt)

PUSH PREDECESSORS (per backup) O(|Pred( s)| log |S| )

Arnoldi (every Tarn ) O(j|S| · deg + j2|S| ), j = 15 

Dense refreshes (R EFRESH TAU , Arnoldi) are triggered only at reward shifts and lazily every Tτ = |S| 

backups; the amortized incremental cost per plan step is O(|S| ) for the LU tri-solves and O(|A| · deg) 

for the host Bellman backup. 

# B.3 The unified Prioritized Sweeping interface 

We should view any priority-based system as a database for the host. The host agent gains complete control over when entries are pushed, queried, refreshed, and removed during planning. The priority module should exposes a small set of stateful operations that the host can invoke inside DP or Dyna-style procedures, while preserving the same residual and topology bookkeeping across all variants. This makes the interface reusable across different replanning schedules, reward-update patterns, transition models and stopping rules. We do not specify how this should be implemented programmatically. However you can see the GTA-PS + DP/Dyna pseudocode in E.1 for the concrete host-side call sequence. 

# B.4 The GTA-PS module 

B.4.1 Residual gating mechanism 

GTA-PS precautiously mix topology as an ordering correction specifically over the residual support. At each planning step, define the residual-active set 

It ≜ {s ∈ S : |ξt(s)| > 0}.

If It̸ = ∅, the next backup state is selected only from It:

st ∈ arg max 

> s∈I t

{| ξt(s)| + βtτt(s)} .

27 and if not, exploratory action is triggered to collect more data. Thus the topology term βtτt(s) can reorder states that already have positive Bellman residual, but it cannot select a topology-only state while residual-active states exist. This residual gate gives the anti-starvation arguments [10, 64], needed for the asynchronous con-traction proof. Therefore GTA-PS preserves the standard infinite-residual visit condition used by asynchronous value-iteration convergence arguments. 

B.4.2 Detailed pseudocode 

The GTA-PS module exposes five procedures that any model-based host algorithm can call. The module maintains: (i) the Bellman residual array ξ[s] and a residual heap Qξ over the active set It = {s : ξ[s] > 0};

It = {s : ξ[s] > 0} B.4.1; (ii) the topology array τ [s], a dense heap Qτ over all states, and a filtered frontier Fq =

TOP Q(Qτ , q t) as a small heap Hq B.2.5; (iii) topology state (f out , f in , SLEM , v 2, u 2);(iv) the topology policy πPS  

> t

(Boltzmann over host advantages, temperature b). The host algorithm (DP or Dyna-Q) calls P OP AND PRIORITIZE to select the next state for a Bellman backup, then calls P USH PREDECESSORS to propagate updated residuals to predecessor states. At each backed-up state the host also calls G TA POLICY UPDATE (to refresh the temperature-softened Boltzmann policy row) followed by T OPOLOGY UPDATE (to maintain f out , SLEM, and periodically 

f in via the Sherman–Morrison and Arnoldi incremental machinery). After a reward shift the host calls R EWARD UPDATE to diffuse the new residual field through both resolvents and rebuild priority queues. 

# B.5 Complexity analysis. 

The complexity story splits cleanly into a local per-backup cost and three optional amortized cor-rections: a standalone dense τ refresh, a dense topology-operator rebuild, and an Arnoldi/SLEM sync. The local cost is paid on every host backup and comes from P OP AND PRIORITIZE , the Bell-man or TD backup itself, P USH PREDECESSORS , and, in the online Sherman–Morrison regime, the row-local T OPOLOGY UPDATE . The amortized terms are paid only when one of the dense refresh or spectral-sync events is triggered. For bookkeeping, write 

Cτ = O Cpot + |S| + qt + |I t|

for one R EFRESH TAU call: the resolvent solves, dense mixing, top-qt extraction, and queue re-build/rekey. Let 

Carn = O j|S| · deg + j2|S| 

for one Arnoldi recomputation with Krylov depth j, let Ctopo denote the cost of one dense policy-dependent topology rebuild after batching row changes, and let 

Cshift = O Cres + Cτ



for one reward shift, since a shift recomputes the host residual field and then refreshes τ but does not change Pt or require a fresh Arnoldi pass. If a particular implementation couples some of these events, the corresponding costs simply merge; the split here is only to keep the accounting explicit. The relevant granularity for the host planner is therefore one plan step or one backed-up state. For DP, the exact per-backup cost with GTA-PS is 

CDP (s) = O



|A| · deg + |Pred( s)| log |S| + log |S| + log qt + |S| 



,

where the O(|A| ) row-softmax term is absorbed. Relative to residual-only PS, the extra GTA-PS overhead per DP plan step is therefore tightly bounded by O(log qt + |S| ). For one Dyna simulated planning backup, the same expression holds with the Bellman-style term replaced by the TD-style host cost. 28 Algorithm 5 GTA-PS Module 

Precompute (once, O(|S| 3))

1: P PS 0 ← P 

> a

diag( πPS 0 (·, a )) P a

2: Lout  

> 0

← I − P PS 0 ; Lin  

> 0

← Din  

> 0

− (P PS 0 )⊤

3: (Lout  

> 0

+ ηI )−1 via LU ▷ cached for all subsequent tri-solves 

4: (v2, u 2, SLEM) ← ARNOLDI (P PS 0 , (P PS 0 )⊤), j = 15 

5: β0 ← β1 · (1 − SLEM + β2); choose q0 ∈ [K, |S| ]

6: for each s ∈ S : ξ[s] ← | (T V0)( s) − V0(s)|

7: REFRESH TAU () ▷ initial τ = Gξ ; builds all queues 

8: procedure GTA POLICY UPDATE (s, A PS (s, ·)) ▷ O (|A| )

9: πPS (s, ·) ← softmax( b A PS (s, ·)) 

10: end procedure 

11: procedure TOPOLOGY UPDATE (supd , δπ PS )

12: w ← P 

> a

δπ PS (a) p(·| supd , a ); δP PS ← esupd w⊤ ▷ rank-1, O(deg) 

13: u ← (Lout t + ηI )−1w; v ← (Lout t + ηI )−⊤ esupd ▷ tri-solves: O(|S| )

14: f out += v⊤f out  

> 1−v⊤w

u ▷ SM exact update, O(|S| )

15: δλ 2 ← u2(supd )( w⊤v2)/(u⊤ 

> 2

v2); SLEM += Re( ¯λ2/|λ2| · δλ 2) ▷ O (deg) 

16: if t mod Tarn = 0 then ▷ T arn = ⌈|S| 1/2⌉

17: (v2, u 2, SLEM) ← ARNOLDI (P PS  

> t

, (P PS  

> t

)⊤)

18: βt ← β1(1 − SLEM + β2)

19: f in ← (Lin t + ηI )−1ξ ▷ batched rebuild 

20: REFRESH TAU () 

21: end if 

22: end procedure 

23: procedure REFRESH TAU ▷ O (|S| ) two tri-solves; O(|S| + qt) queue rebuild 

24: f out ← (Lout t + ηI )−1|ξ|; f in ← (Lin t + ηI )−1ξ

25: for each s ∈ S : τ [s] ← (1 − α)|f out [s]| + α |f in [s]|

26: Rebuild Qτ from {(s, τ [s]) }s∈S 

27: Fq ← TOP Q(Qτ , q t); build Hq from {(s, ξ [s] + βtτ [s]) }s∈F q

28: Rebuild Qξ from {(s, ξ [s] + βtτ [s]) }s:ξ[s]>0

29: end procedure 

30: procedure REWARD UPDATE (δr) ▷ called after each reward shift 

31: r += δr; f out += ( Lout t + ηI )−1δr; f in += ( Lin t + ηI )−1δr

32: for each s ∈ S : ξ[s] ← | (T Vt)( s) − Vt(s)|

33: REFRESH TAU () 

34: end procedure 

35: procedure POP AND PRIORITIZE ▷ O (log |S| + log qt)

36: Let sξ , sτ be heads of Qξ , Hq (if nonempty) 

37: return whichever has larger ξ[s] + βtτ [s], removing it from all queues 

38: end procedure 

39: procedure PUSH PREDECESSORS (s, ∆V ) ▷ O (|Pred( s)| log |S| )

40: for s′ ∈ Pred( s) do 

41: ξ[s′] ← | (T V )( s′) − V (s′)|; Qξ .UPDATE (s′, ξ [s′] + βtτ [s′]) 

42: end for 

43: if k mod Tτ = 0 then REFRESH TAU () 

44: end if ▷ T τ = |S| 

45: end procedure 

The dense τ refresh, dense topology rebuild, Arnoldi sync, and reward-update terms are secondary amortized corrections to this dominant local plan-step term. If standalone τ refreshes are triggered every Tτ host backups, dense topology rebuilds every Ttopo host backups, Arnoldi is rerun every 

Tarn host backups, and reward shifts arrive with characteristic scale T , then the full per-step DP cost 29 admits the upper bound 

Cfull DP (s) = CDP (s) + O

 Cτ

Tτ

+ Ctopo 

Ttopo 

+ Carn 

Tarn 

+ Cshift 

T



.

For a Dyna host agent, one real environment interaction is followed by K simulated planning backups. The base per-interaction cost, excluding periodic sync overhead, is 

CDyna = O



|A| + K · |Pred( s)| log |S| + log |S| + log qt + |S| 

,

where the O(|A| ) term is the real-step TD update and model learning, and the K factor scales the local plan-step overhead. To this base cost, each interaction adds its share of periodic work, proportional to the K + 1 host steps it contributes. Contributing the maintaining overhead makes the realized cost on each host step become: 

Cfull Dyna = CDyna + O



(K + 1) 

h Cτ 

> Tτ

+ Ctopo  

> Ttopo

+ Carn 

> Tarn

i

+ Cshift 

> T



.

The first three terms scale with K because both the real step and all K simulated backups advance the host-step counters for τ refresh, topology rebuild, and Arnoldi cadence. The reward-shift term is independent of K since shifts arrive relatively to the host step. The checked-in implementation uses the heuristic estimate Tarn = ⌈p|S|⌉ and Ttopo = Tτ = |S| .They should not be read as asymptotic complexity optima or grounded computational error handler. 30 Appendix C. Intuitions, Design Decisions, Limitations and Future Directions 

# C.1 Key decisions and their rationale 

Why nonstationary reward is the chosen setting With stationary reward, standard PS runs to convergence. At convergence ξt → 0, so f out t = ( Lout t + ηI )−1ξt → 0 and the topology signal vanishes identically (asymptotic-zero property). For deterministic shortest-path problems, Dijkstra’s algorithm computes the optimal policy in O(|S| log |S| + |E|) – strictly superior. Though GTA-PS can excel in both settings, the contribution are not enough to compensate both the computational cost and the enormous potential of the new queue system. Nonstationary reward with bottlenecks is the setting where GTA-PS dominates. When rt shifts at frequency 1/T , the planner has T steps before the next shift. A full PS sweep costs O(|S| 2|A| ); under budget T ≪ |S| 2|A| , the Bellman wavefront has not reached all states and the reactive residual signal is zero at predecessor-blocked states. GTA-PS’s topology signal is nonzero at all states immediately after a reward shift (Prop. 5.3 in the main text), so it can prioritize predecessor-blocked states before the wavefront arrives. The sub-O(|S| 2) multiplier ensures the economy, while the empirical efficiency outperform standard PS in numerous competing domains. 

Why the resolvent, among the diffuser family? There are numerous choices for a diffuser operator, such as k-truncated smoothing (I −ηL )k, heat diffusion exp( −τ L ), the Laplacian pseudoinverse, and the regularized resolvent. GTA-PS uses the resolvent because it is both controllable and economical. • Truncated smoothing has a hard finite horizon, heat diffusion is costly to update incremen-tally, and the pseudoinverse lacks explicit horizon control while depending on the Laplacian nullspace. In contrast, the resolvent (L + ηI )−1 has η naturally acts as a geometric discount-ing factor. • Resolvent has a concise, exact localized update formula in B.2, which we believe would be nontrivial for heat diffusion or pseudoinverse to possess. 

Why low-rank update assumption? A topology-policy change at state supd induces a row-local perturbation δP t = esupd w⊤. This is why low-rank resolvent identities are natural in the analysis: it adhere to learning process being local rather than global. However, this does not restrict the reward non-station capacity, as multiple localized shift can be batched in a vector shift facing host step. 

Relative to competing topology-aware works. The same design choice also clarifies where GTA-PS differs from adjacent methods. Relative to standard PS and VaST [20], GTA-PS is proactive: the resolvent field is dense immediately after a reward shift, so distant states can enter the queue before the Bellman wavefront reaches them. Relative to TVI/FTVI [21], GTA-PS uses a soft, interruptible topology signal rather than an SCC batch schedule; it can be stopped after any backup and re-used after a reward shift without recomputing a decomposition. Relative to the successor representation [22], reward updates remain cheap while the topology operator is applied through sparse linear solves rather than through storage of a full dense successor matrix. 

Two-queue architecture with top-q frontier. Because τ t = Gξ t is dense (nonzero everywhere when ξt̸ = 0), inserting it directly into the residual heap would crowd the queue with low-priority states, adding an excessive O(|S| cost multiplier. The simple solution is to limit the queue capacity by q. Additionally, the implementation uses two heaps: Qξ over the active residual set and Hq over a filtered top-q frontier extracted from the dense Qτ . This decouples the two signals : the residual heap guarantees that states with large Bellman error remain eligible at all times, while the topology frontier injects proactive candidates without flooding the heap. Setting qt = |S| recovers the unfiltered version analyzed theoretically. 

Budget priority: residual-backups first. In P OP AND PRIORITIZE , residual-reducing backups are mandatory for convergence, while topology-only backups (states with ξ = 0 but τ > 0) are exploratory. Under tight budget, the implementation ensures that Hq never starves Qξ . This prevents the topology signal from consuming budget on no-op Bellman backups at already-converged states. 31 LU factorization threshold. For small state spaces, sparse LU factorization provides exact triangu-lar solves at O(|S| ) each and is the default path in the tabular experiments. In the current code the threshold is explicit: _LU_THRESHOLD = 300 . Above that threshold the module switches to GMRES with a sparse-direct fallback. The exact crossover depends on sparsity structure, but the checked-in implementation address this cutoff cleanly. 

# C.2 Failed approaches 

Column cache abandoned. An earlier design maintained an explicit O(|S| 2) column cache gs =

Gout η,t (·, s ) (the s-column of the Green’s operator) to accelerate incremental updates. This was abandoned in favour of the Sherman–Morrison dual tri-solve approach: the column cache introduces 

O(|S| 2) memory and requires maintenance of O(|S| ) columns as the policy evolves, while the dual-solve approach achieves the same update expressiveness at O(|S| ) per step without any persistent column cache. 

Stale-column incremental strategy. An intermediate design proposed maintaining stale approxi-mate columns with periodic refresh, balancing O(|S| ) local cost against O(|S| 2) full recomputation. This was deemed unnecessary: the Sherman–Morrison formula provides exact updates at the same 

O(|S| ) cost without accumulation of refresh lag. 

Full recomputation of in-field per policy step. The in-field perturbation δL in t = diag( w) −

w e ⊤ 

> supd

is not rank-1, so Sherman–Morrison does not apply. An early implementation attempted to track f in incrementally through a generalised rank-(d + 1) update; the fill-in in the update formula made this costlier than periodic batch recomputation. The settled design rebuilds f in in batch at Arnoldi sync points, accepting O(|S| ) periodic cost amortised over Tarn steps. 

Forced Arnoldi on every reward shift. An early version triggered a full SLEM recomputation (Arnoldi) at every reward shift. This was wasteful: the transition matrix Pt does not change at a reward shift (only rt changes), so SLEM( Pt) and its eigendecomposition remain valid. The current design lets Arnoldi fire on its natural cadence Tarn , independent of reward shifts. 

Forward-only ablation is not uniformly best. The repository keeps a dedicated forward-only comparison by setting α = 0 inside the asymmetry sweeps (RQ4 and RQ6). Those scripts treat it as an ablation, not as the deployed default. This reflects the underlying design lesson: in directed environments, relying only on f out  

> t

can miss upstream source–sink structure that becomes visible once the backward field f in  

> t

is mixed in. The code therefore tunes a fixed α for the full GTA variants rather than collapsing the method to the forward-only special case. 

LU fill-in at large |S| . For very large state spaces, sparse LU factorization fill-in grows beyond memory or incurs prohibitive one-time cost. The GMRES fallback is present precisely for this regime. The checked-in benchmark scripts probe this overhead on FourRooms and on GARNET instances up to |S| = 200 ; future work should push that scaling study further and characterize the LU-to-iterative crossover as a function of transition sparsity rather than only of |S| .

# C.3 Limitations 

The current study is still limited to small-to-medium tabular domains. Although GTA-PS’s mechanism is sound and grounded, its scaling frontier on much larger state spaces is not addressed. Our experiments also focus on localized positive reward shifts. Empirically, the method is not uniformly strong under every perturbation: highly adversarial, drift-heavy, or spark-like transports can distort the topology signal and weaken guidance quality. On the algorithmic side, we validate mainly DP and Dyna-style hosts, leaving broader orthogonal integrations such as Dyna-2 or uncertainty-aware planners. Finally, GTA-PS incurs real wall-clock overhead from topology refreshes and queue maintenance, so the present paper demonstrates superior replanning under fixed backup budgets rather than lower raw compute cost. 32 C.4 Future work and potential improvements 

Fast-shift asymptotics. In the regime ρ = T /t mix (1 /4) ≪ 1 (reward shifts arrive much faster than the chain can mix), it is conjectured that the mean per-episode advantage ¯A grows as 1/ρ . A formal scaling argument for this conjecture, together with empirical validation across the full K/t mix 

ratio range in GARNET, would establish the precise operating regime where GTA-PS’s benefit is maximised relative to its computational cost. 

Integration with learned models. The current theoretical analysis (Sections 5, 7) is confined to the exact-model setting (known p). Extending operator-norm bounds, decision-accuracy guarantees, and the ergodic cumulative advantage theorem to the learned-model case ( ˆpt with estimation error) would cover Dyna-style deployment, where model noise interacts with topology diffusion. The multi-step TD residual oracle (Appendix B.2) provides a practical interface; the formal gap remains. 

SLEM as a continuous signal for other RL components. The SLEM serves here as a scheduling statistic for βt. More broadly, SLEM and the resolvent-based topology potentials could inform exploration bonuses (intrinsic rewards proportional to τt(s) for under-visited states), model-learning budgets (allocating more environment interactions when SLEM detects bottleneck formation), or curriculum generation (reward-shift scheduling that targets the low-conductance regime where GTA-PS’s advantage is largest). 

GPU acceleration and batching. Dense vector updates, sparse matrix-vector products, and Arnoldi iterations admit batching and GPU execution. 

Unified design for the topology policy. The Boltzmann policy with fixed temperature b is a prag-matic baseline that ensures ergodicity. Alternative topology-policy designs – such as learned attention policies that minimise SLEM, or topology policies designed to align with specific competitive base-lines – could replace the softmax heuristic. The only invariant requirement is that the induced chain remains communicating with infinite-visit exploration. 33 Appendix D. Environments, Hyperparameters, and Experimental Protocol 

# D.1 Environment setup 

We use two environments that provide direct control over the relevant MDP characteristics. 

FourRooms [16] The tabular class FourRoomsTabular represents four rooms separated by walls and linked through one-cell corridors. Its default action set has size |A| = 4 , and the state space ranges from 20 to 120 passable cells. Most checked-in configurations use k ∈ { 1, 4}

active rooms; a separate k = 2 setting supports fine-grained time-series measurements. The 

drift and one_way_prob arguments control transport asymmetry. 

GARNET [4]. The synthetic benchmark is the tabular class GarnetEnv . The checked-in configs use 

(|S| , |A| ) = (75 , 4) and (200 , 5) , with branching factor 3, neighbor-rate n_nbr_rate con-trolling locality, and optional dedicated reversible , bipartite , and reward_locality 

switches. Several experiment scripts also instantiate ad hoc GARNET settings with 

|S| ∈ { 75 , 200 }. This is the main environment for controlled sweeps over mixing rate, reversibility, reward support, and solver overhead. We construct various custom attributes on top of these base environments, such as mixing rate, reward span, reversibility factor, branching factor, and planning-budget effects under controlled nonstationarity. 

# D.2 Experiment Configurations 

All reported curves are aggregated over independent random seeds. The main orchestrator script suggest running the experiment on at least 1000 seeds and ≥ 10 logical processors. Confidence bands are computed as 95% Student-t intervals over the seed means. Hyperparameter tuning uses the standalone Optuna driver [2]. Our tuning trial uses 200 trials with 

seed_offset =100000 so that tuning seeds stay disjoint from the evaluation seeds, therefore avoid inducing selection bias. For the deployed GTA-PS module, the numerical linear-solver tolerance is fixed across experiments: exact sparse LU is used for |S| ≤ 300 , and otherwise the code switches to GMRES with restart =30 , maxiter = 500 , and absolute tolerance atol =10 −4, falling back to spsolve if GMRES returns nonzero status. For the Dyna host agent, an exploration rate ϵ = 0 .2 is maintained to escape stale local loops, where the agent sample the current greedy action with probability 1 − ϵ and samples a random admissible action with probability ϵ.

# D.3 Reward protocol 

All environments use state-based rewards, but the reward pattern are designed to differ across setting purposes. 

FourRooms. The environment samples a Gaussian-mixture reward field instead of a single-cell goal. It chooses k active rooms and places several Gaussian bumps in each selected room. The parameter n_peaks_per_room sets the number of bumps, and room_peak_ranges sets their room-specific heights. The final reward vector is normalized to unit maximum. Each reward shift resamples the field. 

GARNET. The default reward generator is sparse over reward_support states with amplitudes drawn uniformly from [0 .5, 1.5] . When reward_locality=True , those rewarded states are drawn as a BFS-local connected cluster from the union graph of the transition tensor rather than as independent global samples. 34 D.4 Hyperparameters Tuning and Selection 

The repository includes a standalone Optuna tuner [2]. The selected configuration minimizes the mean L∞ value error to the exact DP solution over 30 tuning seeds, 4 reward shifts, K = 20 planning backups, and 50 host steps per shift. The exact hyperparameter search interval and tuned values are in the supplement code. 

# D.5 Baselines and fair-comparison controls 

The checked-in experiment harness instantiates three method variants: • standard_ps : the Moore-style prioritized-sweeping baseline [42], using Bellman residual magnitude as the queue key. • gta_base : GTA-PS with fixed α and fixed β.• gta_beta : GTA-PS with fixed α and adaptive βt.All three variants are implemented through the unified priority-based planning interface B.3. This lets us run them under the exact same host setting, MDP instance, backup budget, and evaluation protocol, with no method-specific entanglement. The differences can only happen in their inner states themselves. 

# D.6 Why no baselines apart from Standard-PS? 

We use Standard-PS as the sole direct baseline because it is the only comparison that matches our assumption set and intervention scope without introducing additional confounds. Similar works discussed in the paper, including TVI [21], VaST [20], Dyna-2 [56], or Generalized Prioritized Solver (GPS) [68] are therefore are not really click as direct baselines. Under the communicating-MDP assumption, TVI effectively reduces to a Standard-PS update schedule. VaST introduces supervised representation learning, which is orthogonal to our fixed-MDP priority-design setting. Dyna-2 and concerns host-agent architecture and model-based decision organization, whereas our study focuses only on the management of sequential value backups inside a given planning interface. Generalized prioritized solvers [68] is the most close candidate, with a priority score of Value-plus-error. However, the issue of stale value-function entries (by nonstationary rewards) misleading the queue is clear logically. In one hand, we have empirically observed that value-function can barely adapts in a reasonable pace E.2. In the other hand, GPS was shown to degrade under environments with a high dose of stochasticity, in which our method holds close performance from standard PS 6. We instead use these methods as major contextual guidances, which aids our grasp in the setting of nonstationary RL. 

# D.7 Computational details and optimizations 

We implements some nontrivial optimizations to further bring the wall-clock performance of GTA-PS down: 

Sparse representation. The topology module stores the policy-induced systems in SciPy sparse form. In GtaPS , Pπ is converted to CSR, near-zero entries below 10 −12 are dropped, and the forward system is assembled as (1 + η)I − Pπ . The in-field system uses in_laplacian(P_pi) with an additional diagonal stabilizer of η + 10 −6 in the initialization and lazy-rebuild path. 

Solver selection in the deployed module. The experiment implementation uses the threshold 

_LU_THRESHOLD = 300 . For |S| ≤ 300 , both forward and backward systems are factored with 

scipy.sparse.linalg.splu and then reused for all subsequent solves. For |S| > 300 , the module calls gmres with restart=30 , maxiter=500 , and atol=1e-8 , with a fallback to spsolve 

if GMRES reports nonzero status. The separate helper functions in utils/spectral.py also expose LU/GMRES resolvent routines and the ARPACK-based SLEM estimator used by the topology layer. 35 Table 2: Empirical wall-clock planning cost, reported as median milliseconds over 120 repetitions. GTA-PS incurs a moderate but stable runtime multiplier from topology refreshes and dual-queue maintenance.                               

> Environment |S| KStandard-PS GTA-base GTA-βOverhead FourRooms 40 10 4.10 9.73 9.31 2.37 ×/2.27 ×
> GARNET 75 18 18.79 48.41 48.85 2.58 ×/2.60 ×
> GARNET 200 50 51.20 112.39 114.00 2.20 ×/2.23 ×

Queue and predecessor maintenance. The module rebuilds the dense top-q frontier with 

np.argpartition , uses Python heapq heaps for both residual and topology queues, and filters predecessor edges at threshold 10 −2 when building the cached predecessor sets. This threshold is used both when the oracle graph is first cached and when empirical model rows are updated online. 

# D.8 Additional experiment results. 

We spend our remaining supporting sweeps to this subsection. Together, Figs. 4–7 provide the broader experimental ground behind the main text. They show how to calibrate topology strength, how tolerable backward mixing is, and in which stochastic or navigational regimes the topology signal is most effective. 36 Figure 4: Figure 4 studies a single reward-shift event from a zero-initialized reward field in two en vironments: GARNET with |S| = 75 and FourRooms with |S| = 40 when varying over calibration weight β. The vertical axis is Spearman correlation ρ between the initial topology ranking and the realized pop order during the first K backups. Larger values indicate that the topology field is better aligned with the backups the planner actually performs. Moderate β gives the strongest agreement between the topology field and the realized backup order, peaking at β = 0 .02 in GARNET and β = 0 .1 in FourRooms. Larger topology gain sharply degrades the ordering quality, which complements the smallness condition in Prop. 5.1. 

Figure 5: Figure 5 is a GARNET design that calibrates over fixed α = αt. The left vertical axis reports top-1 accuracy, namely whether the state ranked first by the topology score matches the oracle state ranked first by the greedy propagation surrogate. The right vertical axis reports how much of the oracle’s high-priority predecessor set is recovered by the top-ranked topology states. The results favor the use of small α, which favor out-degree Laplacian signal, prioritizing proactive backward propagation. 37 Figure 6: Figure 6 reports shift-end L1 value error after repeated replanning under a fixed budget. In the left panel, the GARNET horizontal axis is the reversibility coefficient, where 0 is maximally directed and 1 is exact symmetry (reversible chain); lower values therefore correspond to more strongly directional random dynamics. In the right panel, FourRooms is perturbed by three transport modes: symmetric dynamics, a one-way corridor bias, and a windy drift field. GTA-PS remains competitive in strongly directed or one-way regimes, but the benefit is not monotone in asymmetry: drift-heavy transport can weaken or reverse the gain. 

Figure 7: Figure 7 also measures shift-end L1 value error under the same repeated-shift budgeted replanning protocol. In the left GARNET panel, the horizontal axis is the neighbor-rate parameter 

n_nbr_rate , which controls how globally connected the random transition graph is; moving right-to-left corresponds to increasingly localized connectivity.The right FourRooms panel ablates over the depth ò the bottleneck corridor, measured in grid cells. Lower error therefore means that the planner is coping better with either random sparsification in GARNET or geometric bottlenecking in FourRooms. In corridor-heavy FourRooms layouts, topology-aware prioritization remains substantially stable than Standard-PS, while excels in long-corridor bottenecks. 38 Appendix E. Miscellaneous Supplement 

# E.1 Dyna/DP + GTA-PS pseudocode 

To deeply specifying the algorithms, we provide detailed pseudocodes describing how the host agent interacts with the module in each setting of DP and Dyna B.1 

Algorithm 6 DP + GTA-PS 

Require: APS 0 (s, a ) ← γ P 

> s′

p(s′|s, a ) V0(s′) − max a′ γ P 

> s′

p(s′|s, a ′) V0(s′); module temperature 

b > 0; πPS 0 (a|s) ← softmax( b A PS 0 (s, ·)) , ∀s

1: while not converged do 

2: if REWARD SHIFTED () then 

3: r += δr; REWARD UPDATE (δr)

4: end if 

5: for k = 1 to K do ▷ K Bellman backups available per replanning 

6: s ← POP AND PRIORITIZE () 

7: if s = ∅ then break 

8: end if 

9: ∆V (s) ← BELLMAN BACKUP (s, P, r, V ) ▷ host backup, O(|A| · deg) 

10: UPDATE VALUE (s, V (s)) ▷ sync current host value into the module 

11: APS (s, a ) ← γ P 

> s′

p(s′|s, a ) V (s′) − max a′ γ P 

> s′

p(s′|s, a ′) V (s′) for each a ▷ O (|A| · deg) 

12: NOTIFY BACKUP (s, A PS (s, ·), ∆V (s)) ▷ encapsulates local policy/topology update; no dense queue refresh 

13: PUSH PREDECESSORS (s, ∆V (s)) 

14: end for 

15: end while 

Algorithm 7 Dyna (one-step TD) + GTA-PS 

Require: module temperature b > 0; TD learning rate ηtd ∈ (0 , 1] ; APS 0 (s, a ) ← Q0(s, a ) −

max a′ Q0(s, a ′); πPS 0 (a|s) ← softmax( b A PS 0 (s, ·)) , ∀s

1: while not converged do 

2: (s, a, r, s ′) ← REAL STEP () ▷ interact with environment; a ∼ πt (host policy) 

3: Update ˆp(·| s, a ), ˆr from (s, a, s ′) ▷ model learning; uses empirical model 

4: ξs ← r + γ max a′ Q(s′, a ′) − Q(s, a ); Q(s, a ) += ηtd ξs

5: UPDATE MODEL (ˆ p, ˆr); UPDATE VALUE (s, max a Q(s, a )) 

6: PUSH PREDECESSORS (s, |ξs|) ▷ real step activates local residuals through the module interface 

7: if REWARD SHIFTED () then REWARD UPDATE (δr)

8: end if 

9: for k = 1 to K do ▷ K simulated planning backups available 

10: s ← POP AND PRIORITIZE () 

11: if s = ∅ then break 

12: end if 

13: a ← arg max a′ Q(s, a ′)

14: (rsim , s ′

> sim

) ∼ ˆp(·| s, a )

15: ξs ← rsim + γ max a′ Q(s′

> sim

, a ′) − Q(s, a ); Q(s, a ) += ηtd ξs

16: UPDATE VALUE (s, max a Q(s, a )) 

17: APS (s, a ) ← Q(s, a ) − max a′ Q(s, a ′) for each a

18: NOTIFY BACKUP (s, A PS (s, ·), |ξs|) ▷ encapsulates local policy/topology update; no dense queue refresh 

19: PUSH PREDECESSORS (s, |ξs|)

20: end for 

21: end while 

39 E.2 Additional ablations 

We report a focused subset of the ablation study, in which highlights some influential insights, as additional supplements for the appendix. These ablations isolate short-horizon error tracking, host-level reward accumulation, and the effect of the mixing-time threshold. The remaining ablations, implementation details, and extended results are provided in the code supplement. 

Figure 8: GARNET cumulative tracking ablation. The left panel shows per-episode value error, the middle panel accumulates the tracking error over episodes, and the right panel plots the theorem-facing cumulative advantage. GTA-beta reduces the episode-end error faster than Standard-PS, and the gap widens rather than collapsing. The cumulative tracking error TE (N ) gap increases throughout the series, empirically backs A.3. The advantage curve remains close to linear with a positive slope, matching the intended interpretation of 5.1. This is the cleanest evidence that GTA-PS is doing useful work when reward shifts are frequent and the planner must repeatedly recover from fresh residual fields. 

Figure 9: FourRooms ablation under a Dyna host. The top row reports Standard-PS and the bottom row reports GTA-PS β on the same seeds, budgets, and shift schedule. GTA-PS β achieves higher cumulative reward while simultaneously lowering cumulative ℓ1 error, so the improvement was multi-faceted. The coverage traces remain in a comparable band for both methods, which shows that GTA-PS does not exploit aggressively. In the corridor-heavy FourRooms geometry, this is the right signal to look at because the topology module should help recover from bottlenecked propagation. 40 Figure 10: Threshold test against the K versus tmix claim. Each panel fixes K = 20 , 50 , 100 and varies the corridor width, which changes the mixing scale and therefore the ratio K/t mix . When 

K ≪ tmix , Standard-PS performs significantly worse than GTA-PS. As the ratio increases, the curves cross and the full GTA variant becomes competitive or superior. We can see that GTA-PS performs well over small corridors, empirically backs the intuition of proactiveness. GTA-PS also excels when given more compute, enabling scaling capabilities. 41