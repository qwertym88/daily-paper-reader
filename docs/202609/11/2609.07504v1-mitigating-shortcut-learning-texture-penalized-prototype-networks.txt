Title: Mitigating Shortcut Learning: Texture-Penalized Prototype Networks

URL Source: https://arxiv.org/pdf/2609.07504v1

Published Time: Wed, 09 Sep 2026 02:23:01 GMT

Number of Pages: 6

Markdown Content:
Accepted at the 22nd IEEE International Conference on Advanced Visual and Signal-Based Systems (AVSS 2026). © 2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses. 

# Mitigating Shortcut Learning: Texture-Penalized Prototype Networks 

1st Akshay Anilkumar Girija 

AI Engineering Department Institute for AI Safety and Security 

Ulm, Germany akshay.anilkumargirija@dlr.de ORCID: 0000-0002-4384-9739 

2nd Elena Hoemann 

AI Engineering Department Institute for AI Safety and Security 

Sankt Augustin, Germany elena.hoemann@dlr.de ORCID: 0000-0001-9315-548X 

4th Sven Hallerbach 

AI Engineering Department Institute for AI Safety and Security 

Sankt Augustin, Germany sven.hallerbach@dlr.de ORCID: 0009-0007-9163-0567 

3rd Frank K¨ oster 

Institute for AI Safety and Security 

Braunschweig, Germany frank.koester@dlr.de 

Abstract —Standard Convolutional Neural Networks (CNNs) exhibit severe performance degradation due to a strong inductive texture bias that prioritizes local, high-frequency patterns over global structural shapes. This dependency causes confident mis-classifications during textural changes or environmental effects. To address this flaw, this study introduces the Texture-Penalized Prototype Network (TPPN), a novel architectural framework that shifts this inherent bias without depending on resource-intensive augmented datasets. Specifically, a Texture-Penalization Branch (TPB) imposes a penalty to suppress the extraction of local texture proxies, forcing the network backbone to discard high-frequency cues and extract purified, shape-biased repre-sentations. By evaluating similarities within a prototype-based hypersphere derived from the final convolutional features, the approach enforces strict geometric constraints, treating objects as compositions of essential parts to achieve robust classification. Evaluations on texture-shape cue-conflict datasets and synthetic noise benchmarks demonstrate the stronger shape bias of this structural disentanglement. The proposed framework reduces the inherent texture bias of a baseline ResNet-50 from 55.11 % to 29.73 %, surpassing the texture-suppression capabilities of an off-the-shelf Vision Transformer (ViT-B/16). Furthermore, the approach demonstrates robust generalization under cue-conflict conditions, resisting textural shortcut learning when encountering Out-of-Distribution (OOD) shapes. The model maintains stronger shape accuracy against elevated perturbations. On clean valida-tion data, the architecture incurs a minimal drop in accuracy of 0.90 percentage points. This provides a structural, efficient solution to CNN texture bias. 

Index Terms —Inductive Bias, Representation Learning, Gra-dient Reversal Layer, Feature Disentanglement, Trustworthy AI 

I. I NTRODUCTION 

# IMAGE-BASED recognition is foundational to many critical systems, including medical imaging and industrial anomaly detection [1], [2]. In automated systems, dependable percep-tion is essential to navigate through dynamic environments [3] and safely operate within a specified Operational Design Do-main (ODD) [4]. However, standard CNNs exhibit structural vulnerabilities in dynamic environments. These architectures possess an inductive bias toward texture over true semantic ge-ometry, prioritizing local, high-frequency surface statistics [5], [6]. This inductive “texture bias” causes misclassifications under deceptive textural overlays or environmental shifts, despite un-derlying object geometry. Existing mitigation strategies cause distinct trade-offs. Training on stylized datasets (Stylized-ImageNet (SIN)) forces a shape bias by stripping images of natural local textures, which can inadvertently degrade accuracy on standard, unperturbed distributions [5]. Vision Transformers (ViTs) [7], [8] possess an intrinsic bias toward global shape. However,they impose computational overheads preventing deployment in real-time edge applications. To address these limitations, the Texture-Penalized Proto-type Network (TPPN) shifts the inductive bias of standard CNNs from local texture to global shape without relying on specialized external datasets. The architecture pairs ex-plicit spatial prototype constraints with a Texture-Penalization Branch (TPB) inspired by Domain Adversarial Neural Net-works (DANNs) [9]. Instead of passively augmenting the training data, the framework applies on-the-fly perturbations (e.g., speckle noise, blur, color jitter) to create a secondary 4-category texture proxy task. Utilizing a Gradient Reversal Layer (GRL), the TPB attempts to classify these augmen-tations while penalizing the backbone for extracting high-frequency features needed to recognize them. This active textural suppression reduces the network’s sensitivity to these artificial textures during optimization, compelling deep seman-tic layers to extract robust, shape-biased representations. The primary contributions of this work include: arXiv:2609.07504v1 [cs.CV] 7 Sep 2026 • Texture-Penalization Branch (TPB): A GRL-based mechanism that actively suppresses high-frequency tex-tural proxies during feature extraction.  

> •

TPPN Architecture: A framework that combines textural suppression with spatial prototypes to achieve attention-level shape bias without needing stylized datasets.  

> •

Robustness to Texture Deception: The resulting rep-resentations resist cue-conflict conditions and extreme perturbations while preserving clean baseline accuracy. II. R ELATED WORK 

Although standard CNNs excel on familiar distributions, they often process images as collections of local patches, ex-hibiting a strong inductive bias toward high-frequency textures rather than global geometric shapes [5], [6]. This texture bias leads to “shortcut learning” under deceptive textural overlays. While training on SIN forces a shape bias [5], it is compu-tationally expensive and often degrades standard classification accuracy by disrupting discriminative natural features. ViT ar-chitectures [7] and hierarchical variants [10] use self-attention to globally integrate spatial information, thereby establishing a strong inherent shape bias that resists severe textural shifts [8]. However, these architectures impose computational overheads that often hinder real-time edge deployment. A critical challenge remains: matching the shape bias of Transformers with the efficiency of standard CNNs. While DANN introduced the GRL to penalize domain-discriminative features for unsupervised domain adaptation [9], the proposed framework repurposes it as a textural penalty head. By pre-dicting and penalizing synthetic high-frequency perturbations (e.g., speckle noise, blur), the GRL forces the convolutional backbone to discard superficial textures and rely predomi-nantly on robust geometric shapes. Beyond regularizing the backbone, an interpretable proto-type network structures the feature space by classifying objects based on spatial similarity to learned prototypical parts [11]. Coupling this with the GRL-driven texture penalty encourages the architecture to prioritize shape over texture. III. M ETHODOLOGY 

The TPPN metric-learning architecture decouples geometric shape from surface textures by penalizing high-frequency vi-sual statistics, mapping structural features onto a hypersphere. As illustrated in Fig. 1, the framework operates through a series of sequential mechanisms, detailed in the following subsections. 

A. Deep Semantic Feature Extraction 

Let X ∈ R3×Hin ×Win denote an input image. A pre-trained ResNet-50 backbone is utilized for base feature extrac-tion [12]. To intervene at the level where high-order semantic representations are formed, operations are focused on the deep-est convolutional stage, Layer 4 (l4). The intermediate feature map F4 ∈ RCin ×H×W is processed by a projection head consisting of a 1 × 1 convolution, batch normalization, and a ReLU activation. This yields a dimensionally reduced spatial feature tensor Z4 ∈ Rd×H×W , where d is the bottleneck projection dimension. 

B. Hyperspherical Prototype Matching 

Drawing on part-based prototypical reasoning [11], a learn-able prototype tensor W ∈ R(c×m)×d is maintained, where c

represents the total target shape classes and m represents the prototypes allocated per class to capture intra-class geometric variance. Inspired by angular margin formulations that stabi-lize deep metric boundaries [13], [14], the semantic similarity is decoupled from feature magnitude. Both the spatial features and the prototypes are projected onto a unit L2-hypersphere: 

ˆZ4 = Z4

|| Z4|| 2

, ˆW = W

|| W|| 2

. (1) The raw cosine similarity between the spatial feature maps and the prototypes is calculated as the dot product, yielding 

Sraw = ˆZ4 · ˆW.

C. Dynamic 50 % Spatial Pooling 

Standard global average pooling aggregates the entire spa-tial map, diluting the discriminative geometric signal with irrelevant background pixels and localized spatial noise [15]. To combat this spatial pollution, a dynamic Top-K pooling strategy is introduced, similar to adaptive fractional pool-ing [16], operating on the spatial similarities extracted in Eq. 1. The network applies a descending sort to Sraw across spatial dimensions and averages the top 50 % of the similarity tokens. The token count, K, depends on the input spatial dimensions: 

K = max (1 , ⌊0.5 × H × W ⌋) . (2) The aggregated similarity matrix Spool, raw represents the mean of these top K values, isolating object geometry from back-ground interference. 

D. Feature Purification via Texture Penalization 

To force the network to discard local textures, a TPB connects to the unprojected l4 feature maps (see Fig. 1, center). During training, the pipeline subjects each input image to a stochastic selection of one of four high-frequency syn-thetic texture proxies: clean (no augmentation), Gaussian blur, speckle noise, or color jitter. To process the high-dimensional l4 features, an adaptive average pooling layer reduces the spatial dimensions. A GRL [9] follows the pooling layer. After the GRL, a secondary Multi-Layer Perceptron (MLP) acts as a texture head to predict the applied proxy. During the forward pass, the GRL acts as an identity func-tion. During backpropagation, it multiplies the gradients by a negative scalar ( −λAdv ). The texture head learns to identify the perturbations, while the optimization of the backbone weights θbackbone maximizes the texture prediction error: 

θbackbone ← θbackbone + αλ Adv 

∂LAdv 

∂θ backbone 

, (3) where α represents the learning rate of the backbone, and λAdv 

denotes the gradient reversal penalty scalar. By penalizing the backbone for the extraction of high-frequency signals l4 

Resnet 

Backbone 

l3 

l2 

l1 

Layer 4 

projected 

features 

Texture-Penalized Prototype Network 

> Prototype Latent Space Generation
> ℒ𝐷𝑖𝑣
> ℒ𝐷𝑖𝑣
> Other (class) Prototypes
> Car Prototypes
> Linear
> Classifier

ℒDiv 

ℒPull (Contrastive) 

ℒCE 

> Inference (Cue-Conflict & Robustness Evaluation

l4  Anomaly  Car 

l4 

> * During Training
> ID Features

Stem 

> Contrastive
> Repulsion

Blur Input 

Speckle Noise Input 

Color Jitter Input 

Clean Input 

Image Data 

4-Category Texture Proxy 

(Random Selection of 1) 

Input Texture Proxy data 

1x1 Projection & 

Spatial  Prototype 

Pooling  Adaptive Average Pooling  

> Gradient Reversal Layer (GRL) Texture Head (MLP)
> 4-Category
> Texture
> Prediction

ℒAdv 

Negative Gradients ( −𝜆 Adv )

> Texture-Penalization Branch
> Shape Match
> (Robust) Shape
> Classifier
> Perturbed /Cue -
> Conflict Image
> Input
> Forward Pass via
> Trained
> Backbone
> Texture Match (Biased)

Fig. 1. Overview of the proposed TPPN framework. The network accepts standard image data while applying a singular, random texture proxy augmentation (clean, blur, speckle noise,or color jitter) per image to formulate a 4-category texture training set. During training (top), the architecture splits deep structural features from Layer 4 (l4) into two distinct pathways. In the main classification branch, l4 features are projected onto a shared hypersphere, undergoing spatial prototype matching and pooling to formulate shape-biased predictions guided by Cross-Entropy ( LCE ), Pull ( LPull ), and Diversity ( LDiv ) losses. In parallel, the TPB isolates raw l4 features to predict the applied texture augmentation. A GRL inverts the gradient flow scaled by a penalty factor ( −λAdv )back through layers l2 to l4, imposing a penalty on the backbone for recognizing high-frequency texture cues and encouraging the emergence of highly shape-biased representations. During inference (bottom), the adapted backbone processes cue-conflict or perturbed images. The system utilizes the learned shape classifier to quantify structural robustness via the Shape Match metric, contrasting this against residual shortcut learning, evaluated by Texture Match. 

necessary to identify noise or blur, the bottleneck forces the latent space to discard natural textures and rely on global shapes [5]. 

E. Temperature Scaling and Reasoning Head 

To control the sharpness of the similarity distribution during contrastive learning, a critical dynamic for bounded hyper-spherical embeddings [17], Spool, raw is scaled by a learnable temperature parameter τ . A softplus activation with a config-urable floor ( τfloor ) ensures numerical stability: 

Sscaled = Spool, raw 

Softplus (τ ) + τfloor 

, (4) where τ receives an initialization of 1.0, and τfloor is set to 

0.02 to prevent division by zero. To map the similarities into final class predictions without distorting the underlying cosine geometry, a bias-free linear layer is applied to the raw similarities. Drawing on the principles of imprinted weights [18], connections between a class logit and its assigned prototypes receive an initialization of 1.0, while non-target connections receive an initialization of −0.1. This yields the final shape prediction vector, zshape .

F. The Composite Objective 

Optimization of the framework utilizes a composite loss function, balancing discriminative shape learning against tex-ture suppression: 

LTotal = λCE LCE + λPull LPull 

+ λDiv LDiv + λAdv LAdv . (5) 

Cross-Entropy ( LCE ): The standard classification loss applied to zshape .

Contrastive Pull (LPull ): Adapted from Supervised Con-trastive Learning [19], an Information Noise-Contrastive Esti-mation (InfoNCE) based push-pull dynamic [20] is formulated on the scaled similarities. Using Dynamic Dropout with a mar-gin δ, target prototypes that fall below a similarity threshold are masked to force the utilization of all m prototypes. The loss is calculated against the best valid positive prototype s+best :

LPull = − log exp( s+best )exp( s+best ) + P exp( s−) . (6) 

Diversity ( LDiv ): To prevent feature collapse and ensure broad spatial coverage [21], this loss enforces orthogonality among the m prototypes within the same class: 

LDiv =

> c

X

> k=1

X

> i̸=j

( ˆWk,i · ˆWk,j )2. (7) 

Texture-Penalization Loss ( LAdv ): The cross-entropy loss calculated over the 4-class texture predictions, optimized via the GRL mechanism defined in Eq. 3. IV. E XPERIMENTS 

An empirical testing paradigm is established to evaluate the proposed TPPN. 

A. Datasets and Evaluation Metrics 

Datasets. The framework is evaluated across three distinct configurations to isolate shape and texture dependencies. Im-ageNet dataset [22] (Training): Models are trained on a balanced 10-class ImageNet subset containing 20,000 total images (2,000 images per class). These 10 categories define the In-Distribution (ID) classes: airplane, bear, bicycle, bird, boat, bottle, car, cat, chair, and clock. To generate the 4-category texture proxy dataset, images are augmented during training with four states: clean, Gaussian blur, speckle noise, or color jitter. ImageNet Validation dataset (Testing): A clean set of 1,600 unperturbed ID images (100–200 per class) is utilized to verify that the structural intervention preserves baseline performance on natural distributions. Cue-Conflict Dataset (Testing) [5]: This dataset contains style-transferred images with conflicting shape and texture features. These images present a contradiction between semantic geometry and surface patterns (e.g., the shape of a cat rendered with the texture of an elephant). The geometric shape defines the correct target class. To test vulnerability to texture deception and generalization, six unobserved categories define the OOD classes: dog, elephant, keyboard, knife, oven, and truck. From the initial 1,280 images, 230 images were removed because they share identical shape and texture categories. The filtered dataset contains 1,050 images divided into three subsets: 450 images with ID shapes and ID textures, 300 images with ID shapes and OOD textures, and 300 images with OOD shapes and ID textures. To assess structural robustness, the evaluation applies escalating levels of speckle noise ( η ∈{0 % , 25 % , 50 % , 75 % }) to the filtered cue-conflict images. 

Metrics. Classification performance is assessed via Top-1 and Top-3 accuracies on the clean validation dataset. Inductive bias is quantified on the cue-conflict dataset using three metrics. Shape Accuracy (ID Texture) the percentage of pre-dictions matching the underlying ID shape when paired with a conflicting ID texture. Texture Bias measures the percentage of classifications aligning with the conflicting ID texture instead of the true object shape. Shape Accuracy (OOD Texture) eval-uates shape recognition when the conflicting texture originates from OOD classes. Texture Deception Vulnerability (Fooled by Texture) measures the rate at which the model is fooled by an ID texture overlaid on an OOD shape. 

B. Baselines and Ablation Configurations 

The primary baseline is a standard ResNet-50 trained on the ID classes. A second baseline, ResNet-50 with TPB, evaluates the texture penalization branch in isolation to demonstrate the necessity of prototypical reasoning. Two ImageNet-1K pre-trained models establish external benchmarks: VGG-16 [23] (texture-dependent) and ViT-B/16 [7], [8] (shape-biased). Comparing these generalized, large-scale models against spe-cialized 10-class architectures could introduce a evaluation mismatch. To enable zero-shot evaluation without retraining, model outputs require adaptation. The 1,000 fine-grained Im-ageNet classes are mapped into the 10 broad ID categories, where the highest confidence score within each group serves as the final category prediction. To contrast these external benchmarks, an ablation model (TPPN Warmup checkpoint) evaluates the network with prototypes trained on semantics without TPB influence. 

C. Implementation Details 

Structural features for the proposed TPPN are extracted from Layer 4 of the ResNet-50 backbone. These are com-pressed via a 1 × 1 projection to a dimension of d = 64 , with 

m = 5 learnable prototypes allocated per class. Classification loss weights are set to λCE = 1.0, λPull = 0.5, and 

λDiv = 0 .75 . Dynamic dropout employs a vicinity margin 

δ = 0 .15 with a probability p = 0 .65 . The penalization weight (λAdv ) is scaled via a sigmoid schedule ( 0.0 → 1.0) to prevent early backbone destabilization. Training utilizes Stochastic Gradient Descent (batch size 48, momentum 0.9, weight decay 10 −4) via a two-phase protocol. A 10-epoch Warmup Phase freezes the backbone, optimizing the projection, prototype, and reasoning heads at a learning rate of 10 −3. This initial freezing establishes stable prototypical decision boundaries prior to the introduction of penalization gradients. A 30-epoch Texture Penalization Phase unfreezes the backbone for joint optimization with the texture head via the TPB. Differential learning rates (backbone 10 −4, heads 10 −3) are applied and managed by a ReduceLROnPlateau scheduler with a 5-epoch patience. Early stopping halts training upon the stagnation of a composite metric representing the difference between validation accuracy and texture accuracy. V. R ESULTS AND DISCUSSION 

For reproducibility, TPPN performance metrics on the cue-conflict dataset are reported as the mean and standard deviation across five runs with distinct random seeds. Unless otherwise specified, baselines reflect single-run evaluations, and pre-trained models are evaluated zero-shot. 

A. Performance on ImageNet Validation Dataset 

Table I presents the classification results on the clean ImageNet validation subset. The baseline ResNet-50 achieves a Top-1 accuracy of 99.06 % (Top-3: 99.81 %). The proposed TPPN achieves a Top-1 accuracy of (98.16 ± 0.20) % (Top-3: (99.62 ± 0.10) %). This 0.90 % difference in standard accuracy demonstrates that the TPB forces the network to extract shape features without destroying baseline classification capabilities on natural image distributions.               

> TABLE I CLASSIFICATION ACCURACY ON CLEAN IMAGE NET VALIDATION
> DATASET
> Model Architecture Top-1 Accuracy (%) Top-3 Accuracy (%)
> ResNet-50 (Baseline) 99.06 99.81 TPPN (Final Model) 98.16 ±0.20 99.62 ±0.10

B. Texture-Shape Disentanglement on Cue-Conflict Data 

Initial evaluations on the cue-conflict dataset utilize clean conditions (0.0 % speckle noise) to measure inherent bias without any synthetic high-frequency perturbation.                                              

> TABLE II EVALUATION ON CLEAN CUE -C ONFLICT DATASET (0% S PECKLE NOISE )
> Model Shape Acc Texture Shape Acc Fooled Architecture (ID Tex) % Bias % (OOD Tex) % by Tex %
> VGG-16 (Pretrained) 8.22 76.00 21.00 78.00 ResNet-50 (Baseline) 19.11 55.11 22.00 70.33 ResNet-50 (w/ TPB) 19.11 48.22 22.33 56.67 ViT-B/16 (Pretrained) 33.78 39.78 31.67 54.67 TPPN (w/o TPB) 16.22
> ±0.61 62.76
> ±1.46 23.73
> ±0.57 73.40
> ±1.95
> TPPN (Final) 29.82
> ±0.81 29.73
> ±1.55 30.13
> ±0.62 40.47
> ±2.40

As shown in Table II, the VGG-16 and ResNet-50 baseline demonstrate a dependency on local texture, exhibiting texture biases of 76.00 % and 55.11 %. Under deceptive conditions where the network observes an OOD shape overlaid with a familiar ID texture, the ResNet-50 baseline is “fooled” by the texture 70.33 % of the time. The pre-trained ViT-B/16 achieves the highest ID shape accuracy at 33.78 %, reflecting its architectural shape bias. Adding the TPB to the baseline (ResNet-50 with TPB) reduces the deception failure rate from 70.33 % to 56.67 %. The TPPN ablation without the TPB yields a(73.40 ± 1.95) % deception failure rate, suggesting that prototype layers alone are insufficient to fully mitigate texture dependency without the TPB branch. An analysis of these isolated components establishes a structural synergy. The TPB alone is limited in maximizing shape accuracy (19.11 %), and prototypes alone struggle to adequately suppress texture bias (∼ 73 .40 % failure rate). Therefore, the architecture benefits significantly from the combination of both mechanisms to achieve robust spatial reasoning and texture suppression. By combining the prototype architecture with the TPB, a realignment of the network’s inductive bias is observed. The proposed TPPN (Final Model) reduces the texture bias to (29.73 ± 1.55) % and improves ID shape accuracy from 19.11 % (ResNet-50 baseline) to (29.82 ± 0.81) %. The deception failure rate drops to (40.47 ± 2.40) %, indi-cating that the network resists familiar textures when they do not align with coherent global geometry. Furthermore, when evaluating known ID shapes rendered with unfamiliar OOD textures, the TPPN preserves more of its geometric reasoning, achieving a shape accuracy of (30.13 ± 0.62) % compared to the ResNet-50 baseline’s 22.00 %. VGG-16 (Pretrained) ResNet-50 (Baseline) ResNet-50 (w/ TPB) ViT-B/16 (Pretrained) TPPN (w/o TPB) TPPN (Final) 020 40 60 80 100  

> Texture Prediction Rate (%)  Bias on Known Shapes (ID) Bias on Unknown Shapes (OOD)
> Fig. 2. Texture prediction rates across ID and OOD shapes. When evaluating cue-conflict images with unfamiliar OOD shapes, baseline models increase their dependence on familiar textures. The proposed TPPN exhibits reduced fluctuations between clean and deceptive conditions.

Figure 2 illustrates the stability of each model when evalu-ating OOD shapes. When the ResNet-50 baseline encounters a known texture overlaid on an unknown OOD shape, its dependency on texture increases from 55.11 % to 70.33 %. The ViT-B/16 is susceptible to this texture pull, experiencing a 14.89 percentage points increase in texture bias. The proposed TPPN restricts this increase to 10.74 percentage points (from 29.73 % to 40.47 %), demonstrating a consistent reasoning process across different shape distributions. The ablation model isolates the source of this consis-tency. Without the penalization branch, texture bias reaches (62.76 ± 1.46) % on known shapes and (73.40 ± 1.95) % on unfamiliar geometries. The penalization mechanism substan-tially reduces these dependencies, prioritizing geometry over localized patterns. While the ViT-B/16 achieves the highest shape accuracy on clean ID images (33.78 %), the TPPN on a standard ResNet backbone achieves a lower baseline texture bias of (29.73 ± 1.55) % compared to 39.78 %, and is fooled by deceptive textures at a lower rate of (40.47 ± 2.40) % compared to 54.67 %. This indicates that CNNs, when constrained by high-frequency penalization, can exhibit texture-resilience charac-teristics comparable to, or exceeding, those of attention-based architectures. 

C. Shape Resilience Under Texture Perturbation 

To evaluate shape accuracy (ID texture - ID shape) under texture perturbation, the cue-conflict dataset is subjected to Gaussian speckle noise ( η ∈ { 0 % , 25 % , 50 % , 75 % }). This forces the model to maintain geometric reasoning, as the noise degrades high-frequency textural patterns while leaving spatial geometries intact. As depicted in Figure 3, at moderate perturbation ( η =50 % ), the ResNet-50 baseline yields 40.89 % shape accuracy. 0 25 50 75  

> Speckle Noise Level (%)
> 010 20 30 40 50 60
> Shape Accuracy (%)
> VGG-16 (Pretrained) ResNet-50 (Baseline) ResNet-50 (w/ TPB) ViT-B/16 (Pretrained) TPPN (w/o TPB) TPPN (Final)

Fig. 3. Shape accuracy trajectories under increasing speckle noise. As textures are perturbed, the TPPN relies on global geometry, attaining higher accuracy than baseline architectures at extreme noise levels. 

The ResNet-50 baseline with the TPB improves this baseline to 42.44 %. The proposed TPPN processes the remaining spatial geome-tries to reach a shape accuracy of (47.24 ± 1.06) %. Under extreme perturbation ( η = 75 % ), the TPPN archi-tecture reaches (48.13 ± 0.94) % shape accuracy, surpassing both the ResNet-50 baseline (34.89 %) and the ViT-B/16 (45.78 %). This trajectory provides empirical evidence that penalizing local perturbations via the TPB during training mitigates texture bias under clean conditions and encourages a spatial representation that is more resilient to perturbation. VI. C ONCLUSION AND FUTURE WORK 

This paper introduces the Texture-Penalized Prototype Net-work, a structural approach to overcome the inductive texture bias in CNNs. By utilizing a Texture-Penalization Branch to penalize the recognition of synthetic image perturbations, the network is forced to discard high-frequency textural shortcuts and learn robust geometric shapes. This penalization bottle-neck reduces dependency on texture by half, without requiring expensive stylized datasets. Compared to off-the-shelf ViT baselines, the resulting architecture exhibits stronger texture suppression and demonstrates resilience to severe image per-turbations within the controlled evaluation setting. Future work will explore deeper MLP configurations for the texture head to capture complex noise patterns. Building upon these architectural refinements, scaling the framework to all ImageNet-1K [22] classes will be essential. While the restricted class subset was necessary to strictly isolate shape-texture conflicts, evaluating the full dataset alongside finer-grained hyperparameter ablations will be vital to validate the model’s broad applicability. ACKNOWLEDGMENT 

The authors utilized LLMs strictly for editing and format-ting. All scientific content is entirely original. REFERENCES [1] H. Anthony and K. Kamnitsas, “On the use of mahalanobis distance for out-of-distribution detection with neural networks for medical imaging,” in International workshop on uncertainty for safe utilization of machine learning in medical imaging . Springer, 2023, pp. 136–146. [2] J. B¨ uhler, J. Fehrenbach, L. Steinmann, C. Nauck, and M. Koulakis, “Domain-independent detection of known anomalies,” arXiv preprint arXiv:2407.02910 , 2024. [3] D. Bogdoll, M. Nitsche, and J. M. Z¨ ollner, “Anomaly detection in autonomous driving: A survey,” in Proceedings of the IEEE/CVF Confer-ence on Computer Vision and Pattern Recognition (CVPR) Workshops ,June 2022, pp. 4488–4499. [4] A. Anilkumar Girija, J. M. Christensen, T. Stefani, E. Hoemann, U. Du-rak, F. K¨ oster, S. Hallerbach, and T. Kr¨ uger, “Towards the monitoring of operational design domains using temporal scene analysis in the realm of artificial intelligence in aviation,” in 2024 AIAA DATC/IEEE 43rd Digital Avionics Systems Conference (DASC) . San Diego, CA, USA: IEEE, 09 2024, pp. 1–8. [5] R. Geirhos, P. Rubisch, C. Michaelis, M. Bethge, F. A. Wichmann, and W. Brendel, “Imagenet-trained cnns are biased towards texture; in-creasing shape bias improves accuracy and robustness,” in International conference on learning representations , 2018. [6] W. Brendel and M. Bethge, “Approximating cnns with bag-of-local-features models works surprisingly well on imagenet,” arXiv preprint arXiv:1904.00760 , 2019. [7] A. Dosovitskiy, L. Beyer, A. Kolesnikov, D. Weissenborn, X. Zhai, T. Unterthiner, M. Dehghani, M. Minderer, G. Heigold, S. Gelly et al. ,“An image is worth 16x16 words: Transformers for image recognition at scale,” arXiv preprint arXiv:2010.11929 , 2020. [8] M. M. Naseer, K. Ranasinghe, S. H. Khan, M. Hayat, F. Shahbaz Khan, and M.-H. Yang, “Intriguing properties of vision transformers,” Ad-vances in Neural Information Processing Systems , vol. 34, pp. 23 296– 23 308, 2021. [9] Y. Ganin and V. Lempitsky, “Unsupervised domain adaptation by back-propagation,” in International conference on machine learning . PMLR, 2015, pp. 1180–1189. [10] Z. Liu, Y. Lin, Y. Cao, H. Hu, Y. Wei, Z. Zhang, S. Lin, and B. Guo, “Swin transformer: Hierarchical vision transformer using shifted windows,” in Proceedings of the IEEE/CVF international conference on computer vision , 2021, pp. 10 012–10 022. [11] C. Chen, O. Li, D. Tao, A. Barnett, C. Rudin, and J. K. Su, “This looks like that: deep learning for interpretable image recognition,” Advances in neural information processing systems , vol. 32, 2019. [12] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image recognition,” in Proceedings of the IEEE conference on computer vision and pattern recognition , 2016, pp. 770–778. [13] F. Wang, X. Xiang, J. Cheng, and A. L. Yuille, “Normface: L2 hypersphere embedding for face verification,” in Proceedings of the 25th ACM international conference on Multimedia , 2017, pp. 1041–1049. [14] J. Deng, J. Guo, N. Xue, and S. Zafeiriou, “Arcface: Additive angular margin loss for deep face recognition,” in Proceedings of the IEEE/CVF conference on computer vision and pattern recognition , 2019, pp. 4690– 4699. [15] K. He, G. Gkioxari, P. Doll´ ar, and R. Girshick, “Mask r-cnn,” in 

Proceedings of the IEEE international conference on computer vision ,2017, pp. 2961–2969. [16] B. Graham, “Fractional max-pooling,” arXiv preprint arXiv:1412.6071 ,2014. [17] Z. Wu, Y. Xiong, S. X. Yu, and D. Lin, “Unsupervised feature learning via non-parametric instance discrimination,” in Proceedings of the IEEE conference on computer vision and pattern recognition , 2018, pp. 3733– 3742. [18] H. Qi, M. Brown, and D. G. Lowe, “Low-shot learning with imprinted weights,” in Proceedings of the IEEE conference on computer vision and pattern recognition , 2018, pp. 5822–5830. [19] P. Khosla, P. Teterwak, C. Wang, A. Sarna, Y. Tian, P. Isola, A. Maschinot, C. Liu, and D. Krishnan, “Supervised contrastive learn-ing,” Advances in neural information processing systems , vol. 33, pp. 18 661–18 673, 2020. [20] A. v. d. Oord, Y. Li, and O. Vinyals, “Representation learning with contrastive predictive coding,” arXiv preprint arXiv:1807.03748 , 2018. [21] N. Bansal, X. Chen, and Z. Wang, “Can we gain more from orthogo-nality regularizations in training deep networks?” Advances in Neural Information Processing Systems , vol. 31, 2018. [22] J. Deng, W. Dong, R. Socher, L.-J. Li, K. Li, and L. Fei-Fei, “Imagenet: A large-scale hierarchical image database,” in 2009 IEEE conference on computer vision and pattern recognition . Ieee, 2009, pp. 248–255. [23] K. Simonyan and A. Zisserman, “Very deep convolutional networks for large-scale image recognition,” arXiv preprint arXiv:1409.1556 , 2014.