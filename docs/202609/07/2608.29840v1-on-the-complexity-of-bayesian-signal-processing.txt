Title: On the Complexity of Bayesian Signal Processing

URL Source: https://arxiv.org/pdf/2608.29840v1

Published Time: Tue, 01 Sep 2026 01:28:32 GMT

Number of Pages: 48

Markdown Content:
# On the Complexity of Bayesian Signal Processing 

# Yi Liu ∗

# August 31, 2026 

Abstract 

We develop a computational framework for Bayesian decision-making. We show that as long as no action is optimal in every state, Bayes-optimal choice is intractable. This hardness need not arise from large action, state, or signal spaces, nor from a complicated represented utility function: extracting enough information from a hard-to-interpret signal to act optimally can itself be computationally hard. We also characterize tractability across approximation notions and identify their sources of difficulty. Under the probably approximately correct criterion, sample-based Bayesian learning is tractable if and only if the signal support is bounded. Our results provide justifications for bounded rationality, costly Bayesian inference, and sample-based Bayesian learning. 

Keywords: bounded rationality, Bayesian decision theory, computational complexity, Bayesian learning  

> ∗Department of Economics, Yale University. yi.liu.yl2859@yale.edu

1arXiv:2608.29840v1 [econ.TH] 30 Aug 2026 1 Introduction 

Inference through Bayes’ rule is a standard assumption in Bayesian decision theory. This assumption provides a foundational motivation for the Blackwell-experiment framework, under which every signal-generating process can, without loss of generality, be represented in likelihood-table form. However, implementing this assumption requires decision makers to have infinite computational power. This leaves a natural question: Can a computa-tionally bounded decision maker act optimally, as Bayesian decision theory predicts? In real-world signal-processing problems, the decision maker typically knows how signals are generated but is not given the corresponding likelihood table. An investor may understand how a firm’s underlying condition, accounting records, managerial reporting choices, and auditing procedure produce its final disclosure; a regulator may understand how a drug’s underlying safety profile, trial design, patient outcomes, and laboratory procedures produce a safety-test result; and a user may learn from a platform’s disclosure how profile data, item attributes, and its algorithm produce a recommendation. In each case, the decision maker observes the realized output and understands how it is produced without being given its state-contingent likelihoods. Therefore, signal processing is essentially the computational problem of transforming knowledge of the signal-generating process and the realized signal into a decision. Knowing how a signal is generated does not itself provide the likelihood table required for Bayesian choice. We analyze signal processing by reframing Bayesian decision theory within a compu-tational framework. To isolate the difficulty arising from signal processing, we fix the decision environment: the state space, action space, and utility function are all fixed, with utility determined by the unobserved state and the chosen action. The signal-generating process is represented by a randomized sampler circuit that takes the state and a random tape as inputs and produces a binary-string signal. The decision maker’s choice rule is modeled as a selector algorithm that takes the description of the sampler circuit and the realized signal as inputs and returns an action as output. We adopt the classical view in computer science that a selector is tractable if and only if it always runs in time polynomial in the size of its input. Our analysis covers selectors that always output a Bayes-optimal choice as well as selectors that output an approximately optimal choice. Our 2goal is to determine which notions of optimality–exact Bayes optimality or approximate optimality–admit a tractable selector and to identify where the hardness comes from. Our first result (Theorem 1 and Corollary 1) establishes a sharp dichotomy for exact Bayes-optimal choice. Under a standard complexity assumption, Bayes-optimal choice is tractable for every signal-generating process if and only if one action is optimal in every state. The difficulty comes from two requirements: a posterior can lie arbitrarily close to an action boundary, creating an extreme precision requirement for comparing implicitly represented likelihoods, and the selector must succeed after every supported signal. This hardness already appears with only two supported signals, so it need not be driven by a large signal space. Theorem 2 and Corollary 2 further show that relaxing exactness does not gradually restore tractability when performance must still be guaranteed after every supported signal. There is a sharp threshold: an efficient deterministic selector exists only for guarantees that can already be attained by ignoring the signal and always choosing the same action. Any improvement over this signal-independent benchmark, however small, is intractable under a standard complexity assumption. Since this result also holds with only two supported signals, the universal quantifier–the requirement that the selector succeed after every supported signal–is itself a source of difficulty, and the hardness is not necessarily caused by the size of the signal space. In Theorem 3 and Theorem 4, we relax the guarantee further by requiring good approximate performance only over a high-probability mass of signals, without requiring the guarantee to hold for every supported signal. Even so, under a standard complexity assumption, neither an efficient deterministic selector nor an efficient randomized probably approximately correct (PAC) selector can uniformly improve on the no-information benchmark over unrestricted signal-generating processes. However, Proposition 1 implies that the hard instances rely solely on possibly exponentially large signal support. With a known polynomial bound on support, a Monte Carlo empirical-Bayes selector efficiently achieves any fixed nonexact approximation with any fixed confidence level (Proposition 1). Thus, for PAC ex ante performance, the potentially exponential size of the signal support is the only remaining source of computational difficulty. 3In summary, we show that Bayesian signal processing is essentially a computationally hard problem. We identify the universal quantifier as the main source of this hardness: it requires good performance after every signal, including signals that may be difficult to interpret. The resulting difficulty includes, but is not limited to, the burden created by a potentially large signal space. We also provide a justification for using sample-based Bayesian learning in economics, computer science, and statistics. This approach appears to be the way to solve the weaker approximation problem when the signal support is bounded–the only problem in our framework that may admit a tractable solution. 

Related Literature. Our work relates to a broad literature in economics on bounded rationality and the computational limits of choice and information processing. Camara [2022] studies rational and computationally tractable choice over high-dimensional product menus, where hardness comes from the large, input-dependent action space and additive separability restores tractability. By contrast, here the finite action space and utility matrix are fixed; the difficulty lies instead in exact inference from a succinct experiment and possibly large signal space. Wilson [2014] models bounded rationality in Bayesian information processing by imposing a finite-memory constraint. By contrast, we impose no exogenous memory bound; instead, we ask whether the requisite inference from a sampler representation can be carried out in an asymptotic time limit. Hazla et al. [2021] establish hardness of Bayesian decisions in social-learning networks under fixed natural utilities. Their hardness comes from the exponentially large joint private signal space created by neighbors in the network. Echenique et al. [2011] find that the hardness of the consumer’s problem can arise from a complex representation of the utility function. By contrast, our hardness result admits a clear economic interpretation and is independent of how utility is revealed. Eboli [2003] studies how the computational cost of Bayesian updating depends on the representation of information. Grether [1980] finds experimental evidence of base-rate neglect, especially among inexperienced or weakly incentivized subjects. Our work also relates to research in machine learning and statistics on learning Bayes-optimal decision rules. Haussler [1992] develops a decision-theoretic PAC-learning framework for learning bounded-loss decision rules from labeled samples when the data distribution is unknown; Elkan [2001] and ´Avila Pires et al. [2013] study cost-sensitive 4classification, while Audibert and Tsybakov [2007] analyzes plug-in classifiers that estimate conditional class probabilities before applying the Bayes rule. Le et al. [2017] amortize posterior inference, Sainsbury-Dale et al. [2024] train neural Bayes estimators, and Pudlo et al. [2016] formulate approximate Bayesian computation model choice as a classification problem, training a random forest on model-labeled data generated from the prior predictive distribution. Ortiz and Kaelbling [2000] use sampling to select near-optimal actions in one-decision influence diagrams, while Alsing et al. [2023], Gorecki et al. [2024], and Polson et al. [2024] learn expected-utility surfaces, posterior expected costs, or posterior distributional utilities that support action choice. These approaches are generally model-specific and sample-based, emphasizing statistical risk, amortization, or simulation efficiency. Thus, these studies develop model-specific, simulation-based methods for directly learning Bayes actions, which inspires our construction of the empirical Bayes selector. By contrast, we treat a sampler description as part of the input and ask whether one polynomial-time selector can implement or approximate the Bayes optimal choice uniformly over all such samplers in a fixed finite decision environment. Our contribution is the resulting fixed-environment classification of exact and approximate tractability, together with finite-support sample bounds under an explicit sample-only access model. 

# 2 Model 

A computationally bounded decision maker faces a finite decision making problem where the state θ ∈Θ is unknown to the decision maker who will choose an action a∈A.The state is drawn according to a full-support prior μ0 ∈∆(Θ) where every component is rational, and the decision maker’s utility function is specified by 

u:A×Θ−→ Q>0.

These objects constitute the decision environment and are fixed. Before choosing an action, the decision maker receives a signal from a sampler-described experiment. A sampler q is represented by a finite randomized Boolean circuit that, given input θ and a uniformly random seed R, outputs a signal s=q(θ;R). Therefore, it induces the Blackwell experiment 

q(s|θ)=Pr  

> R

(q(θ;R)= s).

5By a slight abuse of notation, we use q for both the sampler circuit and the experiment it induces. Fix a standard gate-list encoding of sampler descriptions and write ⟨q⟩ for the bit string encoding q. Appendix C provides an informal guide to circuits, gate-list encodings, and the complexity classes used below. For any binary string x, let |x| denote its number of bits. Thus |⟨ q⟩| is the description length of the sampler, whereas |s| is the length of the realized signal string. The size of an instance is 

|⟨ q⟩| +|s|.

We model the decision maker’s choice rule as a selector α(⟨q⟩,s ). A selector is a function that maps ( ⟨q⟩,s ) to an action α(⟨q⟩,s )∈A. Because the decision environment (Θ ,A,μ 0,u ) is fixed, the sampler description ⟨q⟩ and the realized signal s are the selector’s only varying arguments. For a signal s, let 

Z(q,s )= X

> θ∈Θ

μ0(θ)q(s|θ).

Posterior statements below are restricted to Z(q,s )>0. Define the unnormalized posterior payoff 

Wa(q,s )= X

> θ∈Θ

μ0(θ)q(s|θ)u(a,θ ).

When Z(q,s )>0, the posterior expected payoff is Wa(q,s )/Z (q,s ), so its maximizers are exactly the maximizers of Wa(q,s ). We call a selector α an exact Bayes selector if it satisfies 

α(⟨q⟩,s )∈argmax  

> a∈A

Wa(q,s )on every input. A selector α is a deterministic polynomial-time selector if there exists a deterministic algorithm that, on every input ( ⟨q⟩,s ), outputs α(⟨q⟩,s ) in time polynomial in |⟨ q⟩| +|s|.We call such a selector tractable . Unless explicitly stated as randomized, we also use 

polynomial-time selector to refer to this deterministic notion. 

# 3 Exact Bayesian Selectors 

Define the set of dominant actions 

D(u)= {d∈A:u(d,θ )≥u(a,θ ) for every a∈A, θ ∈Θ}.

6Theorem 1. If P̸ =PP , there is a polynomial-time exact Bayes selector for all sampler descriptions q and signals s if and only if D(u)̸ =∅.Proof Overview. If D(u)̸ = ∅, the claim is immediate: a selector can always return a dominant action, independently of the sampler and the observed signal, and hence runs in constant time. When D(u)= ∅, we reduce from Majsat (Majority-SAT), a canonical 

PP -complete problem [Gill, 1977]. The reduction uses a sampler with only two possible signals and translates the acceptance probability of a Boolean circuit into the location of a posterior along a line segment. We sketch the main idea here and leave the technical details to Appendix A. Because no action is dominant, one can find two nearby posterior beliefs p− and p+ on opposite sides of a decision boundary. Let p0 =( p− +p+)/2. Along the segment joining 

p− and p+, the uniquely optimal payoff vector is that of some action a− on the p−-side of p0, and that of another action a+ on the p+-side. Thus, an exact selector evaluated at a posterior on this segment reveals on which side of p0 that posterior lies. Now take a Majsat instance φ. We can determine whether the acceptance probability of φ is greater than 1 /2 or not. For each state θ, let E− 

> θ

and E+ 

> θ

be auxiliary random events calibrated so that they implement the posteriors p− and p+, respectively, upon observing a target signal s⋆. Using independent random seeds R and U for φ and these auxiliary events, define the two-signal sampler 

qφ(θ;R,U )= 



s⋆,  φ(R)=0 ∧ E− 

> θ

(U) ∨  φ(R)=1 ∧ E+ 

> θ

(U),s◦, otherwise .

By Bayes’ rule, Pr( ·| s⋆)=(1 −zφ)p− +zφp+.

Therefore, the posterior lies on the p+-side of p0 exactly for yes-instances of Majsat ,and on the p−-side otherwise. A fixed decoder applied to the output of any exact Bayes selector consequently determines whether φ∈Majsat .Theorem 1 reveals two features that jointly generate this computational difficulty. First, exact optimality can require extreme precision. Whenever acting optimally requires some information about the state–that is, whenever no single action is optimal in every state–an experiment can place the posterior arbitrarily close to a decision boundary, so that even a 7minute difference in signal likelihoods changes the optimal action. Second, the guarantee is uniform over signals: an exact Bayes selector must choose optimally after every supported signal and hence cannot ignore even a single signal whose interpretation is computationally difficult. Thus, even though the underlying decision problem has only finitely many states and actions, exact Bayesian choice may require arbitrarily fine likelihood distinctions to be resolved after every possible observation. To further explore the driven force of the hardness, the same argument of the proof yields the following corollary. 

Corollary 1. Suppose D(u)= ∅ and P̸ =PP . There is no polynomial-time exact Bayes selector even for the restricted class of samplers q satisfying 

|{ s:Z(q,s )>0}|≤ 2.

Corollary 1 separates the role of the requirement to succeed after every supported signal from the size of the signal space. The hardness persists even when the experiment has at most two supported signals, so the cost of this universal quantifier cannot be solely attributed to having to cover an exponentially large signal space. Its force is instead that the selector is not permitted to fail on even the one signal that encodes a difficult likelihood comparison. This distinction raises the natural question studied in the next section: does the hardness disappear if exactness, and hence the required precision, is relaxed while the every-signal requirement is retained? Theorem 2 and Corollary 2 show that it does not. 

# 4 Approximate Selectors 

In this section, we consider two notions of multiplicative approximation and identify the source of complexity associated with each. 

# 4.1 Signalwise approximation 

To relax exactness while retaining the requirement that the guarantee hold after every supported signal, we introduce the following notion of signalwise approximation. 

Definition 1 (Signalwise deterministic approximation) . A deterministic signalwise 

c-selector is a selector α such that 

Wα(⟨q⟩,s )(q,s )≥cmax  

> a∈A

Wa(q,s ) (1) 8for every sampler q and every signal s satisfying Z(q,s )>0.

For each state, let Mθ :=max a∈Au(a,θ ), and define 

γsig (u):=max  

> d∈A

min 

> θ∈Θ

u(d,θ )

Mθ

. (2) 

Theorem 2. Fix c ∈ (0 ,1] . If P̸ = NP , then there is a deterministic polynomial-time signalwise c-selector if and only if c≤γsig (u).Proof Overview. When c≤γsig (u), the constant selector that always returns an action attaining the maximum in (2) satisfies the approximation requirement. For the hardness direction, fix c>γ sig (u) and suppose that a polynomial-time signalwise c-selector α exists. We show how to use α to solve SAT , a standard NP -complete problem. The key observation is that, if α outputs an action a after observing a signal s, then 

s cannot perfectly reveal a state 

θi ∈argmin 

> θ∈Θ

u(a,θ )

Mθ

.

Indeed, if s perfectly revealed θi, then 

Wa(q,s )max a′∈AWa′ (q,s ) = u(a,θ i)

Mθi

≤γsig (u)<c, 

contradicting the signalwise c-approximation guarantee. To exploit this observation, let Θ= {θ1,...,θ m} and, for m formulas ψ1,...,ψ m, construct the two-signal sampler 

q(θ;R)= 



s⋆, θ =θi for some i∈{ 1,...,m } and ψi(R)=1 ,s◦, otherwise .

After calling α(⟨q⟩,s ⋆) and obtaining a, choose an index i minimizing u(a,θ i)/M θi . The formula ψi cannot be the unique satisfiable formula; otherwise, s⋆ would perfectly reveal 

θi. Hence, m_

> j=1

ψj is satisfiable ⇐⇒ _ 

> j̸=i

ψj is satisfiable .

Repeatedly applying this deletion rule while pruning the standard SAT self-reduction tree keeps the list of residual formulas at constant size after each branching step. Once all variables have been assigned, the remaining assignments can be checked directly, yielding a polynomial-time algorithm for SAT , a contradiction. Theorem 2 answers the question raised in the previous section: exactness is not essential to the hardness. The threshold γsig (u) has a direct information-processing interpretation. 9When c≤γsig (u), a single fixed action attains the guarantee without consulting either the sampler or the realized signal, so information processing is dispensable. Once c>γ sig (u), however, the guarantee requires the selector to extract some decision-relevant information from its input, and even an arbitrarily small improvement over the signal-independent benchmark is intractable under P̸ =NP . Thus, when performance must be guaranteed after every supported signal, approximation does not gradually restore tractability: tractability returns only when the requirement is weak enough that the signal can be ignored altogether. Moreover, the hardness construction uses only two signals, yielding the following corollary. 

Corollary 2. Fix c ∈ (γsig (u), 1] and assume P̸ = NP . There is no deterministic polynomial-time signalwise c-selector even for the restricted class of samplers q satisfying 

|{ s:Z(q,s )>0}|≤ 2.

Corollary 2 shows that the hardness need not be driven by the straightforward difficulty created by a large signal space. An exponentially large support can certainly impose a com-putational burden on procedures that must enumerate or cover many possible signals, but such a large support is not necessary here: the hardness persists even when the experiment has at most two supported signals. The force of the “every-signal” requirement therefore lies in its quantifier, not in the number of signals. A signalwise selector is not permitted to aver-age away, ignore, or sacrifice even the one signal whose state-contingent likelihoods encode a hard computational distinction. Together, Theorem 2 and Corollary 2 show that, as soon as the desired guarantee exceeds the best signal-independent benchmark, however slightly, retaining this universal requirement can force the selector to resolve an intractable likelihood comparison at a signal it is not allowed to ignore. Thus, beyond any difficulty caused by the size of the signal space, the central information-processing barrier highlighted here is the requirement to extract enough decision-relevant information from every supported signal. This conclusion raises a natural question: does the hardness persist if good approximate performance is required only over a high-probability mass of signals, rather than after every supported signal? Theorem 3 and Theorem 4 show that it does, while Proposition 1 indicates that the remaining difficulty comes only from having to explore a potentially large signal space. 10 4.2 Ex ante approximation 

To formalize good aggregate performance over a high-probability mass of signals, we introduce the following notion of ex ante approximation. For a deterministic selector 

α, define its ex ante payoff by VAL α(q):= X

> s

Wα(⟨q⟩,s )(q,s ), (3) and define the unrestricted Bayes-optimal ex ante payoff by OPT( q):= X

> s

max  

> a∈A

Wa(q,s ).

Definition 2 (Ex ante approximation) . A deterministic ex ante c-selector is a selector 

α such that 

VAL α(q)≥cOPT( q)

for every sampler q.

The relevant full-information and no-information benchmarks are 

V FI := X

> θ∈Θ

μ0(θ)Mθ and V NI :=max 

> a∈A

X

> θ∈Θ

μ0(θ)u(a,θ ).

Define 

γEA (u,μ 0):= V NI 

V FI .

Theorem 3. Fix c∈(0 ,1] . If BPP ̸ =SZK , there is a deterministic polynomial-time ex ante c-selector if and only if 

c≤γEA (u,μ 0). (4) The assumption BPP ̸ =SZK is stronger than P̸ =NP .1 Nevertheless, the assumption is widely believed by computer scientists: several cryptosystems rely on the presumed hardness of problems in SZK , so an efficient algorithm for all of SZK would compromise these systems. There is also an evidence for the assumption: Bouland et al. [2019] construct an oracle A relative to which BPP A ⊊SZK A.

Proof Overview. If c ≤ γEA (u,μ 0), the constant selector that always returns a prior-optimal action satisfies the ex ante c-approximation guarantee: it achieves V NI , while the Bayes-optimal value is at most V FI . For the hardness direction, fix c > γ EA (u,μ 0)and suppose that a deterministic polynomial-time ex ante c-selector α exists. We use α     

> 1Indeed, BPP ̸=SZK implies P̸=NP ; see Sahai and Vadhan [2003].

11 to construct a randomized polynomial-time algorithm for Statistical Difference, an SZK -complete problem [Sahai and Vadhan, 2003], with two-sided error probability at most 1 /3. After applying the standard polarization argument, it suffices, for a sufficiently small 

ε> 0, to distinguish between 

dTV (P0,P 1)≤ε and dTV (P0,P 1)≥1−ε, 

where dTV denotes total variation distance. Assign each state θ a distinct binary codeword. We construct a sampler q by replacing each bit of the codeword with an independent draw from P0 or P1, according to whether that bit is zero or one. If dTV (P0,P 1)≥1−ε, the samples almost reveal every bit of the codeword and hence almost reveal the state. Therefore, OPT( q)≥V FI −O(ε), VAL α(q)≥c V FI −O(ε).

If dTV (P0,P 1)≤ε, the signal distributions are nearly the same across states, so the selector can obtain at most the no-information value up to a small error: VAL α(q)≤V NI +O(ε).

Because c > γ EA (u,μ 0) = V NI /V FI , these two bounds are separated by a fixed positive gap when ε is sufficiently small. Therefore, we can estimate VAL α(q) by sampling and use this gap to distinguish the two cases with bounded error, placing Statistical Difference in BPP and contradicting BPP ̸ =SZK .Theorem 3 shows that the hardness survives the move from signalwise to ex ante approximation. Relaxing the guarantee does not eliminate the information-processing problem: once the target exceeds the no-information benchmark, the decision maker must condition her action on the observed signal, and even achieving good aggregate performance over a high-probability mass of signals remains intractable. Unlike the earlier every-signal hardness results, however, this difficulty may have a more straightforward source. A high-probability mass can itself be dispersed over exponentially many distinct signals, so performing well on that mass may require the selector to explore and learn the decision implications of exponentially many realizations. In fact, the reduction in the proof of Theorem 3 uses an exponentially large signal support: each realized signal has polynomial length so that there are exponentially many possible realizations generated from the outputs of P0 and P1. Therefore there is open problem whether deterministic ex ante hardness 12 persists under polynomially bounded support; we currently have neither a polynomial-time selector nor a matching hardness result for this restricted case. We believe that the problem becomes tractable once restricted to signal spaces of polynomially bounded size. The next section provides supporting evidence by considering a slight relaxation: the ex ante approximation guarantee is required to hold only with high probability, rather than with probability one, thereby allowing randomized selectors. Under this relaxation, the hardness stems solely from the large signal space. 

# 5 Probably Approximately Correct Selectors 

In this section, we turn to randomized selectors and require their approximation guarantees to hold only with high probability. 

# 5.1 PAC ex ante selectors 

We begin by formally defining randomized selectors and the corresponding notion of probably approximately correct (PAC) ex ante approximation. 

Definition 3 (PAC ex ante selector) . A randomized polynomial-time selector is a uniform probabilistic algorithm α that, on input (⟨q⟩,s ) and random tape R, outputs α(⟨q⟩,s ;R)∈A

in time polynomial in |⟨ q⟩| +|s| for every R. For fixed R, write αR(⟨q⟩,s ):= α(⟨q⟩,s ;R).For fixed parameters c∈(0 ,1] and δ ∈(0 ,1) , α is a (c,δ )-PAC ex ante selector if, for every sampler q,

Pr  

> R

[VAL αR (q)≥cOPT( q)] ≥1−δ. 

Here VAL is defined in (3) . The fair random tape R is independent of the test signal and is held fixed throughout this ex ante value. αR is a deterministic selector. 

The PAC notion also implies an expected-payoff guarantee for randomized selectors. Any ( c,δ )-PAC ex ante selector satisfies, for every sampler q,

ER[VAL αR (q)] ≥c(1 −δ)OPT( q).

Indeed, the selector attains at least cOPT( q) with probability at least 1 −δ; dropping its positive payoff on the remaining random tapes gives the inequality. Hence, a ( c,δ )-PAC guarantee yields a c(1 −δ)-approximation in expectation. The PAC analysis can therefore 13 also be interpreted as an analysis of ex ante approximation for randomized selectors evaluated by their expected payoffs. 

Theorem 4. Fix a factor c ∈ (0 ,1] and an error probability δ ∈ (0 ,1) . If BPP ̸ = SZK ,there is a polynomial-time randomized (c,δ )-PAC ex ante selector if and only if 

c≤γEA (u,μ 0).

Proof Overview. If c ≤ γEA (u,μ 0), the constant selector that always returns a prior-optimal action satisfies the ( c,δ )-PAC ex ante guarantee with probability one. For the hardness direction, fix c>γ EA (u,μ 0) and suppose that a polynomial-time randomized (c,δ )-PAC ex ante selector α exists. We use the same polarized Statistical Difference instance and state-encoding sampler q as in the proof of Theorem 3. If dTV (P0,P 1)≥1−ε, then OPT( q)≥V FI −O(ε),

so the PAC guarantee implies Pr 

> R

VAL αR (q)≥c V FI −O(ε) ≥1−δ. 

If dTV (P0,P 1)≤ε, the signal distributions are nearly the same across states. For every fixed random tape R, the resulting selector αR is deterministic. By the similar argument, VAL αR (q)≤V NI +O(ε) for every R. 

Because cV FI >V NI , the two bounds are separated by a fixed positive gap for sufficiently small ε.The only new issue is that the lower bound holds with probability 1 − δ over the selector’s random tape, whereas the upper bound holds for every tape. We therefore draw a constant number of independent tapes. With high constant probability, at least one tape produces a high-value selector in the first case, while no tape can do so in the second. Estimating the value associated with each tape by sampling from q and comparing the largest estimate with a threshold distinguishes the two cases with bounded error. This again places Statistical Difference in BPP , contradicting BPP ̸ =SZK .Theorem 4 shows that allowing randomized selectors and requiring the approximation guarantee to hold only with high probability does not by itself alleviate the hardness identified in Section 4.2. The next subsection shows that under this relaxation, the remaining hardness is driven entirely by the potentially exponential size of the signal space. 14 5.2 Monte Carlo empirical Bayes selectors 

In this section, we provide a construction of a selector that is based on Monte Carlo sampling from the sampler q. We also show its potentials to be computed in polynomial time. Write 

Sq := [

> θ∈Θ

supp {q(·| θ)}, Kq := |S q|, U := max 

> a∈A,θ ∈Θ

u(a,θ ),

Fix a sample size N, a total order on A, and a fallback action a◦ ∈A. Our Monte Carlo empirical Bayes selector has following three main steps: 1. Draw and fix the training sample. Because the selector is given ⟨q⟩, it can simulate q(·| θ) in polynomial time. Independently for each state θ, draw 

Sθ, 1,...,S θ,N ∼q(·| θ).

The resulting training sample is held fixed throughout evaluation. 2. Form the empirical experiment. For every state θ and signal s, define 

bqN (s|θ):= 1

N

> N

X

> i=1

1{Sθ,i =s}.

The corresponding empirical unnormalized payoff of action a at signal s is 

cWa(s):= X

> θ∈Θ

μ0(θ)bqN (s|θ)u(a,θ ). (5) 3. Define the fixed policy. For every query signal s∈S q, set 

bαN (s)∈argmax 

> a∈A

cWa(s),

with ties resolved by the fixed order, except that bαN (s)= a◦ when all empirical counts at s are zero. The fixed training sample together with the evaluation rule is an implicit representation of bαN . On a query signal s, the selector can counts the observations equal to s state by state, and computes the finitely many values in (5). Therefore, it need not enumerate 

Sq or store a complete signal-to-action table. We next analyze how many samples are sufficient for the empirical Bayes selector to satisfy a ( c,δ )-PAC guarantee. 

Proposition 1. Assume Kq =|S q|<∞. Let Cμ := P

> θ∈Θ

μ0(θ)2. For every c,δ ∈(0 ,1) , if 

N ≥ 2U2Cμ

(1 −c)2(V NI )2



Kqlog |A|+log 2

δ



,

then 

Pr[VAL bαN (q)≥cOPT( q)] ≥1−δ. 

15 Thus, because the decision environment is fixed, Proposition 1 implies that it suffices to draw 

N =O

Kq +log(1 /δ )(1 −c)2



.

This rate is tight, up to constants, within the class of fixed-training-sample forward-sampling selectors. In the fixed two-state matching environment and parameter range of Proposition 2, where K =Kq, the lower bound proved in Appendix B matches the preceding upper bound. Hence the worst-case sample complexity in that setting is 

N =Θ 

K +log(1 /δ )(1 −c)2



samples per state. More generally, suppose that a promise class comes with a known polynomial p such that 

Kq ≤p(|⟨ q⟩| )for every sampler q in the class. For every fixed pair ( c,δ ) with c< 1 and δ ∈(0 ,1), both the training phase and the evaluation of each signal then run in polynomial time. Thus, on this promise class, the Monte Carlo empirical Bayes selector is a uniform randomized polynomial-time ( c,δ )-PAC selector. Thus, Proposition 1 clarifies that, for fixed approximation and confidence parameters, the potentially exponential size of the signal support is the only remaining computational obstacle to achieving an ex ante approximation with high probability. The intuition is straightforward: when probability mass is spread across exponentially many signals, a tractable empirical Bayes selector can draw only polynomially many training samples, which may be insufficient to estimate the state-contingent signal distributions accurately enough for reliable decision making. Moreover, a related point explains why a sampling-based method cannot satisfy the unconditional ex ante guarantee in Definition 2, even when the signal support has constant size. Any finite training sample has a positive–although possibly negligible–probability of being unrepresentative, in which case the empirical distribution and the induced policy may perform poorly. A negligible failure probability is still nonzero and therefore cannot satisfy a guarantee that must hold with probability one. Therefore, whether there is a tractable deterministic selector that can satisfy the ex ante approximation guarantee when the signal support has constant size still remains an open question. 16 6 Discussions 

# 6.1 Randomized selectors 

Randomization affects exact and approximate signalwise selection differently. Suppose that a randomized selector is evaluated by its expected payoff conditional on every supported signal: 

ER

Wα(⟨q⟩,s ;R)(q,s )≥cmax  

> a∈A

Wa(q,s ).

For exact selection, randomization cannot help. Since no realized action can yield more than the Bayes-optimal payoff, attaining that payoff in expectation requires every action chosen with positive probability to be Bayes optimal. Randomization can therefore serve only as a tie-breaking device, and the exact-selection dichotomy in Theorem 1 remains unchanged. For approximate selection, by contrast, randomization convexifies the action set and may improve the best signal-independent guarantee. For a lottery λ∈∆( A), define the mixed-action benchmark 

γmix sig (u):= max 

> λ∈∆( A)

min 

> θ∈Θ

u(λ,θ )

Mθ

.

Because pure actions are degenerate lotteries, γmix sig (u)≥γsig (u). For every c<γ mix sig (u), a sufficiently accurate dyadic approximation to an optimizing lottery gives a polynomial-time selector that ignores both the sampler and the signal. The boundary c=γmix sig (u) is also attainable whenever an optimizing lottery can be implemented exactly in the fair-coin randomization model. For every fixed c>γ mix sig (u), the reduction underlying Theorem 2 can be adapted by independently rerunning the selector and estimating its induced lottery over actions. Amplification makes the probability of any erroneous deletion small over the entire self-reduction. Because the surviving assignments are checked directly, an unsatisfiable instance is never accepted; hence such a selector would imply SAT ∈RP and therefore 

RP = NP . Under the standard assumption RP ̸ = NP , the signalwise hardness thus persists above γmix sig (u). The construction still uses at most two supported signals, so the small-support implication of Corollary 2 also holds. From economic perspective, randomization allows the decision maker to hedge across 17 states without processing the signal, thereby raising the signal-independent benchmark. Once the desired guarantee exceeds the best such hedge, however, the selector must exploit signal-specific information. Requiring it to do so after every supported signal preserves the same qualitative computational barrier. From a computational perspective, it is widely conjectured that randomization does not enlarge the class of problems solvable in polynomial time, that is, P=BPP . Under this conjecture, RP =P, so the assumption RP ̸ =NP used above follows from the standard assumption P̸ =NP . The persistence of the two-signal result has a more direct explanation: amplification repeatedly invokes the selector on the same input and operates only on its internal randomness, leaving the sampler and its signal support unchanged. More broadly, the belief that randomization can often be derandomized provides heuristic support for our conjecture that deterministic ex ante approximation may become tractable under polynomially bounded signal support. 

# 6.2 Economic interpretations for computer science framework 

Worst-case complexity. In this paper, we consider the worst-case complexity. The question is whether a single uniform polynomial-time selector satisfies the relevant guarantee for every sampler description q and, for the exact and signalwise criteria, every supported signal s. Thus, the results do not imply that every sampler is difficult to difficult to process. Particular experiments may admit efficient Bayesian choice because their likelihoods or posterior comparisons have additional structure. Therefore, the appropriate understanding is that fixing a finite decision environment alone does not guarantee tractable information processing. The results leave substantial room for uniform algorithms on economically or statistically structured promise classes of samplers. The polynomial-support result in Proposition 1 provides one example: a known polynomial bound on Kq restores a randomized polynomial-time PAC selector for fixed approximation and confidence parameters. At the same time, the two-signal hardness results for exact and signalwise selection show that small support alone does not restore tractability under stronger, every-signal guarantees, while the constant-support deterministic ex ante case remains open. More generally, tractability may depend on 18 features such as efficiently evaluable likelihoods, restricted circuit or factorization structure, symmetry, or low-dimensional sufficient statistics. Identifying the relevant structural parameters–and determining how running time and sample complexity depend on them– is a natural direction for further research. Average-case or smoothed analyses could complement the present worst-case results by asking how frequently the hard instances arise in economically relevant classes of experiments. Another interpretation of our hardness results is that, for any tractable selector, we can always find an experiment where some signal realizations are sufficiently difficult to interpret. Therefore, this suggests a strategic extension where the signal-generating process is itself a design variable. A sender may seek to make decision-relevant information costly for a computationally bounded receiver to extract. One especially sharp question is whether a designer can change this computational burden while holding the induced Blackwell experiment fixed and varying only its representation; the present results do not establish such representation-only obfuscation. Characterizing when a designer can efficiently create and exploit interpretation costs, how they affect information design and equilibrium behavior, and which restrictions on admissible sampler representations eliminate them is a natural direction for future work. 

Polynomial and asymptotic running time. Our notion of tractability is asymptotic. Let n=|⟨ q⟩| +|s|, polynomial time requires the number of elementary operations to be bounded by Cn k for some constants C and k, uniformly over inputs as n grows. It does not imply that a selector is fast on a particular experiment: a large constant or a high polynomial degree may make a polynomial-time algorithm impractical at economically relevant scales. Conversely, an algorithm with an exponential worst-case bound may still be fast on small or favorable instances. Polynomial time also does not by itself provide an economically transparent interpretation or a plausible heuristic structure for the selector. What this distinction captures is scalability. Polynomial time is a permissive benchmark: it requires only that running time remain bounded by some polynomial as the size of the experiment grows. Our hardness results show that, in the stated hardness regimes, Bayesian signal processing fails even this weak scalability requirement. This makes the conclusion especially strong: the corresponding Bayesian performance requirement is 19 computationally demanding even under a generous notion of tractability, providing a justification for bounded rationality and costly Baysian inference. 

# 6.3 Sources of bounded rationality and computational hardness 

This subsection provides a detailed summary of how the literature explains the sources of bounded rationality and the origins of computational difficulty in decision making. Then, we clarify how our findings differ from, and contribute to, the existing literature. The literature surveyed here can be organized around three distinct questions. The first asks why complete substantive rationality is an inadequate description of decision making. Simon [1955] replaces exhaustive maximization with a satisficing search procedure that stops when an aspiration level is met, while Simon [1978] shifts attention from the optimal-ity of an outcome to the feasibility of the process that produces it through the distinction between substantive and procedural rationality. Conlisk [1996] organizes the case for bounded rationality around behavioral evidence, the explanatory success of boundedly rational models, the limits of methodological defenses of unbounded rationality, and the scarcity of deliberation itself. Radner [2000] similarly treats observation, computation, memory, communication, and delay as economically relevant constraints or costs, while distinguishing models that incorporate such costs into otherwise standard optimization from stronger departures associated with genuinely bounded rationality. In this first strand, the central message is that decision procedures and cognitive capacity are themselves economic objects rather than costless background assumptions; these papers explores new possible modeling approaches motivated by the gap between ideal and implementable choice rather than deriving hardness results for the optimal (Bayesian) decision rule. The second question asks how a resource-constrained decision maker optimally departs from complete Bayesian inference or unconstrained maximization. Halpern and Pass [2010, 2015], Alaoui and Penta [2022] let the agent choose an algorithm, or decide whether to continue reasoning, by trading the expected improvement in the decision against the cost of obtaining it. Sims [2003], Caplin and Dean [2015], Matˇ ejka and McKay [2015] instead make the information structure endogenous: the agent allocates a limited communication capacity or selects a signal structure by balancing its decision value 20 against an entropy-based or more general information cost. Gabaix [2014], Wilson [2014] restrict which dimensions enter the agent’s internal model or which pieces of past evidence remain available, thereby generating selective attention and systematic updating biases. Lieder and Griffiths [2020], Zhu and Griffiths [2026] interpret approximate inference as the solution to an accuracy–resource tradeoff; depending on how cognitive cost is represented, the resulting behavior may take the form of a heuristic inference procedure or a tempered response to new likelihood information. These models generally take a cognitive or informational limitation as a primitive and use it to predict stopping, inattention, sparsity, memory distortions, or approximate updating. The third question is whether the normative objective itself can be implemented by any efficient algorithm. Eboli [2003] measures the cost of processing information under particular representational architectures, showing how associative memory and Bayesian-network structure generate different computational costs. Camara [2022] studies choice from compactly represented combinatorial menus and shows how separable structure can restore tractability, thereby providing a computational rationale for narrow bracketing. Hazla et al. [2021] show that fully Bayesian choice in social-learning networks can be hard because an observed action history may encode a combinatorial set of private-signal histories, while particular network structures restore tractability. Classical work in artificial intelligence makes the role of representation especially explicit: Cooper [1990] shows that compact Bayesian networks can encode NP-hard exact-inference problems; Dagum and Luby [1993] show that relative or additive approximation need not eliminate the encoded distinction; and Roth [1996] connects a broad class of probabilistic and logical reasoning tasks to the hardness of propositional model counting. This strand is closest to our analysis because it asks what a computational procedure can implement, rather than which departure from Bayesian choice is optimal. Relative to these literatures, our analysis differs along four dimensions. First, we do not posit a particular form of bounded cognition: the prior, utility function, and Bayesian objective remain unchanged, and the question is one of algorithmic implementability rather than the rationalization of behavioral deviations. Second, the economic environment–the state space, action set, prior, and utility matrix–is fixed; the difficulty does not come from a 21 growing action menu, state space, or social network, but from the inference from the signal-generating process and the realized signal. Third, the required output is an action rather than a posterior, so only the location of the posterior relative to an action boundary matters; nevertheless, implicitly represented likelihoods can create a precision requirement near that boundary. Fourth, the hardness depends on the form of the guarantee: exact and signalwise hardness can persist with only two supported signals, because the requirement to succeed after every supported signal prevents the selector from ignoring a difficult realization. By contrast, deterministic ex ante hardness currently relies on exponentially large support, while a known polynomial support bound restores randomized PAC tractability. Thus, we separate the roles of precision, the every-signal quantifier, and signal-support size. 

# 7 Conclusions 

In this paper, we develop a computational framework for Bayesian signal processing. Under the standard conjecture that P̸ = PP , we establish a sharp boundary: there is a uniform polynomial-time selector that implements Bayes-optimal choice for every signal-generating process if and only if one action is optimal in every state. This result provides a foundation for boundedly rational choice in real-world decision problems. It also supports models that assign nonnegligible costs to full Bayesian inference. We also identify how the source of computational hardness changes as the performance guarantee is relaxed. Exact choice can require resolving arbitrarily fine likelihood differences near an action boundary, while the universal quantifier requires the selector to succeed after every supported signal, including the hardest one. Relaxing exactness does not remove the every-signal barrier: any signalwise guarantee above the best signal-independent benchmark remains hard, even with only two supported signals, so the hardness is not necessarily caused by the size of the signal space. Moving to ex ante performance also leaves the problem hard over unrestricted signal-generating processes. For PAC ex ante performance, however, the potentially exponential size of the signal support is the only remaining source of computational difficulty: when the support is polynomially bounded, the Monte Carlo empirical Bayes algorithm provides a tractable solution for any fixed nonexact approximation and confidence level. This is the only general, nontrivial, 22 signal-dependent tractability result established in this paper. They also suggest that, in the uniform worst case, knowing the full signal-generating process may offer no tractability advantage over black-box sampling access. Therefore, these results provide a computational justification for sample-based methods in economics, computer science, and statistics. Our framework opens the door to a broader analysis of the tractability of optimal and approximately optimal Bayesian decision making and the sources of their computational hardness. One natural direction for future work is to identify restricted classes of samplers that admit efficient selectors implementing Bayes-optimal or approximately optimal choices and to characterize the structural properties that restore such tractability. A particularly immediate open question is whether bounded signal support also restores deterministic ex ante tractability where we conjecture that it does. 

# References 

Larbi Alaoui and Antonio Penta. Cost-benefit analysis in reasoning. Journal of Political Economy , 130(4):881–925, 2022. Justin Alsing, Thomas D. P. Edwards, and Benjamin D. Wandelt. Optimal simulation-based Bayesian decisions. arXiv preprint arXiv:2311.05742 , 2023. Jean-Yves Audibert and Alexandre B. Tsybakov. Fast learning rates for plug-in classifiers. 

The Annals of Statistics , 35(2):608–633, 2007. Bernardo ´Avila Pires, Csaba Szepesv´ ari, and Mohammad Ghavamzadeh. Cost-sensitive multiclass classification risk bounds. In Proceedings of the 30th International Conference on Machine Learning , volume 28, pages 1391–1399. PMLR, 2013. Adam Bouland, Lijie Chen, Dhiraj Holden, Justin Thaler, and Prashant Nalini Vasudevan. On the power of statistical zero knowledge. SIAM Journal on Computing , 49(4): FOCS17–1, 2019. Jean Bretagnolle and Catherine Huber. Estimation des densit´ es: risque minimax. 

Zeitschrift f¨ ur Wahrscheinlichkeitstheorie und verwandte Gebiete , 47(2):119–137, 1979. 23 Modibo K. Camara. Computationally tractable choice. In Proceedings of the 23rd ACM Conference on Economics and Computation , page 28. Association for Computing Machinery, 2022. Andrew Caplin and Mark Dean. Revealed preference, rational inattention, and costly information acquisition. American Economic Review , 105(7):2183–2203, 2015. John Conlisk. Why bounded rationality? Journal of Economic Literature , 34(2):669–700, 1996. Gregory F. Cooper. The computational complexity of probabilistic inference using Bayesian belief networks. Artificial Intelligence , 42(2–3):393–405, 1990. Paul Dagum and Michael Luby. Approximating probabilistic inference in Bayesian belief networks is NP-hard. Artificial Intelligence , 60(1):141–153, 1993. Rick Durrett. Probability: Theory and Examples . Cambridge University Press, 5 edition, 2019. Mario Eboli. Two models of information costs based on computational complexity. 

Computational Economics , 21(1–2):87–105, 2003. Federico Echenique, Daniel Golovin, and Adam Wierman. A revealed preference approach to computational complexity in economics. In Proceedings of the 12th ACM Conference on Electronic Commerce , pages 101–110. Association for Computing Machinery, 2011. Charles Elkan. The foundations of cost-sensitive learning. In Proceedings of the Seventeenth International Joint Conference on Artificial Intelligence , pages 973–978, 2001. Xavier Gabaix. A sparsity-based model of bounded rationality. The Quarterly Journal of Economics , 129(4):1661–1710, 2014. John Gill. Computational complexity of probabilistic Turing machines. SIAM Journal on Computing , 6(4):675–695, 1977. Mila Gorecki, Jakob H. Macke, and Michael Deistler. Amortized Bayesian decision making for simulation-based models. Transactions on Machine Learning Research , 2024. 24 David M. Grether. Bayes rule as a descriptive model: The representativeness heuristic. 

The Quarterly Journal of Economics , 95(3):537–557, 1980. Joseph Y. Halpern and Rafael Pass. I don’t want to think about it now: Decision theory with costly computation. In Principles of Knowledge Representation and Reasoning: Proceedings of the Twelfth International Conference (KR 2010) , pages 182–190, 2010. Joseph Y Halpern and Rafael Pass. Algorithmic rationality: Game theory with costly computation. Journal of Economic Theory , 156:246–268, 2015. David Haussler. Decision theoretic generalizations of the PAC model for neural net and other learning applications. Information and Computation , 100(1):78–150, 1992. Jan Hazla, Ali Jadbabaie, Elchanan Mossel, and M. Amin Rahimian. Bayesian decision making in groups is hard. Operations Research , 69(2):632–654, 2021. Tuan Anh Le, Atilim Gunes Baydin, and Frank Wood. Inference compilation and universal probabilistic programming. In Proceedings of the 20th International Conference on Artificial Intelligence and Statistics , volume 54, pages 1338–1348. PMLR, 2017. Falk Lieder and Thomas L. Griffiths. Resource-rational analysis: Understanding human cognition as the optimal use of limited computational resources. Behavioral and Brain Sciences , 43:e1, 2020. Filip Matˇ ejka and Alisdair McKay. Rational inattention to discrete choices: A new founda-tion for the multinomial logit model. American Economic Review , 105(1):272–298, 2015. Luis E. Ortiz and Leslie Pack Kaelbling. Sampling methods for action selection in influence diagrams. In Proceedings of the Seventeenth National Conference on Artificial Intelligence , pages 378–385, 2000. Nick Polson, Fabrizio Ruggeri, and Vadim Sokolov. Generative Bayesian computation for maximum expected utility. Entropy , 26(12):1076, 2024. Pierre Pudlo, Jean-Michel Marin, Arnaud Estoup, Jean-Marie Cornuet, Mathieu Gautier, and Christian P. Robert. Reliable ABC model choice via random forests. 

Bioinformatics , 32(6):859–866, 2016. 25 Roy Radner. Costly and bounded rationality in individual and team decision-making. 

Industrial and Corporate Change , 9(4):623–658, 2000. Dan Roth. On the hardness of approximate reasoning. Artificial Intelligence , 82(1–2): 273–302, 1996. Amit Sahai and Salil P. Vadhan. A complete problem for statistical zero knowledge. 

Journal of the ACM , 50(2):196–249, 2003. Matthew Sainsbury-Dale, Andrew Zammit-Mangion, and Rapha¨ el Huser. Likelihood-free parameter estimation with neural Bayes estimators. The American Statistician , 78 (1):1–14, 2024. Herbert A. Simon. A behavioral model of rational choice. The Quarterly Journal of Economics , 69(1):99–118, 1955. Herbert A. Simon. Rationality as process and as product of thought. American Economic Review , 68(2):1–16, 1978. Christopher A. Sims. Implications of rational inattention. Journal of Monetary Economics , 50(3):665–690, 2003. Andrea Wilson. Bounded memory and biases in information processing. Econometrica ,82(6):2257–2294, 2014. Jian-Qiao Zhu and Thomas L. Griffiths. Computation-limited Bayesian updating: A resource-rational analysis of approximate Bayesian inference. Psychological Review ,133(3):619–635, 2026. 

# A Omitted Proofs 

Proof of Theorem 1. 

Suppose D(u)̸ =∅ and choose d∈D(u). For all a∈A, q, and s,

Wd(q,s )−Wa(q,s )= X

> θ∈Θ

μ0(θ)q(s|θ)u(d,θ )−u(a,θ )≥0.

26 Thus the constant selector ( q,s )7 →d is exactly optimal. For the converse, suppose D(u)= ∅.

Step 1: Construct a rational posterior line crossing a decision boundary. 

By the standard rationalizability result, together with arbitrarily small generic rational perturbations, there are payoff-distinct actions a and b and rational full-support beliefs 

μa and μb such that the payoff vector of a is uniquely optimal at μa but not optimal at 

μb, while the payoff vector of b is uniquely optimal at μb but not optimal at μa; moreover, their connecting segment crosses the finitely many indifference hyperplanes one at a time. Along the segment (1 −τ)μa +τμ b, the optimal payoff vector must therefore change. Since the endpoints, utilities, and indifference hyperplanes are rational, every crossing parameter is rational. Choose such a parameter t∈(0 ,1) ∩Q and let 

p0 :=(1 −t)μa +tμ b, h:= μb −μa.

Let a− and a+ have the distinct optimal payoff vectors immediately before and after this crossing. For a sufficiently small rational ε> 0, define 

p− =p0 −εh, p+ =p0 +εh. 

Then p− and p+ are rational and have full support, and the payoff vectors of a− and a+

are uniquely optimal on the two corresponding open half-segments. Hence, for z ∈[0 ,1], 

p(z)=(1 −z)p− +zp +

=p0 +(2 z−1) εh. 

(6) The unique optimal payoff vector along the two open half-segments is therefore summarized by (

u(c, ·): c∈argmax 

> a∈A

X

> θ∈Θ

pθu(a,θ )

)

=



{u(a−,·)}, 0≤z < 1/2,

{u(a+,·)}, 1/2<z ≤1.

(7) At z =1 /2, the posterior is exactly p0, where both a− and a+ are optimal, possibly along with other actions. 

Step 2: Build a fair-coin sampler that realizes the posterior line. 

To make the posterior after a target signal proportional to p±, the statewise likelihood of that signal should be proportional to p± 

> θ

/μ 0(θ). Define the fixed positive rational numbers 

ρ− 

> θ

:= p−

> θ

μ0(θ), ρ+ 

> θ

:= p+

> θ

μ0(θ).

Choose a positive integer M divisible by the reduced denominator of every ρ± 

> θ

. Then 

k± 

> θ

:= Mρ ±

> θ

27 is a positive integer. Choose a fixed integer L large enough that 

k± 

> θ

≤2L for every θ ∈Θ,

and set 

λ:= M

2L , e± 

> θ

:= λρ ± 

> θ

= k±

> θ

2L .

Thus e± 

> θ

∈(0 ,1]. If U is the integer represented by L independent fair bits, then 

U ∼Unif {0,..., 2L −1}, Pr( U <k ± 

> θ

)= e± 

> θ

.

Fix two distinct one-bit signals, a target signal s⋆ and an alternative signal s◦. Let 

R be the random seed of any Boolean fair-coin subcircuit B, and let b:= B(R). Draw the L bits defining U independently of R. Write 

z :=Pr  

> R

(B(R)=1) .

The number z is not supplied to the sampler and need not be computed; it is only the ac-ceptance probability induced by the random seed R. On input θ, define the sampler qB by 

qB(θ;R,U )= 



s⋆, b=0 and U <k − 

> θ

,s⋆, b=1 and U <k + 

> θ

,s◦, otherwise .

(8) Consequently, 

qB(s⋆ |θ)=(1 −z)e− 

> θ

+ze + 

> θ

.

The choice of the scale λ makes the ex ante probability of s⋆ independent of z. Indeed, 

X

> θ∈Θ

μ0(θ)e± 

> θ

=λX

> θ∈Θ

p± 

> θ

=λ, 

and therefore 

Z(qB,s ⋆)= X

> θ∈Θ

μ0(θ)qB(s⋆ |θ)=(1 −z)λ+zλ =λ> 0.

(9) By Bayes’ rule, for every θ,Pr( θ |s⋆)= μ0(θ)qB(s⋆ |θ)

Z(qB,s ⋆)=(1 −z)p− 

> θ

+zp + 

> θ

=pθ(z).

(10) Comparing (10) with (6), the acceptance probability z of B is exactly the coordinate on that posterior line. 

Step 3: Reduction. 

We reduce from strict Majsat . Let ψ(x) be an input formula on n variables and let 

S :=#SAT( ψ).

28 Introduce a new variable y and construct a padded formula as follows. 

φ(x,y ):= ψ(x)∧(y∨¬ y).

It has m=n+1 variables and we have 

N :=#SAT( φ)=2 S. 

Hence 

S > 2n−1 ⇐⇒ N > 2m−1,

so the padding preserves strict-majority membership. Draw two fair bits encoding 

J ∈{ 00 ,01 ,10 ,11 }

and independently draw a uniform assignment X ∈{ 0,1}m. Define 

Bφ(J,X )= 



φ(X), J =00 ,φ(X), J =01 ,

1{X̸ =0 m}, J =10 ,

0, J =11 .

(11) Among the 2 m+2 equally likely pairs ( J,X ), the first two branches contribute N accepting seeds each, the third contributes 2 m −1, and the fourth contributes none. Therefore 

zφ :=Pr( Bφ =1)= 2N +2 m −12m+2 ,

and 

zφ − 12 = 2N −2m −12m+2 .

Because m≥1, the numerator is odd and cannot be zero. Furthermore, 2N −2m −1>0 ⇐⇒ N ≥2m−1 +1 ⇐⇒ N > 2m−1.

It follows that 

zφ̸ = 12,ψ ∈Majsat ⇐⇒ zφ > 12,ψ / ∈Majsat ⇐⇒ zφ < 12.

(12) Use the circuit Bφ defined in (11) as the control subcircuit in the sampler rule (8). Explicitly, on input state θ, the constructed circuit qφ draws the independent fair random 29 bits ( J,X,U ) specified in the preceding definitions, computes Bφ(J,X ), and outputs 

qφ(θ;J,X,U )= 



s⋆, Bφ(J,X )=0 and U <k − 

> θ

,s⋆, Bφ(J,X )=1 and U <k + 

> θ

,s◦, otherwise .

This display is the complete definition of the sampler produced from ψ. Averaging over its random bits gives 

qφ(s⋆ |θ)=(1 −zφ)e− 

> θ

+zφe+ 

> θ

, qφ(s◦ |θ)=1 −qφ(s⋆ |θ).

Applying (9) and (10) with B =Bφ gives 

Z(qφ,s ⋆)= λ> 0and the posterior conditional on the target signal is Pr( θ |s⋆)=(1 −zφ)p− 

> θ

+zφp+ 

> θ

=pθ(zφ).

Equations (7) and (12) yield 

ψ ∈Majsat =⇒zφ >1/2 =⇒u(a+,·) is the unique optimal payoff vector ,ψ / ∈Majsat =⇒zφ <1/2 =⇒u(a−,·) is the unique optimal payoff vector .

It remains to verify that this is a polynomial-time construction. All objects obtained from the fixed environment–including a−, a+, p−, p+, M, L, λ, the thresholds k± 

> θ

, and the two signals–are hard-wired constants. Given ψ, the padding takes linear time. The circuit contains one copy of φ, constant-size branch-selection logic, O(m) gates for the test X̸ =0 m, and the fixed state-dependent threshold circuit. It uses exactly 

m+2+ L

fair random bits. Its description size is 

O |ψ|+m+|Θ|L=O(|ψ|),

because m ≤ | ψ| + 1 and Θ and L are fixed. The gate list and wiring can therefore be written in polynomial time, and s⋆ has constant length. Crucially, the constructor never computes either S or N; those counts appear only in the analysis of the circuit’s acceptance probability. Define 

F (ψ):=( ⟨qφ⟩,s ⋆).

The preceding paragraph proves that F is computable in polynomial time. Now let α

30 be any exact Bayes selector and define the fixed output decoder 

g(a):= 1{u(a, ·)= u(a+,·)}.

Since the environment and a+ are fixed, g runs in constant time. By construction, we have that 

g(α(⟨qφ⟩,s ⋆))=1 ⇐⇒ ψ ∈Majsat .

Thus one call to any exact selector, followed by the fixed decoder g, solves the PP -hard source problem. Computing an exact Bayes action is therefore PP -hard under a polynomial-time one-call output-decoding reduction. Finally, if D(u)= ∅ and a polynomial-time exact selector existed, PP -hardness would imply PP ⊆P and hence P=PP , a contradiction. 

Proof of Theorem 2. First suppose that c≤γsig (u), and choose an action d attaining the maximum in (2). This maximum is attained because A is finite. For every θ ∈Θ, the definition of d gives u(d,θ )≥γsig (u)Mθ. Consequently, 

Wd(q,s )= X

> θ∈Θ

μ0(θ)q(s|θ)u(d,θ )

≥γsig (u)X

> θ∈Θ

μ0(θ)q(s|θ)Mθ

≥γsig (u)max  

> a∈A

Wa(q,s ).

Thus the constant selector that always returns d achieves every factor c≤γsig (u). For the converse, fix c > γ sig (u) and suppose that a polynomial-time deterministic signalwise c-selector α exists. We show, step by step, how to use α as an oracle to decide an arbitrary instance of SAT , which is a well-known NP -complete problem. Write Θ= {θ1,...,θ m}.

The hard case necessarily has m≥2. Indeed, if m=1, an action attaining Mθ1 makes (2) equal to one, which is incompatible with c≤1 and c>γ sig (u). 

Step 1: Construct an m-to-(m−1) SAT list compressor. 

Suppose we are given m residual formulas 

ψ =( ψ1,...,ψ m)over the same declared variables y1,...,y r. The compressor will delete one formula while preserving whether at least one formula in the list is satisfiable. 31 Fix two distinct one-bit signals s⋆ and s◦. Construct a fair-coin sampler qψ that draws 

R∼Unif {0,1}r

and, on input state θ, applies the rule 

qψ(θ;R)= 



s⋆, θ =θi for some i∈{ 1,...,m } and ψi(R)=1 ,s◦, otherwise .

In state θi, the target signal is produced by exactly the satisfying assignments of ψi, and therefore 

qψ(s⋆ |θi)= #SAT( ψi)2r . (13) Since m is fixed by the environment, the sampler contains only a constant number of for-mula circuits and its description is polynomial in the total description length of the residual formulas. The same construction also covers r =0, with {0,1}0 interpreted as a singleton. Run the selector on the target signal and choose 

a=α(⟨qψ⟩,s ⋆), j ∈arg min 

> 1≤i≤m

u(a,θ i)

Mθi

.

The index j is computable by a constant-size lookup in the fixed utility table. By the definition of (2), 

u(a,θ j)

Mθj

= min 

> 1≤i≤m

u(a,θ i)

Mθi

≤max  

> b∈A

min 

> 1≤i≤m

u(b,θ i)

Mθi

=γsig (u)<c. (14) The compressor deletes ψj.

Step 2: Prove that the deletion is safe. 

We verify that m_

> i=1

ψi is satisfiable ⇐⇒ _

> i̸=j

ψi is satisfiable . (15) There are three cases. 

Case 1: If no formula is satisfiable, both sides of (15) are false, regardless of which formula is deleted. 

Case 2: If at least two formulas are satisfiable, deleting one formula leaves at least one satisfiable formula, so both sides of (15) are true. 

Case 3: It remains to consider the case in which exactly one formula, say ψk, is satisfiable. We claim that k̸ =j. Suppose instead that k =j.By (13), qψ(s⋆ |θi)=0 for every i̸ =j, whereas qψ(s⋆ |θj)>0. Since μ0 has full support, 

s⋆ perfectly reveals θj, so (14) yields 

Wa(qψ,s ⋆)max b∈AWb(qψ,s ⋆) = u(a,θ j)

Mθj

<c. 

32 This contradicts the signalwise guarantee (1). Thus k̸ =j: the unique satisfiable formula 

ψk remains after ψj is deleted. This establishes the third case and proves (15). 

Step 3: Use the compressor to decide an arbitrary SAT instance. 

Let Φ( y1,...,y n)be an arbitrary Boolean formula. After assigning the first k variables, a prefix is a string 

σ =( σ1,...,σ k)∈{ 0,1}k,

and its residual formula is Φσ(yk+1 ,...,y n):=Φ( σ1,...,σ k,y k+1 ,...,y n).

At depth k, maintain a list Lk of prefixes satisfying the two invariants 

|L k|≤ m−1, Φ is satisfiable ⇐⇒ some Φ σ with σ ∈L k is satisfiable . (16) Initialize 

L0 ={ε}, Φε =Φ ,

The invariant holds initially, and its size bound is valid because m≥2. Suppose Lk has been constructed for some k <n , we will construct Lk+1 . Let 

eLk+1 := [

> σ∈L k

{σ0,σ 1}.

For each prefix σ,Φσ is satisfiable ⇐⇒ Φσ0 or Φ σ1 is satisfiable .

Thus the expansion preserves the second invariant in (16), and 

|eLk+1 |=2 |L k|≤ 2( m−1) .

Now, consider the following procedure to reduce the size of eLk+1 . While |eLk+1 |≥ m,choose any m distinct prefixes 

σ(1) ,...,σ (m) ∈ eLk+1 

and feed their residual formulas 

ψi :=Φ σ(i) , i=1 ,...,m, 

to the compressor constructed in Step 1. If the compressor returns the deletion index 

j, remove the prefix σ(j) from the full list. By Step 2, each deletion preserves the second invariant for the entire list. Each oracle call removes one prefix. Starting from at most 2( m−1) prefixes, at most 2( m−1) −(m−1)= m−133 calls reduce the list to size at most m−1. Set the resulting list Lk+1 . This completes the inductive construction. At depth n, every prefix in Ln is a complete assignment and its residual formula has no remaining variables. Directly evaluate Φ( σ) for each of the at most m−1 surviving assignments. The invariant (16) implies that one of them satisfies Φ if and only if the original formula is satisfiable. Finally, this procedure is polynomial time. There are at most m−1 selector calls per variable and hence at most n(m−1)= O(n) calls in total, because m is fixed by the environ-ment. A residual formula is represented by hard-wiring a prefix into the original formula, so its size remains polynomial in |Φ|. Each queried sampler contains at most the fixed number 

m of such formula circuits, at most n fair random bits, and constant-size state and output logic. Its description can therefore be constructed in polynomial time. All later queries may depend on earlier outputs of α, which is why this is a polynomial-time reduction. We have shown that a polynomial-time signalwise c-selector for c>γ sig (u) would put 

SAT in P and imply P=NP . Under P̸ =NP , no such selector exists. 

Proof of Theorem 3. Choose a prior-optimal action 

a0 ∈argmax 

> a∈A

X

> θ∈Θ

μ0(θ)u(a,θ )and let α0 be the constant selector defined by 

α0(⟨q⟩,s ):= a0.

We know that VAL α0 (q)= V NI (17) for every q. On the other hand, OPT( q)≤X

> s

X

> θ∈Θ

μ0(θ)q(s|θ)Mθ

=V FI .

(18) If c≤γEA (u,μ 0), by equations (17) and (18), VAL α0 (q)= V NI =γEA (u,μ 0)V FI ≥γEA (u,μ 0)OPT( q)≥cOPT( q).

This proves the “if” direction. For the converse, suppose that c > γ EA (u, μ 0) and that a uniform deterministic polynomial-time ex ante c-selector α exists. Choose a rational number ¯ c with 

γEA (u,μ 0)<¯c<c. 

34 The selector α is also an ex ante ¯ c-selector. Define 

δ :=¯ cV FI −V NI = ¯c−γEA (u,μ 0)V FI >0.

We use α to give a BPP algorithm for Statistical Difference, which is a SZK -complete problem [Sahai and Vadhan, 2003]. The reduction constructs a sampler q with the following two properties. When the two input distributions are far, the signal almost reveals the state, so the approximation guarantee forces VAL α(q) to be large. When they are close, the action chosen by α is almost independent of the state, so VAL α(q) is at most V NI up to a small error. 

Step 1: From Polarized Statistical Difference to construct the sampler. 

For two distributions generated by sampler circuits P0 and P1, write 

dTV (P0,P 1):= 12

X

> y

|Pr[ P0 =y]−Pr[ P1 =y]|.

The standard Statistical Difference problem asks us to distinguish whether 

dTV (P0,P 1)> 23 or dTV (P0,P 1)< 13in polynomial time. Let t:= |Θ|. If t =1, then V NI = V FI and γEA (u,μ 0) = 1, which is incompatible with ¯c<c ≤1 and ¯ c>γ EA (u,μ 0). Hence t≥2. Let ℓ:= ⌈log 2t⌉. Assign each state θ a distinct binary codeword 

κ(θ)=  κ1(θ),...,κ ℓ(θ)∈{ 0,1}ℓ.

Choose a fixed rational ε> 0 so small that 

ℓε (¯ c+1) V FI < δ

2.

Apply Polarization Lemma [Sahai and Vadhan, 2003, Lemma 3.3] and relabel the resulting sampler circuits as P0 and P1. For this choice of ε, it suffices to distinguish the promise 

dTV (P0,P 1)≤ε or dTV (P0,P 1)≥1−ε. 

We call these the close and far cases, respectively. From P0 and P1, construct a sampler q that, in state θ, independently draws ℓ samples and outputs 

S =( Y1,...,Y ℓ), Yk ∼



P0, κk(θ)=0 ,P1, κk(θ)=1 (k =1 ,...,ℓ ).

Since the environment and ℓ are fixed, the circuit for q has size polynomial in the descriptions of P0 and P1.35 Step 2: In the far case, the optimum is close to full information. 

Suppose that dTV (P0,P 1)≥1−ε. Let 

E := {y :Pr[ P0 =y]≥Pr[ P1 =y]}.

By the property of total variation distance, we have that 

P0(E)−P1(E)= dTV (P0,P 1).

Therefore, 

P0(Ec)+ P1(E)=1 −dTV (P0,P 1)≤ε. 

Given a signal S =( Y1,...,Y ℓ), construct 

bbk := 



0, Yk ∈E, 

1, Yk /∈E. 

Conditional on state θ,Pr 

hbbk̸ =κk(θ)|θ

i

=



P0(Ec) κk(θ)=0 ,P1(E) κk(θ)=1 

≤ε. 

By union bound, we have Pr 

h

(bb1,..., bbℓ)= κ(θ)|θ

i

≥1−ℓε. 

We now construct a possibly inefficient response rule β. Fix 

a⋆θ ∈argmax  

> a∈A

u(a,θ ) for every θ, 

and fix an arbitrary fallback action a◦ ∈A. Given a signal S =( Y1,...,Y ℓ), first compute the decoded string bb=( bb1,..., bbℓ) and then set 

β(S):= 



a⋆

> bθ

, bb=κ(bθ) for some bθ ∈Θ,a◦, bb / ∈κ(Θ) .

The codewords are distinct, so the state bθ in the first line is unique. If the true state is 

θ, then on the event bb=κ(θ) the rule returns a⋆θ and obtains Mθ. Dropping the positive payoff on the complementary event gives 

ES∼q(·| θ)

u(β(S),θ )≥(1 −ℓε )Mθ.

36 Because the unrestricted Bayes optimum maximizes signal by signal, it weakly dominates β:OPT( q)= X

> s

max  

> a∈A

Wa(q,s )

≥X

> s

Wβ(s)(q,s )=X

> θ∈Θ

μ0(θ)ES∼q(·| θ)

u(β(S),θ )

≥(1 −ℓε )X

> θ∈Θ

μ0(θ)Mθ

=(1 −ℓε )V FI .

(19) The ¯ c-guarantee for α now implies VAL α(q)≥¯cOPT( q)≥¯c(1 −ℓε )V FI .

Step 3: In the close case, the selector acts nearly no-information. 

Suppose instead that dTV (P0,P 1)≤ε. Let 

Qθ := 

> ℓ

O

> k=1

Pκk(θ) and Q:= P ⊗ℓ 

> 0

.

These are, respectively, the signal distribution in state θ and a common reference distribu-tion. By the product inequality for total variation distance [Durrett, 2019, Lemma 3.6.5], 

dTV (Qθ,Q )≤ X

> k:κk(θ)=1

dTV (P1,P 0)≤ℓε. 

For the fixed sampler q, write 

fq(S):= α(⟨q⟩,S ).

Let πθ be the distribution of fq(S) under S ∼ Qθ, and let π be its distribution under 

S ∼Q. By data-processing inequality, 

dTV (πθ,π )≤dTV (Qθ,Q )≤ℓε. 

Since 0 <u (a,θ )≤Mθ, it follows state by state that 

Ea∼πθ [u(a,θ )] ≤Ea∼π[u(a,θ )]+ ℓεM θ.

Consequently, VAL α(q)= X

> θ∈Θ

μ0(θ)Ea∼πθ [u(a,θ )] 

≤X

> a∈A

π(a)X

> θ∈Θ

μ0(θ)u(a,θ )+ ℓεV FI 

≤V NI +ℓεV FI .

(20) The last inequality holds because the first term is a convex combination, with weights 

π(a), of the prior payoffs of fixed actions, each of which is at most V NI .37 Step 4: Obtain a fixed gap between the two cases. 

Define the far-case lower bound and the close-case upper bound by 

V far :=¯ c(1 −ℓε )V FI , V close := V NI +ℓεV FI .

Their difference is 

V far −V close =δ−ℓε (¯ c+1) V FI > δ

2 >0.

Thus VAL α(q) ≥ V far in the far case, whereas VAL α(q) ≤ V close in the close case. The midpoint 

τ := V far +V close 

2separates the cases with margin greater than δ/ 4. 

Step 5: Estimate the value and decide Statistical Difference. 

It remains only to estimate VAL α(q) without enumerating the signal space. For a positive integer Nsamp to be chosen below and for each state θ, draw Sθ, 1,...,S θ,N samp 

independently from q(·| θ) and define the true and empirical statewise payoffs 

vθ := ES∼q(·| θ)[u(α(⟨q⟩,S ),θ )] , bvθ := 1

Nsamp 

> Nsamp

X

> r=1

u(α(⟨q⟩,S θ,r ),θ ).

Let 

bV := X

> θ∈Θ

μ0(θ)bvθ.

Since VAL α(q)= X

> θ∈Θ

μ0(θ)vθ,

the estimator bV is unbiased. Let 

umax := max 

> a∈A,θ ∈Θ

u(a,θ ).

For every fixed θ, the Nsamp realized payoffs in bvθ are independent random variables in [0 ,u max ]. By Hoeffding’s inequality, Pr 



|bvθ −vθ|≥ δ

8



≤2exp 



−Nsamp δ2

32 u2max 



.

Because t=|Θ|, a union bound over the states yields Pr 



max  

> θ∈Θ

|bvθ −vθ|≥ δ

8



≤2texp 



−Nsamp δ2

32 u2max 



.

Choose 

Nsamp := 

32 u2max 

δ2 ln(6 t)



.

The preceding failure probability is then at most 1 /3. On the complementary event, 

bV −VAL α(q) ≤X

> θ∈Θ

μ0(θ)|bvθ −vθ|≤ max  

> θ∈Θ

|bvθ −vθ|< δ

8.

38 Thus bV has the required accuracy with probability at least 2 /3. Since δ/ 8 < δ/ 4, the midpoint comparison from Step 4 then correctly separates the far and close cases. Finally, t, umax , and δ depend only on the fixed environment and the fixed factor ¯ c, not on the Statistical Difference input. Hence Nsamp is a fixed integer, and the estimator makes 

tN samp sampler calls and the same number of selector calls. Each draw from q runs the fixed number ℓ of polarized sampler circuits, and each call to the uniform selector α takes poly-nomial time in the description of q and the sampled signal. The fixed rational payoffs and prior weights add only polynomial-time arithmetic. Therefore computing bV and declaring the instance far when bV >τ and close otherwise is a uniform randomized polynomial-time algorithm for polarized Statistical Difference with bounded error at most 13 .Therefore, this algorithm places Statistical Difference in BPP . By SZK -completeness, 

SZK ⊆BPP . Since it is well known that BPP ⊆SZK , we get SZK =BPP , contradicting the assumption. 

Proof of Theorem 4. If c≤γEA (u,μ 0), the prior-optimal constant selector α0 from the proof of Theorem 3 remains valid and completes the proof of this part. Conversely, suppose that c>γ EA (u,μ 0) and that a uniform polynomial-time randomized (c,δ )-PAC ex ante selector α exists. Choose a rational ¯ c∈(γEA (u,μ 0),c ) and set ∆:=¯ cV FI −V NI >0.

Let t := |Θ| and ℓ := ⌈log 2t⌉; as in the preceding proof, the present case implies t ≥ 2. Choose a fixed rational ε> 0 such that 

ℓε (¯ c+1) V FI < ∆2 .

Apply exactly the polarization, state encoding, and sampler construction from Step 1 of the proof of Theorem 3. Thus the polarized circuits satisfy 

dTV (P0,P 1)≤ε or dTV (P0,P 1)≥1−ε, 

and the resulting sampler q has polynomial description size. In the far case, the decoding argument remains valid and we can get that by (19), OPT( q)≥(1 −ℓε )V FI .

By ¯ c-approximation, the PAC guarantee implies Pr 

> R

VAL αR (q)≥¯c(1 −ℓε )V FI ≥1−δ. 

Set F :=¯ c(1 −ℓε )V FI .39 In the close case, fix an arbitrary tape R, including one on which the PAC guarantee fails. The rule αR is a deterministic total selector, so (20) still applies. Hence, for every R,VAL αR (q)≤V NI +ℓεV FI =: C. 

Consequently, 

G:= F −C =∆ −ℓε (¯ c+1) V FI > ∆2 >0.

The new issue is that the far-case lower bound holds only for a 1 −δ fraction of random tapes, whereas the close-case upper bound holds pointwise for every tape. To handle this difference, let 

m:= 

 ln9 ln(1 /δ )



and draw independent tapes R1, ... , R m. In the far case, with probability at least 1−δm ≥8/9, at least one of the fixed-tape rules αRi has value at least F ; in the close case, every one of them has value at most C.Similar to Step 5 of the proof of Theorem 3, let 

umax := max 

> a∈A,θ ∈Θ

u(a,θ ), Nval := 

8u2max 

G2 ln(18 mt )



.

For each i and θ, draw independent samples Si,θ, 1,...,S i,θ,N val ∼q(·| θ), independently of the tapes, and define 

bVi := X

> θ∈Θ

μ0(θ) 1

Nval 

> Nval

X

> r=1

u(αRi (⟨q⟩,S i,θ,r ),θ ).

Conditional on the tapes, Hoeffding’s inequality and a union bound over the mt tape–state pairs give Pr 



max 

> 1≤i≤m

bVi −VAL αRi (q) < G

4 R1,...,R m



≥ 89.

Let τ := ( F +C)/2 and declare the polarized instance far if and only if max i bVi > τ .In the far case, a successful tape together with accurate validation gives max 

> i

bVi ≥F − G

4 =τ + G

4 >τ ;this joint event has probability at least 1 −1/9−1/9=7 /9. In the close case, the pointwise bound for every tape and accurate validation give max 

> i

bVi ≤C + G

4 =τ − G

4 <τ 

with probability at least 8 /9. The analysis of the size and time complexity of this reduction is the same as the proof of Theorem 3. The above procedure is therefore a randomized polynomial-time algorithm for polarized Statistical Difference with bounded error, which contracts to SZK ̸ =BPP .40 Proof of Proposition 1. For a policy h:Sq →A, define its empirical ex ante value by 

dVAL N (h):= X

> θ∈Θ

μ0(θ) 1

N

> N

X

> i=1

u h(Sθ,i ),θ .

Before the main proof, we firstly show the following lemma. 

Lemma 1. For every realization of the training sample, the restriction of bαN to Sq satisfies 

bαN ∈argmax 

> h∈H q

dVAL N (h), Hq := ASq .

Proof of Lemma 1. Regrouping the empirical objective by signal gives 

dVAL N (h)= X

> θ∈Θ

X

> s∈S q

μ0(θ)bqN (s|θ)u(h(s),θ )= X

> s∈S q

cWh(s)(s).

Only signals appearing in the finite training sample have a nonzero summand. The choice of h(s) therefore separates signal by signal, and bαN (s) maximizes each summand. Hence it maximizes their sum. Let 

rc := (1 −c)V NI 

2 .

Fix h∈H q. The variables 

Xhθ,i := μ0(θ)

N u h(Sθ,i ),θ , θ ∈Θ, i∈{ 1,...,N },

are independent, satisfy 0<X hθ,i ≤ μ0(θ)UN ,

and obey 

X

> θ,i

Xhθ,i = dVAL N (h), E

"X

> θ,i

Xhθ,i 

#

=VAL h(q).

Moreover, the sum of the squared interval lengths is 

X

> θ∈Θ
> N

X

> i=1

μ0(θ)UN

2

= U2Cμ

N .

By Hoeffding’s inequality, Pr 

h dVAL N (h)−VAL h(q) ≥t

i

≤2exp 



− 2Nt 2

U2Cμ



.

Since |H q|=|A|Kq , substituting t=rc and taking a union bound yields Pr 

"

sup 

> h∈H q

dVAL N (h)−VAL h(q) >r c

#

≤2exp 



Kqlog |A|− N(1 −c)2(V NI )2

2U2Cμ



≤δ. 

On the complementary event, let α∗ be a Bayes-optimal policy, so that VAL α∗ (q) = OPT( q). By Lemma 1, VAL bαN (q)OPT( q) ≥ dVAL N (bαN )−rc

OPT( q) ≥ dVAL N (α∗)−rc

OPT( q) ≥1− 2rc

OPT( q) ≥1− 2rc

V NI =c. 

41 B Sample Lower Bounds 

Consider the fixed two-state matching environment Θ= A={− 1,+1 }, μ0(−1)= μ0(+1)= 12, u(a,θ )= 1+ 1{a=θ}

2 . (21) Let K =2 m be even, and let the signal set be 

SK := {(j,x ): j ∈{ 1,...,m }, x ∈{− 1,+1 }} .

For a hidden vector b=( b1,...,b m)∈{− 1,+1 }m and γ ∈(0 ,1/2], define 

qb(( j,x )|θ):= 1+ θγb jxK .

This is a valid experiment. Indeed, all probabilities are positive and, for every θ,

> m

X

> j=1

X

> x∈{− 1,+1 }

qb(( j,x )|θ)= 2mK =1 .

The marginal probability of each signal is 

Z(qb,(j,x ))= 121+ γb jxK + 121−γb jxK = 1

K .

By Bayes’ rule, Pr( θ =t|j,x )= 1+ tγb jx

2 , t∈{− 1,+1 },

and hence 

E[θ |j,x ]= γb jx. 

Thus the posterior-optimal action is bjx. Its posterior payoff is (3+ γ)/4, whereas the payoff of the other action is (3 −γ)/4. In particular, OPT( qb)= 3+ γ

4 .

For every deterministic policy h, define its fraction of incorrect signalwise choices by 

eb(h):= 1

K

> m

X

> j=1

X

> x∈{− 1,+1 }

1{h(j,x )̸ =bjx}.

Then VAL h(qb)OPT( qb) = 

> 3+ γ
> 4

− γ 

> 2

eb(h)

> 3+ γ
> 4

=1 − 2γ

3+ γ eb(h). (22) A fixed-training-sample forward-sampling selector with sample size N is a randomized learning rule that observes, for each state θ, N independent draws from q(·| θ) and has no other access to the experiment. From this training transcript, including any auxiliary randomness, it produces a deterministic policy bα. The transcript is held fixed throughout evaluation: on receiving a query signal, the policy makes no additional calls to the sampler. 

Proposition 2 (Monte Carlo ( c,δ )-PAC sample lower bound) . There is a universal con-stant C0 >0 with the following property. For every even K ≥2, every c∈[31 /32 ,1) , and ev-

42 ery δ ∈(0 ,1/64] , set γ :=16(1 −c). Any fixed-training-sample forward-sampling selector that uses N independent samples from each state in (21) and satisfies the (c,δ )-PAC guarantee 

Pr[VAL bα(qb)≥cOPT( qb)] ≥1−δ

uniformly over b∈{− 1,+1 }K/ 2 must have 

N ≥C0

K +log(1 /δ )(1 −c)2 . (23) 

Proof of Proposition 2. Since c≥31 /32, we have γ = 16(1 −c) ≤ 1/2. We prove the K

and confidence terms separately. 

Step 1: Bound the divergence between neighboring hidden vectors. 

Let b(j) be obtained from b by flipping only coordinate j. For either fixed state θ, the two conditional signal distributions differ only on ( j, −1) and ( j, +1). We can directly calculate the KL divergence, 

DKL (qb(·| θ)∥qb(j) (·| θ))= 2γK log 1+ γ

1−γ ≤ 8γ2

K . (24) The inequality uses log((1+ γ)/(1 −γ)) ≤ 4γ for γ ≤ 1/2. For hidden vector b, let the complete training transcript be 

Tb :=  R, (Sθ,i )θ∈{− 1,+1 },1≤i≤N

,

where Sθ, 1,...,S θ,N are independent draws from qb(·| θ) and R contains any auxiliary ran-domness used to form the fixed policy. The random variable R is independent of the train-ing observations and has the same distribution under every hidden vector. Let Pb denote the distribution of Tb. The resulting fixed policy is a deterministic function of Tb. Since R

contributes zero relative entropy, additivity over the independent observations and (24) give 

DKL (Pb∥Pb(j) )= X

> θ∈{− 1,+1 }

ND KL (qb(·| θ)∥qb(j) (·| θ)) 

≤2N 8γ2

K = 16 Nγ 2

K .

(25) 

Step 2: Obtain the bound for K.

From the induced deterministic selector define an estimator of the hidden vector by 

bbj := bα(j, +1) , j =1 ,...,m. 

Every coordinate error bbj̸ =bj makes the policy choose the wrong action at signal ( j, +1). Therefore 

eb(bα)≥ 1

K dH(bb,b ) (26) where dH(bb,b ) is the Hamming distance between bb and b.Let b be uniformly distributed over the hypercube of hidden vectors. Pairing b with 43 b(j) and using the elementary testing bound that the sum of the two errors is at least one minus total variation yields 

EbEP

> b

dH(bb,b )≥ m

2



1−max  

> b,j

dTV (Pb,Pb(j) )



.

By Pinsker’s inequality and (25), 

dTV (Pb,Pb(j) )≤

r8Nγ 2

K .

If N ≤K/ (128 γ2), this distance is at most 1 /4. Since m=K/ 2, (26) and the preceding display then imply 

EbEP

> b

eb(bα)≥ 316 . (27) On the other hand, (22) shows that whenever the ( c,δ )-PAC guarantee succeeds, 

eb(bα)≤ (1 −c)(3+ γ)2γ = 3+ γ

32 ≤ 764 .

Since 0 ≤eb(bα)≤1 and δ ≤1/64, it follows for every b that 

EP

> b

eb(bα)≤ 764 +δ ≤ 18.

This contradicts (27). Hence 

N > K

128 γ2 =Ω 

 K

(1 −c)2



. (28) 

Step 3: Obtain the confidence term. 

Fix any b and consider the binary testing problem 

Hb :T ∼Pb versus H−b :T ∼P−b.

The posterior-optimal action at signal ( j,x ) is bjx under qb and −bjx under q−b. Since the action set is {− 1,+1 }, every policy h therefore satisfies, signal by signal, 

1{h(j,x )̸ =bjx}+1{h(j,x )̸ =−bjx}=1 .

Averaging this identity over the K signals gives 

eb(h)+ e−b(h)=1 . (29) Given a realized transcript T , let hT be the fixed policy produced by the training rule. Define the test 

ϕ(T ):= 



b, if eb(hT )≤e−b(hT ),

−b, otherwise .

This is a valid information-theoretic test because the two candidate vectors are fixed and 

hT is a total policy on the finite signal set. By (22), a policy satisfying the multiplicative guarantee under qb obeys 

eb(hT )≤ (1 −c)(3+ γ)2γ = 3+ γ

32 ≤ 764 < 12.

Equation (29) then implies e−b(hT ) > 1/2, so the test selects b. Hence the uniform 44 (c,δ )-PAC guarantee gives 

Pb[ϕ(T )= −b]≤Pb[VAL hT (qb)<c OPT( qb)] ≤δ. 

The same argument with b and −b interchanged gives 

P−b[ϕ(T )= b]≤δ. 

We next bound the divergence between the two transcript distributions. For either fixed state θ, the two signal probabilities are interchanged within every pair ( j, −1) ,(j, +1). The contribution of each pair to relative entropy is the expression in (24); summing over the m=K/ 2 coordinates gives 

DKL (qb(·| θ)∥q−b(·| θ))= 

> m

X

> j=1

2γK log 1+ γ

1−γ

=γlog 1+ γ

1−γ .

There are N independent observations from each of the two states, while the auxiliary randomness again contributes zero relative entropy. Consequently, 

DKL (Pb∥P−b)= X

> θ∈{− 1,+1 }

ND KL (qb(·| θ)∥q−b(·| θ)) =2 Nγ log 1+ γ

1−γ ≤8Nγ 2,

where the inequality again uses log((1+ γ)/(1 −γ)) ≤4γ. The Bretagnolle–Huber testing inequality [Bretagnolle and Huber, 1979], applied to the test ϕ, now gives 2δ ≥Pb[ϕ(T )= −b]+ P−b[ϕ(T )= b]≥ 12exp( −DKL (Pb∥P−b)) ≥ 12e−8Nγ 2

.

Equivalently, e−8Nγ 2

≤4δ, and hence 

N ≥ 18γ2 log 14δ = 12048(1 −c)2 log 14δ =Ω 

log(1 /δ )(1 −c)2



. (30) The final comparison uses γ = 16(1 − c) and δ ≤ 1/64, which implies log(1 /(4 δ)) ≥

(2 /3)log(1 /δ ). Taking the maximum of (28) and (30), and using max {x,y }≥ (x+y)/2, proves (23) after adjusting the universal constant C0.

# C Preliminaries 

This section gives an informal, self-contained guide to the computational concepts used in the paper. 45 C.1 Circuits and representations 

A Boolean circuit is a finite directed acyclic network of elementary logical operations. Its input wires carry bits, each internal node (or gate ) applies a fixed Boolean operation such as And , Or , or Not to the bits on its incoming wires, and designated output wires return one or more bits. Acyclicity allows the gates to be evaluated in an order in which every input to a gate has already been computed. Other standard finite, functionally complete collections of gates are equivalent for our purposes, because one such gate basis can be translated into another with at most polynomial overhead. A randomized Boolean circuit has, in addition to its ordinary input wires, random input wires containing independent fair bits. Conditional on the ordinary input and on a realization of these random bits, the circuit is deterministic. In our model the ordinary input is a fixed binary encoding of the state θ, the random inputs form the seed R, and the output bits encode the signal s=q(θ;R). If the circuit uses rq random bits, then 

q(s|θ)=2 −rq {R∈{ 0,1}rq :q(θ;R)= s} .

Thus, one forward draw is easy to generate by choosing R and evaluating the circuit, even though computing the likelihood of an observed signal may require aggregating over exponentially many seeds. This difference between forward simulation and likelihood evaluation is central to the paper. 

Gate-list encoding. Every acyclic circuit can be numbered in topological order. A standard gate-list description records the numbers and roles of the ordinary inputs, random inputs, and outputs and, for each gate, its type and the indices of the earlier wires that feed it. The resulting binary string is denoted by ⟨q⟩. With a bounded-fan-in gate basis, each predecessor index requires only logarithmically many bits, so the gate-list length is polynomially related to the number of gates and wires. The represented circuit can therefore be evaluated in time polynomial in |⟨ q⟩| . The precise reasonable choice of gate basis, numbering convention, or gate-list format does not affect any polynomial-time classification, since standard choices can be translated into one another in polynomial time. 

Generality of the representation. Circuit representation is not tied to a particular eco-nomic or statistical mechanism. States, records, messages, and actions can all be encoded 46 as bit strings, and circuits can compose arbitrary finite logical and arithmetic subroutines on those encodings. More generally, at any fixed input length, a deterministic machine or algorithm that runs for at most a bounded number of steps can be unrolled into a Boolean circuit whose size is polynomial in that running time; a randomized algorithm is repre-sented in the same way by exposing its random tape as additional input wires. Conversely, an explicitly given circuit can be evaluated efficiently from its gate list. Hence, circuits rep-resent general finite, bounded-time processes, machines, and algorithms up to polynomial overhead, which is the level of generality relevant to the complexity classes used below. 

# C.2 Complexity classes 

Complexity classes compare how the resources needed to solve a computational yes-or-no problem grow with the bit length n of its input. Here polynomial time means that the number of elementary computational steps is bounded by Cn k for some constants 

C and k independent of the instance. 

Deterministic and verifiable computation. The class P consists of problems for which a deterministic algorithm can decide the correct answer in polynomial time. The class NP consists of problems for which every yes-instance has a certificate of polynomial length whose validity can be checked deterministically in polynomial time. Boolean satisfiability ( SAT )–deciding whether a Boolean formula has at least one satisfying assignment–is a canonical NP -complete problem. 

Randomized computation. The class RP consists of problems admitting a random-ized polynomial-time algorithm with one-sided error: it never accepts a no-instance, and it accepts every yes-instance with probability at least 1 /2. The particular fixed positive constant can be changed by independent repetition. The class BPP allows two-sided error: a randomized polynomial-time algorithm gives the correct answer with probability at least 2 /3 on every instance. Again, any fixed success probability strictly above 1 /2gives the same class because repetition can amplify the success probability. The class PP also uses randomized polynomial time, but it requires only a strict majority of accepting random tapes: a yes-instance is accepted with probability greater 47 than 1 /2, whereas a no-instance is accepted with probability at most 1 /2. In Majsat , one asks whether a Boolean formula is satisfied by strictly more than half of all assignments; this is a canonical PP -complete problem [Gill, 1977]. 

Statistical zero knowledge. The class SZK consists of problems having interac-tive proofs in which a computationally unbounded prover can convince a randomized polynomial-time verifier of a yes-instance, a cheating prover cannot convincingly establish a no-instance except with bounded probability, and the interaction reveals essentially no addi-tional information: on yes-instances, the verifier’s view can be efficiently simulated without the prover up to negligible statistical distance. A canonical SZK -complete problem is Sta-tistical Difference [Sahai and Vadhan, 2003]. Given descriptions of two sampling circuits, it asks whether their output distributions are far apart or close in total variation distance. 

Containments and standard conjectures. The unconditional containments relevant to this paper include 

P⊆RP ⊆BPP ⊆PP , RP ⊆NP ⊆PP , BPP ⊆SZK .

It is widely conjectured that P̸ =NP . Since P⊆NP ⊆PP , that conjecture implies P̸ =PP ,so the latter is a logically weaker separation assumption. It is also widely conjectured that randomization does not enlarge polynomial-time computation, namely P=BPP ; this would imply RP =P and, together with P̸ =NP , would imply RP ̸ =NP . Finally, the paper assumes BPP ̸ =SZK for its ex ante lower bounds. Because BPP ⊆SZK , this assumption says that the containment is strict; as noted in the main text, it is a stronger assumption than P̸ =NP [Sahai and Vadhan, 2003]. 48