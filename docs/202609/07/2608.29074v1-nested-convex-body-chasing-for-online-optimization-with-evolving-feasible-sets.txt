Title: Nested Convex-Body Chasing for Online Optimization with Evolving Feasible Sets

URL Source: https://arxiv.org/pdf/2608.29074v1

Published Time: Tue, 01 Sep 2026 00:29:43 GMT

Number of Pages: 29

Markdown Content:
# NESTED CONVEX -B ODY CHASING FOR ONLINE OPTIMIZATION WITH EVOLVING FEASIBLE SETS 

Dhruv Sarkar 1 Aprameyo Chakrabartty 2 

> 1

Massachusetts Institute of Technology, Cambridge, MA  

> 2

Purdue University, West Lafayette, IN 

dhruv.sarkar@mit.edu chakr190@purdue.edu 

August 28, 2026 

# ABSTRACT 

We study online optimization under nested, shrinking feasible regions in two models: convex opti-mization with nested evolving feasible sets ( CONES ) and adversarial constrained online convex optimization ( COCO ). Our algorithms separate loss control from geometric movement control. Constrained minimizers and cumulative-loss tests preserve the relevant regret budget, while a de-terministic resettable nested convex-body chaser controls movement between resets. For CONES with a G-Lipschitz, μ-strongly convex objective on a diameter-D domain, we run the chaser on intersections of the current feasible set with adaptively chosen objective sublevel sets. Using the known Euclidean chasing ratio O(pd log(1 + d)) , the resulting algorithm has nonpositive regret at every prefix and movement 

O pd log(1 + d)

s

GD log( eT )

μ

!

.

More finely, the movement adapts to the total increase in the constrained optimum value. Already in dimension two, with all geometric and objective parameters fixed independently of T , every ran-domized algorithm with terminal expected regret O(T β ), for any fixed β < 1, incurs Ω( √log T )

expected movement on some deterministic nested sequence. Thus the horizon dependence is opti-mal. If the objective grows at least linearly with distance from each current constrained minimizer set, Steiner-point tracking gives movement independent of T .For general convex COCO , one-step-delayed chasing with regularized-leader resets gives 

Reg T = O



Gf Dpd log(1 + d) √T



, CCV T = O



Gg Dpd log(1 + d) √T



.

For strongly convex losses, both quantities are O(d log(1 + d) log( eT )) when the remaining prob-lem parameters are fixed. Across these results, the nested-chasing reduction replaces the O(dd/ 2)

projection-path factor in the prior CONES and COCO analyses by the polynomial dependence inherited from Euclidean nested convex-body chasing. arXiv:2608.29074v1 [cs.AI] 29 Aug 2026 A PREPRINT - A UGUST 28, 2026 

# Contents 

1 Introduction 32 Related work 53 Preliminaries 64 Endpoint control for nested-body chasing 85 Applications to CONES 96 One-step-delayed chasing for COCO 12 7 Conclusion 15 A Detailed comparison with the two versions of the CONES paper 17 B Technical details for nested-body chasing 18 C Deferred proofs for CONES 18 D Deferred proofs for COCO 24 

2A PREPRINT - A UGUST 28, 2026 

# 1 Introduction 

Online optimization is often studied in settings where the feasible region is fixed. Classical online convex optimization is the canonical example: the learner repeatedly chooses a point from a fixed convex set, observes a loss, and tries to compete with the best fixed decision in hindsight. In many applications, however, the feasible region itself evolves over time. A safety requirement may rule out a previously available decision, a resource constraint may become tighter, or a planning problem may receive new information that restricts the set of admissible actions. When the feasible regions are nested, the learner faces a geometric problem in addition to the usual optimization problem: it must move through a shrinking sequence of convex sets without paying too much movement. We study two models in which this nested geometry is central. The first model is convex optimization with nested evolving feasible sets, abbreviated CONES . In this model, the objective function is fixed and known from the be-ginning, while the feasible set is revealed gradually as a nested sequence. At each round, the learner observes the current feasible set and then chooses a feasible point. The goal is to obtain small regret relative to the final constrained optimum while keeping the total movement small. The second model is constrained online convex optimization, ab-breviated COCO . In COCO , the learner chooses an action before seeing the current loss and constraint. After the action is played, the loss and constraint are revealed, and the cumulative feasible set is updated. Thus the learner can-not generally satisfy the newly revealed constraint on the same round. We treat both the general convex-loss regime, where the optimal regret scale is √T , and the strongly convex-loss regime, where logarithmic regret is possible. In both cases the goal is to control regret against a fixed action feasible for all constraints and cumulative constraint violation. Although these two models differ in their information pattern, they share the same geometric backbone. The feasible sets form a nested chain. In CONES this chain is observed before the learner acts. In COCO it is learned with one-round delay. The main question in both settings is how to construct a path through the shrinking feasible regions that has small movement while still respecting the loss objective. Our central idea is to separate these two tasks. Loss control is handled through constrained minimizers, cumulative-loss budgets, and reset rules. Movement control is delegated to nested convex-body chasing. Nested convex-body chasing is an online geometric problem. An algorithm receives a nested sequence of compact convex bodies and, after seeing each body, must choose a point in it. The objective is to minimize total movement. Competitive algorithms for this problem give movement guarantees relative to the best offline path. We use such algorithms as black-box movement-control primitives. This is useful because the best known Euclidean nested-body chasers have polynomial dependence on the dimension, whereas more direct geometric arguments based on greedy projections or path-length estimates can have significantly worse dimension dependence. 

Main ideas. A natural first attempt is to run a nested-body chaser on the feasible sets and play its outputs. This alone is insufficient: the chaser controls movement but has no access to the objective. The main algorithmic challenge is therefore to determine when a proposed point preserves the relevant cumulative-loss budget and when the algorithm should reset to an optimizer. For strongly convex CONES , let vt denote the minimum of the fixed objective over the current feasible set. A phase begins at a constrained minimizer at round r. Writing dt = vt − vr , the algorithm starts a sublevel epoch when dt first becomes positive, sets its scale to R = dt, and feeds the chaser the nested requests 

St ∩ { x : f (x) ≤ vr + 2 R} .

Whenever dt > 2R, a new epoch begins at scale dt; hence the scales more than double. Strong convexity converts each scale R into an O(pR/μ ) distance bound, and singleton augmentation controls both movement within the epoch and the terminal jump. Summing the square roots of the geometrically increasing scales gives a phase cost proportional to the square root of the increase in the constrained optimum. A cumulative-loss test permits a reset only at a round 

s > 2r, so there are only O(log T ) phases. This yields the O(√log T ) upper bound, while a matching lower bound establishes the optimal horizon dependence. Under uniform linear growth away from the current constrained minimizer set, a direct Steiner-point strategy is avail-able. The learner plays the Steiner point of that set, attaining the current constrained optimum exactly. One-sided control of the set-valued minimizers then gives a movement bound independent of T .For general convex COCO , the losses provide no uniform curvature, so we add a fixed quadratic regularizer to the cumulative leader. The algorithm maintains a post-update feasible point and plays it with one-round delay. A nested-body chaser proposes feasible points, and a regularized cumulative-loss budget decides whether to retain the proposal or reset to the regularized constrained leader. Balancing the regularizer against the chasing movement yields O(√T )

regret and cumulative constraint violation without an additional log T factor. 

3A PREPRINT - A UGUST 28, 2026 

For strongly convex COCO , the same one-step-delayed feasible sequence makes both regret and current constraint violation reducible to its movement. Chaser proposals are retained only while the post-update cumulative loss remains below the current constrained cumulative-loss minimum; otherwise the algorithm resets to that minimizer. Retaining the resulting nonnegative slack strengthens the reset inequality, and strong convexity yields logarithmic movement, regret, and cumulative constraint violation. 

1.1 Main results 

We summarize the main guarantees. Throughout, D is an upper bound on the diameter of the decision set, and ρd is the competitive-ratio guarantee of the fixed Euclidean nested-body chaser used by the algorithms. For CONES , let 

vt := min 

> x∈St

f (x), ∆v := vT − v1, MT := 

> T

X

> t=2

∥xt − xt−1∥ .

If an initial point x0 ∈ X is prescribed before S1 is revealed, at most an additional D must be added to the movement. 

Strongly convex CONES . For a G-Lipschitz, μ-strongly convex objective, budgeted adaptive sublevel-set chasing has nonpositive regret at every prefix and, for a universal constant C > 0,

MT ≤ Cρ d

s

∆v (1 + log T )

μ ≤ Cρ d

s

GD log( eT )

μ .

With the known Euclidean ratio ρd = O(pd log(1 + d)) , this becomes 

MT = O pd log(1 + d)

s

GD log( eT )

μ

!

.

Already in dimension two, with the diameter, Lipschitz constant, and strong-convexity parameter fixed, every random-ized algorithm with terminal expected regret at most RT has expected movement 

Ω

r

log TRT + 1 

!

on some deterministic nested sequence. Hence the √log T dependence is optimal for every guarantee RT = O(T β )

with fixed β < 1.

Uniform linear growth in CONES . If 

f (x) − vt ≥ α dist( x, At), At := arg min 

> y∈St

f (y),

then Steiner-point tracking has nonpositive regret and 

MT ≤ dD + 2dα ∆v ≤ dD + 2dGD α ,

which is independent of T .

General convex COCO . For convex Gf -Lipschitz losses and Gg -Lipschitz scalar constraints, the regularized one-step-delayed algorithm gives 

Reg T = O



Gf Dρ d

√T



, CCV T = O



Gg Dρ d

√T



.

Substituting the known Euclidean ratio yields 

Reg T = O



Gf Dpd log(1 + d) √T



, CCV T = O



Gg Dpd log(1 + d) √T



.

The formal theorem also gives a second tuning that removes the dimension factor from the growing √T term in cumulative constraint violation at the cost of a larger regret factor. 

4A PREPRINT - A UGUST 28, 2026 

Strongly convex COCO . For Gf -Lipschitz, μ-strongly convex losses and Gg -Lipschitz scalar constraints, the one-step-delayed chasing algorithm satisfies 

Reg T = O

"

Gf Dpd log(1 + d) + G2

> f

μ d log(1 + d)

#

log( eT )

!

and 

CCV T = O

 

Gg Dpd log(1 + d) + Gf Gg

μ d log(1 + d)



log( eT )



.

Thus both quantities are logarithmic in T and have explicit polynomial dependence on d.

Organization. Section 2 discusses related work. Section 3 gives notation, the two models, and the nested-body chasing primitive. Section 4 gives the endpoint-control lemma used throughout the paper. Section 5 applies the framework to CONES . Section 6 applies the chasing framework to COCO , first for strongly convex losses and then for general convex losses. Section 7 summarizes the implications and open directions. Detailed proofs are deferred to the appendices. 

# 2 Related work 

Online convex optimization and long-term constraints. Online convex optimization provides the standard model for sequential convex decision-making with adversarial losses; see Hazan [5] for a textbook treatment. A large body of work extends this model to settings with constraints that may be violated in the short term but should be controlled over time. Early work on online convex optimization with long-term constraints includes primal-dual and penalty-based methods such as Mahdavi et al. [8]. In adversarial COCO , the learner chooses a point before observing both the loss and the constraint, and performance is measured by regret together with cumulative constraint violation. Sinha and Vaze [12] obtained near-optimal guarantees through a Lyapunov-weighted surrogate loss. Subsequent work studied instance-dependent and projection-based guarantees; in particular, Vaze and Sinha [13] emphasized the role of the nested feasible sets generated by revealed constraints. The present work develops a chasing-based route: a competitive nested-body chaser controls feasible-set movement, while constrained minimization and budget invariants control loss. 

Nested projections, self-contracted curves, and geometric movement bounds. Projection-based methods exploit the nested cumulative feasible sets by repeatedly projecting onto the newest set. Their trajectories are related to self-contracted curves, whose finite-length theory goes back to Manselli and Pucci [9]. This viewpoint underlies the COCO guarantees of Sarkar and Sinha [10], which establish O(√T ) regret and cumulative constraint violation for convex losses and logarithmic guarantees for strongly convex losses. Our analysis recovers these horizon orders through nested-body chasing and replaces projection-path finite-length constants, which can grow superpolynomially with dimension, by the polynomial factor ρd.

Nested convex-body chasing. Convex-body chasing was introduced by Friedman and Linial [4]. For nested re-quests, Bansal et al. [2] proved chaseability, Argue et al. [1] obtained a nearly linear dependence on dimension in general norms, and Bubeck et al. [3] gave the nearly optimal Euclidean ratio 

ρd = O

p d log(1 + d)



.

We use the latter algorithm as a deterministic black-box subroutine. Singleton augmentation—appending a single-ton request at a desired reset point only for the analysis—allows one competitive guarantee to control both within-phase movement and the final reset jump. Because our requests may be lower-dimensional, we also give a lower-dimensional-request extension of the full-dimensional chasing theorem. 

Optimization with nested evolving feasible sets. The CONES model was introduced by Krishna et al. [6, 7]. Be-cause arXiv:2605.07386 changed materially between its first and second versions, we distinguish their formal results. Version 1 proposed O(log T ) upper and lower bounds for smooth strongly convex objectives and uniformly sharp ob-jectives. Its strongly convex lower-bound parameters do not permit a growing number of phases when the remaining problem parameters are fixed, and its proposed sharp instance does not satisfy the stated uniform sharpness condition. Version 2 repairs the strongly convex construction and proves the formal lower bound Ω( plog T / log log T ) under a regret condition imposed at every prefix. The algebraic and geometric details are given in Section A. 

5A PREPRINT - A UGUST 28, 2026 

Version 2 was posted on August 16, 2026. The adaptive sublevel-set upper bound and lower-bound construction in this paper were developed independently and concurrently with that revision. Relative to Version 2, our upper bound improves the horizon dependence from O(log T ) to O(√log T ), replaces the O(dd/ 2) projection-path factor by 

O(pd log(1 + d)) , and removes smoothness. Our lower bound improves the rate to Ω( √log T ), applies to randomized algorithms, and requires only a terminal expected-regret guarantee. The adversarial sequence is deterministic once the algorithm is fixed and does not depend on its realized random seed.                          

> Version 2 This paper
> Strongly convex upper bound O(log T)horizon dependence O(√log T)horizon dependence Dimension factor O(dd/ 2)O(pdlog(1 + d))
> Smoothness Required Not required Strongly convex lower bound Ω( plog T / log log T)Ω( √log T)
> Regret condition in lower bound Every prefix Terminal expectation; randomized algorithms

For objectives with uniform linear growth away from each current minimizer set, our Steiner-point algorithm gives movement at most 

dD + 2dα (vT − v1) ≤ dD + 2dGD α ,

which is independent of T . Hence a lower bound growing with the horizon cannot hold under this sharpness condition. 

Relation to convex-loss COCO guarantees. First-order methods attain O(√T ) regret and O(√T ) cumulative con-straint violation in the convex-loss regime. In particular, the method of Sarkar and Sinha [10] attains these rates without an additional log T factor. Our result reaches the same horizon order through a different geometric mechanism and makes the dimension dependence inherited from nested-body chasing explicit. The method uses exact regularized constrained minimization and tentative evaluation of a deterministic resettable nested-body chaser. A quadratic regularizer controls displacement between reset points, while singleton-augmented chasing controls both movement inside a phase and the terminal reset jump. This yields a self-contained chasing-based analysis that avoids projection-specific kinetic inequalities and finite-length constants. 

Relation between CONES and COCO . Although CONES and COCO both generate nested feasible sets, their information patterns differ substantially. In CONES the current feasible set is known before the action and the objective is fixed. In COCO the learner acts before seeing the current constraint, and the losses change over time. The delayed feasible-sequence algorithm in this paper can be viewed as a COCO analogue of the reset-based CONES 

idea: it uses chasing to control movement inside phases and resets to a constrained cumulative-loss minimizer when loss control would otherwise fail. The analysis is correspondingly more delicate because it must account for possible negative regret accumulated before a reset. The nonnegative slack variable in our proof records this effect and makes the reset inequality stronger. 

# 3 Preliminaries 

Throughout, ∥·∥ denotes the Euclidean norm. For a compact set K ⊆ Rd, let 

diam( K) := sup 

> x,y ∈K

∥x − y∥ .

For a point x and set K, write dist( x, K ) = inf y∈K ∥x − y∥. The Euclidean unit ball is denoted by B2. Throughout, 

D denotes a fixed upper bound on diam( X).

3.1 The CONES model 

In CONES , a compact convex set X ⊆ Rd and a fixed continuous convex objective f : X → R are known. At each round t, the learner observes a nonempty compact convex set St, where 

X = S0 ⊇ S1 ⊇ S2 ⊇ · · · ⊇ ST ,

and then chooses xt ∈ St. Define 

vt := min 

> x∈St

f (x), At := arg min 

> x∈St

f (x).

6A PREPRINT - A UGUST 28, 2026 

The regret benchmark is the final constrained optimum: 

Reg CONES  

> T

:= 

> T

X

> t=1

f (xt) − T v T .

The internal movement is 

MT := 

> T

X

> t=2

∥xt − xt−1∥ .

If a point x0 ∈ X is prescribed before S1 is revealed, we write 

MT (x0) := ∥x1 − x0∥ + MT .

Since x0, x 1 ∈ X, one always has MT (x0) ≤ D + MT .

3.2 The COCO model 

In COCO , the learner chooses xt ∈ X before observing the round-t loss and constraint. After xt is played, the adversary reveals a convex loss ft : X → R and one scalar convex constraint gt : X → R. The adversary may be adaptive; all guarantees below are deterministic and hold pathwise for every realized sequence satisfying common feasibility. Define 

S0 := X, St := St−1 ∩ { x ∈ X : gt(x) ≤ 0} .

We assume common feasibility, i.e. ST̸ = ∅. The regret and CCV are 

Reg T := 

> T

X

> t=1

ft(xt) − min 

> x∈ST
> T

X

> t=1

ft(x), CCV T := 

> T

X

> t=1

[gt(xt)] +.

We use the following regularity assumptions for the general convex-loss result. 

Assumption 3.1 (Convex COCO ). The set X ⊆ Rd is compact, convex, and has diameter at most D. Each loss ft is convex and Gf -Lipschitz on X. Each constraint gt is convex and Gg -Lipschitz on X. The cumulative feasible sets St

are nonempty. For the strongly convex result, we impose the stronger condition below. 

Assumption 3.2 (Strongly convex COCO ). Assumption 3.1 holds, and each loss ft is μ-strongly convex on X, with 

μ > 0.For strongly convex losses, let 

Ft := 

> t

X 

> τ=1

fτ , ut := arg min 

> x∈St

Ft(x), vt := Ft(ut).

The point ut is unique because Ft is μt -strongly convex. We shall repeatedly use the inequality 

Ft(y) − Ft(ut) ≥ μt 

2 ∥y − ut∥2 , y ∈ St. (1) A proof is given in Section D.2. 

3.3 Nested convex-body chasing 

We now describe the geometric primitive used throughout the paper. The input to this primitive is a nested sequence of convex sets. At each step, the algorithm must choose a point in the newest set, and its goal is to keep the total movement small. Concretely, suppose a point starts at a ∈ Rd, and then a sequence of nonempty compact convex sets is revealed: 

K1 ⊇ K2 ⊇ · · · ⊇ Kn.

After seeing Ki, the chaser must output a point yi ∈ Ki. The quality of the chaser is measured by comparing its movement to the best offline path that knew the entire sequence in advance. 

7A PREPRINT - A UGUST 28, 2026 

Definition 3.3 (Resettable nested-body chaser) . Fix a starting point a ∈ Rd. A nested-body chaser is an online rule that, for every nested sequence of nonempty compact convex requests 

K1 ⊇ K2 ⊇ · · · ⊇ Kn,

outputs yi ∈ Ki after observing Ki. We write y0 = a. The chaser is ρd-competitive if, for every such request sequence, nX

> i=1

∥yi − yi−1∥ ≤ ρd inf 

> wi∈Ki
> w0=an

X

> i=1

∥wi − wi−1∥ .

A family of chasers is called resettable if a fresh instance of the same rule can be started from any prescribed point 

a ∈ Rd.Here and throughout, ρd denotes the competitive-ratio guarantee of the specific chaser used by our algorithms, rather than the optimal ratio over all chasers. We may and do take ρd ≥ 1, since enlarging a valid competitive-ratio guarantee preserves its validity. Resetability is essential because both the CONES and COCO algorithms operate in phases. At a reset, the algorithm moves to a point obtained by constrained optimization and starts a fresh chaser there. This movement remains part of the algorithm’s trajectory and is charged in full; resetability only permits the geometric subroutine to begin a new phase from the reset point. We also require the chaser to be deterministic. Given the retained request history and the current request, its next output is uniquely determined and can therefore be evaluated tentatively. If the proposal passes the acceptance test, the current request is retained in the phase history. Otherwise the tentative update is discarded and the algorithm either keeps the previous history or starts a new phase. This evaluation uses only the current and past requests and reveals no future information. The known Euclidean nested-body chasing guarantee is usually stated for convex bodies with nonempty interior. We will also need to use requests that may have empty interior, such as a line segment, a lower-dimensional face, or a singleton {b}. We therefore use a compactness argument: thicken each compact convex request K to K + δB 2, run the full-dimensional chaser, and take a limit as δ ↓ 0. This preserves the same competitive ratio. The details are given in Section B.1. 

Theorem 3.4 (Euclidean nested-body chasing) . There exists a deterministic resettable nested-body chaser in Eu-clidean space with competitive ratio 

ρd = O

p d log(1 + d)



.

After the lower-dimensional-request extension in Section B.1, the chaser may be used on arbitrary nonempty compact convex requests, including lower-dimensional sets and singletons. 

The full-dimensional version of Theorem 3.4 is due to Bubeck et al. [3]. We use it as a black-box geometric subroutine. 

Access assumptions and computational scope. Our results are information-theoretic. We assume exact access to constrained minimizers, minimizer sets, and Steiner points. We also treat the deterministic chaser as a functional subroutine whose next output can be evaluated tentatively from its retained request history. If the proposal is accepted, that request is retained; after rejection, the algorithm starts a fresh chaser at the reset point. The lower-dimensional-request extension in Section B.1 is an existence argument. Efficient realization of this extension and of the optimization primitives under standard oracle access remains a separate computational question. 

# 4 Endpoint control for nested-body chasing 

We use one basic consequence of nested-body chasing throughout the paper. If a phase starts at a point a and later has a distinguished feasible point b, then we can analyze the chaser as if the singleton request {b} were appended at the end of the phase. This augmentation is used only in the analysis; the algorithm need not know b in advance. Because the chaser is deterministic and online, appending the future singleton request leaves all earlier outputs unchanged. 

Lemma 4.1 (Terminal-point control by singleton augmentation) . Let a ρd-competitive chaser start from a, and let 

K1 ⊇ K2 ⊇ · · · ⊇ Kn

8A PREPRINT - A UGUST 28, 2026 

be compact convex requests with outputs y1, . . . , y n. If b ∈ Kn, then 

> n

X

> i=1

∥yi − yi−1∥ + ∥yn − b∥ ≤ ρd ∥a − b∥ , y0 = a. 

Consequently, for every j ≤ n,

∥yj − b∥ ≤ ρd ∥a − b∥ .

Proof. Consider the augmented request sequence 

K1 ⊇ K2 ⊇ · · · ⊇ Kn ⊇ { b}.

This is a valid nested request sequence because b ∈ Kn. Since the chaser is deterministic and online, the outputs on the first n requests are still y1, . . . , y n.The offline path that moves directly from a to b and then stays at b is feasible for the augmented sequence, because 

b ∈ Kn ⊆ Ki for every i ≤ n. Its total movement is ∥a − b∥. Competitiveness gives 

> n

X

> i=1

∥yi − yi−1∥ + ∥yn − b∥ ≤ ρd ∥a − b∥ .

This proves the first claim. For the second claim, apply the same argument to the prefix K1 ⊇ · · · ⊇ Kj and then append {b}. This is valid because b ∈ Kn ⊆ Kj . Dropping the nonnegative movement sum gives ∥yj − b∥ ≤ 

ρd ∥a − b∥.

# 5 Applications to CONES 

We now apply the toolkit to the model in which the objective is fixed and the current feasible set is known before the learner acts. 

5.1 Strongly convex objectives 

Assume that f is G-Lipschitz and μ-strongly convex on X. For every t, define the constrained minimizer and value 

ut := arg min 

> x∈St

f (x), vt := f (ut).

The values vt are nondecreasing because the feasible sets are nested. The algorithm starts each phase at a constrained minimizer. Consider a phase beginning at round r, and write dt := 

vt − vr . While dt = 0 , the proposal remains at ur . At the first round of a nontrivial sublevel epoch, set R := dt, start a fresh chaser from the preceding proposal, and feed it the requests 

Kt := St ∩ { x : f (x) ≤ vr + 2 R} . (2) The sublevel set is fixed within an epoch, so the requests are nested. When dt > 2R, the current epoch ends before round t, and a new epoch begins with scale R := dt. Thus successive scales more than double. This adaptive sublevel-set procedure depends only on information available at the current round. The full algorithm tentatively evaluates the next proposal and retains it only if the cumulative-loss budget Ct ≤ tv t

remains valid. Otherwise it resets to ut and starts a new phase. The following lemma isolates the movement and loss properties of the sublevel-set procedure. 

Lemma 5.1 (Adaptive sublevel-set chasing) . Let 

Sr ⊇ Sr+1 ⊇ · · · ⊇ Ss

be nonempty compact convex sets, and let f be μ-strongly convex. Define 

ut := arg min 

> x∈St

f (x), vt := f (ut),

and set 

a := ur , b := us, ∆ := vs − vr .

9A PREPRINT - A UGUST 28, 2026 

The adaptive sublevel-set procedure above, using a resettable ρd-competitive nested-body chaser, produces points 

yt ∈ St, t = r + 1 , . . . , s , with yr = a, such that 

f (yt) − vs ≤ ∆ for every t = r + 1 , . . . , s, 

and sX

> t=r+1

∥yt − yt−1∥ + ∥ys − b∥ ≤ Csub ρd

s

∆

μ ,

where Csub > 0 is universal; one may take Csub = 12 . The endpoint s, the point b, and the quantities vs, ∆ occur only in the guarantee and are not used by the online procedure. 

To see the mechanism behind the lemma, consider an epoch of scale R. Strong convexity places both its starting point and a terminal feasible minimizer within O(pR/μ ) of the phase minimizer. Singleton augmentation therefore bounds the epoch movement, including its terminal distance, by O(ρd

pR/μ ). Since the epoch scales more than double, 

X

> e

pRe = O(√∆ ) ,

which gives the stated movement bound. Membership in the sublevel request (2) gives the loss bound. A complete proof appears in Section C.1. 

Algorithm 1 Budgeted adaptive sublevel-set chasing for strongly convex CONES 

Input: Nested sets St, objective f , resettable chaser C HASE  

> 1:

Observe S1, compute u1 ← arg min x∈S1 f (x), and set v1 ← f (u1). 

> 2:

Play x1 ← u1, set C1 ← v1, and start a phase at r ← 1. 

> 3:

Initialize the adaptive sublevel-set proposal procedure at u1. 

> 4:

for t = 2 , 3, . . . , T do  

> 5:

Observe St, compute ut ← arg min x∈St f (x), and set vt ← f (ut). 

> 6:

Tentatively evaluate the sublevel-set procedure for the current phase, obtaining yt ∈ St. 

> 7:

if Ct−1 + f (yt) ≤ tv t then  

> 8:

Accept: play xt ← yt and retain the updated proposal history.  

> 9:

else  

> 10:

Reset: play xt ← ut, discard the tentative update, set r ← t, and restart the proposal procedure at ut. 

> 11:

end if  

> 12:

Set Ct ← Ct−1 + f (xt). 

> 13:

end for Theorem 5.2 (Strongly convex CONES upper bound) . Suppose f is G-Lipschitz and μ-strongly convex on a compact convex set X of diameter at most D. Algorithm 1 satisfies nonpositive prefix regret, 

> t

X 

> τ=1

f (xτ ) − tv t ≤ 0 for every t ≤ T, 

and internal movement 

MT ≤ Cρ d

s

(vT − v1)(1 + log T )

μ ≤ Cρ d

s

GD log( eT )

μ .

Here C > 0 is a universal constant. If an initial point x0 ∈ X is prescribed, then MT (x0) is bounded by the same right-hand side plus D. With the Euclidean chaser of Theorem 3.4, 

MT ≤ Cpd log(1 + d)

s

GD log( eT )

μ ,

which gives the displayed polynomial dependence on the dimension. 

The proof is given in Section C.2. The main point is that if a phase begins at round r and ends with a reset at round s,the sublevel-set requests force s > 2r. The movement in each phase is bounded by a constant multiple of the square root of the increase in the constrained optimum over that phase. Summing over phases gives the O(√log T ) bound. 

10 A PREPRINT - A UGUST 28, 2026 

We next show that this dependence on the horizon is optimal even when the dimension and all geometric and objective parameters are fixed. The lower bound is stated for randomized algorithms with an expected terminal-regret guarantee. For every fixed nested sequence, convexity allows a randomized policy to be replaced by its mean policy without increasing either expected terminal loss or expected movement; this reduction is proved in Section C.3. 

Theorem 5.3 (Terminal-regret lower bound for strongly convex CONES ). There exist a fixed compact convex domain 

X = [ −1, 1] × [1 , 2] ,

a fixed objective 

f (x) = 12 ∥x∥2 ,

and a universal constant c > 0 such that the following holds. Fix a horizon T and a number RT ≥ 0. Suppose a randomized feasible CONES algorithm for this fixed domain and objective satisfies 

E

" TX

> t=1

f (Xt) − T v T

#

≤ RT

for every nested sequence of nonempty compact convex feasible sets contained in X and having length T . If T / (RT +1) is sufficiently large, then there is a deterministic nested feasible-set sequence, constructed for that algorithm but independent of its realized random seed, for which 

E[MT ] ≥ c

s

log 

 TRT + 1 



.

Moreover, diam( X) = √5, f is 1-strongly convex, and f is √5-Lipschitz on X, all independently of T and RT . In particular, if RT ≤ Creg T β for fixed Creg > 0 and β ∈ [0 , 1) , then 

E[MT ] = Ω( plog T ),

where the implicit constant may depend on Creg and β.

The proof is given in Section C.4. The construction uses K = Θ(log( T / (RT + 1))) nested halfspaces whose con-strained minimizers alternate horizontally by Θ( K−1/2). If the learner does not approach the current minimizer quickly enough, the adversary stops introducing new sets and the terminal regret budget is violated. Therefore the learner must visit neighborhoods of all K minimizers, forcing movement Ω( √K).

5.2 Uniformly sharp objectives 

The next result shows that a stronger growth condition removes the logarithmic dependence on T . For a compact convex set K, let s(K) denote its Steiner point. Algorithm 2 forms the constrained minimizer set At exactly and evaluates its Steiner point. 

Assumption 5.4 (Uniform set-relative sharpness) . There is a number α > 0 such that for every t and every x ∈ St,

f (x) − vt ≥ α dist( x, At), At := arg min 

> y∈St

f (y), vt := min 

> y∈St

f (y).

Algorithm 2 Steiner tracking for uniformly sharp CONES  

> 1:

for t = 1 , 2, . . . , T do  

> 2:

Observe St, form At = arg min x∈St f (x), and play xt = s(At). 

> 3:

end for Theorem 5.5 (Uniformly sharp CONES ). Suppose that f is G-Lipschitz on X and that Assumption 5.4 holds. Then Algorithm 2 satisfies 

Reg CONES  

> T

≤ 0

and 

MT ≤ dD + 2dα (vT − v1) ≤ dD + 2dGD α .

If an initial point x0 ∈ X is prescribed, at most an additional D is required. In particular, the movement is independent of T .

11 A PREPRINT - A UGUST 28, 2026 

The proof is in Section C.5. The key point is that sharpness implies 

At ⊆ A t−1 + vt − vt−1

α B2.

The support-function representation of the Steiner point then converts this one-sided inclusion into a telescoping movement bound. 

# 6 One-step-delayed chasing for COCO 

Both COCO algorithms maintain a post-update feasible point zt ∈ St and play the previously computed point xt =

zt−1, because St becomes available only after the round-t action. For either algorithm, write 

P zT := 

> T

X

> t=1

∥zt − zt−1∥

for the movement of this post-update feasible sequence. Since zt satisfies the newly revealed constraint, Lipschitzness bounds both the round-t violation and the loss incurred by the one-step delay through ∥zt − zt−1∥. The remaining task is therefore to control P zT .For strongly convex losses, curvature of the cumulative loss controls the displacement between successive reset points and yields logarithmic dependence on T . For general convex losses, we add a fixed quadratic regularizer; the resulting algorithm obtains O(√T ) regret and cumulative constraint violation without an additional log T factor. 

6.1 Strongly convex COCO 

Assume throughout this subsection that the losses satisfy Assumption 3.2. Define 

Ft(x) := 

> t

X 

> τ=1

fτ (x), ut := arg min 

> x∈St

Ft(x), vt := Ft(ut).

The point ut is the constrained minimizer of the cumulative loss over the current feasible set. The algorithm tentatively evaluates the chaser on the current set St. It accepts the response if the post-update cumulative loss remains no larger than the current constrained cumulative minimum vt. Otherwise, it resets to ut.Let 

At := 

> t

X 

> τ=1

fτ (zτ ), A0 := 0 .

12 A PREPRINT - A UGUST 28, 2026 

Algorithm 3 One-step-delayed chasing with constrained-leader resets 

Input: Initial point z0 ∈ X, resettable chaser C HASE  

> 1:

Set S0 ← X, F0 ≡ 0, and A0 ← 0. 

> 2:

Play x1 = z0 and observe f1, g 1. 

> 3:

Set 

S1 ← S0 ∩ { x ∈ X : g1(x) ≤ 0} , F1 ← f1. 

> 4:

Compute 

u1 ← arg min 

> x∈S1

F1(x), v1 ← F1(u1). 

> 5:

Set z1 ← u1, A1 ← v1, and initialize C HASE at z1. 

> 6:

for t = 2 , 3, . . . , T do  

> 7:

Play xt = zt−1 and observe ft, g t. 

> 8:

Update 

St ← St−1 ∩ { x ∈ X : gt(x) ≤ 0} , Ft ← Ft−1 + ft. 

> 9:

Compute 

ut ← arg min 

> x∈St

Ft(x), vt ← Ft(ut). 

> 10:

Tentatively evaluate C HASE on request St, obtaining bzt ∈ St. 

> 11:

if At−1 + ft(bzt) ≤ vt then  

> 12:

Accept: set zt ← bzt and retain the updated chaser history.  

> 13:

else  

> 14:

Reset: set zt ← ut, discard the tentative update, and restart C HASE at zt. 

> 15:

end if  

> 16:

Set At ← At−1 + ft(zt). 

> 17:

end for Theorem 6.1 (Strongly convex COCO ). Under Assumption 3.2, let Algorithm 3 use a resettable ρd-competitive Euclidean nested-body chaser. Then, for every T ≥ 1,

P zT ≤ (1 + ρd)D + ρd



D + 2Gf ρd

μ



log( eT ),

and 

Reg T ≤ Gf P zT , CCV T ≤ Gg P zT .

More precisely, if 

BT := vT − AT ,

then BT ≥ 0 and 

Reg T ≤ Gf P zT − BT .

Corollary 6.2 (Explicit dimension-dependent bounds) . With the Euclidean chaser of Theorem 3.4, there is a universal constant C > 0 such that 

Reg T ≤ C Gf Dpd log(1 + d) log( eT ) + G2

> f

μ d log(1 + d) log( eT )

!

,

and 

CCV T ≤ C



Gg Dpd log(1 + d) log( eT ) + Gf Gg

μ d log(1 + d) log( eT )



.

The proof of Theorem 6.1 is given in Section D. The proof has three main steps. First, because zt ∈ St and the algorithm plays zt−1, Lipschitzness bounds the round-t constraint violation by Gg ∥zt − zt−1∥. Second, the same movement controls regret up to the nonnegative slack BT . Third, at a reset, strong convexity forces the elapsed time since the previous reset to grow as a function of the displacement between the corresponding constrained cumulative minimizers. Combining these reset inequalities yields the logarithmic movement bound. 

6.2 General convex COCO with square-root regret and violation 

We now consider the general convex-loss regime. The algorithm is similar in spirit to the strongly convex construction above. The losses provide no uniform curvature, so we add a fixed quadratic regularizer. This produces a regularized 

13 A PREPRINT - A UGUST 28, 2026 

leader whose displacement between resets can be controlled using the same singleton-augmentation argument for nested-body chasing. Fix a parameter λ > 0 and an initial point z0 ∈ X. For t ≥ 0, define 

Ft(x) := 

> t

X

> s=1

fs(x), Ht(x) := λ

2 ∥x − z0∥2 + Ft(x),

with F0 ≡ 0. Let 

ut := arg min 

> x∈St

Ht(x), vt := Ht(ut).

At time zero, u0 = z0 and v0 = 0 . The algorithm maintains a post-update feasible point zt ∈ St and the post-update cumulative loss 

At := 

> t

X

> s=1

fs(zs), A0 := 0 .

Algorithm 4 One-step-delayed chasing with regularized-leader resets 

Input: Initial point z0 ∈ X, regularization λ > 0, resettable chaser C HASE 

1: Set S0 ← X, F0 ≡ 0, A0 ← 0, and initialize C HASE at z0.

2: for t = 1 , 2, . . . , T do 

3: Play xt = zt−1 and observe ft, g t.

4: Update 

St ← St−1 ∩ { x ∈ X : gt(x) ≤ 0} , Ft ← Ft−1 + ft.

5: Define 

Ht(x) = λ

2 ∥x − z0∥2 + Ft(x).

6: Compute 

ut ← arg min 

> x∈St

Ht(x), vt ← Ht(ut).

7: Tentatively evaluate C HASE on request St, obtaining yt ∈ St.

8: if At−1 + ft(yt) ≤ vt then 

9: Accept: set zt ← yt and retain the updated chaser history. 

10: else 

11: Reset: set zt ← ut, discard the tentative update, and restart C HASE at zt.

12: end if 

13: Set At ← At−1 + ft(zt).

14: end for Theorem 6.3 (Convex COCO with square-root bounds) . Under Assumption 3.1, let Algorithm 4 use a resettable 

ρd-competitive Euclidean nested-body chaser. For every λ > 0,

P zT ≤ ρdD + 2Gf ρ2

> d

Tλ ,

and 

Reg T ≤ λD 2

2 + Gf P zT , CCV T ≤ Gg P zT .

Consequently, with 

λ = 2Gf ρd

√TD ,

we have 

Reg T = O



Gf Dρ d

√T



, CCV T = O



Gg Dρ d

√T



.

Using the Euclidean chaser of Theorem 3.4, this gives 

Reg T = O



Gf Dpd log(1 + d)√T



, CCV T = O



Gg Dpd log(1 + d)√T



.

Thus both quantities are O(√T ) in the horizon, with no additional log T factor and with the explicit dimension dependence shown above. 

14 A PREPRINT - A UGUST 28, 2026 

If instead 

λ = 2Gf ρ2

> d

√TD ,

then 

Reg T = O



Gf Dρ 2

> d

√T



= O



Gf D d log(1 + d)√T



, CCV T = O



Gg D(√T + ρd)



.

Thus the growing √T term in CCV T can be made independent of the dimension at the cost of a larger dimension-dependent regret constant. 

Remark 6.4 (Tuning and degenerate cases) . The bounds preceding the choices of λ hold for every λ > 0 and require no advance knowledge of the horizon. The two displayed choices of λ assume that T is known and that D, G f > 0. If 

D = 0 , the decision set is a singleton and movement and violation are trivial. If Gf = 0 , every loss is constant on X,so any positive λ may be used in place of the displayed tuning formulas; the parameter-free inequalities remain valid. The proof of Theorem 6.3 is given in Section D.1. It follows the same two-step template used in the strongly convex case. A budget invariant controls regret, and singleton augmentation converts the chaser’s competitive guarantee into a movement bound. The quadratic regularizer supplies the curvature needed to bound reset displacements even though the losses themselves are merely convex. 

# 7 Conclusion 

We developed a common movement-based approach to online optimization with nested feasible regions. For strongly convex CONES , adaptive sublevel-set chasing gives nonpositive regret at every prefix and 

O pd log(1 + d)

s

GD log( eT )

μ

!

movement. The finer guarantee adapts to the increase in the constrained optimum value. A two-dimensional random-ized lower bound of Ω( √log T ), with all geometric and objective parameters fixed, shows that the horizon dependence is optimal for every terminal-regret guarantee O(T β ) with fixed β < 1. Under uniform linear growth away from the constrained minimizer sets, Steiner-point tracking instead yields movement independent of T .For COCO , one-step-delayed feasible sequences reduce regret and cumulative constraint violation to geometric move-ment. Regularized-leader resets give O(√T ) guarantees for general convex losses without an additional logarithmic factor, while constrained-leader resets give O(log T ) guarantees for strongly convex losses. Substituting the Euclidean nested-chasing ratio makes the dependence on dimension explicit and polynomial. The results are information-theoretic and use exact constrained optimization, exact Steiner points, and tentative eval-uation of a deterministic chaser. Developing comparably sharp guarantees under standard projection, separation, or first-order oracle access is an important next step. It also remains open to determine the optimal joint dependence on dimension, regret, and cumulative constraint violation, and to close the dimension-dependent gap in strongly convex 

CONES .

# References 

[1] C. J. Argue, S´ ebastien Bubeck, Michael B. Cohen, Anupam Gupta, and Yin Tat Lee. A nearly-linear bound for chasing nested convex bodies. In Proceedings of the Thirtieth Annual ACM-SIAM Symposium on Discrete Algorithms , pages 117–122. SIAM, 2019. doi: 10.1137/1.9781611975482.8. [2] Nikhil Bansal, Martin B¨ ohm, Marek Eli´ aˇ s, Grigorios Koumoutsos, and Seeun William Umboh. Nested convex bodies are chaseable. Algorithmica , 82:1640–1653, 2020. doi: 10.1007/s00453-019-00661-x. [3] S´ ebastien Bubeck, Bo’az Klartag, Yin Tat Lee, Yuanzhi Li, and Mark Sellke. Chasing nested convex bodies nearly optimally. In Proceedings of the Thirty-First Annual ACM-SIAM Symposium on Discrete Algorithms ,pages 1496–1508. SIAM, 2020. doi: 10.1137/1.9781611975994.91. [4] Joel Friedman and Nathan Linial. On convex body chasing. Discrete & Computational Geometry , 9:293–321, 1993. doi: 10.1007/BF02189324. [5] Elad Hazan. Introduction to Online Convex Optimization . MIT Press, second edition, 2022. [6] Karthick M. Krishna, Haricharan Balasundaram, and Rahul Vaze. Convex optimization with nested evolving feasible sets, 2026. arXiv:2605.07386v1, submitted 8 May 2026. 

15 A PREPRINT - A UGUST 28, 2026 

[7] Karthick M. Krishna, Haricharan Balasundaram, and Rahul Vaze. Convex optimization with nested evolving feasible sets, 2026. arXiv:2605.07386v2, revised 16 August 2026. [8] Mehrdad Mahdavi, Rong Jin, and Tianbao Yang. Trading regret for efficiency: Online convex optimization with long-term constraints. Journal of Machine Learning Research , 13:2503–2528, 2012. [9] Paolo Manselli and Carlo Pucci. Maximum length of steepest descent curves for quasi-convex functions. Ge-ometriae Dedicata , 38(2):211–227, 1991. doi: 10.1007/BF00181220. [10] Dhruv Sarkar and Abhishek Sinha. A geometric approach to constrained online learning, 2026. arXiv:2605.21107v3, revised 22 July 2026. [11] Rolf Schneider. Convex Bodies: The Brunn–Minkowski Theory . Cambridge University Press, second expanded edition, 2014. [12] Abhishek Sinha and Rahul Vaze. Optimal algorithms for online convex optimization with adversarial constraints. In Advances in Neural Information Processing Systems , volume 37, pages 41274–41302. Curran Associates, Inc., 2024. doi: 10.52202/079017-1306. [13] Rahul Vaze and Abhishek Sinha. O(√T ) Static Regret and Instance-Dependent Constraint Violation for Con-strained Online Convex Optimization. In Advances in Neural Information Processing Systems , volume 38, pages 61602–61629. Curran Associates, Inc., 2025. doi: 10.52202/085713-2059. 

16 A PREPRINT - A UGUST 28, 2026 

# A Detailed comparison with the two versions of the CONES paper 

This appendix records the calculations underlying the concise comparison in Section 2. We distinguish the first and second versions of arXiv:2605.07386 because their formal statements and constructions differ materially. 

A.1 Version 1: strongly convex lower-bound parameters 

Version 1 imposes the conditions 

a > B2

4δ , δ ≤ b − aK .

The first inequality is equivalent to 

δ > B2

4a ,

rather than δ < B 2/(4 a). Combining it with the second condition gives 

K < 4a(b − a)

B2 .

Consequently, when a, b, B and the domain parameters are fixed independently of T , the number of phases is bounded by a constant. Under these fixed parameters, the construction therefore cannot yield a lower bound based on a loga-rithmically growing number of phases. 

A.2 Version 1: proposed uniformly sharp instance 

The sharp-objective construction in Version 1 uses f (x) = c ∥x∥ at nonzero constrained minimizers. At a target minimizer u, the current supporting halfspace is active, all preceding halfspaces contain u strictly, and at least one sufficiently small nonzero tangent displacement remains inside the rectangle. Along such a feasible displacement 

h ⊥ u,

f (u + h) − f (u)

∥h∥ = c ∥h∥∥u + h∥ + ∥u∥ −→ 0.

Hence no positive constant independent of the horizon can satisfy the stated set-relative sharpness inequality for that instance. 

A.3 Version 2 and comparison with the present results 

Version 2 repairs the strongly convex construction by taking 

B = K−1/2, δ = b − aK ,

so that B2/(4 δ) = 1 /(4( b − a)) is independent of K. It tracks a phase-growth factor γ = Θ( K) and chooses 

K = Θ(log T / log log T ). Its formal lower-bound theorem is therefore 

Ω

s

log T

log log T

!

under a regret condition imposed at every prefix. The formal uniformly sharp lower-bound theorem from Version 1 is absent from Version 2. Although a few surrounding sentences retain the older phrase Ω(log T ), the abstract, formal theorem, and final calculation state the rate above. The adaptive sublevel-set upper bound and lower-bound construction in this paper were developed independently and concurrently with Version 2. Our upper bound reduces the horizon dependence from O(log T ) to O(√log T ), replaces the O(dd/ 2) projection-path factor by the Euclidean chasing ratio O(pd log(1 + d)) , and removes smoothness. The lower bound improves the rate to Ω( √log T ), applies to randomized algorithms, and assumes only a terminal expected-regret guarantee. The adversarial sequence is deterministic once the algorithm is fixed and is independent of its realized random seed. 

17 A PREPRINT - A UGUST 28, 2026 

# B Technical details for nested-body chasing 

B.1 Extension to lower-dimensional requests 

Proof of the lower-dimensional-request extension in Theorem 3.4. Assume first that a deterministic prefix-consistent 

ρd-competitive chaser is given for full-dimensional compact convex requests, from every prescribed starting point. We construct a deterministic chaser for arbitrary nonempty compact convex requests. Fix a starting point a and a finite nested request prefix K1 ⊇ · · · ⊇ Kj . For each integer n ≥ 1, define the full-dimensional enlargements 

K(n) 

> i

:= Ki + n−1B2, i = 1 , . . . , j. 

Run the full-dimensional chaser on this enlarged prefix and let 

Y (n) 

> j

= ( y(n)1 , . . . , y (n) 

> j

)

be the resulting output vector. Since all enlarged sets lie in the compact set K1 + B2 for all large n, the sequence Y (n)

> j

has cluster points. Let Cj be the compact set of all such cluster points, and choose the lexicographically least element 

Y j of Cj using a fixed coordinate basis and chronological block order. The choices are prefix-consistent. Indeed, the projection of Cj onto its first j −1 blocks is exactly Cj−1. One inclusion follows because the original chaser is prefix-consistent. For the reverse inclusion, take a convergent subsequence producing any point of Cj−1; compactness gives a further subsequence of the last output block, which yields an element of Cj projecting to the chosen point. Lexicographic minimization is compatible with this projection, so the first j − 1 blocks of Y j equal Y j−1. Therefore the last block of Y j defines a deterministic online chaser. Each limiting output belongs to the original request. To see this, note that y(n) 

> i

∈ Ki +n−1B2, hence dist( y(n) 

> i

, K i) ≤

n−1. Passing to a convergent subsequence gives yi ∈ Ki, since Ki is closed. It remains to check competitiveness. For every n, the full-dimensional guarantee gives 

> j

X

> i=1

y(n) 

> i

− y(n) 

> i−1

≤ ρd OPT( a; K(n)1 , . . . , K (n) 

> j

),

where y(n)0 = a. Since Ki ⊆ K(n) 

> i

,

OPT( a; K(n)1 , . . . , K (n) 

> j

) ≤ OPT( a; K1, . . . , K j ).

Taking a subsequential limit that realizes Y j and using continuity of the norm proves the same ρd-competitive inequal-ity for the extended chaser. Since the construction can be performed from any starting point, the chaser is resettable. Prefix consistency makes the extended rule deterministic, so its next output is a well-defined function of the retained request history. This argument establishes existence of the extension; efficient finite-time evaluation of the selected cluster point remains a separate computational question. 

# C Deferred proofs for CONES 

C.1 Adaptive sublevel-set chasing 

Proof of Lemma 5.1. We first record a standard consequence of constrained optimality and strong convexity. Since a

minimizes f over Sr , for every x ∈ Sr ,

f (x) − f (a) ≥ μ

2 ∥x − a∥2 .

To see this, fix θ ∈ (0 , 1) . The point (1 − θ)a + θx belongs to Sr . By optimality of a and strong convexity, 

f (a) ≤ f ((1 − θ)a + θx ) ≤ (1 − θ)f (a) + θf (x) − μ

2 θ(1 − θ) ∥x − a∥2 .

After subtracting (1 − θ)f (a), dividing by θ, and letting θ ↓ 0, the claim follows. For t ≥ r, write dt := vt − vr . The sequence dt is nondecreasing. As long as dt = 0 , the procedure outputs yt = a.This is feasible because strong convexity makes the minimizer over Sr unique, so dt = 0 implies ut = a ∈ St.

18 A PREPRINT - A UGUST 28, 2026 

Once dt > 0, the procedure works in sublevel epochs. At the first round p of an epoch, set R := dp and start a fresh nested-body chaser from the previous output. During this epoch, feed the chaser the requests 

Kt := St ∩ { x : f (x) ≤ vr + 2 R} .

The requests are nested inside the epoch because the sublevel set is fixed and the feasible sets are nested. The request is nonempty whenever dt ≤ 2R, since ut ∈ St and f (ut) = vr + dt ≤ vr + 2 R. If dt > 2R, the epoch is stopped before round t, a new epoch begins, and the new scale is set to R := dt. Hence successive epoch scales more than double. Consider one epoch with scale R, starting from a point q. In the first nontrivial epoch, q = a. In a later epoch, q is the final output of the previous epoch. Since the previous scale is less than R/ 2, the construction gives f (q) − vr < R .The strong-convexity inequality above implies 

∥q − a∥ ≤ 

s

2Rμ .

Consider first a nonfinal epoch, and let e denote its last round. Since no new epoch was started on round e, we have 

de ≤ 2R, so ue ∈ Ke. The same strong-convexity inequality gives 

∥ue − a∥ ≤ 2

s

Rμ .

The point ue belongs to every request in the epoch: it lies in Se ⊆ St for earlier times and has objective value at most 

vr + 2 R. Thus the offline path that moves from q to ue and then stays there is feasible for the epoch, with movement at most 

∥q − ue∥ ≤ (2 + √2) 

s

Rμ .

By competitiveness, the chaser movement in this epoch is at most the same quantity multiplied by ρd.For the final epoch, append the singleton request {b}. At the end of this epoch, ∆ = ds ≤ 2R, otherwise a new epoch would have started. Hence b belongs to every request in the final epoch. Singleton augmentation gives the same bound for the movement in the final epoch plus the terminal distance ∥ys − b∥.Let R1, . . . , R E be the sublevel scales used up to time s. Since the scales more than double and the final scale is at most ∆, EX

> e=1

pRe ≤ (2 + √2) √∆.

Summing the epoch bounds gives 

> s

X

> t=r+1

∥yt − yt−1∥ + ∥ys − b∥ ≤ 12 ρd

s

∆

μ .

This proves the movement bound. It remains to prove the loss bound. If yt = a, then f (yt) = vr ≤ vs. Otherwise, yt belongs to an epoch of scale R,so f (yt) ≤ vr + 2 R. That scale was equal to dp at the beginning of some epoch with p ≤ s, hence R ≤ ds = ∆ .Therefore 

f (yt) − vs ≤ vr + 2 R − (vr + ∆) ≤ ∆.

The proof is complete. 

C.2 Proof of the strongly convex CONES upper bound 

Proof of Theorem 5.2. Let Ct := Ptτ =1 f (xτ ). We first prove the budget invariant 

Ct ≤ tv t for every t. 

The invariant holds at t = 1 because x1 = u1. Suppose it holds up to time t − 1. If the proposal is accepted, the acceptance test gives Ct = Ct−1 + f (yt) ≤ tv t. If the proposal is rejected, the algorithm plays ut, and therefore 

Ct = Ct−1 + vt ≤ (t − 1) vt−1 + vt ≤ tv t,

19 A PREPRINT - A UGUST 28, 2026 

because vt is nondecreasing. Hence the invariant holds. Since vt ≤ vT , it implies CT ≤ T v T , and therefore 

Reg CONES  

> T

≤ 0. The same argument gives nonpositive regret at every prefix. Let 1 = r0 < r 1 < · · · < r m ≤ T be the reset times, with r0 = 1 . Consider a completed phase that begins at r and resets at s. Set ∆ = vs − vr . At time s, the proposal ys is rejected, so 

Cs−1 + f (ys) > sv s.

During the phase, all previous proposals were accepted, so 

Cs−1 = Cr +

> s−1

X

> t=r+1

f (yt).

Using Cr ≤ rv r , we obtain sX

> t=r+1

f (yt) > sv s − rv r .

Subtracting (s − r)vs gives sX

> t=r+1

 f (yt) − vs

 > r (vs − vr ) = r∆.

By Lemma 5.1, each summand is at most ∆. The case ∆ = 0 is impossible in the last strict inequality. Thus 

(s − r)∆ > r ∆, and hence s > 2r. Therefore the reset times more than double and there are at most 1 + log 2 T

completed phases. For a completed phase from rk to rk+1 , define ∆k := vrk+1 − vrk . For the final unfinished phase, define ∆m := 

vT − vrm . These increments telescope: mX

> k=0

∆k = vT − v1.

By Lemma 5.1, the movement in each completed phase, including the actual reset jump, is at most Csub ρd

p∆k/μ :the chaser path includes the rejected tentative proposal, and the triangle inequality bounds the subsequent reset jump by the movement to that proposal plus its distance to the reset point. The final phase is handled by appending uT as a terminal singleton and then dropping the nonnegative terminal distance. Therefore the internal movement satisfies 

MT ≤ Csub ρdmX

> k=0

s

∆k

μ .

Cauchy–Schwarz yields 

> m

X

> k=0

p∆k ≤

vuut(m + 1) 

> m

X

> k=0

∆k ≤ p(1 + log 2 T )( vT − v1).

This proves 

MT ≤ Cρ d

s

(vT − v1)(1 + log T )

μ .

If an initial point x0 ∈ X is prescribed, then ∥x1 − x0∥ ≤ D, which gives the stated bound for MT (x0). Finally, since 

uT , u 1 ∈ X, diam( X) ≤ D, and f is G-Lipschitz, 

vT − v1 = f (uT ) − f (u1) ≤ G ∥uT − u1∥ ≤ GD. 

The second displayed bound follows. 

C.3 Derandomization for CONES lower bounds Lemma C.1 (Mean-policy derandomization) . Let A be a randomized feasible CONES algorithm. For every deter-ministic request prefix S1: t := ( S1, . . . , S t), let Xt(S1: t; ω) be the action of A under random seed ω, and define the deterministic mean policy 

¯xt(S1: t) := Eω [Xt(S1: t; ω)] .

20 A PREPRINT - A UGUST 28, 2026 

Then ¯xt(S1: t) ∈ St. Moreover, on every fixed deterministic nested sequence and for every convex objective f ,

> T

X

> t=1

f (¯ xt) ≤ E

" TX

> t=1

f (Xt)

#

,

and TX

> t=2

∥¯xt − ¯xt−1∥ ≤ E

" TX

> t=2

∥Xt − Xt−1∥

#

.

Proof. For a fixed deterministic prefix, Xt(S1: t; ω) ∈ St almost surely. Convexity of St therefore gives ¯xt(S1: t) ∈ St,so the mean policy is a deterministic feasible online policy defined on every prefix. On a fixed full request sequence, Jensen’s inequality gives f (¯ xt) ≤ E[f (Xt)] . The norm is convex as well, and hence 

∥¯xt − ¯xt−1∥ = ∥E[Xt − Xt−1]∥ ≤ E ∥Xt − Xt−1∥ .

Summing proves both inequalities. 

C.4 Proof of the terminal-regret lower bound 

Proof of Theorem 5.3. Let A be a randomized algorithm satisfying the theorem’s terminal expected-regret guarantee, and form its deterministic mean policy from Lemma C.1. Apply the deterministic construction below to that mean policy. This produces a deterministic nested sequence that may depend on the distributional rule of A, but not on its realized random seed. On the resulting fixed sequence, the mean policy has terminal regret at most RT , while its movement is no larger than the expected movement of A. It is therefore enough to establish the claimed movement lower bound for a deterministic feasible policy. Fix such a deterministic policy. Set 

ZT := TRT + 1 

and assume that ZT is sufficiently large. Let 

K := ⌊κ log ZT ⌋ ,

where κ > 0 is a sufficiently small universal constant to be chosen later. We may assume K ≥ 2. Define 

h := 18√K , δ := 4 h2 = 116 K , ε := h

4 , η := ε2

2 = 12048 K .

For k = 1 , . . . , K , set 

σk := ( −1) k, uk := ( σkh, 1 + kδ ).

Since Kδ = 1 /16 , all these points lie in X = [ −1, 1] × [1 , 2] .For each k, define the halfspace 

Hk := x ∈ R2 : ⟨uk, x − uk⟩ ≥ 0

and the nested set 

Sk := X ∩

> k

\

> j=1

Hj .

We first verify that uk ∈ S k. For j < k ,

⟨uj , u k − uj ⟩ = h2(σj σk − 1) + (1 + jδ )( k − j)δ. 

The first term is either 0 or −2h2, while the second is at least δ = 4 h2. Hence the inner product is at least 2h2 > 0.Also uk ∈ Hk with equality, so uk ∈ S k.For any x ∈ S k, membership in Hk gives ⟨uk, x − uk⟩ ≥ 0. Since f (x) = 12 ∥x∥2,

f (x) − f (uk) = ⟨uk, x − uk⟩ + 12 ∥x − uk∥2 ≥ 12 ∥x − uk∥2 .

Thus uk is the unique minimizer of f over Sk. Let vk = f (uk). If ∥x − uk∥ ≥ ε, then f (x) − vk ≥ η.

21 A PREPRINT - A UGUST 28, 2026 

The increase in constrained optimum satisfies, for k < K ,

∆k+1 := vk+1 − vk = δ



1 + 



k + 12



δ



.

Since Kδ = 1 /16 ,

δ ≤ ∆k+1 < 17 16 δ < 5h2.

Using η = h2/32 , this gives ∆k+1 ≤ 160 η.The adversary reveals the sets S1, . . . , SK in phases. Let sk be the first round of phase k. For each phase whose target neighborhood uk + εB 2 is reached, let ℓk denote the number of rounds from the start of phase k through the first entry. For k < K , phase k + 1 begins on the following round. After the first entry in phase K, the adversary keeps SK fixed for all remaining rounds; those filler rounds are not included in ℓK . Thus, whenever phases 1, . . . , k − 1 have reached their targets, 

s1 = 1 , sk = 1 + 

> k−1

X

> i=1

ℓi.

At the beginning of phase k, define the accumulated benchmark credit 

Pk := 

> k−1

X

> i=1

ℓi(vk − vi).

This is the largest amount by which losses incurred in earlier phases can lie below the new benchmark value vk, using only the fact that an action played while the feasible set was Si has loss at least vi. Set 

Lk := 

 Pk + RT

η



+ 1 .

During phase k, the adversary repeatedly presents Sk. If the learner enters uk + εB 2 during the first Lk rounds, then, when k < K , the next phase begins on the following round. If the learner does not enter the target neighborhood during those Lk rounds, the adversary keeps Sk fixed for all remaining rounds. We now verify that every target is reached early enough for the construction to continue within the horizon. For each 

j whose target has been reached, write 

Nj := 

> j

X

> i=1

ℓi, N0 := 0 .

At the beginning of phase k, define 

Qk := Pk

η , B := RT

η , Wk := Nk−1 + Qk + B + 1 .

We prove by induction over k that phase k begins, its target is reached by its deadline, ℓk ≤ Lk, and 

Wk ≤ 324 k−1(B + 1) .

For k = 1 , phase 1 begins on round 1, and P1 = N0 = 0 , so W1 = B + 1 .Assume that phase k begins and that the displayed bound on Wk holds. Because 

Lk ≤ Qk + B + 1 ≤ Wk and Nk−1 ≤ Wk,

the phase-k deadline occurs no later than 

Nk−1 + Lk ≤ 2Wk ≤ 2 · 324 k−1(B + 1) .

Since η−1 = 2048 K,

B + 1 ≤ 2049 K(RT + 1) .

Choose κ < 1/(4 log 324) . For every k ≤ K, the choice K ≤ κ log ZT gives 

324 k−1 ≤ 324 K ≤ Z1/4 

> T

.

Therefore 

2 · 324 k−1(B + 1) ≤ C0K(RT + 1) Z1/4 

> T

= C0KT Z −3/4 

> T

.

22 A PREPRINT - A UGUST 28, 2026 

Because K = O(log ZT ), the ratio C0KZ −3/4 

> T

tends to zero. Hence, for sufficiently large ZT , every phase deadline is at most T / 2. In particular, reaching a target at its deadline still leaves a subsequent round on which the next phase can begin. Suppose the learner failed to enter uk + εB 2 by this deadline. The adversary would then freeze the feasible set at Sk,so the terminal optimum value would be vk. Earlier phases can contribute at worst −Pk to terminal regret relative to vk, while the first Lk rounds of phase k contribute at least Lkη by the excess-loss bound above. All later rounds contribute nonnegatively because the learner remains feasible in Sk. Consequently, 

Reg CONES  

> T

≥ − Pk + Lkη > R T ,

where the strict final inequality follows from the definition of Lk. This contradicts the assumed terminal-regret guar-antee. Thus the learner reaches uk + εB 2 by the deadline, and ℓk ≤ Lk.It follows that 

Nk = Nk−1 + ℓk ≤ 2Wk.

If k < K , then 

Pk+1 = Pk + ∆ k+1 Nk.

Using ∆k+1 ≤ 160 η, we obtain 

Qk+1 ≤ Qk + 160 Nk ≤ Wk + 320 Wk = 321 Wk.

Since B + 1 ≤ Wk,

Wk+1 = Nk + Qk+1 + B + 1 ≤ 2Wk + 321 Wk + Wk = 324 Wk.

Moreover, the target in phase k is reached by time T / 2, so phase k + 1 begins within the horizon. This closes the induction for k < K .For the final phase, 

NK ≤ 2WK ≤ 2 · 324 K−1(B + 1) ≤ T

2 .

Hence all K target neighborhoods are reached within the horizon. After the learner reaches uK + εB 2, the adversary keeps SK fixed for the remaining rounds. Let τk be the first round in phase k when the learner enters uk + εB 2. Then ∥xτk − uk∥ ≤ ε. Consecutive minimizers satisfy 

∥uk+1 − uk∥ = p4h2 + δ2 ≥ 2h. 

Thus 

xτk+1 − xτk ≥ 2h − 2ε = 32 h. 

The times τ1 < · · · < τ K are ordered, so the corresponding trajectory segments are disjoint. Therefore 

MT ≥

> K−1

X

> k=1

xτk+1 − xτk ≥ 32 (K − 1) h ≥ 332 

√K

for K ≥ 2. Since K = Θ(log ZT ), this gives 

MT = Ω 

s

log 

 TRT + 1 

!

.

This proves the deterministic lower bound and, by the derandomization step at the beginning, the randomized expected-movement lower bound. 

C.5 Steiner-point facts and uniformly sharp objectives 

We use the standard Steiner point and its support-function representation; see, for example, Schneider [11] and Bubeck et al. [3]: 

s(K) := d

Z

> Sd−1

hK (θ)θ dσ (θ),

where hK (θ) = sup x∈K ⟨θ, x ⟩ and σ is the normalized rotation-invariant measure on the sphere. For every nonempty compact convex set K, s(K) ∈ K. Moreover, 

∥s(K) − s(L)∥ ≤ d

Z

> Sd−1

|hK (θ) − hL(θ)| dσ (θ). (3) 

23 A PREPRINT - A UGUST 28, 2026 

Proof of Theorem 5.5. Since At is compact and convex, its Steiner point belongs to At. Hence f (xt) = vt. Because the feasible sets are nested, vt ≤ vT for every t. Therefore 

Reg CONES  

> T

=

> T

X

> t=1

(vt − vT ) ≤ 0.

We now bound movement. Fix t ≥ 2 and let a ∈ A t. Since At ⊆ St ⊆ St−1, the sharpness condition at time t − 1

gives 

α dist( a, At−1) ≤ f (a) − vt−1 = vt − vt−1.

Thus 

At ⊆ A t−1 + rtB2, rt := vt − vt−1

α . (4) Consequently, for every unit vector θ,

hAt (θ) − hAt−1 (θ) ≤ rt. (5) For any real sequence (at)Tt=1 ,

> T

X

> t=2

|at − at−1| ≤ | aT − a1| + 2 

> T

X

> t=2

(at − at−1)+.

Apply this with at = hAt (θ). Since all sets At lie in X, their supports in any fixed direction vary by at most D, so 

|aT − a1| ≤ D. Also (5) gives (at − at−1)+ ≤ rt. Therefore 

> T

X

> t=2

hAt (θ) − hAt−1 (θ) ≤ D + 2 

> T

X

> t=2

rt.

Integrating this inequality over θ, using (3), and exchanging the finite sum and the integral gives 

> T

X

> t=2

∥s(At) − s(At−1)∥ ≤ dD + 2 d

> T

X

> t=2

rt.

Finally, 

> T

X

> t=2

rt = vT − v1

α ≤ GD α ,

because f is G-Lipschitz and all minimizers lie in the diameter-D set X. Hence, more precisely, 

MT ≤ dD + 2dα (vT − v1) ≤ dD + 2dGD α .

If an initial point x0 ∈ X is prescribed, the additional first movement is at most D. This proves the theorem. 

# D Deferred proofs for COCO 

D.1 Proof of the convex COCO theorem 

We prove Theorem 6.3. Throughout this subsection, Ht, u t, v t, z t, A t are the quantities generated by Algorithm 4. Recall that 

Ht(x) = λ

2 ∥x − z0∥2 +

> t

X

> s=1

fs(x).

Thus Ht is λ-strongly convex on X, even though the losses fs are only convex. 

Lemma D.1 (Regularized budget invariant) . For every t ≥ 0, Algorithm 4 satisfies 

At ≤ vt.

24 A PREPRINT - A UGUST 28, 2026 

Proof. At time zero, A0 = 0 . Since H0(x) = λ 

> 2

∥x − z0∥2 and z0 ∈ S0 = X, we have u0 = z0 and v0 = H0(z0) = 0 .Hence A0 = v0.Assume the claim holds at time t − 1. If the chaser proposal is accepted, then the acceptance rule gives 

At = At−1 + ft(yt) ≤ vt.

If the proposal is rejected, then zt = ut. Since ut ∈ St ⊆ St−1, the definition of vt−1 gives 

vt−1 = min 

> x∈St−1

Ht−1(x) ≤ Ht−1(ut).

Using the induction hypothesis, 

At = At−1 + ft(ut)

≤ vt−1 + ft(ut)

≤ Ht−1(ut) + ft(ut) = Ht(ut) = vt.

The induction is complete. We now control movement. Let 

0 = r0 < r 1 < · · · < r m ≤ T

be the reset times of Algorithm 4; that is, rk, k ≥ 1, are the rounds at which a tentative chaser proposal is rejected. Put a0 = z0 = u0. For k ≥ 1, set ak = zrk = urk , and define 

δk := ∥ak+1 − ak∥ , k = 0 , . . . , m − 1.

Lemma D.2 (Reset displacement for convex losses) . For every completed phase k = 0 , . . . , m − 1,

δk ≤ 2Gf ρd(rk+1 − rk)

λ .

Consequently, m−1X

> k=0

δk ≤ 2Gf ρdTλ .

Proof. Fix a completed phase and abbreviate r = rk, s = rk+1 , a = ak, b = ak+1 = us, and δ = ∥a − b∥. The chaser is started at a at the beginning of the phase. Let yt be the tentative chaser output on request St for t = r + 1 , . . . , s .The tentative output ys is rejected. The rejection condition gives 

As−1 + fs(ys) > v s.

For t = r + 1 , . . . , s − 1, the tentative proposals were accepted, and hence zt = yt. Therefore 

As−1 = Ar +

> s−1

X

> t=r+1

ft(yt).

Combining the previous two displays, sX

> t=r+1

ft(yt) > v s − Ar .

Since vs = Hs(b) = Hr (b) + Pst=r+1 ft(b), this becomes 

> s

X

> t=r+1

 ft(yt) − ft(b) > H r (b) − Ar . (6) By Lemma D.1, Ar ≤ vr . Also a = ur , so vr = Hr (a). Thus 

Hr (b) − Ar = Hr (b) − Hr (a) + vr − Ar ≥ Hr (b) − Hr (a).

The point b belongs to Ss ⊆ Sr , and a minimizes Hr over Sr . Since Hr is λ-strongly convex, the standard constrained-minimizer inequality gives 

Hr (b) − Hr (a) ≥ λ

2 ∥b − a∥2 = λ

2 δ2.

25 A PREPRINT - A UGUST 28, 2026 

Therefore the right-hand side of (6) is at least λ 

> 2

δ2.On the other hand, the singleton-augmentation lemma applied to the phase and to the terminal singleton {b} gives 

∥yt − b∥ ≤ ρdδ, t = r + 1 , . . . , s. 

By Gf -Lipschitzness, 

ft(yt) − ft(b) ≤ Gf ∥yt − b∥ ≤ Gf ρdδ. 

Summing over s − r terms and comparing with (6), we get 

(s − r)Gf ρdδ > λ

2 δ2.

If δ = 0 , the desired inequality is trivial. Otherwise divide by δ to obtain 

δ < 2Gf ρd(s − r)

λ .

Summing over completed phases and using disjointness of the phase intervals gives 

> m−1

X

> k=0

δk ≤ 2Gf ρd

λ

> m−1

X

> k=0

(rk+1 − rk) ≤ 2Gf ρdTλ .

Lemma D.3 (Movement bound for Algorithm 4) . The post-update movement satisfies 

P zT := 

> T

X

> t=1

∥zt − zt−1∥ ≤ ρdD + 2Gf ρ2

> d

Tλ .

Proof. Consider a completed phase from rk + 1 through rk+1 , with start ak and reset endpoint ak+1 . The actual movement during this phase is the movement through the accepted chaser outputs, together with the final reset jump. If yt denotes the tentative chaser output, then this actual movement is at most 

> rk+1

X 

> t=rk+1

∥yt − yt−1∥ + yrk+1 − ak+1 , yrk = ak.

The singleton-augmentation lemma bounds this by ρdδk. Summing over completed phases gives a contribution at most 

ρd

P 

> k

δk.There may be an unfinished final phase after the last reset. Choose any point x⋆ ∈ ST , which exists by common feasibility. Since x⋆ ∈ St for every request in the final phase, an offline path can move directly from the phase start to 

x⋆ and then remain there. This offline path has cost at most D. The chaser’s movement in the final phase is therefore at most ρdD. Combining these estimates with Lemma D.2, 

P zT ≤ ρdD + ρdm−1X

> k=0

δk ≤ ρdD + 2Gf ρ2

> d

Tλ .

Proof of Theorem 6.3. The movement bound is exactly Lemma D.3. We next prove regret and violation. Fix any x⋆ ∈ ST . By Lemma D.1, 

AT ≤ vT = min 

> x∈ST

HT (x) ≤ HT (x⋆).

Therefore 

AT −

> T

X

> t=1

ft(x⋆) ≤ HT (x⋆) −

> T

X

> t=1

ft(x⋆)= λ

2 ∥x⋆ − z0∥2 ≤ λD 2

2 .

26 A PREPRINT - A UGUST 28, 2026 

Since the algorithm plays xt = zt−1,

> T

X

> t=1

ft(xt) −

> T

X

> t=1

ft(x⋆) = 

> T

X

> t=1

ft(zt−1) −

> T

X

> t=1

ft(x⋆)= AT −

> T

X

> t=1

ft(x⋆)

!

+

> T

X

> t=1

 ft(zt−1) − ft(zt)

≤ λD 2

2 + GfTX

> t=1

∥zt − zt−1∥ .

Taking the minimum over x⋆ ∈ ST gives 

Reg T ≤ λD 2

2 + Gf P zT .

For violation, note that zt ∈ St, so gt(zt) ≤ 0. Hence 

[gt(zt−1)] + ≤ [gt(zt−1) − gt(zt)] + ≤ Gg ∥zt − zt−1∥ .

Summing over t yields CCV T ≤ Gg P zT .Substituting P zT ≤ ρdD + 2 Gf ρ2

> d

T /λ proves the first part. Direct substitution of the two stated choices of λ gives the corresponding corollaries; the simplified regret bound for the second choice uses ρd ≥ 1 and T ≥ 1. Finally, Theorem 3.4 gives ρd = O(pd log(1 + d)) .

D.2 Strong convexity of the constrained cumulative minimizer 

Proof of (1) . Let ut minimize Ft over St, and fix y ∈ St. For λ ∈ (0 , 1) , the point 

xλ = (1 − λ)ut + λy 

belongs to St by convexity. By optimality of ut,

Ft(ut) ≤ Ft(xλ).

Since Ft is μt -strongly convex, 

Ft(xλ) ≤ (1 − λ)Ft(ut) + λF t(y) − μt 

2 λ(1 − λ) ∥y − ut∥2 .

Combining the two inequalities and subtracting (1 − λ)Ft(ut) gives 

λF t(ut) ≤ λF t(y) − μt 

2 λ(1 − λ) ∥y − ut∥2 .

Divide by λ > 0, then let λ ↓ 0. This yields 

Ft(y) − Ft(ut) ≥ μt 

2 ∥y − ut∥2 .

D.3 Cumulative-loss invariant Lemma D.4. For every t ≥ 1, Algorithm 3 satisfies 

At ≤ vt.

Consequently Bt := vt − At ≥ 0.Proof. At t = 1 , the initialization gives z1 = u1, hence 

A1 = f1(z1) = f1(u1) = v1.

Assume now that At−1 ≤ vt−1. If the chaser proposal is accepted, then by the acceptance rule 

At = At−1 + ft(bzt) ≤ vt.

If the proposal is rejected, then zt = ut. Since ut ∈ St ⊆ St−1, the definition of vt−1 gives 

vt−1 = min 

> x∈St−1

Ft−1(x) ≤ Ft−1(ut).

Therefore 

At = At−1 + ft(ut) ≤ vt−1 + ft(ut) ≤ Ft−1(ut) + ft(ut) = Ft(ut) = vt.

The induction is complete. 

27 A PREPRINT - A UGUST 28, 2026 

D.4 Regret and violation reduce to movement Lemma D.5. For every T ,

Reg T ≤ Gf P zT − BT , CCV T ≤ Gg P zT .

Proof. The terminal comparator is uT , so 

Reg T =

> T

X

> t=1

ft(zt−1) − FT (uT )=

> T

X

> t=1

 ft(zt−1) − ft(zt) +

> T

X

> t=1

ft(zt) − vT

=

> T

X

> t=1

 ft(zt−1) − ft(zt) − BT .

By Lipschitzness, 

ft(zt−1) − ft(zt) ≤ Gf ∥zt − zt−1∥ .

Summing proves the regret inequality. For violation, zt ∈ St, so gt(zt) ≤ 0. Therefore 

[gt(zt−1)] + ≤ [gt(zt−1) − gt(zt)] + ≤ Gg ∥zt − zt−1∥ .

Summing over t proves CCV T ≤ Gg P zT .

D.5 Reset inequality and movement bound 

Let 

1 = r0 < r 1 < · · · < r m ≤ T

be the reset times of Algorithm 3: r0 = 1 , and rk, k ≥ 1, are the rounds at which a tentative chaser proposal is rejected. Put 

ak := zrk = urk , δk := ∥ak+1 − ak∥ , k = 0 , . . . , m − 1.

Lemma D.6 (Reset inequality) . For every completed phase k = 0 , . . . , m − 1,

(rk+1 − rk)Gf ρdδk > μr k

2 δ2 

> k

+ Brk .

Proof. Fix a completed phase and abbreviate r = rk, s = rk+1 , a = ak, b = ak+1 = us, and δ = ∥a − b∥. During the phase, the chaser is started at a. Let yt denote its tentative output on request St for t = r + 1 , . . . , s ; the output ys

is the rejected tentative proposal. At time s, rejection means 

As−1 + fs(ys) > v s.

For the accepted times r + 1 , . . . , s − 1, we have zt = yt. Hence 

As−1 = Ar +

> s−1

X

> t=r+1

ft(yt).

Combining the last two displays gives sX

> t=r+1

ft(yt) > v s − Ar .

Since vs = Fs(b) = Fr (b) + Pst=r+1 ft(b), we obtain 

> s

X

> t=r+1

 ft(yt) − ft(b) > F r (b) − Ar . (7) Write Br = vr − Ar . Since a = ur , vr = Fr (a). Therefore 

Fr (b) − Ar = Fr (b) − Fr (a) + Br .

28 A PREPRINT - A UGUST 28, 2026 

Because b ∈ Ss ⊆ Sr , (1) at time r gives 

Fr (b) − Fr (a) ≥ μr 

2 ∥b − a∥2 = μr 

2 δ2.

Thus the right side of (7) is at least μr  

> 2

δ2 + Br .On the other hand, by the endpoint lemma, ∥yt − b∥ ≤ ρdδ for each t = r + 1 , . . . , s . Since each ft is Gf -Lipschitz, 

ft(yt) − ft(b) ≤ Gf ρdδ. 

Summing this upper bound over s − r terms and comparing with (7) proves the claim. 

Lemma D.7. The reset displacements satisfy 

> m−1

X

> k=0

δk ≤



D + 2Gf ρd

μ



log( eT ).

Proof. If Gf = 0 , every ft is constant on X. Positive strong convexity then forces X to be a singleton: otherwise, applying strong convexity at the midpoint of two distinct points would contradict constancy. Hence every δk = 0 , and the claim is immediate. We may therefore assume Gf > 0.If δk = 0 , it contributes nothing. Otherwise, drop the nonnegative term Brk from Lemma D.6 and divide by Gf ρdδkrk.This gives 

rk+1 − rk

rk

> μδ k

2Gf ρd

,

or rk+1 

rk

> 1 + μδ k

2Gf ρd

.

Multiplying over completed phases and using r0 = 1 , rm ≤ T , yields 

> m−1

Y

> k=0



1 + μδ k

2Gf ρd



≤ T. 

Taking logarithms, m−1X

> k=0

log 



1 + μδ k

2Gf ρd



≤ log T. 

Since ak, a k+1 ∈ X, each δk ≤ D. For 0 ≤ x ≤ M , log(1 + x) ≥ x/ (1 + M ). Apply this with x = μδ k/(2 Gf ρd)

and M = μD/ (2 Gf ρd). We obtain 

μ

2Gf ρd + μD 

> m−1

X

> k=0

δk ≤ log( eT ),

which is equivalent to the claimed bound. 

Proof of Theorem 6.1. We first bound P zT . The initial movement is at most D. For a completed phase k, apply Lemma 4.1 to the chaser requests in the phase and append the next reset point ak+1 as a singleton request. As explained in the proof of Lemma D.6, the actual movement during the phase, including the reset jump, is bounded by 

ρdδk. The unfinished final phase has movement at most ρdD, by appending any point in the final feasible set, which lies at distance at most D from the phase start. Therefore 

P zT ≤ D + ρdm−1X

> k=0

δk + ρdD. 

Using Lemma D.7, 

P zT ≤ (1 + ρd)D + ρd



D + 2Gf ρd

μ



log( eT ).

Finally, Lemma D.5 gives 

Reg T ≤ Gf P zT − BT ≤ Gf P zT , CCV T ≤ Gg P zT .

This proves the theorem. 

29