Title: NepScript Genesis: Neural Architecture Search for Handwritten Devanagari Digit Synthesis

URL Source: https://arxiv.org/pdf/2608.29540v1

Published Time: Tue, 01 Sep 2026 01:05:35 GMT

Number of Pages: 11

Markdown Content:
# NepScript Genesis: Neural Architecture Search for Handwritten Devanagari Digit Synthesis 

## Mausam Gurung 

## mausaam.gurung593@gmail.com 

## Prabin Neupane 

## neupaneprabin2058@gmail.com 

## Sajjan Acharya 

## sajjanacharya11@gmail.com 

## September 1, 2026 

# Abstract 

This paper introduces NepScript Genesis, a Neural Archi-tecture Search (NAS) framework for automated Generative Adversarial Network (GAN) discovery, applied to conditional Devanagari handwritten digit synthesis. We compare five NAS strategies against a carefully constructed Deep Convolutional GAN (DCGAN) baseline ( F ID = 332 .28 ). Architecture se-lection utilizes a two-stage pipeline guided by a novel domain-aware evaluation metric (Enhanced Score). Results demon-strate that Adaptive Exploration achieves the optimal quality-efficiency trade-off, attaining an F ID of 79 .12 —a 76 .19% 

improvement over the baseline—and the highest mode cov-erage among the NAS strategies ( Recall = 0 .531 ) in under one GPU-hour. Furthermore, we demonstrate that incorporat-ing script-specific structural heuristics into the search phase prevents early-stage mode collapse. In a downstream low-resource evaluation, augmenting 250 real training samples per class with GAN-generated digits from the best NAS model improves CNN classification accuracy from 91 .0% to 96 .5% 

(+5 .5 percentage points), demonstrating that NAS-optimized synthesis produces digits of sufficient quality to benefit practi-cal recognition pipelines when real data is scarce. 

Keywords: Neural Architecture Search, GANs, Devanagari, Conditional Image Synthesis, DCGAN, Adaptive Exploration, FID, Inception Score 

# 1 Introduction 

The synthesis of handwritten characters from low-resource scripts is an important yet underexplored problem in generative modeling. Devanagari [1], the script underlying Nepali, Hindi, Sanskrit, and several other South Asian languages, presents particular challenges for generative models: its characters fea-ture complex stroke topologies, conjunct consonant forms, vowel diacritics, and a distinctive horizontal headline ( shi-rorekha ) that must be faithfully reproduced for generated sam-ples to be legible. Automated synthesis of such script is prac-tically valuable for data augmentation in OCR pipelines [2, 3], educational tool development, and digital preservation of hand-written documents in low-resource language settings [4]. Yet GAN training remains sensitive to architectural choices: network depth, normalization scheme, activation functions, latent space dimensionality, and regularization must all be selected by the practitioner through costly trial-and-error iter-ation. For a script as structurally demanding as Devanagari, poor architectural choices lead directly to mode collapse or failure to reproduce stroke topology. Neural Architecture Search (NAS) [5] replaces this manual process with automated exploration guided by performance feedback. NAS has been extended to generative settings through AdversarialNAS [6] and AutoGAN [7], but has not been systematically applied to GAN architecture search for Devanagari or other Brahmic script synthesis tasks. We address this gap with NepScript Genesis , a NAS frame-work that automates GAN architecture discovery for condi-tional Devanagari digit synthesis. We compare five search strategies against a manually designed DCGAN baseline and show that automated search consistently recovers better archi-tectures within a practical single-GPU budget. We make the following contributions: 1. We design and evaluate a manual DCGAN baseline [8] for Devanagari digit synthesis, establishing reference FID, IS, Precision, and Recall scores against which all NAS-discovered architectures are compared. Manual DCGAN: FID = 332 .28 | IS = 2 .00 | P = 0 .693 | R = 0 .552 

2. We implement and compare five NAS strategies — Ran-dom Search [9], Progressive Search, Adaptive Exploration, Multi-fidelity Search [10, 11], and AdversarialNAS [6] — under a fixed evaluation budget, demonstrating that in-telligent search strategies consistently improve upon the manual baseline. 1arXiv:2608.29540v1 [cs.CV] 30 Aug 2026 3. We show that Adaptive Exploration achieves the best quality-efficiency trade-off , surpassing the manual DC-GAN baseline on FID by 76.2% (332.28 → 79.12) while requiring only 56.56 minutes of search time — less than half the cost of AdversarialNAS (124.50 min). 4. We release an open-source reproducible framework [12] for NAS experimentation on Devanagari and other Brahmic scripts, publicly available at https://github.com/ M9star/NepScript-Genesis-Framework .

# 2 Related Work 

## 2.1 Generative Adversarial Networks 

Architectural advances in GAN — including DCGAN [8], Progressive GAN [13], StyleGAN [14], and BigGAN [15] — have dramatically improved image quality and stability. Training stability has been improved through Wasserstein divergence [16], gradient penalty regularization [17], and spec-tral normalization [18]. Normalization inside the network is commonly handled with batch normalization [19] or in-stance normalization [20]. The FID score [21] has become the standard evaluation metric for generative models, as it correlates well with perceptual quality and captures both fi-delity and diversity. Conditional image synthesis — enabling class-conditioned generation — was formalized by Mirza and Osindero [22]. 

## 2.2 Neural Architecture Search 

NAS methods can be categorized by their search strategy and performance estimation strategy [5]. 

Search strategies: 

• Random search: Uniform sampling from the configuration space; a surprisingly competitive baseline that is often un-derestimated in high-dimensional spaces [9]. It establishes a lower bound on the difficulty of a search problem: if random search performs well, the landscape is relatively uniform; if it performs poorly, structured search strategies provide clear value. • Reinforcement learning: Uses a controller trained via policy gradients to propose architectures, rewarding configurations that achieve high validation performance. NASNet [23] and ENAS [24] demonstrate strong results but require thousands of GPU-hours for full search. • Evolutionary: Maintains a population of architectures and iteratively mutates and selects the fittest candidates. Amoe-baNet [25] shows evolutionary search can match or exceed RL-based methods at comparable cost. • Gradient-based: Relaxes discrete architecture choices to continuous parameters, enabling joint optimization of archi-tecture and weights via gradient descent. DARTS [26] and SNAS [27] reduce search cost to tens of GPU-hours but can overfit the supernet. • Multi-fidelity: Evaluates candidates at reduced fidelity (fewer epochs, smaller datasets) and promotes only promis-ing ones to full evaluation. Successive Halving [11] and Hyperband [10] formalize this as a bandit problem, provid-ing theoretical guarantees on sample efficiency. 

NAS for generative models: AdversarialNAS [6] intro-duces gradient-based joint optimization of G and D architec-tures, directly targeting GAN training stability. AutoGAN [7] applies an RL-based controller to progressively search genera-tor architectures at multiple resolutions. A recent survey [28] reviews the broader field of automated GAN design, confirm-ing that systematic NAS for low-resource script synthesis remains an open problem. 

Efficiency of NAS: Gradient-based NAS (DARTS [26]) can reduce architecture search from thousands of GPU-hours (RL-based methods) to tens of GPU-hours by sharing weights across the supernet. Multi-fidelity methods [10] achieve fur-ther savings by early stopping of poor candidates. 

## 2.3 Handwritten Character Synthesis and De-vanagari 

Prior work on Devanagari handwriting has largely focused on recognition rather than synthesis. The Devanagari Handwrit-ten Character Dataset (DHCD) [29, 30] established a standard benchmark for recognition tasks. Synthesis efforts include DCGAN-based augmentation [3], conditional GAN genera-tion [31], and GAN-based synthetic data for improving rec-ognizer accuracy [2]. Low-resource script generation more broadly has been studied through adversarial learning [4]. A multi-purpose Devanagari numeral dataset is provided by Pant et al. [1]. To our knowledge, NepScript Genesis is the first work to apply systematic NAS to GAN architecture search for De-vanagari digit synthesis, bridging the gap between automated architecture search and low-resource script generation. 

# 3 Methodology 

## 3.1 Problem Formulation 

We frame the task as conditional image synthesis. Given a dataset D = {(xi, y i)}Ni=1 of handwritten Devanagari digit images with class labels yi ∈ { 0, 1, . . . , 9}, we seek to learn a generator Gθ : Z × Y → X such that the distribution of generated samples pg = Gθ (z, y ), z ∼ p(z), y ∼ p(y)

approximates the real data distribution pdata conditioned on digit class [22]. Generator and discriminator engage in the standard condi-tional adversarial objective [32, 22]: 2min  

> G

max  

> D

Ex∼pdata [log D(x)] + Ez∼p(z)[log(1 − D(G(z)))] 

(1) Rather than manually specifying G and D, we automate architecture selection via NAS over a predefined search space 

Λ. The NAS problem is: 

λ∗ = arg min  

> λ∈Λ

Leval (Gλ, D λ, D) (2) where Leval is the FID score [21] (lower is better), used as the primary criterion for architecture ranking. 

## 3.2 Manual DCGAN Baseline 

To establish a reference point for NAS evaluation, we design a DCGAN [8] baseline following established best practices. The manual baseline architecture is: • Generator: 4 transposed convolutional layers, latent dim =100 , BatchNorm [19] after each layer except output, ReLU activations, Tanh output. • Discriminator: 4 convolutional layers, LeakyReLU, Batch-Norm, Sigmoid output. • Training: Adam optimizer, lr = 0 .0002 , β1 = 0 .5, batch size = 64 , discriminator updated every 2 batches, N = 500 

epochs.      

> z∼ N (0 , I )
> dz= 100
> ConvT
> 100 →512
> BN, ReLU ConvT
> 512 →256
> BN, ReLU ConvT
> 256 →128
> BN, ReLU ConvT
> 128 →1
> Tanh Fake Image
> 1×32 ×32
> Generator G
> Real Image
> 1×32 ×32
> Conv
> 1→64
> LReLU Conv
> 64 →128
> BN, LReLU Conv
> 128 →256
> BN, LReLU Conv
> 256 →1
> Sigmoid Real/Fake Score
> Discriminator D
> Gradient

Figure 1: Manual DCGAN baseline: Generator G (top) and Discriminator D (bottom) [8]. The manual DCGAN baseline achieves: 

Model FID ↓ IS ↑ Prec. ↑ Recall ↑

Manual DCGAN 332.28 2.00 0.693 0.552 This is what a practitioner reaches by following standard DCGAN conventions without any architecture search, and it sets the floor that NAS strategies must clear to be worthwhile. 

## 3.3 Dataset 

We use the Devanagari Handwritten Character Dataset [29, 30], comprising 20,000 handwritten Devanagari digit samples spanning ten digit classes (0–9 in Devanagari script), with 2,000 samples per class. Sample images for each class are shown in Figure 2. Images are resized to 32 × 32 pixels and pixel values nor-malized to [−1, 1] . The dataset is class-balanced. 

Figure 2: Sample grid showing 5 examples per digit class (10 × 5 = 50 images). 

## 3.4 NAS Search Space 

The search space Λ is defined over five architectural dimen-sions, applied independently to both the Generator G and Discriminator D:The generator-side search space contains 4×2×2×4×2 = 128 unique configurations. Since normalization, activation, dropout, and residual connections are searched independently for G and D, the joint generator–discriminator space is 128 ×

2 × 2 × 4 × 2 = 4 ,096 unique paired configurations. This is far too large to evaluate exhaustively, which is precisely why we turn to NAS rather than manual search. The five dimensions were selected to cover the pri-mary sources of architectural variation in DCGAN-style net-works [8] while keeping the search tractable for a single-GPU setting. Deeper network variants, attention mechanisms, and alternative loss functions represent natural extensions of this search space and are left for future work. All configurations are trained with fixed hyperparameters: learning rate = 0 .0002 , batch size = 64 , label smoothing 

= 0 .1 [35], Adam optimizer with β1 = 0 .5, β2 = 0 .999 ,consistent with the manual DCGAN baseline (Section 3.2). 

## 3.5 NAS Algorithms 

We implement and evaluate five NAS strategies. Each operates over the 128-configuration search space Λ defined in Table 1, using the Enhanced Score (Equation 4) after 30-epoch training as the evaluation criterion. 

3.5.1 Random Search 

Uniformly samples from Λ without learning between evalua-tions, following Li and Talwalkar [9]. Serves as the primary NAS baseline requiring no surrogate model. 3Table 1: NAS Search Space Definition. Each dimension is searched independently for G and D, yielding a joint search space substantially larger than the per-network count. 

Dimension Choices Applied to Motivation 

Latent dim ( dz ) {64, 100, 128, 200} G only Controls generator capacity for ten-class conditional synthesis Normalization {BatchNorm, InstanceNorm} G and D [19, 20] Activation {ReLU, LeakyReLU} G and D [8] Dropout rate ( G) {0.0, 0.1, 0.2, 0.3} G only [33] Dropout rate ( D) {0.0, 0.2, 0.3, 0.5} D only [33] Residual connections {False, True} G and D [34] 

Algorithm 1 Random Search  

> 1:

Initialize: results ← [ ], best_score ← 0, tested ← ∅  

> 2:

for i = 1 to N do  

> 3:

λ ← SAMPLE RANDOM (Λ\ tested)  

> 4:

score ← TRAIN EVALUATE (λ, 30 epochs)  

> 5:

Update best if score > best_score  

> 6:

end for  

> 7:

return best architecture 

3.5.2 Progressive Search 

Searches in three phases of increasing complexity, with 

[4 , 5, 6] architectures per phase (15 total). Phase 1 uses conservative search spaces (small latent dims, no residual); Phase 3 uses aggressive spaces (large latent dims, residual enabled) [13]. 

Algorithm 2 Progressive Search  

> 1:

phases ← [conservative, moderate, aggressive], targets ←

[4, 5, 6]  

> 2:

for phase p = 1 , 2, 3 do  

> 3:

Set search space ← Λp 

> 4:

for i = 1 to targets[ p] do  

> 5:

λ ← SAMPLE RANDOM (Λp) 

> 6:

score ← TRAIN EVALUATE (λ, 30 epochs)  

> 7:

Update best if score > best_score  

> 8:

end for  

> 9:

end for  

> 10:

return best architecture 

3.5.3 Adaptive Exploration 

Maintains a history of successful configurations (score ≥ 0.5)and uses this history to bias sampling — with probability 

0.7 mutating a successful configuration, otherwise exploring randomly. Conceptually related to exploitation-biased evolu-tionary search. 

Algorithm 3 Adaptive Exploration  

> 1:

Initialize: successful_configs ← [ ], iteration ← 0 

> 2:

while iteration < N do  

> 3:

if |successful_configs | < 3 then  

> 4:

λ ← SAMPLE RANDOM (Λ) 

> 5:

else if random() < 0.7 then  

> 6:

c ← RANDOM CHOICE (successful_configs)  

> 7:

λ ← MUTATE (c) 

> 8:

else  

> 9:

λ ← SAMPLE RANDOM (Λ) 

> 10:

end if  

> 11:

score ← TRAIN EVALUATE (λ, 30 epochs)  

> 12:

if score ≥ 0.5 then  

> 13:

successful_configs.append( λ); keep last 10  

> 14:

end if  

> 15:

Update best; iteration ← iteration +1  

> 16:

end while  

> 17:

return best architecture 

3.5.4 Multi-fidelity Search 

Evaluates each candidate at three progressive fidelity stages, eliminating poor performers early. Related to Successive Halv-ing [11] and Hyperband [10]. 

Algorithm 4 Multi-fidelity Search  

> 1:

Initialize: evaluation_history ← [ ]  

> 2:

while architecture_count < N do  

> 3:

λ ← SAMPLE RANDOM (Λ) 

> 4:

score 1 ← TRAIN EVALUATE (λ, 10 epochs)  

> 5:

if score 1 < 0.5 then  

> 6:

skip  

> 7:

end if  

> 8:

score 2 ← TRAIN EVALUATE (λ, 20 epochs)  

> 9:

if score 2 < 0.7 then  

> 10:

record score 2; continue  

> 11:

end if  

> 12:

score 3 ← TRAIN EVALUATE (λ, 30 epochs)  

> 13:

Update best if score 3 > best_score  

> 14:

end while  

> 15:

return best architecture 43.5.5 Adversarial Gradient-based Search (Adversarial-NAS) 

Following [6], architecture parameters are relaxed to contin-uous values and jointly optimized with model weights via gradient descent, similar to DARTS [26]. Generator and dis-criminator architectures are represented as weighted mixtures over candidate operations: 

¯o(i,j ) = X

> o∈O

exp( α(i,j ) 

> o

)

P 

> o′

exp( α(i,j ) 

> o′

) · o(x) (3) where α(i,j ) are learnable architecture parameters and Gumbel-Softmax [36] enables gradient flow through discrete selections. Following the bilevel scheme of DARTS [26], we hold out a validation split and alternate two updates each epoch: network weights wG, w D are updated on a training 

batch via the adversarial loss, while the architecture param-eters αG, α D are updated on a validation batch, so that α is optimized for validation performance rather than training fit. After search, the discrete architecture is derived by taking the 

arg max over each α(i,j ).

Algorithm 5 Adversarial Gradient-based Search  

> 1:

Initialize weights w and architecture parameters α 

> 2:

while epoch < N do  

> 3:

Sample training batch Btr , validation batch Bval  

> 4:

wG, w D ← update on Btr by adversarial loss {weights}  

> 5:

αG, α D ← update on Bval by validation loss {architec-ture}  

> 6:

epoch ← epoch +1  

> 7:

end while  

> 8:

return architecture from arg max o α(i,j )

> o

## 3.6 Evaluation Metrics 

We employ a two-stage evaluation pipeline to separate archi-tecture search from final performance reporting. 

Stage 1 — Architecture Filtering (30 epochs): Each can-didate architecture is trained for 30 epochs and ranked using a composite Enhanced Score — a weighted combination of training health, output diversity, structural consistency, conver-gence efficiency, and script-specific visual quality: 

Enhanced Score = 0 .30 × Base + 0 .20 × Diversity 

+ 0 .20 × Consistency + 0 .15 × Efficiency 

+ 0 .15 × QNepali (4) 

Base Score measures fundamental training health: 

Base = 0 .4 × Stability + 0 .3 × Balance + 0 .3 × Quality (5) 

where Stability tracks training consistency over time: 

Stability = Stable Epochs Reference Epochs (10) (6) 

Balance measures proximity of generator G and discrimina-tor D losses to the ideal value of 1.0:

Balance = max(0 , 1 − | Gℓ − 1|) + max(0 , 1 − | Dℓ − 1|)2 (7) 

and Quality assesses pixel integrity via intensity ¯Ic and standard deviation σc across N classes: 

Quality = 1

N

> N

X

> c=1

max(0 , 1 − | ¯Ic − 0.5|) + min(1 , 2σc)2 (8) 

Diversity penalizes mode collapse via average pairwise distance between generated samples: 

Diversity = min 



1, Avg. Pairwise Distance 

2.0



(9) 

Consistency measures label-conditional stability across B

batches and K classes: 

Consistency = 1

K

> K

X

> k=1

1

B

> B

X

> b=1

11 + Var (Xk,b ) (10) 

Here Var (Xk,b ) is the pixel variance among samples gener-ated for class k in batch b, so Consistency rewards a label reli-ably producing a coherent digit rather than collapsing to noise. It therefore pulls against Diversity (Eq. 9), which rewards cross-sample variation; the two carry equal weight ( 0.20 each) so the Enhanced Score favours outputs that are recognizable per class yet varied across samples, rather than maximizing either alone. 

Efficiency rewards fast convergence and compact latent spaces: 

Efficiency = 12

"

11 + |Gℓ − Dℓ| + 11 + dz

> 100

#

(11) 

where dz is the latent dimension and Gℓ, Dℓ denote the final generator and discriminator losses respectively. The compact-ness sub-term 1/(1+ dz /100) is a soft preference, not a hard penalty: Efficiency carries only 15% of the Enhanced Score, so its effect is small (a shift from dz =64 to dz =128 changes the total score by ∼0.01 ). In practice it did not override the other terms — Adaptive Exploration still selected dz =128 

— so the larger latent space was retained where it improved quality. 

Nepali Quality We introduce QN epali as a domain-aware heuristic to strictly penalize topological failures in early train-ing. By explicitly rewarding stroke integrity ( Sstroke ) and variance-based sharpness ( Ssharp ), the search algorithm dis-cards topologically unsound architectures prior to full conver-gence. 

QNepali = 1

N

> N

X

> i=1

Ssharp (xi) × Sstroke (xi) (12) 

where sharpness Ssharp = min 



1, Var (∇2xi)0.15 



uses the Laplacian variance, and stroke integrity Sstroke is based on 5the number of connected components n: 1.0 if n=1 , 0.9 if 

n=2 , and max(0 , 1 − (n − 2) × 0.25) if n> 2.The architecture with the highest Enhanced Score per strat-egy is selected and passed to Stage 2. 

Stage 2 — Full Training and Final Evaluation (500 epochs): The selected architecture is retrained from scratch for 500 epochs and evaluated using four standard metrics: 

FID [21] (lower is better): FID = ∥μr − μg ∥2 + Tr 



Σr + Σ g − 2pΣr Σg



(13) 

Inception Score (IS) [37] (higher is better): IS = exp  Ex∼pg [DKL (p(y|x) ∥ p(y))]  (14) 

Precision & Recall [38]: Precision = |{ xg : xg ∈ supp (preal )}| |{ xg }| (15) Recall = |{ xr : xr ∈ supp (pgen )}| |{ xr }| (16) Precision measures sample fidelity; Recall measures mode coverage. A model with high Precision but low Recall is mode-collapsing; high Recall but low Precision indicates diversity at the cost of fidelity. All Stage 2 FID values are computed on 20,000 samples for consistency [39]. 

Justification of 30-epoch proxy. The 30-epoch filtering stage follows the multi-fidelity principle [10, 11] that short runs yield reliable relative rankings before convergence, es-pecially when the criterion captures training dynamics rather than final output quality [9]. The Enhanced Score is built for this setting: measured at 30 epochs, its Stability, Balance, and diversity terms indicate which architectures will train well over 500 epochs at a fraction of the cost. We use it strictly as a within-strategy criterion — to select the best candidate each strategy proposes, not to predict absolute final FID across strategies. The best scores from all strategies fall in a narrow band (0.976–0.989), yet within each strategy the Enhanced Score spreads candidates widely (per-strategy ranges of 0.15– 0.47). The proxy therefore discriminates effectively among the architectures a strategy proposes, even though the per-strategy winners cluster tightly: the FID gap across strategies reflects the quality of candidates each proposes , not differ-ences in proxy score, and Adaptive Exploration’s advantage is proposing better candidates. 

## 3.7 CNN Classifier for Downstream Evaluation 

To assess the practical utility of GAN-generated digits beyond distributional metrics, we train a lightweight CNN classifier in a low-resource setting and measure whether augmenting real training data with GAN-generated samples improves recogni-tion accuracy. 

Architecture. The classifier is intentionally lightweight to ensure the baseline has meaningful headroom for improve-ment, making the effect of GAN augmentation clearly ob-servable. It comprises a single convolutional layer ( 5 × 5

kernel, 8 filters) followed by ReLU activation and 2 × 2 max pooling, yielding feature maps of size 14 × 14 . The flattened 

8 × 14 × 14 representation is projected through a 32-unit hid-den layer (dropout p = 0 .6, ReLU) to a 10-class output. The model contains significantly fewer parameters than standard CNN classifiers, making it sensitive to training data quantity and quality. 

Training protocol. The model is trained with Adam (lr = 0 .001 , batch size = 32 ) for 30 epochs with no early stopping. Training images use light augmentation (random rotation ±10 ◦, random translation ±10% ); test images use only resize and normalize. Random seed is fixed at 42 for reproducibility. The validation split (10% of training data) is used only for monitoring — final evaluation is performed separately on the held-out test set (3,000 samples, 300 per class). 

Evaluation conditions. We simulate a single low-resource scenario by limiting real training data to 250 samples per class (2,500 total), and evaluate two conditions on the same held-out test set (3,000 samples, 300 per class): 1. Baseline: CNN trained on 250 real samples per class only (2,500 total). 2. GAN-augmented: CNN trained on 250 real samples per class augmented with approximately 1,000 GAN-generated samples per class (9,935 total) from the best NAS model — Adaptive Exploration (Section 4.4), yielding approximately 1,250 training samples per class. Both conditions use identical architecture, hyperparameters, and training protocol. Results are reported in Section 4.6. 

# 4 Experiments and Results 

## 4.1 Experimental Setup 

All experiments use the dataset of Section 3.3. The four sampling strategies (Random, Progressive, Adaptive Explo-ration, and Multi-fidelity) each evaluate N = 15 candidates by Enhanced Score (Equation 4) after short-epoch filtering, whereas AdversarialNAS instead refines a single architecture over ∼300 gradient-based iterations. The best architecture per strategy is then retrained from scratch for 500 epochs (Table 2). All runs use an NVIDIA RTX 4050 GPU and PyTorch 2.0 with Adam [8] (learning rate 0.0002 , β1 = 0 .5, β2 = 0 .999 ,batch size 64 ). Owing to compute constraints we report a sin-gle run per architecture; multi-seed estimation is left to future work [12]. Our aim is to quantify the benefit of automated search over a manual design point, not to claim state-of-the-art 6synthesis. For Multi-fidelity Search, candidates pass through 10-, 20-, and 30-epoch stages with promotion thresholds of 0.5 and 0.7; those failing Stage 1 are discarded and those failing Stage 2 are recorded but not promoted. 

## 4.2 NAS vs. Manual DCGAN Baseline 

Table 2 presents the main results comparing the manual DC-GAN baseline to all five NAS strategies. All five NAS strategies improve FID over the manual base-line. Even Random Search reduces FID by 62.59% in under 58 minutes; principled sampling of the search space already beats a single manual design point. Adaptive Exploration gives the largest FID improvement overall — 332 .28 → 79 .12 (76 .19% ) — in 56 .56 minutes, the same budget as Random and Progressive Search. It also has the best Recall among the NAS strategies ( 0.531 ). The manual baseline scores slightly higher ( 0.552 ), but paired with much worse FID and lower Precision ( 0.693 ), so its “diversity” reflects low-fidelity output rather than genuine coverage. Multi-fidelity Search attains the highest IS (2.08), just above the manual baseline (2.00). Thus NAS sharply improves distri-butional similarity (FID) while the baseline stays competitive on IS — the known FID–IS divergence [39]: IS rewards con-fident, class-distinct predictions even under poor coverage, whereas FID measures the full distributional distance and is the more reliable metric. Adaptive Exploration and Progres-sive Search score 1.72 on IS, trading it for the FID gains that are this work’s primary criterion. AdversarialNAS reduces FID by 67.11% with perfect Preci-sion (1.00); its 124.50-minute search reflects gradient-based optimization of one architecture and is not directly comparable to the sampling strategies. Per-strategy training loss curves are omitted here for space; their key signal — convergence stability and generator– discriminator balance — is already quantified by the Stability and Balance components of the Enhanced Score (Section 3.6), and the full curves are available with the released code. 

## 4.3 Computational Efficiency 

Search time refers to the filtering stage — training and scoring all candidates before the best architecture proceeds to 500-epoch full training. Among the sampling strategies, Adaptive Exploration, Ran-dom, and Progressive Search all finish within 56–58 minutes, so history-guided sampling adds no meaningful overhead over uniform sampling. Multi-fidelity Search takes 125.88 minutes: the long runtime indicates that most candidates cleared the 0.5 Stage 1 threshold and advanced to later stages, so early stopping saved little here. This matches prior findings that multi-fidelity methods save most when many configurations fail early [11]; larger or denser search spaces would benefit more. 

Figure 3: Search time (minutes) per NAS strategy during the filtering stage. Dashed line marks the 60-minute threshold. AdversarialNAS records 124.50 minutes, reflecting ∼300 

epochs of gradient-based optimization of one architecture — depth of search rather than breadth — and is not directly comparable on search time. The manual baseline needs no search but fixes a single one of 128 generator configurations; exhaustively exploring the joint G–D space of 4,096 pairs is infeasible on a single GPU. NAS recovers a substantially better configuration in under 60 minutes. 

## 4.4 Architecture Analysis 

Table 3 presents the complete layer-by-layer architecture of the best NAS-discovered model (Adaptive Exploration), com-pared against the manual DCGAN baseline. The Generator 

G progressively upsamples from a 128 -dimensional latent vector concatenated with a one-hot class label through three residual blocks and transposed convolution stages, reaching a 

1 × 32 × 32 output image. The Discriminator D mirrors this structure, downsampling the concatenated image-label input through three residual blocks to a scalar real/fake score. We note that the first discriminator block (Down-1) carries no normalization. This is not a searched choice but a fixed con-vention we adopt from standard DCGAN practice [8]: the in-put layer operates directly on raw pixels, where normalization would standardize away the low-level intensity statistics the discriminator relies on; all deeper blocks retain the searched normalization. The searched dimensions thus apply to layers Down-2 onward, with the raw-pixel layer held fixed. 

## 4.5 Qualitative Results 

Adaptive Exploration’s generated digits show clear, legible strokes across all ten classes. The shirorekha (horizontal head-line) is consistently reproduced and inter-class boundaries are well preserved — each row is a visually distinct digit, confirm-ing effective conditioning on y. Some within-class variation in stroke thickness (notably digits 2 and 3) is consistent with the Precision of 0.980 , while the Recall of 0.531 reflects broad coverage with no visible mode collapse. 7Table 2: Performance comparison: Manual DCGAN baseline vs. NAS-discovered architectures on Devanagari digit synthesis. Best result per metric (across all rows, including the baseline) in bold . ∆FID computed relative to manual DCGAN (332.28); all NAS strategies improve upon the baseline. †AdversarialNAS optimizes a single architecture over ∼300 gradient steps rather than evaluating 15 discrete candidates; search time is not directly comparable to sampling-based strategies. 

Method FID ↓ IS ↑ Precision ↑ Recall ↑ Search Time (min) ∆FID vs. Manual 

Manual Baseline 

Manual DCGAN 332.28 2.00 0.693 0.552 – –

NAS Strategies: sampling strategies use 15 configs × 30-epoch filtering → 500-epoch full training 

Random Search [9] 124.30 1.86 0.861 0.516 57.87 −62.59% Progressive Search 127.70 1.72 0.961 0.515 55.86 −61.57% Adaptive Exploration 79.12 1.72 0.980 0.531 56.56 −76 .19 %

Multi-fidelity [10] 123.67 2.08 1.00 0.512 125.88 −62.78% AdversarialNAS † [6] 109.29 1.84 1.00 0.504 124.50 † −67.11% Table 3: Best NAS-discovered architecture (Adaptive Exploration)                                                                                           

> Generator G(dz=128 )
> Block Operation Output Norm Act. Add-ons
> Input z128 ⊕y→R256 256 ×1×1–––Initial ConvT 256 →512 512 ×4×4BN ReLU –Up-1 ConvT 512 →256 256 ×8×8BN ReLU Drop 0.2, ResBlock Up-2 ConvT 256 →128 128 ×16 ×16 BN ReLU ResBlock Output ConvT 128 →11×32 ×32 –Tanh –
> Discriminator D
> Block Operation Output Norm Act. Add-ons
> Input x1×32 ×32 ⊕y→2ch 2×32 ×32 –––Down-1 Conv 2→64 64 ×16 ×16 none LReLU Drop 0.5, ResBlock Down-2 Conv 64 →128 128 ×8×8BN LReLU Drop 0.5, ResBlock Down-3 Conv 128 →256 256 ×4×4BN LReLU Drop 0.5, ResBlock Output Conv 256 →1scalar –Sigmoid –
> Conv/ConvT: 4×4, stride 2, pad 1 (Initial & D-output: stride 1, no pad). ResBlock: 2×Conv 3×3+ identity skip.

Figure 4: Generated Devanagari digits from Adaptive Explo-ration (500-epoch checkpoint). One row per digit class (0–9), random samples. 

## 4.6 Downstream Classification Utility of GAN-Generated Data 

To validate the practical utility of NAS-generated digits be-yond distributional metrics, we train a lightweight CNN clas-sifier under a low-resource setting (250 real samples per class) with and without GAN augmentation from the best NAS model (Adaptive Exploration). Table 4: CNN classification results in low-resource setting (250 real samples per class; test set: 3,000 samples, 300 per class; 30 epochs, batch size 32). 

Training Data Real only Real + GAN 

Accuracy 91.0% 96.5% 

Precision 91.1% 96.6% 

Recall 91.0% 96.5% 

F1 91.0% 96.5% 

∆ Accuracy +5.6 percentage points Augmenting the 250 real samples per class with GAN-generated digits raises CNN accuracy from 91 .0% to 96 .5% 

(+5 .6 percentage points). The gain holds across precision, recall, and F1: augmentation raises overall classifier quality rather than trading one metric for another. Both conditions already apply standard image augmentation (random rotation and translation; Section 3.7), so this is the benefit of GAN-generated samples on top of conventional augmentation, not a 8replacement for it. The classifier is deliberately lightweight to model a resource-constrained deployment; a higher-capacity classifier would narrow the absolute headroom, so we read the +5 .6-point gain as evidence that the synthetic samples are useful rather than as an upper bound on the effect. 

# 5 Discussion 

## 5.1 Why Adaptive Exploration Outperforms All Strategies 

The 76.19% FID improvement of Adaptive Exploration over the manual baseline stems from two factors. First, it evaluates 15 architectures rather than one, covering a meaningful frac-tion of the joint generator–discriminator space (4,096 pairs) that manual design cannot. Second, its history-guided muta-tion biases later candidates toward regions that scored well on the Enhanced Score, avoiding unstable or mode-collapsing configurations early. 

## 5.2 The Quality-Diversity Trade-off 

The four metrics trace a quality-diversity spectrum rather than a single winner [38]. On FID, Adaptive Exploration leads clearly ( 79 .12 vs. 62 –

67% gains for the others), suggesting history-guided sampling is especially effective at closing the overall distributional gap. On IS, Multi-fidelity Search edges out even the manual baseline ( 2.08 vs. 2.00 ). This reflects how IS is scored: locally sharp, confident predictions from the Inception classifier can occur even without good distributional coverage, so a high-FID model can still post a decent IS. NAS methods like Adaptive Exploration trade some of that sharpness for better FID, which we treat as the more reliable metric [39]. On Precision, AdversarialNAS and Multi-fidelity both hit 

1.00 — their gradient-based and multi-stage filtering actively suppress low-fidelity samples, at the cost of Recall. On Recall, Adaptive Exploration leads among NAS meth-ods ( 0.531 ); only the manual baseline is higher ( 0.552 ), for the reason noted above. AdversarialNAS is lowest ( 0.504 ), consistent with its narrower, high-confidence optimization. In practice: use Adaptive Exploration for overall quality and coverage, AdversarialNAS or Multi-fidelity when low-fidelity samples are costly (e.g., augmentation pipelines), and Multi-fidelity when IS is the target metric. 

## 5.3 The Role of the Enhanced Score 

A key design choice in NepScript Genesis is using the En-hanced Score (Equation 4) rather than a proxy FID for archi-tecture filtering. The Enhanced Score is computable after only 30 epochs and captures training health signals (Stability, Bal-ance) that predict final model quality without requiring the full 500-epoch run. The inclusion of Nepali Quality ( QNepali ) as a domain-specific component ensures that architectures are re-warded for producing legible Devanagari strokes — a criterion that generic metrics like IS would not capture. 

## 5.4 Limitations 

• Search space scope: Our 128-configuration search space covers only five dimensions. Deeper networks, attention mechanisms, and alternative loss functions (e.g., Wasser-stein [16], spectral normalization [18]) are excluded. • Single-seed results: Due to compute constraints, each ar-chitecture is evaluated with a single training run. Multi-seed variance estimation [12] would strengthen the reliability of the reported numbers. • Script specificity: Results are on Devanagari digits; general-ization to the full Devanagari character set or other Brahmic scripts requires additional validation [4]. • Evaluation metric sensitivity: FID is known to be sensi-tive to the number of samples and the choice of Inception network [39]. All FID values in this paper are computed on 

20 ,000 samples to minimize this sensitivity. 

# 6 Conclusion 

We presented NepScript Genesis, a NAS framework for automated GAN architecture discovery for conditional De-vanagari handwritten digit synthesis. All five NAS strate-gies outperform the manually designed DCGAN baseline: within a fixed compute budget, automated search consistently finds better configurations than a single hand-crafted design. Adaptive Exploration achieves the best overall result — FID 

= 79 .12 , a 76.19% improvement over the manual baseline (FID = 332 .28 ) — while matching the search time of the simplest strategies (56.56 min). AdversarialNAS delivers per-fect Precision (1.00) through gradient-based joint optimization of G and D, and Multi-fidelity Search achieves the highest IS (2.08). The two-stage evaluation pipeline — 30-epoch Enhanced Score filtering followed by 500-epoch full train-ing — provides an efficient and domain-aware mechanism for architecture selection, with the Nepali Quality compo-nent ( QNepali ) ensuring that Devanagari stroke characteristics are explicitly rewarded during search. In a downstream low-resource evaluation, augmenting 250 real training samples per class with GAN-generated digits from Adaptive Exploration improves CNN classification accuracy by +5 .5 percentage points ( 91 .0% → 96 .5% ), confirming that NAS-optimized synthesis produces digits of sufficient quality to benefit practi-cal recognition pipelines when real data is scarce 9References 

[1] Ashok Kumar Pant et al. A multi-purpose dataset of Devanagari script comprising of isolated numerals and vowels. Data in Brief , 39:107642, 2021. [2] Soumyadeep Guha et al. Enhancing handwritten Devana-gari character recognition via GAN-generated synthetic data. In Proceedings of the 2024 Sixteenth International Conference on Contemporary Computing (IC3) , 2024. [3] Shivam Mishra et al. Augmentation of handwritten De-vanagari character dataset using DCGAN. In Springer Lecture Notes in Networks and Systems , 2022. [4] Ayan Kumar Bhunia, Abhirup Das, Ankan Kumar Bhu-nia, Perla Sai Raj Kishore, and Partha Pratim Roy. Hand-writing recognition in low-resource scripts using adver-sarial learning. In Proceedings of the IEEE/CVF Con-ference on Computer Vision and Pattern Recognition (CVPR) , pages 4767–4776, 2019. [5] Xin He, Kaiyong Zhao, and Xiaowen Chu. AutoML: A survey of the state-of-the-art. Knowledge-Based Systems ,212:106622, 2021. [6] Chen Gao, Yunpeng Chen, Si Liu, Zhenwei Hao, and Jiawei Tan. AdversarialNAS: Adversarial neural architec-ture search for GANs. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , 2020. [7] Xinyu Gong, Shiyu Chang, Yifan Jiang, and Zhangyang Wang. AutoGAN: Neural architecture search for gen-erative adversarial networks. In Proceedings of the IEEE/CVF International Conference on Computer Vi-sion (ICCV) , 2019. [8] Alec Radford, Luke Metz, and Soumith Chintala. Un-supervised representation learning with deep convolu-tional generative adversarial networks. arXiv preprint arXiv:1511.06434 , 2015. [9] Liam Li and Ameet Talwalkar. Random search and repro-ducibility for neural architecture search. In Proceedings of the 36th Conference on Uncertainty in Artificial Intel-ligence (UAI) , 2020. [10] Lisha Li, Kevin Jamieson, Giulia DeSalvo, Afshin Rostamizadeh, and Ameet Talwalkar. Hyperband: A novel bandit-based approach to hyperparameter optimiza-tion. Journal of Machine Learning Research (JMLR) ,18(185):1–52, 2017. [11] Kevin Jamieson and Ameet Talwalkar. Non-stochastic best arm identification and hyperparameter optimization. In Proceedings of the 19th International Conference on Artificial Intelligence and Statistics (AISTATS) , 2016. [12] Joelle Pineau, Philippe Vincent-Lamarre, Koustuv Sinha, Vincent Larivière, Alina Beygelzimer, Florence d’Alché Buc, Emily Fox, and Hugo Larochelle. Improving re-producibility in machine learning research. volume 22, pages 1–20, 2021. [13] Tero Karras, Timo Aila, Samuli Laine, and Jaakko Lehti-nen. Progressive growing of GANs for improved quality, stability, and variation. In International Conference on Learning Representations (ICLR) , 2018. [14] Tero Karras, Samuli Laine, and Timo Aila. A style-based generator architecture for generative adversarial networks. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) ,pages 4401–4410, 2019. [15] Andrew Brock, Jeff Donahue, and Karen Simonyan. Large scale GAN training for high fidelity natural im-age synthesis. In International Conference on Learning Representations (ICLR) , 2019. [16] Martin Arjovsky, Soumith Chintala, and Léon Bottou. Wasserstein generative adversarial networks. 2017. [17] Ishaan Gulrajani, Faruk Ahmed, Martin Arjovsky, Vin-cent Dumoulin, and Aaron Courville. Improved training of Wasserstein GANs. In Advances in Neural Informa-tion Processing Systems (NeurIPS) , 2017. [18] Takeru Miyato, Toshiki Kataoka, Masanori Koyama, and Yuichi Yoshida. Spectral normalization for generative adversarial networks. In International Conference on Learning Representations (ICLR) , 2018. [19] Sergey Ioffe and Christian Szegedy. Batch normalization: Accelerating deep network training by reducing internal covariate shift. In International Conference on Machine Learning (ICML) , pages 448–456, 2015. [20] Dmitry Ulyanov, Andrea Vedaldi, and Victor Lempitsky. Instance normalization: The missing ingredient for fast stylization. arXiv preprint arXiv:1607.08022 , 2016. [21] Martin Heusel, Hubert Ramsauer, Thomas Unterthiner, Bernhard Nessler, and Sepp Hochreiter. GANs trained by a two time-scale update rule converge to a local Nash equilibrium. In Advances in Neural Information Process-ing Systems (NeurIPS) , volume 30, 2017. [22] Mehdi Mirza and Simon Osindero. Conditional gener-ative adversarial nets. arXiv preprint arXiv:1411.1784 ,2014. [23] Barret Zoph and Quoc V. Le. Neural architecture search with reinforcement learning. In International Conference on Learning Representations (ICLR) , 2017. 10 [24] Hieu Pham, Melody Guan, Barret Zoph, Quoc Le, and Jeff Dean. Efficient neural architecture search via param-eter sharing. In International Conference on Machine Learning (ICML) , 2018. [25] Esteban Real, Alok Aggarwal, Yanping Huang, and Quoc V. Le. Regularized evolution for image classi-fier architecture search. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 33, pages 4780–4789, 2019. [26] Hanxiao Liu, Karen Simonyan, and Yiming Yang. DARTS: Differentiable architecture search. In Interna-tional Conference on Learning Representations (ICLR) ,2019. [27] Sirui Xie, Hehui Zheng, Chunxiao Liu, and Liang Lin. SNAS: Stochastic neural architecture search. In Interna-tional Conference on Learning Representations (ICLR) ,2019. [28] Ghadah Alotaibi. Automating generative adversarial networks using neural architecture search: A review. 

arXiv preprint , 2025. [29] Shailesh Acharya, Ashok Kumar Pant, and Prashnna Ku-mar Gyawali. Deep learning based large scale hand-written Devanagari character recognition. In Proceed-ings of the 9th International Conference on Software, Knowledge, Information Management and Applications (SKIMA) , 2015. [30] Shailesh Acharya and Prashnna Kumar Gyawali. De-vanagari handwritten character dataset. UCI Machine Learning Repository, 2015. [31] Ruchi Giri et al. Conditional generative adversarial net-work for Devanagari handwritten character generation. In IEEE International Conference on Computing, Com-munication and Networking Technologies (ICCCNT) ,2021. [32] Ian Goodfellow, Jean Pouget-Abadie, Mehdi Mirza, Bing Xu, David Warde-Farley, Sherjil Ozair, Aaron Courville, and Yoshua Bengio. Generative adversar-ial nets. Advances in Neural Information Processing Systems (NeurIPS) , 27, 2014. [33] Nitish Srivastava, Geoffrey Hinton, Alex Krizhevsky, Ilya Sutskever, and Ruslan Salakhutdinov. Dropout: A simple way to prevent neural networks from over-fitting. Journal of Machine Learning Research (JMLR) ,15(1):1929–1958, 2014. [34] Kaiming He, Xiangyu Zhang, Shaoqing Ren, and Jian Sun. Deep residual learning for image recognition. In 

Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 770–778, 2016. [35] Christian Szegedy, Vincent Vanhoucke, Sergey Ioffe, Jon Shlens, and Zbigniew Wojna. Rethinking the inception architecture for computer vision. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 2818–2826, 2016. [36] Eric Jang, Shixiang Gu, and Ben Poole. Categorical repa-rameterization with Gumbel-Softmax. In International Conference on Learning Representations (ICLR) , 2017. [37] Tim Salimans, Ian Goodfellow, Wojciech Zaremba, Vicki Cheung, Alec Radford, and Xi Chen. Improved techniques for training GANs. In Advances in Neural Information Processing Systems (NeurIPS) , 2016. [38] Tuomas Kynkäänniemi, Tero Karras, Samuli Laine, Jaakko Lehtinen, and Timo Aila. Improved precision and recall metric for assessing generative models. In 

Advances in Neural Information Processing Systems (NeurIPS) , 2019. [39] Ali Borji. Pros and cons of GAN evaluation measures: New developments. Computer Vision and Image Under-standing , 215:103329, 2022. 11