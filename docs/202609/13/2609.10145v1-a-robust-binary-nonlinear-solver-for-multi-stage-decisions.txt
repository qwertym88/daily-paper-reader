Title: A Robust Binary Nonlinear Solver for Multi-stage Decisions

URL Source: https://arxiv.org/pdf/2609.10145v1

Published Time: Thu, 10 Sep 2026 01:04:17 GMT

Number of Pages: 19

Markdown Content:
# A Robust Binary Nonlinear Solver for Multi-stage Decisions 

## Mathematical Note 

Kashif Rashid 1

Scientist 

May 2026 

## Abstract 

This document describes two developments. The first is a Multi-stage Decision Framework that permits the formulation of various sequential decision-making problems in a generic manner. The second con-cerns a robust stochastic solution procedure designed to handle the binary nonlinear optimization problem stemming from the proposed decision framework. The method adopts Monte-Carlo search with incremen-tal probabilistic learning with reinforcement. The two developments are described herein along with a demonstrative application example. 

Key words: Multi-stage, decisions, binary systems, probabilistic and reinforcement learning. 

> 1

Formerly, Scientific Advisor, Schlumberger-Doll Research, One Hampshire Street, Cambridge, MA 02139. Contact email: kash4questions@yahoo.com arXiv:2609.10145v1 [math.OC] 9 Sep 2026 DECISION FRAMEWORK 

## Multi-stage Decision Framework 

Multi-stage decision problems, with or without availability of new information, are not uncommon in real-world settings. These concern situations in which decisions are made at well-defined points (or stages), considered either spatially or temporally, to achieve a desired outcome over the entire horizon subject to local and global constraints [1]. In this setting, local constraints apply to individual stages or components, while global constraints may apply to some metric over the entire decision horizon or some part thereof. The generic decision problem can be posed as one in which there are N stages, each with M components or choices. This gives rise to M N combinations or possible decision paths. Evidently the problem quickly becomes computationally involved with an increasing size for N or M .The solution process serves to identify the optimal path (the selection of components in each stage) so as to maximize the desired objective. Consider the classic knapsack problem as an example. Here, given a collection of N items, each with a given weight and utility, the goal is to select a group of items that maximize the total utility subject to a permitted weight limit [2]. With only two choices per step ( M =2), to select or not to select, this gives rise to 2 N possible outcomes. This number grows significantly with increasing N . While, efficient methods exist for mixed-integer linear problems of this type, for nonlinear cases in the general, the problem quickly becomes intractable due to its combinatorial nature. This is further exacerbated with an increasing number of choices M . Such problems may concern the selection of unit operations in a process train [3]. Here the notion of the super-structure is employed in which all design choices are presented by stage and an optimal selection is sought to maximize yield, profit or other intended goal [4, 5, 6]. Mixed-integer nonlinear formulations are used in which component nonlinearity is invariably assuaged in preference for ease of resolution. Nonetheless, well-structured formulations are necessary to capture design optionality and restrictions using generalized disjunctive programming [7, 8]. Such formulations are non-trivial and particularly cumbersome to develop for large-scale models comprising many design options. Thus, efficient solution methods are not only desirable, but necessary for highly nonlinear problems. This serves as the motivation for the multi-stage decision framework and the robust binary nonlinear solution approach. In the multi-stage decision framework, the posed problem is reduced to a graph. A starting node connects to the nodes in the first layer, each of these will connect to those in the next layer and so on, until the nodes in the last layer connect to one terminating node (example shown in Figure 1). Thus, the goal is to find an optimal path from the start to the final node. The framework permits a fully connected graph (a node in one layer connects to all the nodes in the next layer) or a partially connected graph (where the feasible node connections are stipulated in advance) as per Figure 2. In this regard, the graph may be initialized as fully connected and all undesired links are subsequently removed. Alternately, the graph may commence with zero connections and each permitted link is added in turn. We refer to a connection map in which node i in layer ( j − 1) connects to node k in layer j if the flag is true, given by the tuple [ j − 1 ij k 1], or indicates a deactivation if it is false, with [ j − 1 i j k 0]. In this manner, any arrangement can be posed for consideration. For a sparse graph, only the feasible links need be described. Note that the scheme is flexible to permit the arrangement of connected sub-graphs or other configurations by need. The only stipulation is that the graph is directed (from left to right) and that the nodes connect layer-by-layer without jumps or return paths. Indeed, these elements, should they exist, can be removed from the outset by suitable graph construction. That is, with the insertion of dummy nodes and viable links as appropriate. Thus, many problems, indicative of restricted or conditioned choice, can be posed and formulated on the underlying directed graph. Each node in a layer represents a selectable component. In the foregoing, this was noted as a simple  

> Multi-stage Framework 2DECISION FRAMEWORK

connection in that the node had no other purpose than to indicate the selected path, where the objective value is a function of the chosen components. Now, each node can be described in further detail. We can assert the notion of a functional with integer input y given by v = f (y), where y = 1 indicates if the node is selected, and is zero otherwise. More generally, we can define fjk (y), as the functional representing node 

k in layer j, given as N (j, k ) in general. An active path link is defined between layer j − 1 and layer j for nodes i and k if node N (j − 1, i ) and node N (j, k ) are both active. Thus, the marked path is derived from the set of selected nodes, and the collective measure must ensure that the path is feasible ( i.e. connected). A penalty term (P2) is introduced to account for path infeasibility, one stemming from invalid connections given the nodes selected. Note that no path calculation is necessary for a fully connected graph as each is automatically valid, and the penalty term P2 is zero by default. The node functional (for component k in layer j) may also comprise a set of continuous variables X. Thus, the function fjk (X, y ) can be stated for the general case for component k in stage j. In this manner, each functional may represent a mixed-integer form, and the collective measure F (X , Y) indicates the function over all nodes and stipulated variables. Similarly, a local constraint can be defined as gjk (X, y )at node k in stage j, or as a global constraint G(X , Y) over the entire decision horizon. In general terms, these constraints may be defined as linear, nonlinear, as equality or inequality, and specified by need. The definitions should be provisioned at the outset for each node N (j, k ) and for the overall objective along with constraints as required. These are anticipated definitions in the problem statement. Details can be found in the Algorithm section below. Note that path-dependent constraints can also be defined, as a function of the selected nodes. For example, to address variations arising from selected edges as per the intended problem specification. The evaluation of path infeasibility (P2) is one such example. Note that all constraints must be managed implicitly as penalties as the proposed binary solver is unconstrained. The formulated problem is posed as a binary nonlinear system. The binary variables stem from the selection of components in each stage by index (an integer) represented as a binary string. For example, with M = 2 choices, a single bit gives two options [0 or 1], where 0 represents choice of component 1, while 1 indicates choice of component 2. For M = 4, a two-bit string yields four choices [00, 01, 10, 11] with integer values [0, 1, 2, 3] by index. Here, index 0 is for component 1, index 1 is for component 2, and so on. The same notion applies for arbitrary size M , where the bit string size is first established as a function of the anticipated number of choices Mj in stage j. If the resulting bit string range ( ymax j ), is greater than 

Mj − 1 (with the offset), then the excess index values will represent dummy nodes in the graph. A feasible or valid choice is one where the selected index yj is less than Mj . Thus, we introduce a penalty term (P1) for feasible component selection that serves to ensure only permitted components are selected by the solver. An elegant scheme is to stipulate a feasibility term cjk for component k in layer j a priori , where the value is 0 if feasible and sufficiently high if invalid. P1 is then simply the sum of cjk over all active components with yjk = 1. Evaluation of path penalty P2 is a little more involved as each selected edge must first be identified and then validated as being viable. Thus, problems with a fully connected graphs are faster to evaluate as this evaluation step is avoided. Ultimately, a feasible path is one that connects valid components stage-by-stage over valid links between the selected nodes, with penalty terms P1 and P2 as zero. For invalid paths, P1 and P2 will be non-zero. The penalty terms are established and added to the composite penalty function. Similarly, additional user specified constraints (both local and global) must be accounted for in the combined measure. Thus, the overall problem can be stated as follows:  

> Multi-stage Framework 3DECISION FRAMEWORK

min V (Z) = F (Z) + P1(Z) + P2(Z) + PL(Z) + PG(Z) (1) 

Z ∈ Bn

zi ∈ { 0, 1} ∀i ∈ [1 n]where V indicates the collective penalty function and Z is the representative set of binary variables of size 

n. P1 and P2 are the component and path penalty terms, respectively. In addition, PL represents the set of local constraints: 

PL = X

> i,j,k

γy jk (Z) max {0, g ijk (Z)}2 (2) where γ is a penalty multiplier, yjk asserts if component k in stage j is active, with i-th local constraint 

gijk . Laslty, PG represents the set of global constraints: 

PG = X

> i

γmax {0, G i(Z)}2. (3) A feasible path through a graph with full connectivity is shown in Figure 1, as compared to one with partial connectivity in Figure 2, where the goal is to maximize the collective value ensuing from each decision. Hence, the objective value decreases from 227 to 158 when certain links are no longer available. The robust stochastic solver is described in the next section, with algorithm details to follow.  

> Multi-stage Framework

4DECISION FRAMEWORK 

Figure 1: Solution path over 6 stages - fully connected case. 

6 stages with [4 3 3 4 2 3] components per stage. The binary system is 2 11 with 2048 choices. Blue squares represent valid choices, black squares indicate invalid choices and the edges show feasible links. 

Figure 2: Solution path over 6 stages - partially connected case. 

6 stages with [4 3 3 4 2 3] components per stage. The binary system is 2 11 with 2048 choices (not all feasible). Blue squares represent valid choices, black squares indicate invalid choices and the edges show feasible links.  

> Multi-stage Framework

5BINARY SOLVER 

## Binary Nonlinear Solver 

The robust binary nonlinear stochastic solver (RSS) is designed to manage binary unconstrained nonlinear problems. Here, we assume that all continuous variables of interest are suitably encoded, and constraints are handled by penalty reformulation (1). The problem is then stated as follows: 

min F (Z) (4) 

Z ∈ Bn

zi ∈ { 0, 1} ∀i ∈ [1 n]where Z is the set of binary variables of size n and F is the objective function of interest. Note that no stipulations are placed on the nature of this function. Specifically, F (Z) represents the penalty function derived from the multi-stage problem definition, as discussed in the last section, where Z of size sleng is the representative binary string composed by concatenation of decision strings representing all variables in the stipulated problem. The RSS serves to find the argument set Z that optimizes the collective measure F (Z), as per (4). Conversion of the binary string Z yields the index set corresponding to the selected component at each stage, along with any related continuous variables, if defined. These additional variables are automatically appended in the construction of string Z, with length bleng for the binary set and length cleng for the continuous set. All the variables are suitably extracted and decoded, per the problem definition, prior to evaluation. The stochastic solution procedure is based on the notion of establishing a probability distribution measure for each binary variable in the problem. This is achieved by sampling each binary variable commencing from a uniform distribution that is iteratively updated based on the information gathered from a random sample set. In this manner, the distribution array Xd of size n is sought that will indicate the likely probability of a binary variable taking a value of 1 (or 0 otherwise). It is the intent, based on learning, that sampling the updated Xd over time will yield ˆZ that optimizes F (Z). Indeed, the process is sample dependent and as such, cannot guarantee that the solution is optimal. However, the procedure is robust, able to handle nonlinearity and the noted problem complexity. Empirically, the solver is shown to be highly effective for problems of moderate size ( ∼n=1000). The method employs probabilistic learning with reinforcement and may be construed as a variant of population-based incremental learning used to enhance genetic algorithm operations [9]. The RSS employs an iterative schema based on stochastic generation and evaluation of sample paths. Here a path refers not to the selection of components in each stage of the multi-decision problem, but rather the evolution of the candidate control vector in n-space. That is, it traces the improving control set 

Z on a point-by-point basis with respect to the objective, where a given sample represents a solution over the problem graph 2. See for example Figure 4. Thus, an evolving sample path should not be confused with a solution path in the graph. Initially, a collection of random candidates are generated, using an uniformly initialized distribution array Xd, where each represents a point in n binary-space. A Monte-Carlo (MC) search is performed from each given starting point and is akin to random path evolution in n dimensions per sample. Each generated path j in the sample set S is evaluated and its corresponding objective fj = F (Zj ) value is stored. Next, the distribution array Xd is updated using the best generated paths based on ranked selection (typically,  

> 2Note that evaluation over a partial horizon would lead to a Monte-Carlo tree-search scheme.
> Multi-stage Framework 6BINARY SOLVER

using the top 20% by objective measure). The array Xd asserts a probability value for element zi to have a value of one in order to optimize the key functional F (Z). Hence, Xd can be construed as an evolving estimate of the PDF of Z for the intended goal. Future samples are thus generated using the updated distribution vector Xd. The process repeats for a given number of iterations for each initial candidate, where the best solution is updated to reflect to the best sample identified. Note that the sampling method is akin to epsilon-greedy approach, in which a small chance for exploration is always retained [10]. At the end of this cycle, information from all candidates is collated and an informed distribution Xd is derived over the group. A new cycle is then initiated using the updated distribution Xd (that was initially uniform) over all candidates for each bit zi in the string of size n. These candidates will thus begin to converge (enforcing 

exploitation ), yet maintain a small probability for random search (to enable exploration ). Demonstrative early and late stage distribution evolution plots (for the Knapsack problem) are given in Figures 5 and 6, respectively [2]. In summary, the RSS process employs Monte-Carlo search with probabilistic-informed learning with reinforcement. Each design candidate carries its own distribution Xd that evolves by iteration. Shared information over all candidates results in a collective update with a stipulated learning rate. The method is robust and empirically works well for moderate dimensionality ( ∼n = 1000) 3. The scheme is applicable to larger problem sizes but at greater sample cost. This is an issue that impacts most reinforcement learning methods and is a possible area for future research. Two test cases are presented next and the algorithmic details of the solver are presented in the section to follow.  

> 3The prototype was developed in MatLab and will benefit from a higher-level implementation.
> Multi-stage Framework 7TEST CASES

## Test Cases 

Test Case 1 

The proposed scheme is demonstrated using a classic Knapsack problem [2]. A decision-maker is given 50 items, each with a known utility and weight value, along with a weight capacity limit of 850. The goal is to pick a number of items to maximize the utility subject to the weight limit. This gives rise to N = 50 stages, each with M = 2 choices, to pick or not to pick an item. The solution path, indicating an objective value ˆF = 7534, is shown in Figure 3. Demonstrative candidate samples paths are shown in Figure 4. Lastly, early-stage and late stage evolving distributions are presented in Figures 5 and 6, respectively. Note that in Figure 6 the lower bound is -7534 (the anticipated result when the solver is minimizing). 

Figure 3: Solution path over 50 stages - Knapsack example [2]. 

50 stages with 2 components per stage. The binary system is 2 50 with 1.1259e 15 choices. 

Figure 4: Candidate sample path evaluation - Knapsack example [2]. 

Shows improving sample paths for a set of candidates for a given iteration.  

> Multi-stage Framework

8TEST CASES 

Figure 5: Early stage distribution estimation - Knapsack example [2]. 

Shows distribution evolution (overlapping) with objective (x-axis) statistics: lower, upper, range and mean. 

Figure 6: Late stage distribution estimation - Knapsack example [2]. 

Shows distribution evolution (overlapping) with objective (x-axis) statistics: lower, upper, range and mean.  

> Multi-stage Framework

9TEST CASES 

Test Case 2 

In this example, we consider the n-dimensional Rosenbrock benchmark case [11]. The problem is defined as follows: min f (X) = 

> n−1

X

> i=1

100( xi+1 − x2 

> i

)2 + ( xi − 1) 2 (5) s.t. − 2 ≤ xi ≤ 2

∀i ∈ [1 . . . n ]

X ∈ Rn

where the known solution is given by: ˆX=[1 . . . 1] with ˆF = 0. This continuous problem can be posed as a multi-stage problem with N stages and M = 1024 choices in each. The resulting 10-bit representation per stage gives a spatial granularity of 0.0039 (4/1024) over each dimension xi with bounds [-2 2]. The results are shown in Table 1 for increasing size N . Clearly, this is a hard problem using binary encoding as simple variations result in dramatic changes in the continuous space impeding progress and convergence for larger instances 4. The method is intended for integer selectivity and is not ideal for con-tinuous variable representation on the graph, i.e. , with a large number of choices, M = 1024. Nonetheless, the example serves to demonstrate robustness under increasing model complexity. 

Table 1: Test Case 2 - Results 

Stages Components Graph Size Model Search Solution Time 

N M N xM Complexity Space Fbest mins 

2 1024 2048 220 1.0486e 6 0.000096 1.71 5 1024 5120 250 1.1259e 15 0.002684 2.98 10 1024 10240 2100 1.2677e 30 0.000867 4.81 25 1024 25600 2250 1.8093e 75 0.037647 22.15 50 1024 51200 2500 3.2734e 150 16.82819 43.92  

> 4The use of Gray code may be more apt in this situation.
> Multi-stage Framework 10 ALGORITHM DETAILS

## Algorithm Details 

The multi-stage decision framework and solver scheme are presented algorithmically in this section. The high-level procedure, described in Solution Procedure , comprises four steps. These entail defining the problem, setting algorithm parameters, parsing the problem and then solving the problem. First, the problem is defined as per Define MyProb . This includes stating the number of stages and the number of selectable components in each. For non-fully connected graphs, the array of active links (ALIST) or the array of deactivated links (DLIST) must be provided. In addition, the number of continuous variables by stage and component are also defined here by need. The problem definition is returned in the structure 

PDEF .The algorithm parameters are returned in the structure PARAS as defined in Set Paras .The problem is then parsed in Process MyProb to size the binary system and to automatically generate the problem graph. The complete problem definition is returned in the structure PROB .Finally, the method Run RSS calls the solver with the required input data and returns with the solution structure SOLN .The inner workings of the solver are relayed in the remaining procedures: 

• Run Cycle 

• Evaluate Path 

• Sample Set 

• Evaluate Set 

• Evaluate Sample 

• Iteration Evaluation 

The reader may review these methods qualitatively or in detail by interest. Note that the definitions are in line with those given in the preceding sections. An example test problem (the Knapsack case) is presented in Evaluate MyFunc .Finally, note that for a given problem, the user need only furnish the definition and its evaluation method in define MyProb and evaluate MyFunc , respectively. 

Solution Procedure 

Set problem specific parameters Establish one-time pre-processed data Run problem instance: Define the problem ( define MyProb ) → PDEF 

Set algorithm parameters ( set Paras ) → PARAS 

Parse the problem ( process MyProb ) → PROB 

Run the solver ( run RSS ) → SOLN 

Show and plot results Create new instance  

> Multi-stage Framework 11 ALGORITHM DETAILS

Define MyProb 

Specify pertinent parameters: Number of decision stages, N

Maximization problem flag, fmax [solver minimizes by default] Selected function by index, feval 

Penalty violation factor, γ

Actual solution, if known, Fact 

Establish model data given parameters Specify choices for each stage j in N , e.g. , STAGE (j).choices = 2. Set array of active links (ALIST, size nadd ) where flag connecting N( j − 1, i) to N( j, k) is true. Set array of deactived links (DLIST, size ndel ) where flag connecting N( j − 1, i) to N( j, k) is false. Specify total number of continuous variables required ( fcvar ). Set the continuous variable structure CVAR (if fcvar > 0). 

CVAR indicates the continuous variable distribution by stage and component with desired ranges. Problem data is stored in the definition PDEF .Return: PDEF Set Paras 

Specify algorithm parameters: Number of cycles, ncyc = 6 Outer loop iterations, kitns = 12 Inner loop trial paths, npath = 48 Number of path steps, pstep = 120 Initial sample size, ns1 = 1e4 Iteration sample size, ns2 = 1e3 Relaxation factor, α = 0.01 Uniform sample flag, fun = 0 Fast update flag, fupd = 0 Plot distribution, fplt 1 = 0 Plot sample paths, fplt 2 = 0 Return: PARAS  

> Multi-stage Framework

12 ALGORITHM DETAILS 

Process MyProb 

Method parses specifed problem 

function PROB = process MyProb (PDEF )Get parameters: nadd , ndel , N and STAGE 

Initialize total bit count Mtot and total number of combinations Mc.for each stage k in N :Get choices mk

Establish number of bits required, mbk given choices mk.Establish combinations mck over mbk bits and the slack given mk required choices. Set selected index range [1 mk]Update cumulative values: Mtot and Mc

Store data in STAGE (k) Establish the string block for each stage k over the total length Mtot by index [ kst ked ]Establish if the graph is fully connected and set flag IsCon. Set problem structure comprising: Updated STAGE information Combinations by stage, mck

Binary system length, bleng = Mtot 

Binary problem flag, IsBin Fully connected flag, IsCon Generated problem graph GRPH given stage information and connectivity map. Variable range definition with LU arrays. Continuous variables and ranges, with cumulative string size cleng .Total string length, sleng = bleng + cleng .Full problem combinations, Mtot given size sleng .Return: PROB Run RSS 

Execute the solver given the parameter and problem statement 

function SOLN = run RSS (PARA , PROB )Get parameters: ncyc , α and feval 

Initialize empty arrays Xin, Vin, Vbest, PV and PF. Set Fin=0, etime=0, Fbest=1e6. for each cycle j in ncyc :[Xd, V, F, et] = run cycle (PARAS , PROB , Xin, Vin, Fin) Store results PV [ N m ], PF [ m 1] and update etot, with stages N and samples m.if(F < Fbest): update Fbest=F and Vbest=V end Perform final evaluation step over all data in PV: Xd = iteration evaluation (PV, α/10) [Xsol, Vsol, Fsol, et] = run cycle (PARAS , PROB , Xd, Vbest, Fbest) Update elapsed time, etot Set decoded solution D [1 N ] with path link data PLINK Evaluate at solution: RESP = eval set (Vsol, PARAS , PROB )Get Ysol, X, XS, PLINK from RESP Store solution: PF, Xsol, Vsol, Fsol, Ysol, Csol, X, XS, PLINK, etot, optgap Return: SOLN  

> Multi-stage Framework

13 ALGORITHM DETAILS 

Run Cycle 

Evaluation call given input parameters (with existing distribution array Xd) 

function [Xd, Vbest, Fbest, et] = run cycle (PARAS , PROB , Xdin, Vin, Fin) Get parameters: Relaxation factor, α

Outer loop iterations, kitns 

Inner loop trial paths, npath 

Problem dimensionality, sleng 

Maximization problem flag, fmax 

Fast update flag, fupd 

Get length of Xdin as ntest 

if( ntest = 0): initialize Xd as uniform distribution [ N 1], Vbest=[ ] and Fbest=1e6 if( ntest > 0): set Xd=Xdin [ N 1], Vbest=Vin [ N 1] and Fbest=Fin if( fmax ): fmult=-1 for j in kitns :set PTH struct [ ] for all paths k in npath :[v, f] = evaluate path (PARAS , PROB , Xd) Store v and f in PTH( k)end Initialize array V=[ ] for each path k in npath :Get v and f in PTH( k)if(f < Fbest): Fbest = f and Vbest = v Store v in V [ N n path ]end Update Xd for next iteration j: Xd = iteration evaluation (V, α, Xd) if(fupd): fast update: Xd = iteration evaluation (Vbest, α, Xd) end Set final Xd = iteration evaluation (Vbest, α, Xd) Return: Xd, Vbest, Fbest  

> Multi-stage Framework

14 ALGORITHM DETAILS 

Evaluate Path 

Generate and evaluate a path with given distribution 

function [Ybest, Fbest] = eval path (PARAS , PROB , Xd) Get parameters: Initial sample size, ns1

Iteration sample size, ns2

Relaxation factor, α

Number of path steps, pstep 

Uniform sample flag, fun 

Initialize STORE = [ ] for k in pstep :Set X = Xd if( k = 1): S = sample set (X, ns1, fun )if( k > 1): S = sample set (X, ns2, 0) [F, Y, RESP] = eval set (S, PARAS , PROB )if( k = 1): initialize Fbest=F and Ybest=Y if(F < Fbest): Fbest=F and Ybest=Y Update Xd = iteration eval (Y, α, X) given pop Y Update STORE: k, α, F , Fmean , Flwr , Fupr 

end Plot generated path (steps vs measure) Return: Ybest, Fbest 

Sample Set 

Generate m sample paths S [m n] given distribution array X and the uniform sampling flag 

function S = sample set (X, m, fun )Get dimensions, n

Initialize random variable array, RV [ m n ]Initialize output sample array S [ m n ] as ones for each dim k in n:

x = X( k)if( fun ): x = 0.5 for each sample j in m:

β = RV( j,k)if( β < x ): S( j, k) = 0 if( j = 1) and ( x ≥ 0.5): S( j, k) = 0 end end Return: sample set S  

> Multi-stage Framework

15 ALGORITHM DETAILS 

Evaluate Set 

Evaluates the function value F for each sample in B in BPOP and returns the best 

function [Fbest, Ybest, RESP] = evaluate set (BPOP, PARAS , PROB )Get number of samples, m in BPOP [ m n ]Initialize the solution array, FS [ m 1] for each sample j in m:Get j-th sample B [1 n]Evaluate sample FS( j) = evaluate sample (B, PROB )end Get population statistics (including Fmean) Establish Fbest and corresponding Bbest Get properties for Ybest: [Fchk, Y, X] = evaluate sample (Bbest, PROB )Establish path link information PLINK given design D for Ybest Store solution responses RESP: Fmean, Y, X, PLINK Return: Fbest, Bbest, RESP 

Evaluate Sample 

Evaluate binary sample B giving objective F, integer set Y, continuous set X 

function [F, Y, X] = evaluate sample (B, PROB )Get parameters: Penalty factor, γ

Binary problem flag, IsBin Fully connected problem flag, IsCon Binary problem size, bleng 

Continuous variable size, fcvar 

Maximization flag, fmax 

Stage data structure, STAGE 

Extract binary set BY from B of size bleng 

if(IsBin=1): integer selection directly, Y = BY if(IsBin=0): integer selection by string conversion, Y = bin2control (BY, STAGE )Initialize continuous variable set X = [ ] if( fcvar > 0): X = bin2cont (B, PROB ) - convert appended string (size cleng ) for continuous variables. Initialize the objective and penalty terms: f = P1 = P2 = 0 if(IsBin=0): get component penalty P1 = eval cmpt penalty (Y, STAGE , 10 γ)if(IsCon=0): get path penalty P2 = eval path penalty (Y, PROB )if(P1=0) and (P2=0): f = evaluate myfunc (X, Y, PROB )if( fmax ): set f = -f Set penalty function F = f + P1 + P2 Return: F, Y, X  

> Multi-stage Framework 16 ALGORITHM DETAILS

Iteration Evaluation 

Establishes distribution over the set of m samples in V [n m] for a given relaxation and learning rate. 

function Xd = iteration eval (V, α, Xold) Get parameters: Relaxation factor, α

Learning rate, λ = 0.8 Get dimensions, n

Get samples, m

Estimate the row mean for each dimension n over all m samples giving S [ n 1] Initialize the distribution array Xnew [ n 1] as zero for each dimension k in n:Get estimate, sk = S( k)Set probability measure Xnew( k) = 1 - sk

if( sk=0): Xnew( k) = 1-α (relaxed 0, with tiny probability for 1) if( sk=1): Xnew( k) = α (relaxed 1, with tiny probability for 0) end Set distribution with learning rate, Xd = Xold + λ(Xnew - Xold) Return: Xdt 

Evaluate MyFunc 

Knapsack example with binary input B [1 n] and problem struct PROB 

function f = eval myfunc (B, PROB )Get parameters: Utility array, U [ n 1] Weight array, W [ n 1] Capacity limit, C Penalty multiplier, γ

Get utility sum, u = B U Get weight sum, w = B W Get constraint term, g = max(0, w-C) Set objective measure, f = u - γ g 2 [to be maximized] Return: f  

> Multi-stage Framework 17 SUMMARY

## Summary 

The key points of interest are as follows: 

• A framework for multi-stage decision problems represented on an underlying graph. 

• A robust nonlinear binary solver based on incremental probabilistic learning with reinforcement to treat the combinatorial definition. 

• Method is applicable to various cases and allows treatment of complex graph arrangements by need. 

• Allows partial connectivity between stages and components, with varying number of choices by stage. 

• Deals with nonlinear relations between components and permits consideration of alternative pathways. 

• Problems are easier to define as the method does not require linearization or the use of MINLP methods with disjunctions to manage options. 

• The solution path is an abstraction for evaluation. The decision order is fungible. 

• Problem size dictates computational cost. A large problem requires more samples for exploration. 

• Solution is not guaranteed to be optimal. It is sample and paramterization dependent. 

• The prototype will benefit from implementation in a higher language. 

• Testing and validation on other suitable problems is desirable to demonstrate utility 5. 

> 5This method was successfully applied to an Energy Harvesting problem. The results are not reported here.
> Multi-stage Framework 18 REFERENCES

# References 

[1] Powell, W. B., ‘Reinforcement Learning and Stochastic Optimization: A Unified Framework for Se-quential Decisions’, Wiley, 2022. [2] Google OR-Tools, Knapsack test case, https://developers.google.com/optimization/pack/knapsack. [3] Mencarelli, L., Chen, Q., Pagot, A. & Grossman, I.E., ‘A review on superstructure optimization in process system engineering’, Computers and Chemical Eng., Vol. 136, 2020. [4] Yeomans, H. & Grossman, I.E., ‘A systematic modeling framework of superstructure optimization in process synthesis’, Computers and Chemical Eng., Vol. 23, pp.709-731, 1999. [5] Mencarelli, L., Chen, Q., Pagot, A. & Grossman, I.E., ‘A review on superstructure optimization approaches in process system engineering’, Computers and Chemical Eng., Vol. 136, no.106808, 2020. [6] Kenkel, P., Wassermann, T., Rose, C & Zondervan, E., ‘A generic superstructure modeling and optimization framework on the example of bi-criteria power-to-methanol design process’, Computers and Chemical Eng., vol. 150, no.107327, 2021. [7] Gopinath, S. & Adjiman, C., ‘Superstructure optimization with rigorous models via an exact refor-mulation’, Computers and Chemical Eng., vol. 194, no.108972, 2025. [8] Grossmann, I.E. & Ruiz, J.P., ‘Generalized Disjunctive Programming: A Framework for Formulation and Alternative Algorithms for MINLP Optimization’. appearing in: Lee, J. & Leyffer, S. (eds) Mixed-integer nonliner programming. IMA volume in Mathematics and its Applications, vol. 154. Springer, New York, NY, 2012. [9] Baluja, S., ‘Population-based incremental learning: A method for integrating genetic search-based function optimization and competitive learning’, CMU, Dept. Computer Science, manuscript, 1994. [10] Sutton, R.S. & Barto, A.G., ‘Reinforcement Learning, An Introduction’, Adaptive computation and machine learning series, Bradford Books, 2018. [11] Rosenbrock Test Function, Wikipedia, https://en.wikipedia.org/wiki/Rosenbrock function. 

# Acknowledgement 

This work was undertaken in 2025 at Schlumberger-Doll Research (SDR), One Hampshire Street, Cam-bridge, MA 02139.