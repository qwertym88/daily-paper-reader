Title: Real-Time Synthesis of Robust Controlled Invariant Sets for Monotone Systems

URL Source: https://arxiv.org/pdf/2609.14115v1

Published Time: Tue, 15 Sep 2026 00:56:23 GMT

Number of Pages: 6

Markdown Content:
# Real-Time Synthesis of Robust Controlled Invariant Sets for Monotone Systems 

Yasin Sonmez, Mahmoud Khaled, Majid Zamani, Murat Arcak 

Abstract —Safety-critical control of autonomous systems re-quires formal safety certificates, such as controlled invariant sets, that must be computed online as conditions change. Al-though standard synthesis algorithms scale poorly with state dimension, monotone dynamical systems with lower-closed safety specifications allow for accelerated computation of controlled invariant sets. In particular, lazy fixed-point algorithms exploit monotonicity and track only the antichain basis of the set. However, membership tests and redundancy checks against an evolving basis remain major bottlenecks. We introduce a threshold-function reformulation in which a lower-closed set on a d-dimensional grid is represented by its column heights along a designated axis. This reformulates the greatest-fixed-point iteration as independent one-dimensional binary searches, one per grid column, yielding an embarrassingly parallel iteration with asymptotically lower computational complexity than the lazy fixed-point algorithm. Experiments synthesize invariant sets on 3D grids with 10 9 cells in under 50 ms and 10 14 cells in under two minutes. We further demonstrate online re-synthesis in a safety-informed model predictive controller example. 

I. I NTRODUCTION 

Controlled invariant sets characterize safe regions in which trajectories can be kept for all future time through appropriate control actions. Symbolic abstractions [1] locate controlled invariant sets through finite-state fixed-point computations, but their cost grows sharply with state dimension. One approach to overcome the scalability barrier is to exploit monotone dynamics: When the dynamics are monotone with respect to a partial order and the safe set is lower-closed with respect to the same order, the maximal robust controlled invariant set is also lower-closed and is uniquely determined by its antichain basis [2]. On an N d grid, the basis size is 

O(N d−1), one order of magnitude smaller than the underlying grid. The lazy fixed-point algorithms of [2], [3], [4] track only the basis and update the antichain boundary between iterations. Two bottlenecks remain despite this reduction: 1) Quadratic per-iteration cost. Each iteration performs 

O(N d−1) safety checks, and each check scans the basis to test membership in its lower closure, giving 

O(N 2( d−1) ) work per iteration. 2) Sequential neighbor generation. After unsafe elements are removed, new candidate neighbors must be added to the basis, each requiring a redundancy check—an inherently sequential step that limits parallelism. 

Y. Sonmez and M. Arcak are with the University of California, Berkeley, USA ( {yasin sonmez,arcak }@berkeley.edu); M. Zamani is with the Univer-sity of Colorado Boulder, USA (majid.zamani@colorado.edu); M. Khaled is with the Ludwig-Maximilian University, Germany (m.mahmoud@lmu.de). This work is supported in part by the NSF grant CNS-2111688. 

Instead of representing the boundary of the lower-closed set with basis points, which must be iteratively updated and searched, in this paper we store this surface as a threshold function along a designated axis. We then reformulate the greatest-fixed-point (GFP) iteration to operate solely on this threshold function, eliminating basis tracking and sequential neighbor generation. The contributions are:  

> •

We encode lower-closed sets by a threshold function with 

O(N d−1) storage and O(1) membership queries. Parallel column updates yield per-iteration work O(N d−1 log N )

without basis enumeration or neighbor checks.  

> •

We prove one-step equivalence with the predecessor operator and equivalence with the lazy basis formulation.  

> •

We show experimentally that the threshold iteration yields speedups on the order of thousands over basis-based lazy synthesis. The 10 14 -cell scaling benchmarks complete in under two minutes, while the separate 10 10 -cell closed-loop experiment completes in approximately 62–77 ms. Existing synthesis methods that leverage structural prop-erties include sparse and compositional constructions for monotone systems and directed specifications [5], flexible abstraction-based pipelines [6], lazy exploration of exposed boundaries using antichain representations [4], and ordered reductions for robust controlled invariance [7]. Height encod-ings of staircase sets and discrete Pareto frontiers are well-established. Our contribution lies not in the height encoding, but in its use as the sole representation in robust invariant-set GFP iteration, together with the column-prefix result, binary-search update, equivalence proof, and parallel implementation. II. P RELIMINARIES 

A. Monotone systems and lower sets 

We study continuous-time systems of the form 

˙ξ = f (ξ, u, ϑ ), (1) where ξ ∈ Rd is the continuous state, u ∈ Rm is the control input, and ϑ collects exogenous effects such as disturbances and uncertain parameters. We obtain a finite model from (1) using sampled-data symbolic abstraction [1], [8], [9]. Given a sampling period, state grid, and finite input alphabet, we overestimate one-step reachable sets under all admissible exogenous signals and obtain a finite transition system, 

x+ = F (x, u, δ ). (2) Here, x belongs to a finite grid X, u belongs to a finite ad-missible control set U, and δ belongs to a finite adversarial set arXiv:2609.14115v1 [eess.SY] 12 Sep 2026 ∆. This set is finite at the abstraction level and conservatively indexes all abstract successor cells induced by every admissi-ble continuous exogenous signal ϑ, uncertain parameter, and discretization over-approximation over one sampling interval. It is not an arbitrary finite sample of a continuous disturbance set. Replacing a continuous disturbance set by finitely many extremal modes is sound only under an additional dominance property, such as disturbance-state monotonicity. 

Definition 1 (Componentwise order) . We equip the state grid with the componentwise order: x ⪯ y if and only if xj ≤ yj

for all j ∈ { 1, . . . , d }. When partial orders on U and ∆ are used, they are denoted by ⪯U and ⪯∆.

Assumption 1 (Finite grid abstraction) . The abstract state space is the finite partially ordered set 

X = {1, . . . , N 1} × · · · × { 1, . . . , N d},

with the componentwise order of Definition 1. We write N := max j Nj and |X| = Q 

> j

Nj .

Definition 2 (Lower set and antichain basis) . For S ⊆ X,define its downward closure by 

↓S := {q ∈ X : ∃ s ∈ S such that q ⪯ s}.

A set K ⊆ X is a lower set if q ∈ K and q′ ⪯ q imply 

q′ ∈ K. Its antichain basis is the set of its maximal elements: 

Bas( K) := {q ∈ K : ∄ q′ ∈ K \ { q}, q ⪯ q′}.

A lower set K is fully determined by its basis: K = ↓ Bas( K).

Remark 1 (Antichain bound) . On the grid of Assumption 1, the maximal antichain size is bounded by O(N d−1); every lower set therefore admits a basis of size at most O(N d−1),one order of magnitude smaller than |X| = O(N d).B. Monotone dynamics and the robust controlled pre-operator 

Definition 3 (Restricted robust controlled predecessor) . For 

A, B ⊆ X and V ⊆ U , define 

Pre( A, V, B ) := {q ∈ A : ∃u ∈ V ∀δ ∈ ∆, F (q, u, δ ) ∈ B}.

(3) 

For the safety iteration, define 

Φ( K) := Pre( K, U, K ),

which extracts from K those states that can be kept in K for one step despite the disturbances. 

Definition 4 (Controlled invariant set) . A subset K ⊆ XS

is controlled invariant (with respect to system (2) and safe set XS ) if for every x ∈ K, there exists u ∈ U such that 

F (x, u, δ ) ∈ K for all δ ∈ ∆. Equivalently, K = Φ( K).

Assumption 2 (Lower-closed safety) . The safe state set XS ⊆

X is a lower set under ⪯.

Definition 5 (State-monotonicity (SM)) . The abstract transi-tion map (2) is state-monotone if 

x1 ⪯ x2 =⇒ F (x1, u, δ ) ⪯ F (x2, u, δ )

for all u ∈ U and all δ ∈ ∆.

Remark 2 (Stronger monotonicity variants) . Reference [7] also equips the control set U and/or the disturbance set ∆

with partial orders to define control-state monotone (CSM), disturbance-state monotone (DSM), and control-disturbance-state monotone (CDSM) systems. The correctness results be-low require only state monotonicity (SM). When the stronger variants hold, they allow the predecessor check to be restricted to minimal controls, maximal adversarial modes, or both. 

Proposition 1 (Lower-closed predecessor) . [4, § 4.1, Lemma 2] Suppose Assumptions 1 and 2 hold and F is state-monotone (Definition 5). Then Φ( K) is a lower set for every lower set K ⊆ X.Proof. Let x ∈ Φ( K) and y ⪯ x. Since x ∈ K and K is lower, y ∈ K. Choose u ∈ U such that F (x, u, δ ) ∈ K for all 

δ ∈ ∆. By state monotonicity, 

F (y, u, δ ) ⪯ F (x, u, δ ) ∈ K ∀ δ ∈ ∆.

Lower-closedness of K then gives F (y, u, δ ) ∈ K for all δ,hence y ∈ Φ( K).

Corollary 1 (Ordered reductions) . [7, § 5, Thm. 1 and Prop. 2] Let K ⊆ X be a lower set and let x ∈ K. If the stronger CSM variant of Remark 2 holds, then it is enough to test minimal controls: 

x ∈ Φ( K) ⇐⇒ ∃ u ∈ Min( U) ∀ δ ∈ ∆ : F (x, u, δ ) ∈ K. 

If the stronger DSM variant holds, then for every fixed u ∈ U 

it is enough to test maximal adversarial modes: 

x ∈ Φ( K) ⇐⇒ ∃ u ∈ U ∀ δ ∈ Max(∆) : F (x, u, δ ) ∈ K. 

If the CDSM variant holds, one may therefore test only mini-mal controls against maximal adversarial modes. Let C denote the per-state successor-evaluation count for testing x ∈ Φ( K).In the general state-monotone case, C = |U| | ∆|. The or-dered reductions [7] give CCSM = |Min( U)| | ∆|, CDSM =

|U| | Max(∆) |, and CCDSM = |Min( U)| | Max(∆) |.

Theorem 1 (Descending predecessor sequence) . Suppose As-sumptions 1 and 2 hold and F is state monotone. Define 

K0 := XS , Ki+1 := Φ( Ki), i ∈ N.

Then each Ki is a lower set, the sequence is descending, and it converges in finitely many steps to a lower set K∗ ⊆ XS

which is the maximal robust controlled invariant subset of XS .Proof. Proposition 1 gives lower-closedness of each Ki by induction. Since Φ( K) ⊆ K for every K, the sequence is descending. Because X is finite, the descending sequence converges after finitely many strict inclusions. If K ⊆ XS is a controlled invariant set, then K ⊆ K0.Assume inductively that K ⊆ Ki. Since K ⊆ Φ( K) and the map Φ is monotone with respect to set inclusion, 

K ⊆ Φ( K) ⊆ Φ( Ki) = Ki+1 .

Hence K ⊆ Ki for all i, and therefore K ⊆ K∗. Thus K∗ is the maximal robust controlled invariant subset of XS .Algorithm 1 Lazy fixed point iteration [2] 

Require: Lower set XS , transition F , inputs U, adversarial set ∆ 

> 1:

B ← Bas( XS ) 

> 2:

repeat  

> 3:

Bpass ← B 

> 4:

Bu ← ∅  

> 5:

for all b ∈ Bpass do ▷ safety check  

> 6:

if ∄ u ∈ U s.t. ∀ δ ∈ ∆ : F (b, u, δ ) ∈ ↓ B then  

> 7:

Bu ← Bu ∪ { b} 

> 8:

B ← B \ { b} 

> 9:

for all j ∈ { 1, . . . , d } do ▷ neighbor gen.  

> 10:

c ← b − ej 

> 11:

if cj ≥ 1 and c / ∈ ↓ B then  

> 12:

B ← B ∪ { c} 

> 13:

until Bu = ∅ 

> 14:

return ↓B

C. Lazy fixed-point iteration 

Proposition 1 together with Remark 1 means that K∗ and every iterate Ki can be represented by its O(N d−1)-size antichain basis rather than by the full O(N d) grid. The lazy 

algorithm of [2], summarized in Algorithm 1, exploits this by storing only the current basis B and updating it. Let Rlazy denote the number of complete outer basis passes until convergence and |B| = O(N d−1) denote the basis-size bound. If the membership query “ x ∈ ↓B” scans B, the safety-check phase costs O(Rlazy CN 2( d−1) ), up to dimension factors. Neighbor generation performs dominance rather than successor queries and contributes O(Rlazy N 2( d−1) ) with the same scan-based representation. Since an unsafe basis element is removed and neighbors are inserted immediately, subsequent safety and neighbor checks use an evolving B and are sequen-tial across basis updates. For a removed element b, the explicit update in Algorithm 1 implements Bas( ↓B \ { b}) from [2]. III. T HRESHOLD -FUNCTION ITERATION 

Section III-A introduces the threshold-function representa-tion of lower sets. Section III-B reformulates one GFP step as a parallel collection of binary searches. Section III-C shows equivalence with Algorithm 1 and analyzes complexity. 

A. The threshold-function representation 

The main idea is to project a d-dimensional lower set K ⊆

X into a (d−1) -dimensional table using a designated axis. 

Definition 6 (Designated axis, projection, threshold function) .

Fix a designated axis d∗ ∈ { 1, . . . , d } and set 

K := Y 

> j̸=d∗

{1, . . . , N j }.

For x ∈ X let π(x) ∈ K be its projection onto the remaining 

d − 1 axes, and write xd∗ for its coordinate along d∗. For a lower set K ⊆ X, the threshold function is 

τK (k) := max xd∗ : x ∈ K, π (x) = k , k ∈ K , (4)   

> key k
> height  vτK(k)

K                     

> (a) Threshold representation
> 12345678
> τ(k)
> prefix
> f(k)
> (b) Column update Fig. 1. Threshold representation and update. (a) A lower set Kis stored by the column height τK(k), so membership is the O(1) test (k, v )∈K⇐⇒
> v≤τK(k). (b) For each column, Φ( K)is a prefix whose height f(k)is found by binary search; all columns are updated independently.

with the convention max ∅ = 0 .

Each k ∈ K indexes a one-dimensional column of cells along the designated axis; lower-closedness forces the safe cells in that column to form a contiguous prefix 

{1, . . . , τ K (k)}. The function τK therefore stores the entire set K in a single integer per column as depicted in Figure 1. 

Proposition 2 (Threshold characterization) . Let K ⊆ X be a lower set. Then 

x ∈ K ⇐⇒ xd∗ ≤ τK (π(x)) .

Moreover, τK is antitone : k ⪯ k′ implies τK (k) ≥ τK (k′).Proof. If x ∈ K, the value xd∗ is one of the values maximized in (4) at k = π(x), so τK (π(x)) ≥ xd∗ . Conversely, if 

xd∗ ≤ τK (π(x)) , pick y ∈ K with π(y) = π(x) and 

yd∗ = τK (π(x)) ; then x ⪯ y and lower-closedness gives 

x ∈ K. Antitonicity follows by the same lifting: for k ⪯ k′,any x ∈ K with π(x) = k′ and xd∗ = v dominates y with 

π(y) = k, y d∗ = v, hence y ∈ K and τK (k) ≥ v.

Remark 3 (Memory and query complexity) . τK has |K| =Q  

> j̸=d∗

Nj = O(N d−1) entries. Storage is O(N d−1) and every inclusion query is O(1) (one projection and one com-parison), compared to O(N d−1) for a scan-based basis check. B. The threshold iteration 

The representation of Section III-A turns one GFP step into a set of independent binary searches along the designated axis. We first state the structural property that enables this (Lemma 1), then derive the per-column update (Corollary 2). 

Definition 7 (Grid column) . For k ∈ K , the column at k is 

Col( k) := {(k, v ) : v ∈ { 1, . . . , N d∗ }} .

Lemma 1 (Column-wise predecessor prefix) . If K ⊆ X is a lower set and (k, v ) ∈ Φ( K), then (k, v ′) ∈ Φ( K) for every 

v′ ∈ { 1, . . . , v }.Proof. Let u ∈ U be a witness for (k, v ) ∈ Φ( K), so 

F (( k, v ), u, δ ) ∈ K for every δ ∈ ∆. Since (k, v ′) ⪯ (k, v ),monotonicity yields F (( k, v ′), u, δ ) ⪯ F (( k, v ), u, δ ) ∈ K,Algorithm 2 Threshold-function iteration 

Require: Lower set XS , transition F , inputs U, adversarial set ∆, designated axis d∗

τ ← τXS

repeat for all k ∈ K do ▷ all columns are independent 

f (k) ← BIN SEARCH (k, τ, F, U, ∆) ▷ using (5) 

τ ← f

until τ unchanged 

return Kτ = {x ∈ X : xd∗ ≤ τ (π(x)) }

and lower-closedness of K gives F (( k, v ′), u, δ ) ∈ K. Hence 

(k, v ′) ∈ Φ( K).Lemma 1 says that, within any column, the set of states from which K can be maintained is a contiguous prefix 

{(k, 1) , . . . , (k, f (k)) }. This height is efficiently computable: 

Corollary 2 (Per-column threshold update) . Let K ⊆ X be lower with threshold function τ = τK . For each k ∈ K , define 

f (k) := max 

n

v ∈ { 1, . . . , τ (k)} : ∃ u ∈ U ∀ δ ∈ ∆,F (( k, v ), u, δ ) ∈ K

o

,

(5) 

with max ∅ := 0 . Then 

Φ( K) ∩ Col( k) = {(k, 1) , . . . , (k, f (k)) }.

Moreover, f (k) can be computed by binary search over 

{1, . . . , τ (k)}. Each probe requires one membership query in 

K per pair (u, δ ), so the cost per column is O C log N .

Proof. By Lemma 1, Φ( K) ∩ Col( k) is a prefix of Col( k) of length equal to (5) and the predicate in (5) is monotone non-increasing in v, so a standard binary search over {1, . . . , τ (k)}

locates f (k) in O(log N ) probes. Since the update τ (k) ← f (k) is independent across columns, all columns can be processed in parallel as in Figure 1. The full iteration is summarized in Algorithm 2. 

C. Correctness, convergence, and complexity 

We now prove that Algorithm 2 computes the same invariant set as Algorithm 1. 

Theorem 2 (One-step equivalence) . Let K ⊆ X be a lower set with threshold τ = τK , and let f be defined by (5) . Then, Assumption 1 and state monotonicity (Definition 5) imply: 

Φ( K) = x ∈ X : xd∗ ≤ f (π(x)) , i.e. f = τΦ( K).

Proof. By Corollary 2, Φ( K) ∩ Col( k) =

{(k, 1) , . . . , (k, f (k)) } for every k ∈ K; taking the union over k gives Φ( K) = {x : xd∗ ≤ f (π(x)) }. Combining this with Proposition 2 yields f = τΦ( K).

Corollary 3 (Correctness and convergence of Algorithm 2) .

Let τ0 := τXS and, for each i ∈ N, let τi+1 be obtained from 

τi by the update (5) . Define 

Ki := {x ∈ X : xd∗ ≤ τi(π(x)) }.                

> TABLE I PER -ITERATION COMPLEXITY COMPARISON .
> Lazy [2] Threshold (ours)
> Predecessor tests O(CN 2( d−1) )O(CN d−1log N)
> sequential over evolving Bparallel over columns Basis update O(N2( d−1) )—
> sequential —

Then K0 = XS , each Ki is a lower set, and 

Ki+1 = Φ( Ki) ∀ i ∈ N.

The sequence (Ki)i∈N is descending and converges in finitely many steps to the maximal robust controlled invariant subset 

K∗ of XS . Equivalently, Algorithm 2 returns K∗.Proof. The identity K0 = XS follows from the definition of 

τXS together with Proposition 2. Since XS is a lower set and Theorem 2 maps the threshold of a lower set to the threshold of its predecessor, each Ki is a lower set and satisfies Ki+1 =Φ( Ki). The conclusion now follows from Theorem 1. 

Theorem 3 (Equivalence with the lazy basis formulation) . Let 

Klazy denote the set returned by Algorithm 1 and let Kthr 

denote the set returned by Algorithm 2, both started from the same lower set XS . Then Klazy = Kthr = K∗.

Proof. By Corollary 3, Algorithm 2 returns the maximal robust controlled invariant subset K∗ of XS . By the complete-ness result for the lazy algorithm in [2], Algorithm 1 returns the same maximal invariant set. The threshold iteration replaces quadratic lazy boundary work with one embarrassingly parallel stage; see Table I. 

Remark 4 (Dense storage and designated-axis choice) . Let 

Md∗ := Q  

> j̸=d∗

Nj . A threshold buffer stores Md∗ values regardless of frontier complexity, whereas a sparse basis stores 

|B| maximal grid points and may require substantially less memory when |B| ≪ Md∗ . The threshold representation is attractive for dense or irregular frontiers and workloads dominated by membership queries. Memory can be minimized by choosing an axis with maximal Nd∗ .

IV. E XPERIMENTS 

We evaluate threshold iteration on three d=3 benchmarks and a d=5 benchmark from the monotone-synthesis literature. 

ACC — adaptive cruise control [2]. The 3D state is 

(h, v e, v ℓ) (headway, ego speed, lead speed), with ego torque command u and adversarial mode δ that captures lead-vehicle braking and parameter uncertainty. The ACC dynamics are 

˙h = vℓ − ve, ˙ve = ae(ve, u, δ ), ˙vℓ = aℓ(vℓ, δ ).

ACC-5D uses state (h, v e, v ℓ, T e, T ℓ), replaces direct torque arguments by actuator states in ae, a ℓ, and adds ˙Te = ( u −

Te)/τ e, ˙Tℓ = ( δT − Tℓ)/τ ℓ. The acceleration model is 

a(v, T, θ ) = 1

M

 TRw

− α



− βM v − γM v2, (6) where θ = ( M, R w, α, β, γ ) ∈ Θ. The safe set is KACC =

{(h, v e, v ℓ) : h ≥ db(ve)}, where db is the worst-case braking-distance map. We use the orthant order 

x ⪯ACC y ⇐⇒ hx ≥ hy , v e,x ≤ ve,y , v ℓ,x ≥ vℓ,y ,

and, for ACC-5D, additionally Te,x ≤ Te,y and Tℓ,x ≥ Tℓ,y .

KACC is lower-closed whenever db is nondecreasing. 

Turn-Ego/Turn-Oncoming. Both unprotected-turn bench-marks [10] use state (se, v e, s o): ego longitudinal position and speed, plus oncoming longitudinal position along the opposing lane centerline. There is no exogenous disturbance channel, so we use a singleton adversarial set. The dynamics are 

˙se = ve, ˙ve = ae(ve, u ), ˙so = vo.

Let Z(s) := 1 + 1{s≥− 10 } + 1{s≥10 } be the shared zone map and write Ze := Z(se), Z o := Z(so). Both benchmarks use the same unprotected-turn geometry for opposite maneuvers: Turn-Oncoming models the ego waiting while the oncoming car passes, Turn-Ego models the ego passing first; the orthant order is flipped between them, thus the ego-priority and oncoming-priority safe sets are 

Kego = {x ∈ Xturn : ¬(Ze < Z o ∨ (Ze = 2 ∧ Zo = 2)) },Konc = {x ∈ Xturn : ¬(Zo < Z e ∨ (Ze = 2 ∧ Zo = 2)) }.

They forbid simultaneous conflict-zone occupation while en-forcing the chosen priority relation. The orthant orders are 

x ⪯E y ⇐⇒ se,x ≥ se,y , v e,x ≥ ve,y , s o,x ≤ so,y ,x ⪯O y ⇐⇒ se,x ≤ se,y , v e,x ≤ ve,y , s o,x ≥ so,y .

Minimal controls are maximum torque for ⪯E and minimum torque for ⪯O. Since Z is monotone, moving downward under ⪯E advances ego and delays the oncoming phase, while downward movement under ⪯O has the reverse effect; thus, priority constraints are lower-closed, and monotonicity follows from the dynamics’ sign structure [10]. 

A. Lazy vs. threshold comparison 

For fair comparison, we also introduce a stronger Lazy+ τ baseline which preserves Algorithm 1’s traversal and immediate-deletion schedule while maintaining the exact threshold table τ↓B . Deleting an uncontrolled maximal element 

b updates τ (π(b)) ← τ (π(b)) − 1. A neighbor c = b − ej is maximal when c ∈ Kτ and every valid immediate successor 

c + eℓ /∈ Kτ , which is checked using threshold queries. Thus, Lazy+ τ changes the representation and boundary-maintenance operations while retaining the sequential Lazy schedule; the basis is materialized after each pass. Table II compares scan-based Lazy, Lazy+ τ , and threshold GFP. The two Lazy methods use the same pass, immediate-deletion schedule, and the same precomputed successor cache. The time required to construct this cache is reported as tpre . The implementation and experiment configurations are publicly available. 1

The threshold method uses inline dynamics: a full successor table would cost O(C|X|) memory and becomes the bottle-neck at larger grids, while threshold membership needs only                                                                                                                                

> 1https://github.com/mkhaled87/pFaces-MonoSynth TABLE II LAZY VS . T HRESHOLD COMPARISON .
> tpre IS THE SUCCESSOR -TABLE GENERATION TIME USED IN LAZY METHODS AND IS EXCLUDED FROM THE REPORTED SPEEDUPS ,WHICH EQUAL tlazy+ τ/t thr ; R = ITERATIONS ;TIMEOUT = 20 MINUTES .Case |X|tpre Lazy Threshold
> Rlazy tlazy tlazy+ τRthr tthr Speedup ACC 10 6.569 ms 182 63 .1 s 106 ms 26 3.18 ms 33 .4×
> ACC 10 9439 ms 1595 Timeout 126 s 24 16 .9 ms 7 ,458 ×
> ACC 10 10 Timeout 24 318 ms -ACC 10 12 Timeout 24 750 ms -ACC 10 14 Timeout 24 7.94 s -ACC-5D 10 6.624 ms 318 299 s 457 ms 25 3.34 ms 137 ×
> ACC-5D 10 9470 ms 835 Timeout 410 s 31 143 ms 2 ,870 ×
> ACC-5D 10 11 Timeout 28 4.94 s -T-Ego 10 61.35 ms 278 329 s 269 ms 22 10 .8 ms 24 .9×
> T-Ego 10 91.18 s 2691 Timeout 341 s 26 41 .6 ms 8 ,188 ×
> T-Ego 10 10 Timeout 26 274 ms -T-Ego 10 12 Timeout 26 3.18 s -T-Ego 10 14 Timeout 26 70 .0 s -T-Onc 10 6.563 ms 273 118 s 173 ms 35 4.22 ms 41 .0×
> T-Onc 10 9458 ms 2462 Timeout 185 s 35 26 .4 ms 6 ,993 ×
> T-Onc 10 10 Timeout 35 123 ms -T-Onc 10 12 Timeout 34 960 ms -T-Onc 10 14 Timeout 35 19 .2 s -

the O(N d−1) table and eliminates antichain maintenance. At 

|X| = 10 9, the threshold iteration is already thousands of times faster; at |X| ≥ 10 10 the basis method exceeds the time or memory budget, but the threshold iteration completes. Let 

Rthr denote the number of threshold rounds. A threshold round computes one full Φ( K) update over all columns, whereas a lazy pass incrementally mutates the exposed antichain bound-ary. Thus, Rlazy and Rthr are algorithm-specific round counts and are not comparable. Experiments were run on an NVIDIA RTX 5090 GPU (32 GB, 170 SMs) with an AMD Ryzen 9 9950X3D CPU. We implement the method in C++/pFaces [11] with OpenCL kernels. Each key k ∈ K evaluates predecessor feasibility and updates τ (k) by binary search. Memory is dominated by 

τ ∈ N|K| with |K| = O(N d−1).

B. Real-time controller: unprotected left turn 

To demonstrate online use of the synthesized invariant sets, we employ the threshold method inside an MPPI [12] controller for an unprotected left turn (Figure 2). The ego vehicle approaches the intersection from the west while two oncoming vehicles approach from the east. Their velocities are sensed online. The ego first waits for onc 1 to clear the conflict zone, then commits to the turn before onc 2 closes the available gap. Each phase is formulated as a 3D robust-safety problem in (se, v e, s onc ), and the corresponding invariant set is re-synthesized when the sensed oncoming speed changes sufficiently. Let Kw ⊆ R3 and Kg ⊆ R3 be the two phase sets. The two-oncoming safety condition is represented by the conflict zone ego onc 1⃗v onc ,1 

> onc 2⃗vonc ,2

Fig. 2. Real-time controller for an unprotected left turn. An MPPI controller samples rollouts and queries the threshold table once per rollout step to evaluate the set-membership penalty. The resulting optimized control input is applied. When oncoming velocities change, the invariant set is resynthesized. 

intersection of these two 3D constraints in lifted coordinates: 

K2 = {(se, v e, s 1, s 2) : ( se, v e, s 1) ∈ Kw, (se, v e, s 2) ∈ Kg}.

This lifted set is used only for online rollout membership queries; we do not claim that the full 4D system is monotone or that K2 is a 4D controlled-invariant set. The reduction is sound for simultaneously checking the two phase certificates under the modeled independent oncoming evolution. A formal 4D invariance claim would additionally require a common witness input for both component constraints, which need not exist because the wait and go constructions impose opposite orthant orders on (se, v e).The invariant-set algorithm returns K∗, not an explicit policy. In closed loop, we use K∗ inside an optimizer: the threshold table gives an O(1) membership query that is easy to insert into a sampling-based MPPI cost. MPPI is a natural choice here because it handles nonlinear dynamics and nonsmooth table penalties without requiring gradients of the threshold representation; first-order MPC methods would need smoothing of the same discontinuous membership test. Let Ts denote the controller sampling period, Hp the predic-tion horizon, Hc the control horizon, Nr the number of rollouts per MPPI iteration, Nit the number of MPPI updates per control step, σu the standard deviation of the Gaussian input perturbations, and [umin , u max ] the admissible torque interval. Their numerical values are reported in Table III. The stage cost is ℓ(xk, u k) = wp(se,k −g)2 +ws1{xk /∈K2} +wj ∥uk −uk−1∥2.Each table is computed for the oncoming-speed parameters and interval vehicle parameters, and re-synthesis is triggered when the sensed velocity change exceeds the threshold εres 

listed in Table III. The safety-informed MPPI performs an 

O(1) threshold lookup per rollout step; states outside K2

accumulate a penalty ws, rather than being prohibited by a hard constraint, and the implemented controller provides no formal closed-loop safety guarantee. Such a guarantee would require a hard one-step invariant constraint or an invariant-preserving backup policy, which is left for future work. The threshold lookup and MPPI control computation times are negligible relative to the re-synthesis times. 

TABLE III REAL -TIME UNPROTECTED LEFT TURN : CONFIGURATION AND TIMINGS .Quantity Value Grid cells |X| 10 10 = 10 4 × 10 2 × 10 4

Sampling period Ts 0.1 sPrediction and control horizon Hp, H c 20 steps MPPI iterations / step Nit 10 

Rollouts / iteration Nr 512 

Perturbation standard deviation σu 1000 Nm Cost weights (wp, w s, w j ) (1 , 10 4, 10 −2)

Resynthesis threshold εres 0.2 m/s Wait and go synthesis twait , t go 77 ± 15 , 62 ± 7 ms 

Average MPPI control time 2 ms 

V. C ONCLUSION 

We introduced a threshold-function reformulation of greatest-fixed-point invariant-set synthesis for monotone sys-tems. The algorithm is embarrassingly parallel and orders of magnitude faster than lazy fixed-point iteration. Experiments on benchmarks confirm speedups on the order of thou-sands over the lazy baseline. The threshold iteration reaches grids exceeding 10 9 cells around 100 ms, enabling real-time re-synthesis for closed-loop control. However, the method requires monotonicity under some orthant order and lower-closedness of the safety set. The threshold table stores only the set and gives no specific control policy. Grid resolution is bounded by GPU memory ( O(N d−1) integers). REFERENCES [1] P. Tabuada, Verification and control of hybrid systems: a symbolic approach . Springer Science & Business Media, 2009. [2] A. Saoud, E. Ivanova, and A. Girard, “Efficient synthesis for monotone transition systems and directed safety specifications,” in IEEE Confer-ence on Decision and Control . IEEE, 2019, pp. 6255–6260. [3] E. Ivanova and A. Girard, “Lazy safety controller synthesis with multi-scale adaptive-sampling abstractions of nonlinear systems,” IFAC-PapersOnLine , vol. 53, no. 2, pp. 1837–1843, 2020. [4] E. Ivanova, A. Saoud, and A. Girard, “Lazy controller synthesis for monotone transition systems and directed safety specifications,” Auto-matica , vol. 135, p. 109993, 2022. [5] E. S. Kim, M. Arcak, and S. A. Seshia, “Symbolic control design for monotone systems with directed specifications,” Automatica , vol. 83, pp. 10–19, 2017. [6] ——, “Flexible computational pipelines for robust abstraction-based control synthesis,” in International Conference on Computer Aided Verification . Springer, 2019, pp. 591–608. [7] A. Saoud and M. Arcak, “Characterization, verification and computation of robust controlled invariants for monotone dynamical systems,” Math-ematics of Control, Signals, and Systems , vol. 36, pp. 71–100, 2024. [8] M. Rungger and M. Zamani, “SCOTS: A tool for the synthesis of sym-bolic controllers,” in Proceedings of the 19th international conference on hybrid systems: Computation and control , 2016, pp. 99–104. [9] Y. Li and J. Liu, “ROCS: A robustly complete control synthesis tool for nonlinear dynamical systems,” in 21st International Conference on Hybrid Systems: Computation and Control , 2018, pp. 130–135. [10] S. W. Smith, A. Saoud, and M. Arcak, “Monotonicity-based symbolic control for safety in driving scenarios,” IEEE Control Systems Letters ,vol. 6, pp. 830–835, 2021. [11] M. Khaled and M. Zamani, “pFaces: An acceleration ecosystem for symbolic control,” in 22nd ACM International Conference on Hybrid Systems: Computation and Control , 2019, pp. 252–257. [12] G. Williams, N. Wagener, B. Goldfain, P. Drews, J. M. Rehg, B. Boots, and E. A. Theodorou, “Information theoretic MPC for model-based reinforcement learning,” in 2017 IEEE international conference on robotics and automation (ICRA) . IEEE, 2017, pp. 1714–1721.