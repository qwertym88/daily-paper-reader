---
title: "AdaVSkip: Adaptive Visual Token Skipping Across Layers For Efficient MLLMs Inference"
title_zh: AdaVSkip：面向高效 MLLM 推理的跨层自适应视觉 token 跳过
authors: "Yuyao Sun, Tao Deng, Shuang Li, Deqing Wang"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15131v1"
tags: ["query:neural-arch"]
score: 6.0
evidence: 面向高效推理的自适应层与token跳过
tldr: 多模态大模型处理大量视觉 token 时需在所有 Transformer 层反复计算，推理开销很高。已有方法通过压缩 token、早退或固定层跳过降低冗余，但冗余程度随输入变化，且在自注意力与 MLP 模块间分布不同。AdaVSkip 为每层引入两个轻量路由器，独立判断视觉 token 是否跳过自注意力或 MLP，从而为每个输入生成自适应的视觉计算路径。它采用两阶段训练，仅更新路由器并冻结主干：第一阶段用模块必要性分数监督初始化路由，第二阶段用强化学习结合答案正确性与跳过一致性奖励优化。在三个 MLLM 主干上，该方法显著减少计算并保持任务性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.15131v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 702, \"height\": 655, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1448, \"height\": 505, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1449, \"height\": 303, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15131v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1460, \"height\": 1098, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15131v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15131v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 425, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15131v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1450, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15131v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 763, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15131v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 695, \"height\": 180, \"label\": \"Table\"}]"
motivation: 现有高效 MLLM 推理多压缩视觉 token，或固定早退/跳层；但冗余随输入变化，且自注意力与 MLP 间分布不同，需要更细粒度自适应跳过。
method: AdaVSkip 为每层设两个轻量路由器，独立决定视觉 token 是否跳过自注意力或 MLP，形成输入自适应路径；并用冻结主干的两阶段监督与强化学习训练路由器。
result: "在三个 MLLM 主干上均保持强任务性能；在 LLaVA-NeXT-7B 上，FLOPs 降低 53.2% 且平均性能不降；结合视觉 token 压缩后，FLOPs 降低 91.2%，仍保留 97.2% 平均性能。"
conclusion: AdaVSkip 表明按层、按模块自适应跳过视觉计算能有效挖掘 MLLM 纵向冗余，并与 token 压缩互补，为高效多模态推理提供新路径。
---

## 摘要
多模态大语言模型（MLLM）需要在所有 Transformer 层中处理大量视觉 token，因而计算开销巨大。现有大多数高效 MLLM 推理方法通过压缩视觉 token 来利用水平冗余。除 token 削减之外，近期研究还通过提前退出或固定层跳过来利用垂直冗余。然而，我们发现这种冗余的程度与分布因输入而异，并且在自注意力模块与 MLP 模块之间存在差异。受这些观察启发，我们提出 AdaVSkip，为每一层配备两个轻量级路由器，独立决定视觉 token 是经过还是跳过自注意力模块与 MLP 模块。这些决策共同定义了一条与输入相关的视觉计算路径，但其离散且不可微的特性使有效路径的学习颇具挑战。为解决这一难题，我们提出一种渐进式两阶段训练框架，仅更新路由器而保持骨干网络冻结。第一阶段通过监督训练建立初始路由策略，其输入相关的监督目标由逐模块必要性得分导出。为进一步使路由策略与任务性能对齐，第二阶段采用强化学习，利用生成答案的直接反馈来优化路由决策。它结合了答案正确性奖励与跳过一致性奖励，后者用于抑制对视觉 token 计算的过度保留。在三种 MLLM 骨干网络上，AdaVSkip 以显著更少的计算量保持了强劲的任务性能。在 LLaVA-NeXT-7B 上，AdaVSkip 将 FLOPs 降低 53.2%，同时保持原模型的平均性能。将其与视觉 token 压缩相结合，可将该降低幅度提升至 91.2%，同时平均保留原性能的 97.2%。

## Abstract
Multimodal large language models (MLLMs) require substantial computation to process numerous visual tokens across all transformer layers. Most methods for efficient MLLM inference exploit horizontal redundancy by compressing visual tokens. Beyond token reduction, recent studies exploit vertical redundancy through early exit or fixed-layer skipping. However, we find that the extent and distribution of this redundancy vary across inputs and differ between self-attention and MLP modules. Motivated by these observations, we propose AdaVSkip, which equips each layer with two lightweight routers that independently determine whether visual tokens pass through by or skip the self-attention and MLP modules. These decisions collectively define an input-specific visual-computation path, but their discrete and non-differentiable nature makes learning effective paths challenging. To address this challenge, we develop a progressive two-stage training framework that updates only the routers while keeping the backbone frozen. Stage I establishes an initial routing policy through supervised training with input-specific targets derived from module-wise necessity scores. To further align the routing policy with task performance, Stage II uses reinforcement learning to optimize routing decisions with direct feedback from generated answers. It combines an answer correctness reward with a skip-consistency reward that discourages excessive retention of visual-token computation. Across three MLLM backbones, AdaVSkip maintains strong task performance with substantially less computation. On LLaVA-NeXT-7B, AdaVSkip reduces FLOPs by 53.2\% while preserving the original model's average performance. Combining it with visual token compression increases this reduction to 91.2\%, while retaining 97.2\% of the original performance on average.