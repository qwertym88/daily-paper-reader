Title: Statistical Convergence of Transformer Encoder-Accelerated Robust Reinforcement Learning

URL Source: https://arxiv.org/pdf/2609.23775v1

Published Time: Tue, 22 Sep 2026 02:20:40 GMT

Number of Pages: 6

Markdown Content:
# Statistical Convergence of Transformer Encoder-Accelerated Robust Reinforcement Learning 

Suman Banerjee and Hiroyasu Tsukamoto 

Abstract — Obtaining the optimal action-value function in Markov decision processes is computationally intensive in large state–action spaces. In this study, we present statistically rigorous convergence results for a robust reinforcement learning algorithm warm-started by a transformer-based action-value function prediction, where natural language prompts encode task specifications. Our framework adopts the R-contamination model to characterize uncertainty in the state transition kernel, and employs conformal prediction to certify convergence via trajectory-level nonconformity scores constructed from the contracting Bellman residual. The resulting conformal quantile bounds the gap between the running and optimal action-value functions simultaneously over all iterations, thereby yielding a pre-certified stopping rule that requires little knowledge of the true transition kernel. Numerical case studies on perturbed maze environments of varying size and contamination level confirm that the transformer-based warm start measurably reduces the initial error and accelerates convergence, while the proposed conformal bounds track the true error trajectory more tightly than existing guarantees. 

I. I NTRODUCTION 

The framework of robust reinforcement learning (RL) [1]– [4] faces the challenge of poor sample efficiency and slow convergence arising from poor initialization in large state– action spaces. Randomly initialized action-value functions must recover all structural information about their MDPs entirely through repeated trials, requiring an impractical amount of exploration followed by a series of Bellman updates before meaningful convergence [2], [5]. Structured environments, however, are not featureless. Visual and natural language descriptions of environments and tasks may carry underlying semantic information of the tasks, which is usually discarded in nominal RL algorithms. One could naturally imagine that a well-informed initial pre-diction of the action-value function through learning-based encodings of such information would accelerate convergence. This work attempts to make that intuition precise by es-tablishing theoretically rigorous, non-conservative bounds on the convergence rate that are both pre-certifiable and prac-tically computable from finite data samples of contracting Bellman residuals. In particular, we fine-tune a pretrained transformer encoder [6] via supervised regression [7] on action-value function tables generated offline, producing an initial estimate that encodes task-relevant structure before any environment interaction occurs.   

> This work benefited from technical discussions within DARPA’s Safe and Assured Foundation Robots for Open eNvironments (SAFRON) Pro-gram, under contract number HR0011-25-3-0331. The authors are with the Department of Aerospace Engineering, The Grainger College of Engineer-ing, University of Illinois Urbana-Champaign, Urbana, IL, USA (email:
> sumanb2@illinois.edu, hiroyasu@illinois.edu )
> Fig. 1: Overview of the proposed framework: using transformer encoder-based Q-function approximation with trajectory-wise conformal prediction to provide time-dependent and high-probability bounds on Q-Learning errors in R−contaminated RL environments.

Our framework considers the R-contamination model [4], [8] to characterize uncertainty in the state transition kernel, and employs conformal prediction to certify convergence via trajectory-level nonconformity scores constructed from the contracting Bellman residual. Our contributions include: (i) We employ conformal prediction to statistically quantify the validity of transformer encoder-based approxima-tions of the optimal action-value function, thus provid-ing a formal tool to analyze the improvement of con-vergence behavior in the R-contaminated environments. (ii) We introduce a new trajectory-wise conformal pre-diction framework that yields tighter, finite-sample, distribution-free bounds on the gap between the running and optimal action-value functions, certifiable solely from observed Bellman residuals. (iii) We demonstrate the effectiveness of these guarantees using bidirectional encoder representations from trans-formers (BERT) in perturbed maze environments of varying sizes and contamination levels. 

Related work: Classical RL algorithms may assume that the environment used to train agents is identical to the environment in which the agent will be deployed [1]. However, this assumption is often violated in practice. Agents trained in simulation environments may encounter state transitions and rewards that differ from the ones of the real world, causing the learned policy to perform poorly upon deployment. This mismatch, commonly referred to as the sim-to-real gap, has motivated the study of robust Markov decision processes (RMDPs) [9], [10], where the true transi-tion kernel is assumed to lie within an uncertainty set around a nominal model, defined via distance measures such as total arXiv:2609.23775v1 [cs.LG] 20 Sep 2026 variation, KL divergence, or the R-contamination model [8]. The goal of robust RL [4], [11]–[13] is to learn a policy that maximizes the return under worst-case transitions in the uncertainty sets. In [4], [11], the R-contamination model is used to derive Q-learning and policy gradient algorithms for the robust MDP, while [12] considers total variation and KL divergence uncertainty sets to propose distributionally robust value iteration. Other formulations treat model uncertainty as adversarial attacks [14], casting the robust RL as a two-player zero-sum discounted game. On the uncertainty quantification side, conformal predic-tion [15] provides a distribution-free, finite-sample statistical framework that produces coverage guarantees for arbitrary prediction models based on rank statistics of nonconformity scores, requiring only exchangeability of the data. The use of conformal prediction as a safety filter for RL in dynamic environments has been discussed in [16], while [17] applies conformal prediction on actions of multiple agents in RL to influence the action of a target agent. On the represen-tation learning side, transformer encoder architectures have shown promise in RL settings due to their ability to capture long-range dependencies in sequential data [18]–[20]. Their self-attention mechanism allows encoding global context that reflects spatial, geometric, and semantic relationships across state representations. In [21], the use of BERT to improve data efficiency using a novel combination of a gated transformer-XL [22] and long short-term memories [23] has been discussed, while in [24], transformer-based encodings are employed in language understanding tasks to detect discriminatory remarks across different languages, hence promoting safer online interactions. Since RL involves se-quential decision-making with correlated, long-term trajec-tories, transformer architectures are well-suited for one-shot modeling of dependencies arising in such settings, capturing the structural patterns of the underlying MDP from task descriptions alone without requiring iterative environment interaction. These results suggest that transformer encoders may offer useful inductive biases for structured RL tasks, though their theoretical integration with robust Q-learning convergence has not been previously addressed. II. P RELIMINARIES 

A. Robust Markov Decision Processes (RMDP) 

Recall that an MDP is defined by a tuple (S, A, P, r, γ ),where S is the state space, A is the action space, P =

{ ¯P (·| s, a ) ∈ ∆|S| : s ∈ S, a ∈ A} is the nominal transition kernel, r(s, a ) : S × A → [0 , 1] is the reward function, and γ ∈ [0 , 1) is the discount factor. Here, ∆n

denotes the (n − 1) -dimensional probability simplex. To account for uncertainties in the state transition kernels, we consider a robust MDP (RMDP) [10]. This is also defined by a tuple (S, A, P, r, γ ), except that the true transition kernel is unknown and lies in the uncertainty set denoted by P given as P := N 

> s∈S , a ∈A

U(s, a ) where U(s, a )

is the uncertainty set of the transition kernel defined with respect to the state-action pair (s, a ) and N denotes the product measure. For a fixed policy π, the robust state value function for the RMDP is the discounted cumulative reward under the worst-case transitions, given by V π (s) = min P ∈P Eπ,P [P∞ 

> t=0

γtr(St, A t) | S0 = s]. Similarly, the robust action-value function is given by Qπ (s, a ) =min P ∈P Eπ,P [P∞ 

> t=0

γtr(St, A t) | S0 = s, A 0 = a]. The goal of robust RL is to find the optimal robust policy π⋆

such that π⋆ := arg max π V π (s). For notational simplicity, we denote V ⋆(s) := V π⋆

(s) and Q⋆(s, a ) := Qπ⋆

(s, a ).The optimal robust value functions satisfy the robust Bell-man equation, given by V ⋆(s) = max a∈A Q⋆(s, a ) and 

Q⋆(s, a ) = T ⋆Q⋆(s, a ) := r(s, a ) + γσ U(s,a )(V ⋆), where 

σU(s,a )(V ) := min P (·| s,a )∈U (s,a ) Es′∼P (·| s,a )V (s′) denotes the worst-case expected next value. It is shown in [10] that the robust Bellman operator T ⋆Q(s, a ) := r(s, a ) + 

γσ U(s,a )(V ) is a contraction mapping and Q⋆ is its fixed point. Therefore, if the uncertainty set P is known exactly, then Q⋆ can be solved by dynamic programming. 

B. R-Contamination Model and Robust Q-Learning 

Since the exact form of P is rarely known in practice, we consider the R-contamination model [4], [8] to construct a tractable uncertainty set P. First, let ¯P (·| s, a ) be the nominal transition model, which generates sample trajectories for learning. The R-contamination uncertainty set is defined as: U(s, a ) = (1 − R) ¯P (·| s, a ) + R q | q ∈ ∆|S| where 

R ∈ [0 , 1] is a parameter that controls the size of the uncertainty. In general, a larger R corresponds to larger uncertainty, leading to a more conservative optimal value function. In our problem setting, we assume that R is known but ¯P (·| s, a ) is unknown , and thus we estimate U(s, a ) online using maximum likelihood estimation (MLE) as follows. Given an observed state transition (s, a, s ′), the MLE estimate is bP (·| s, a ) := 1s′ , which is an unbiased estimate of ¯P (·| s, a ) because the expectation of the indicator function of an event is exactly the probability of that event. Then, the data-driven, estimated R-contaminated uncertainty set can be written as bU(s, a ) = (1 − R)1s′ + Rq | q ∈ ∆|S| 

which yields the following estimated worst-case next value: 

σ bU(s,a )(V ) := R min s′′ ∈S V (s′′ ) + (1 − R)V (s′). For R-contamination models, the regular Q-learning, which does not account for the uncertainty in the transition kernel, can yield a learned optimal policy that degrades significantly in the true environment under such uncertainty. The robust Q-learning algorithm was proposed by [4] to address this issue by explicitly incorporating the R-contamination uncer-tainty set into Q-learning. It is implemented in an online episodic fashion, using multiple trajectory data samples collected under some exploratory policy πb(·| s) (e.g., ϵ-greedy policy). With the robust MDP introduced earlier, the temporal-difference update of robust Q-learning can be written as Qt+1 (s, a ) = (1 − αt)Qt(s, a ) + αt(r +

γσ bU(s,a ) (max a′∈A Qt(s, a ′))) , where αt denotes the learn-ing rate and t is the iteration index. Therefore, by definition of σ bU(s,a ), this becomes 

Qt+1 (s, a ) = (1 − αt)Qt(s, a ) + αt(r + γR min s′′ ∈S Vt(s′′ )+ γ(1 − R)Vt(s′)) (1) TABLE I: Definitions of quantities in Lemma 1                         

> Quantity Definition / meaning
> ∆Qt:= ∥Qt−Q⋆∥∞
> k:= max
> n
> 0,
> jt−tth
> tframe
> k o
> ρ:= (1 −γ) (1 −(1 −α)μframe )
> μframe := 12μmin tframe
> tth := max {
> 2 log
> 1(1 −γ)2ϵ
> 
> αμ min , t frame }
> μmin Minimum state-action visitation frequency under the sampling process
> tmix Mixing time of the Markov chain induced by the behavior policy
> tframe Frame length used in the blockwise finite-time analysis

As can be seen, robust Q-learning is a model-free, online, and off-policy RL algorithm, which has the following con-vergence property. 

Lemma 1 (Robust Q-learning [2]–[4]): If a constant learning rate αt = α is used in (1), then for any ϕ ∈ (0 , 1) 

and ϵ ∈



0, 11−γ



, there exist constants ˆc and c0 such that the following bound holds for all t ≤ T at least with probability 

1 − 6ϕ: ∆Qt ≤ (1 −ρ)k 

> 1−γ

∆Q0

| {z }

> contracting term

+ 5ˆ cγ 

> 1−γ

r

α log 

 |S||A| Tϕ



+ ϵ

| {z }

> steady-state error term

as long as the learning rate satisfies 0 < α log 

 |S||A| Tϕ



< 1

and the time horizon satisfies T ≥ c0

> μmin

 1(1 −γ)5ϵ2 +

> tmix
> 1−γ



log 

 T |S||A| 

> ϕ



log 

 1

> ϵ(1 −γ)2



. The notation is summarized in Table I. 

C. Transformer Natural Language Encoders 

Transformer encoders are neural architectures designed to parse structured input data into a sequence of continuous representations using stacked feed-forward and multi-head attention layers [18]. This architecture enables them to si-multaneously learn short- and long-range contextual relation-ships of input data. When applied to natural language [25], these encoders produce latent representations that distill the meaning of each token in relation to the context of the full input sequence. In this study, we fine-tune the BERT (Bidi-rectional Encoder Representations from Transformers) [6] model to predict the optimal action-value function obtained through the robust Q-learning of Sec. II-B. Specifically, the model is trained to take as input a natural language prompt describing the reinforcement learning task specification and output the corresponding action-value function. 

D. Conformal Prediction 

Conformal prediction is a distribution-free framework for generating finite-sample coverage guarantees for arbitrary uncertainty predictors based on the rank statistics of non-conformity scores. If applied to a regression model Y =ˆf (X), such scores may be defined as si = ∥ ˆf (Xi) −

Yi∥, ∀i ∈ { 1, 2, · · · , N }, where Yi denotes the ground-truth output samples corresponding to the input samples Xi. For a prescribed failure probability δ ∈ (0 , 1) , we define the conformal quantile q1−δ as the ⌈(1 − δ)( N + 1) ⌉/N -th smallest nonconformity score in {si}i∈{ 1,2,..,N } ∪ ∞ , where 

⌈(·)⌉ denotes the ceiling function. 

Lemma 2 (Conformal Prediction [15]): If, for any new sample (Xinew , Y inew ), all the nonconformity scores in 

{si}i∈{ 1,2,..,N }∪{ inew } are exchangeable, then a statistical bound on the prediction error may be postulated as follows: 

P(∥ ˆf (Xinew ) − Yinew ∥ ≤ q1−δ ) ≥ 1 − δ where P is the probability measure induced by the joint distribution of the data {(Xi, Y i)}Ni=1 and the new sample (Xinew , Y inew ).III. M ETHODS 

A. Robust Q Function Prediction from Natural Language 

Directly predicting the action-value function Q in high-dimensional state spaces, particularly when the task is also represented by high-dimensional inputs, can be complex and could produce poor generalization. Raw information on states and task specifications often contains irrelevant features that may distract RL algorithms during extraction [26], leading to inefficient training. In this paper, we consider reinforcement learning tasks specified in natural language and hence intro-duce a transformer encoder Φθ , which first converts states 

s, actions a, and natural-language task specifications L into numerical token embeddings, and then encodes them into latent representations capturing task-relevant features. Here, 

θ denotes the parameters of the transformer. In particular, by selecting the output features to be the predicted optimal action-value function, Qθ (s, a ; L), we define Φθ as: 

Qθ (s, a ; L) = ( ψθ ◦ φθ )( s, a, L), Φθ := ψθ ◦ φθ (2) where φθ (s, a, L) ∈ RL yields the numerical embedding of length L, and ψθ denotes the encoder. Given M data samples of the optimal action-value function Q∗(s, a ; Lj )

corresponding to sampled task specifications {Lj }Mj=1 , the transformer Φθ is trained by supervised regression using the following Huber loss over all state and action pairs: 

L(θ) := PMj=1 1

> |S||A|

P 

> s,a

ℓ(Qθ (s, a ; Lj ) − Q∗(s, a ; Lj )) ,where ℓ(∆) := 12 ∆2 for |∆| ≤ 1 and ℓ(∆) := |∆| − 12

otherwise. Instead of training Φθ from scratch over all tunable parameters in θ, we initialize Φθ with a pre-trained BERT [6] backbone and fine-tune selected parameters using LoRA [7]. Since the embedding function φθ is often defined by linguistic rules and/or fixed structural representations (e.g., numerical indices associated with language tokens), the statistical properties of Qθ (s, a ; L) for sampled task specifications can be characterized through φθ (s, a, L) ∈

RL, yielding the following result. 

Lemma 3: Suppose that the parameter θ of the trans-former Φθ is fixed after training, and we have N data samples of natural-language task specifications, {Li}Ni=1 , not used for training. If, for any new sample Linew , all the numerical embeddings in {φθ (s, a, Li)}i∈{ 1,2,..,N }∪{ inew }

are exchangeable over all state and action pairs, then we have the following bound: 

P {∥ Qθ (Linew ) − Q∗(Linew )∥∞ ≤ q1−δ0 } ≥ 1 − δ0 (3) where δ0 ∈ (0 , 1) is the failure probability, q1−δ0 is the 

⌈(1 − δ0)( N + 1) ⌉/N -th smallest nonconformity score in 

{si}i∈{ 1,2,..,N } ∪ ∞ with si := ∥Qθ (Li) − Q∗(Li)∥∞, and 

∥(·)∥∞ denotes the infinity norm defined as ∥Qθ (Li) −

Q∗(Li)∥∞ := sup (s,a )∈S×A ∥Qθ (s, a ; Li) − Q∗(s, a ; Li)∥

for the predicted/optimal action-value functions Qθ and Q∗.

Proof: This follows from Lemma 2. B. Transformer-Accelerated Robust Q-learning 

Although Lemma 3 provides a theoretical error bound on the action-value function prediction, the quantile q1−δ in (3) is almost always nonzero due to the empirical nature of LoRA fine-tuning. We first derive an explicit dependence of the convergence behavior of robust Q-learning on the initial-ization induced by the transformer prediction Qθ , showing accelerated convergence to Q∗.

Theorem 1 (Robust Q-Learning Warm-Started by Qθ ): 

Suppose we are given a new task specification Linew with all the assumptions in Lemma 3 satisfied. If we initialize the action-value function as Q0(s, a ; Linew ) = Qθ (s, a ; Linew ),we have the following bound for all t ≤ T and some 

δ0 ∈ (0 , 1) at least with probability 1 − 6ϕ − δ0:

∆Q∞(Linew ) ≤ (1 −ρ)k q1−δ0

> 1−γ

| {z }

> contracting term

+ 5ˆ cγ 

> 1−γ

r

α log 

 |S||A| Tϕ



+ ϵ

| {z }

> steady-state error term

(4) where ∆Q∞(Linew ) := ∥Qt(Linew ) − Q∗(Linew )∥∞ with 

Q0(s, a ; Linew ) = Qθ (s, a ; Linew ), and the other notations are consistent with the ones in Lemmas 1 and 3. Also, if we select the learning rate α to be 

α = c1   

> log
> T|S||A|
> ϕ
> 

min 

 1 

> tmix

, ϵ2(1 −γ)4

> γ2



then we have ∆Q∞(Linew ) ≤ 3ϵ whenever k ≥ log   

> q1−δ0
> (1 −γ)ϵ
> 
> log
> 11−ρ
> 

for a suitable constant c1.

Proof: The relation (4) follows from Lemmas 1 and 3. The condition on k is by bounding the first term of (4) by 

ϵ. The constant c1 can be found as in [3]. While Theorem 1 is useful from an analytical standpoint, it is of limited practical value because the steady-state error term can dominate the upper bound of (4), making it overly conservative. Empirically, however, as shown in Sec. IV, the actual convergence of robust Q-learning is substantially faster. We derive the following tighter bound to bridge this gap at the expense of a reduced probability guarantee. 

Theorem 2 (Statistical Convergence): Suppose that we are given a collection of task specifications {Li}Ni=1 with exchangeable embeddings as in Lemma 3. Suppose we are given a new task specification Linew with all the assumptions in Lemma 3 satisfied. For each i, let {Qt(s, a ; Li)}Tt=0 

denote the sequence of Q-functions generated by the robust Q-learning initialized as Q0(s, a ; Li) = Qθ (s, a ; Li) with the transformer prediction Qθ defined in (2). Also, let 

Q∗(s, a ; Li) denote the corresponding optimal action-value function. Inspired by the result (4) of Theorem 1, we define the following step-wise nonconformity scores: 

st,i = max 

n

0, ∆Qt(Li) − ∆Q0(Li)(1 −ρ)k

> 1−γ

o

where ∆Qt(Li) := ∥Qt(Li)−Q∗(Li)∥∞ and ∆Q0(Li) := 

∥Qθ (Li) − Q∗(Li)∥∞. Then, for a task specification Linew 

whose embedding is exchangeable with those of {Li}Ni=1 ,the following holds: 

P



∀t ∈ I, ∆Qt(Linew ) ≤ ∆Q0(Linew )(1 −ρ)k 

> 1−γ

+ qt, 1−δt



≥ 1 −

> T

X

> t=1

δt

(5) where I := {1, · · · , T } and qt, 1−δt is the (1 −δt)-th empirical quantile of the t-th nonconformity scores {st,i }Ni=1 . Also, for any ϵ ∈ (0 , ∞), we have ∆Qt(Linew ) ≤ ϵ + qt, 1−δt at least with probability 1 − PTt=1 δt − δ0 for the defined k, where 

δ0 and q1−δ are again given in (3) of Lemma 3. 

Proof: For each fixed t, Lemma 2 implies 

P (st,i new ≤ qt, 1−δt ) ≥ 1 − δt where st,i new is the new nonconformity score defined for the new sample Linew .For the failure event given as Et := {st,i new > q t, 1−δt }

so that P(Et) ≤ δt yields P

STt=1 Et



≤ PTt=1 δt by Boole’s inequality. Taking the probability of the complement of STt=1 Et gives (5). The rest follows from Theorem 1. Theorem 2 provides a time-uniform, distribution-free and high-probability bound on the convergence rate of pre-trained model-predicted Q-functions. This is in contrast with the standard classical approach to Q-learning, where asymptotic and expectation-based bounds are prescribed. In addition, the theorem provides a finite-sample, non-asymptotic bound which holds for every iteration during the learning process by defining how the t−th step error reduces under a warm start. The uncertainty in the process is represented by data-driven quantiles. This yields a method to certify that the learning dynamics of a transformer-predicted warm start Q-function are reliable and hence, useful in unknown, safety-critical and data-sparse environments. A critical limitation in practically applying the bounds (4) and (5) is the sim-to-real gap. Given a task specification L, let Qt(s, a ; L, Esim ) denote the action value function constructed in a simulated environment Esim 

and Q∗(s, a ; L, Ereal ) be the corresponding optimal action-value function in the real world environment Ereal . At a high level, the discrepancy ∆Qt(L, Esim , Ereal ) := ∥Qt(L, Esim )−

Q∗(L, Ereal )∥∞ can be computed as follows [3], [4]: 

∆Qt(L, Esim , Ereal ) ≤ ∆Pt(L, Esim ) + ∆ Et(L, Esim , Ereal )+ ( ∥I − Λt∥∞ + γ∥Λt∥∞)∆ Qt−1(L, Esim , Ereal ) (6) where Λt ∈ R|S||A|×|S||A| is a diagonal matrix encoding the step size αt at the visited state–action pair (st, a t)

and zero elsewhere, P ∈ R|S||A|×|S| denotes the sample transition matrix from the single observed transition at time 

t, ∆Pt(L, Esim ) denotes the bounded error arising from the discrepancy between the MLE estimated transition model and the true simulated model at t, and ∆Et(L, Esim , Ereal )

captures the cumulative discrepancy between the simulated and real environments. Inspired by recent work on closed-loop statistical inference [27], we finally analyze the ro-bustness of the warm-started robust Q-learning algorithm by applying the idea analogous to Theorem 2 to the model (6). 

Theorem 3 (Robustness of Robust Q-Learning): Suppose that we are given a collection of task specifications and environments, {Li}Ni=1 , {Ei, sim }Ni=1 , and {Ei, real }Ni=1 ,with exchangeable embeddings as in Lemma 3. Suppose we are given a new task specification and environments, Linew , Einew ,sim , and Einew ,real , with all the assumptions in Lemma 3 satisfied. Define the following trajectory-wise nonconformity scores: si, traj =sup t∈[t0,T ] ∆Pt(Li, Ei, sim ) + ∆ Et(Li, Ei, sim , Ei, real ) for 

∆Pt and ∆Et given in (6), where t0 denotes the time when the relation (6) becomes valid with ∥I − Λt∥∞ < 1 [3], [4]. For each i, let {Qt(s, a ; Li, Ei, sim )}Tt=0 denote the sequence of Q-functions generated by the robust Q-learning initialized as Qt0 (s, a ; Li) = Qθ (s, a ; Li, Ei, sim ) with the transformer prediction Qθ defined in (2). Also, let Q∗(s, a ; Li, Ei, real )

denote the corresponding optimal action-value function. Then, for a new task specification and environments, 

Linew , Einew ,sim , and Einew ,real , whose embeddings are exchangeable with their respective calibration samples, the following holds: 

P(∀t ∈ [t0, T ], ∆Qt(Linew , Einew ,sim , Einew ,real )

≤ ∆Qt0 (Linew , Einew ,sim , Einew ,real )(1 − λ)t−t0

+ q1−δtraj  

> λ

(1 − (1 − λ)t−t0 )) ≥ 1 − δtraj 

where λ := inf t∈[t0,T ](1 − ∥ I − Λt∥∞ − γ∥Λt∥∞), and 

q1−δtraj is the (1 − δtraj )-th empirical quantile of the trajectory-wise nonconformity scores {si, traj }Ni=1 . Further-more, for any ϵ ∈ (0 , ∞), we have 

∆Qt(Linew , Einew ,sim , Einew ,real ) ≤ ϵ + q1−δtraj (1 −(1 −λ)t−t0 )

> λ

at least with probability 1 − δtraj − δ0 whenever λ ∈ (0 , 1) 

and t satisfies the following: t ≥ log( q1−δ0 /ϵ )[− log(1 −λ)] 

where δ0 and q1−δ0 are again given in (3) of Lemma 3. 

Proof: Using (6) iteratively, we get ∆Qt ≤

˜λt−t0 ∆Qt0 +Ptτ =t0+1 ˜λt−τ (∆ Pτ +∆ Eτ ) where ˜λ := 1 −λ

and the arguments are omitted for notational simplicity. Using the result of Lemma 2, with probability at least 

1−δtraj , we have ∥∆Pτ +∆ Eτ ∥ ≤ q1−δtraj for the new task and the environments, which implies ∆Qt ≤ ˜λt−t0 ∆Qt0 +

q1−δtraj · 1−˜λt−t0 

> λ

establishing si, traj and the bound on t.IV. N UMERICAL CASE STUDIES 

We perform numerical simulations on randomly generated 

n × n mazes (Fig. 3) and train BERT [6] on vision–language prompt-based datasets. To facilitate the environment con-struction, we add a boundary layer around the original mazes. For simulations, we used an NVIDIA L40S GPU with 48GB of GDDR6 memory. Each dataset contained 30,000 sample prompts with responses, and the data generation and training took about 20 hours on average for each maze size. We use a green square to denote the starting square and the red one to denote the goal (Fig. 3). Q∗ is now obtained using the hyperparameters given in Table II and the prompt is next constructed in two parts, the Instruction and the Response .A sample Instruction is given in Fig. 2. The Response corresponds to string (Q∗). We then con-struct a dataset of these Instruction-Response pairs and train the model on it, keeping the R−Contamination fixed at {0.2, 0.4, 0.5} for 9 × 9, 11 × 11 and 13 × 13 maze sizes, respectively, to approximate the optimal action-value function. For this procedure, we sample 2000 random mazes and compute their corresponding error trajectories. During evaluation, R is sampled randomly from [0 .1, 0.9] to assess the robustness of our framework under varying uncertainty.                            

> Fig. 2: An example Instruction for a 13 ×13 maze
> (a) t=0
> (start)
> (b) t=1
> (w/ BERT)
> (c) t=2
> (w/ BERT)
> (d) t=3
> (w/ BERT)
> (e) t=15
> (w/o BERT)
> Fig. 3: Maze-solving progress for a 13 ×13 maze with R= 0 .5: (a) initial maze; (b)–(d) BERT-based warm start after 1–3 episodes; (e) standard Q-learning after 15 episodes.

The hyperparameters and the corresponding reward policy for Q-learning are given in Table II. 

Remark 1: The rewards in Table II satisfy the r ∈ [0 , 1] 

assumption in Section II-A after an affine normalization, ˜r =(r − rmin )/(rmax − rmin ), which preserves Q-value ordering and the optimal policy. 

A. Accelerated Convergence under Warm-start Initialization 

The trained model generates an initial estimate of Q∗,denoted by Qθ = QBERT during evaluation, and we initialize the learning process with this estimate, i.e., Q0(s, a ) =

QBERT (s, a ), and compare it against the standard initializa-tion: Q0(s, a ) = 0 , ∀(s, a ) ∈ S ×A. For both initialization schemes, we track the error with respect to the optimal Q-function Q∗ over each iteration ∥Qt − Q∗∥∞, distinguish-ing between ∥Qt − Q∗∥BERT and ∥Qt − Q∗∥true , the error trajectories initialized with QBERT and the zero function, respectively. Fig. 4 shows the effect of learned initialization on the convergence behavior of || Qt − Q∗|| ∞, where the blue curves represent the true error under the BERT-based warm start and the red curves correspond to the standard Q-Learning algorithm. Under the influence of the BERT-based warm start, the true error rapidly converges to zero under varying levels of uncertainty, illustrating the impact of learned initialization on convergence behavior.      

> (a) (b)
> Fig. 4: Improvement in convergence behavior using warm start for R∈
> [0 ,1] in mazes of size: (a) 11 ×11 and (b) 13 ×13.

B. Statistical Refinement of Bounds and Conformal Predic-tion 

Fig. 5 shows that the proposed framework from Theo-rems 1 and 2 tightens previous theoretical guarantees in (a) (b) 

Fig. 5: Demonstration of the proposed stricter bounds in mazes of size: (a) 11 ×11 and (b) 13 ×13. 

[4] and the proposed bounds follow the true error. For each maze sample {Li}Ni=1 , we compute {st,i }Tt=1 , and the corresponding (1 − δt)-th empirical quantiles over all episodes. A time-dependent conformal correction is now constructed, which upper bounds the nonconformity scores with high probability, along with the (1 − δ)-th quan-tile for the initial prediction error || QBERT − Q∗|| , to calculate the contracting and the steady-state error term for every sample. Episodes are plotted on the logarith-mic x-axis, the blue curves represent the normalized true error trajectory ∥Qt−Q∗∥∞  

> max t∥Qt−Q∗∥∞

, the red curves represent the normalized conformal correction term q1−δt 

> max tq1−δt

, and the black curves represent the normalized theoretical upper bound (1 −ρ)k q1−δ +5ˆ cγ √α log( |S||A| T /ϕ )+(1 −γ)ϵ  

> max t[(1 −ρ)kq1−δ+5ˆ cγ

√α log( |S||A| T /ϕ )+(1 −γ)ϵ] respec-tively. Fig. 6 shows a steady monotonic decrease in the logarithmic nonconformity score frequencies which suggests a heavy-tailed nature with the 90 th -percentile threshold de-noted. Most scores lie to the left of this bound, although a heavy tail suggests the presence of relatively rarer larger deviations, which is accounted for by Thm. 2. The threshold shifts across different maze sizes, indicating the framework’s response to problem complexity, but the bulk of the dis-tribution remains concentrated to small errors, successfully demonstrating system efficacy.          

> (a) 11 ×11 maze (b) 13 ×13 maze

Fig. 6: Empirical distributions of st,i for the BERT-based warm start in mazes of sizes 11 × 11 and 13 × 13 , with the 90 th -percentile shown. 

Parameter Value Reward Policy Reward 

SIZE {9, 11 , 13 } Free Space −0.1

γ 0.7 Obstacle −1.0

M 9000 Goal +5 .0

α 0.9 Outside Boundary −100 .0

R {0.2, 0.4, 0.5}

TABLE II: Hyperparameters and reward policy used in robust Q-learning for numerical simulations 

V. C ONCLUSION 

This study presents a new framework to combine trans-former encoder-based representation learning with robust Q-learning methods, where a BERT-based warm start seeds the algorithm with a strong initial prediction that reduces initial error and improves sample efficiency. Conformal pre-diction yields tighter, finite-sample, distribution-free Q-error bounds uniformly over all iterations than existing theoretical guarantees. The numerical simulations validate these claims, demonstrating that the convergence behavior improves with the BERT-based warm start, and that the conformal bounds consistently track the true error more closely than prior results. REFERENCES [1] R. S. Sutton and A. G. Barto, Reinforcement Learning: An Introduc-tion . Cambridge, MA, USA: A Bradford Book, 2018. [2] D. P. Bertsekas and J. N. Tsitsiklis, Neuro-Dynamic Programming .Belmont, MA: Athena Scientific, 1996. [3] G. Li et al. , “Sample complexity of asynchronous q-learning: Sharper analysis and variance reduction,” in NeurIPS , vol. 33, 2020, pp. 7031– 7043. [4] Y. Wang and S. Zou, “Online robust reinforcement learning with model uncertainty,” NeurIPS , vol. 34, pp. 7193–7206, 2021. [5] R. Bellman, Dynamic Programming . Princeton University Press, 1957. [6] J. Devlin et al. , “Bert: Pre-training of deep bidirectional transformers for language understanding,” in NAACL-HLT , 2019, pp. 4171–4186. [7] E. J. Hu et al. , “LoRA: Low-rank adaptation of large language models,” in ICLR , 2022. [8] P. J. Huber, “A robust version of the probability ratio test,” The Annals of Mathematical Statistics , vol. 36, no. 6, pp. 1753–1758, 1965. [9] J. A. Bagnell et al. , “Solving uncertain markov decision problems,” Pittsburgh, PA, Tech. Rep. CMU-RI-TR-01-25, August 2001. [10] A. Nilim and L. El Ghaoui, “Robustness in markov decision problems with uncertain transition matrices,” NeurIPS , vol. 16, 2003. [11] Y. Wang and S. Zou, “Policy gradient method for robust reinforcement learning,” in ICML . PMLR, 2022, pp. 23 484–23 526. [12] D. Ghosh et al. , “Orvit: Near-optimal online distributionally robust reinforcement learning,” arXiv preprint arXiv:2508.03768 , 2025. [13] K. Zhang et al. , “Robust multi-agent reinforcement learning with model uncertainty,” NeurIPS , vol. 33, pp. 10 571–10 583, 2020. [14] L. Pinto et al. , “Robust adversarial reinforcement learning,” in ICML .PMLR, 2017, pp. 2817–2826. [15] V. Vovk, A. Gammerman, and G. Shafer, Algorithmic Learning in a Random World . Springer US, 2005. [16] K. J. Strawn et al. , “Conformal predictive safety filter for rl controllers in dynamic environments,” IEEE RAL , vol. 8, no. 11, 2023. [17] N. Gupta et al. , “Cammarl: Conformal action modeling in multi agent reinforcement learning,” arXiv preprint arXiv:2306.11128 , 2023. [18] A. Vaswani et al. , “Attention is all you need,” NeurIPS , vol. 30, 2017. [19] E. Voita et al. , “Analyzing multi-head self-attention: Specialized heads do the heavy lifting, the rest can be pruned,” in ACL , 2019. [20] E. Voita et al. , “The bottom-up evolution of representations in the transformer: A study with machine translation and language modeling objectives,” in EMNLP-IJCNLP , 2019, pp. 4396–4406. [21] A. Banino et al. , “Coberl: Contrastive bert for reinforcement learning,” 

arXiv preprint arXiv:2107.05431 , 2021. [22] E. Parisotto et al. , “Stabilizing transformers for reinforcement learn-ing,” in ICML . PMLR, 2020, pp. 7487–7498. [23] S. Hochreiter and J. Schmidhuber, “Long short-term memory,” Neural Computation , vol. 9, no. 8, pp. 1735–1780, 1997. [24] V. S. Raj, et al. , “Conbert-rl: A policy-driven deep reinforcement learning based approach for detecting homophobia and transphobia in low-resource languages,” Nat. Lang. Proc. J , vol. 6, 2024. [25] T. Wolf et al. , “Transformers: State-of-the-art natural language pro-cessing,” in EMNLP Demos . Online: Association for Computational Linguistics, 2020, pp. 38–45. [26] A. Zhang et al. , “Learning invariant representations for reinforcement learning without reconstruction,” arXiv preprint arXiv:2006.10742 ,2020. [27] T.-W. Hsu and H. Tsukamoto, “Statistical guarantees in data-driven nonlinear control: Conformal robustness for stability and safety,” IEEE Control Systems Letters , 2025.